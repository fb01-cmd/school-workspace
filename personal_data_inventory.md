# 개인정보 데이터 목록 (Personal Data Inventory)

> 2026-07-24 작성 (Firestore 보안 규칙 설계와 동시 조사).
> 이 시스템이 **자체 DB(Firestore)에 저장하는** 개인정보의 전수 목록이다.
> Google Workspace 안의 원본 데이터(메일·드라이브·계정)는 학교–Google 간
> Workspace for Education 계약에 따르며 이 문서의 범위 밖이다.
> 새 컬렉션에 개인정보를 저장하게 되면 **이 표를 반드시 갱신**한다.

## 원칙

- 개인정보는 목적 달성에 필요한 최소한만 저장한다 (이름·학교 이메일·학번 수준).
- 개인 연락처(휴대전화·자택 전화·주소)와 주민등록번호·학부모 정보는 **저장하지 않는다**. 학교가 부여한 내부 업무 연락처(내선번호)만 본인 또는 관리자가 직접 등록하는 선택 항목으로 저장한다.
- 접근 통제는 `firestore.rules`(도메인 게이트 + 역할 기반)로 강제한다.

## 컬렉션별 목록

| 컬렉션 | 담는 개인정보 | 목적 | 접근(규칙 기준) | 보존·파기 |
|---|---|---|---|---|
| `users` | uid, 학교 이메일, 이름, 역할, 승인상태 | 로그인·권한 판별 | 본인 + 교직원 읽기 / 변경은 수퍼어드민 | 계정 삭제 시 함께 삭제 |
| `teacher_profiles` | 교사 이메일, 이름, 부서·담당, 내선번호(선택 입력) | 교직원 조직도 | 교직원만 | 전출·퇴직 처리 시 삭제 |
| `teacher_profiles_pending` | 위와 동일(승인 대기분) | 프로필 승인 절차 | 본인 작성, 교직원 열람 | 승인/거절 즉시 삭제 |
| `graduation_tasks` | 학생 이메일, 이름, 학번, 소속 OU, 서명 여부 | 졸업 계정 정리 예약장부 | 학생은 본인 것만, 교직원 전체 | 계정 삭제 완료 후 상태만 남김 |
| `graduation_consents` | 학생 이메일, 서명(터치 서명 데이터), 제출 시각 | 계정 삭제 안내 고지 증빙 | 교직원 읽기 전용, 쓰기는 서버만 | 제출 시각 기준 **3년 보존 후 자동 파기** (2026-08-05 확정, 크론 집행 — Phase 11 산출물 E) |
| `transfer_out_tasks` | 학생 이메일, 정지·삭제 예정일 | 전출·자퇴 계정 정리 | 교직원만 | 삭제 집행 시 문서 제거 |
| `teacher_transfer_tasks` | 교사 이메일, 이름, 백업 기한 | 교직원 전출 계정 정리 | 당사자 + 교직원 | 삭제 집행 후 상태만 남김 |
| `audit_logs` | 작업자 이메일, 대상자 이메일, 작업 내용, 시각 | 위험 작업 감사 추적 | 교직원 읽기, 수정·삭제 불가 | **5년 경과분 자동 파기** (2026-08-05 확정, 영구 보존 폐지 — Phase 11 산출물 E) |
| `classroom_cleanup_logs` | 교사 이메일, 코스명 | 학기말 정리 이력·복원 | 교직원 읽기, 쓰기는 서버만 | 복원 가능성 위해 보존 |
| `classroom_sync_logs`, `chrome_bookmark_logs` | 작업 교사 이메일, 대상 학급 | 작업 이력 | 교직원 읽기 | 보존 (용량 문제 시 정리) |
| `settings` | 개인정보 없음 (OU 경로·일정 설정) | 도메인 설정 | 구성원 읽기, 교직원 쓰기 | 해당 없음 |
| `discipline_records` | **[민감도 최상]** 학번, 학생 이메일·이름, 지도 항목, 발생 일시, 비고 메모, 기록 교사 | 생활지도 기록 (Phase 6b) | **클라이언트 전면 차단** — API + 서버 권한 판정만 | **졸업/제적 시 파기** (2026-07-25 사용자 확정 — lifecycle 크론 계정 영구삭제 스텝에 연동 예정, 파기 사실만 감사 로그) |
| `discipline_stage_events` | **[민감도 최상]** 학번, 학생 이메일·이름, 도달 단계, 조치 내용, 처리 교사 | 선도 단계 진입·처리 이력 | 위와 동일 | 위와 동일 (학생 기록과 함께 파기) |
| `discipline_permissions` | 교사 이메일, 부여된 권한 범위 | 생활지도 열람·기록 권한 관리 | 클라이언트 전면 차단 | 회수 시 삭제 (감사 로그에 부여·회수 이력) |
| ~~`homeroom_assignments`~~ | *(폐기 2026-07-25)* 담임 판정은 `teacher_profiles`의 승인 프로필로 일원화 — 별도 저장 없음 | — | — | 컬렉션 삭제됨 |
| `discipline_config` | 개인정보 없음 (규정·단계·규칙) | 생활지도 규정 설정 | 클라이언트 전면 차단 | 해당 없음 |
| `memos` | 발신·수신 교직원 이메일·이름, 쪽지 제목·본문·링크·첨부 참조, 수신자별 최초 열람 시각 | 교직원 쪽지(사내 메신저) | 승인된 교직원 중 발신자·수신자 본인만 읽기, 쓰기는 서버만 | **발송 후 365일 보존 후 파기** (2026-08-11 사용자 확정, `settings.memoRetentionDays`로 조정 가능). 파기 주체 = **일일 파기 크론**(daily-sync, 2026-08-18 구현 — `expireAt` 지난 문서를 첨부 Drive 파일과 함께 삭제). **이용자의 "삭제"는 본인 화면에서 감추는 것**(`hiddenBy`)이고 원본·상대 화면·열람 이력은 보존 기간까지 남는다(§12-1, 화면 문구로 고지) |
| 쪽지 첨부 (Drive, `hmnotice@`) | 첨부 이미지(GIF 포함) 파일 실체 — 시간표·명단 등 개인정보가 실릴 수 있음(텍스트보다 민감도 높음). 파일명에 쪽지 id·원본 파일명 | 쪽지 2단계 이미지 첨부 ([`docs/memo_attachment_spec.md`](./docs/memo_attachment_spec.md)) | Drive 열람 권한 = 쪽지 열람 집합과 동일(수신자 개별 reader·메일 발송 억제, 전 교직원 공지만 도메인 내부 링크), 소유 계정 = `hmnotice@`(보호 계정), 회수 시 미열람자 권한 철회 | **쪽지와 같은 시점 파기** — 일일 파기 크론(daily-sync)이 쪽지 삭제 시 Drive 파일도 삭제, 미발송 업로드(고아)는 24시간 후 삭제, 빈 월 폴더 정리 포함 |
| `notifications` | 수신 교직원 이메일, 알림 문구 한 줄(수업 변경·신청 처리·쪽지 도착·양해 요청 요약), 열람·수락 시각 | 알림 센터(개인 통지 원장, [`docs/notification_center_spec.md`](./docs/notification_center_spec.md)) | 수신자 본인만 읽기(서버 라우트 강제), 쓰기는 서버만 | **180일 보존 후 파기**(`expireAt` 스탬프, Firestore TTL 정책). 양해 수락 기록만 365일 — 책임 보호 근거(스펙 §5) |

## 처리 위탁 현황 (2026-08-05 추가 — 고지 문안의 단일 원본)

| 위탁 업체 | 맡기는 일 | 보관 위치 |
|---|---|---|
| Google (Firebase) | 로그인 인증, 시스템 데이터 저장(Firestore) | Google Cloud 서버 |
| Vercel | 웹 애플리케이션 호스팅 | Vercel 서버 (미국) |
| Google (Workspace) | 학교 계정·메일·수업 도구 관리, 알림 발송 | Google 서버 (학교–Google 교육용 계약) |

그 외 제3자 제공 없음. 새 외부 서비스를 붙이면 이 표 갱신 → 문안 갱신 → `POLICY_VERSION` 상향.

## 재고지(`POLICY_VERSION` 상향) 판정 기준 (2026-08-13 신설)

`POLICY_VERSION`을 올리면 **전 교직원에게 전면 고지 화면이 1회 재노출된다.** 이것은 문서의 개정 이력 번호가 아니라 **사람의 업무를 한 번 멈추는 트리거**다. 둘을 섞으면 두 방향으로 다 손해다 — 안 올리면 고지 누락이고, 아무 때나 올리면 모달이 "닫아야 하는 방해물"로 학습되어 정작 중요한 고지가 안 읽힌다.

그래서 **문안 갱신 의무와 재고지 의무를 분리한다.**

| 상황 | 문안(`/privacy`) | `POLICY_VERSION` |
|---|---|---|
| 새 개인정보 항목을 **본인 의사와 무관하게** 수집·기록 (예: 쪽지 열람 시각) | 갱신 **필수** | **상향** |
| 열람 범위 확대 (보는 사람이 늘어남) | 갱신 **필수** | **상향** |
| 보존 기간 연장 | 갱신 **필수** | **상향** |
| 처리 위탁 업체 추가 (데이터가 새 외부로 나감) | 갱신 **필수** | **상향** |
| **본인이 직접 입력하는 선택 항목** 신설, 열람 범위는 기존과 동일 | 갱신 **필수** | **상향하지 않음** — 대신 **입력란 옆 고지**로 대체 |
| 기존 문장의 정밀화·오기 수정 (처리 실태 변화 없음) | 갱신 필수 | 상향하지 않음 |

- 상향하지 않는 개정도 **문안은 반드시 고친다.** 문서가 실태와 어긋나는 상태를 두지 않는다.
- 상향하지 않을 때는 `/privacy` 하단에 **"최종 수정일"**을 따로 표기하고, `src/lib/policy/version.ts` 주석에 그 개정을 한 줄 남긴다(버전 문자열은 그대로). 개정 이력은 버전 상수가 아니라 이 두 곳이 담당한다.
- **"입력란 옆 고지"가 재고지보다 강한 경우가 있다.** 자발 입력 항목의 실제 위험은 *이용자가 의도보다 많은 정보를 적어 넣는 것*이고(내선번호 칸에 개인 휴대전화), 그 위험은 두 주 전에 본 모달이 아니라 입력하는 순간의 한 줄이 막는다.

## 결정 기록 — `teacher_profiles.extension`(내선번호) (2026-08-13 Claude 판단)

[`docs/memo_spec.md`](./docs/memo_spec.md) §11-6의 "착수 전 확인" 항목에 대한 판단이다. **결론: 인벤토리 갱신 필수 / `POLICY_VERSION` 상향 불필요.**

**① 인벤토리 갱신 — 필수. 그리고 행 하나가 아니라 두 곳이다.**
- `teacher_profiles` 행의 "담는 개인정보"에 **내선번호(선택 입력)** 추가.
- 위 **원칙 두 번째 줄**("주민등록번호, 전화번호, 주소, 학부모 정보는 저장하지 않는다")이 그대로 두면 **거짓이 된다.** 내선은 개인 전화번호가 아니지만, 문장이 "전화번호"라고만 적혀 있어 실태와 어긋난다. → *"개인 연락처(휴대전화·자택 전화·주소)와 주민등록번호·학부모 정보는 저장하지 않는다. 학교가 부여한 내부 업무 연락처(내선번호)만 본인 또는 관리자가 직접 등록하는 선택 항목으로 저장한다"*로 정밀화한다.
- 같은 이유로 `/privacy` **2. 학생** 섹션 아래 목록의 *"주민등록번호, 전화번호, 주소, 학부모 정보는 수집·저장하지 않습니다"* 한 줄도 손봐야 한다. 이 줄은 학생 섹션 안에 있지만 **바로 아래 줄이 시스템 전체 얘기라 읽는 사람에게는 전체 선언으로 읽힌다.**

**② `POLICY_VERSION` 상향 — 하지 않는다.** 근거는 위 판정 기준표의 다섯 번째 행이며, 구체적으로:
- **수집 방식이 자발적 등록이다.** 본인이 「정보 수정 신청」에서 직접 적거나 관리자가 조직도에 등록한다. 시스템이 알아서 수집하지 않는다. 쪽지(2026-08-12 상향)와 결정적으로 다른 점이 이것이다 — 쪽지의 열람 시각은 **본인 의사와 무관하게** 기록됐고, 그래서 재고지가 정당했다.
- **열람 범위가 넓어지지 않는다.** 이름·부서·직책이 이미 전 교직원에게 보이는 조직도 행에 칸이 하나 붙는 것이고, 내선번호는 애초에 교무실 전화 회선이라 학교 안에서 이미 공개된 정보다.
- **2026-08-12에 이미 한 번 띄웠다.** 방학 중이라 아직 그 모달을 못 본 사람이 많다 — 지금 문안을 고쳐 두면 **그 사람들은 갱신된 문안을 처음부터 보게 된다.** 오히려 지금이 문안만 고치기에 가장 좋은 시점이다.
- 이 판단은 법률 자문이 아니라, **이 시스템이 '동의'가 아니라 '고지' 구조**라는 기존 전제(2026-08-05 "동의 간주 문구 불채택" 결정) 위에서 내린 것이다.

**③ 상향하지 않는 대신 반드시 딸려야 하는 것 (셋 다 이행해야 ② 판단이 성립한다)**
- **입력란 옆 고지 한 줄** — *"내선번호는 조직도와 쪽지 화면에서 전 교직원에게 보입니다. 개인 휴대전화 번호는 적지 마세요."* 조직도 편집(관리자)과 「내 정보 관리」(본인) **양쪽 모두**.
- **휴대전화 오입력 관문** — `010`·`011`류로 시작하거나 숫자가 9자리 이상이면 경고. 차단이 아니라 경고(형식을 강제하지 않기로 한 §11-6 결정 유지). ~~승인 화면에서 수퍼어드민에게도~~ → **개정(2026-08-13, 2계층 저장 채택)**: 내선번호는 승인 없이 즉시 저장되는 필드가 됐으므로 **사람 관문(승인)이 사라졌다.** 그 자리를 **저장 전 확인 1회**("개인 휴대전화 번호로 보입니다. 그래도 저장할까요?")가 대신한다. 자동 저장 전환은 ② 판단을 흔들지 않는다 — 오히려 *본인이 직접, 본인 계정으로만* 쓸 수 있게 규칙이 좁아져(필드 한정 자기 업데이트) 자발 입력 성격이 더 명확해졌다.
- **`/privacy` 문안 갱신을 필드가 화면에 뜨는 것과 같은 배포에 싣는다.** 2026-08-12 쪽지 때 세운 규칙 그대로 — 고지 없이 항목이 늘어난 기간을 만들지 않는다.

이 세 가지를 빼고 필드만 넣으면 ②의 근거가 무너지므로, 그때는 `POLICY_VERSION`을 올려야 한다.

## 열려 있는 결정 사항

- (2026-08-05 해소) `graduation_consents` 파기 주기 → 3년, `audit_logs` 보존 상한 → 5년으로 확정.
  집행 크론은 [`docs/phase11_privacy_spec.md`](./docs/phase11_privacy_spec.md) 산출물 E 참조.
- (2026-08-13 판단 및 ✅ **이행 완료**) `teacher_profiles.extension`(내선번호) → 위 "결정 기록" 절 참조. **인벤토리 2곳·`/privacy` 2곳 갱신을 필드 배포와 같은 커밋에** 이행.

- 새 컬렉션에 개인정보를 저장하게 되면 이 표 갱신 → Phase 11 문안 파생 갱신. **컬렉션이 아니라 기존 컬렉션에 필드가 하나 느는 경우도 같다** — "새 컬렉션"만 보면 놓친다(내선번호 건에서 확인).
- ~~(2026-08-11) `memos` 등재에 따른 고지 문안(/privacy)·`POLICY_VERSION` 갱신은 **쪽지 화면 공개 시점에** 일괄 수행~~ → **✅ 2026-08-12 이행**: 쪽지 화면 배포와 **같은 커밋**에서 `/privacy` 교직원 표에 쪽지 항목 추가 + `POLICY_VERSION` `2026-08.1` → `2026-08.2`(시행일 2026년 8월 12일). 전 교직원에게 재고지 화면이 1회 노출된다.
  - 문안에 **"받는 분에게는 함께 받은 분 목록과 각자의 읽은 시각이 보인다"**를 명시했다 — 스펙 §1이 수용한 트레이드오프(수신자가 문서를 직독하므로 서로의 읽음 상태가 보인다)는 이용자에게 고지되어야 하는 사실이다.
  - **교훈**: 이렇게 "나중 시점에 하라"고 예약한 항목은 그 시점이 와도 스스로 울리지 않는다. 이번엔 표적 리뷰 중 인벤토리를 훑다가 발견했다. 배포 전 점검은 스펙의 운영 액션 목록만 보지 말고 인벤토리·체크리스트까지 볼 것.

## 개인정보가 있는 곳 — 데이터베이스 밖 (2026-08-14 신설)

인벤토리가 Firestore 컬렉션만 다루면 **"우리 개인정보는 여기 다 있다"가 거짓이 된다.** DB 밖 사본도 같은 표에 있어야 한다.

### 로컬 백업 아카이브 (2026-08-14 신설, [`docs/backup_restore_spec.md`](./docs/backup_restore_spec.md))

| 항목 | 내용 |
|---|---|
| **무엇** | Firestore **전량** JSONL 사본. 위 표의 모든 개인정보가 **평문으로** 들어간다 — 생활지도 기록, 졸업 동의 서명, 쪽지 본문, 교직원 프로필·내선번호 포함 |
| **어디** | `~/school-backups/` — **저장소 밖**(안에 두면 언젠가 커밋된다). `.gitignore`에도 `school-backups/`·`*.jsonl` 방어선. 파일 권한 `0600`, 디렉터리 `0700` |
| **누가 만드나** | 사람이 수동(`npm run backup`, 월 1회 + 학기 경계) + 파괴적 스크립트가 실행 직전 자동(부분) |
| **보관 기한** | **최근 3개 + 학기 경계본 1개.** 나머지는 지운다 — 오래된 백업은 유출 위험만 늘고 복구 가치는 떨어진다 |
| **반출** | **금지.** 클라우드·메일·메신저로 옮기지 않는다. 남에게 전달할 일이 생기면 그 자체를 개인정보 제공으로 보고 판단한다 |
| **암호화** | 하지 않는다 — 로컬 밖으로 안 나간다는 규칙이 먼저이고, 키를 잃으면 복구 불가라는 더 큰 위험이 생긴다(스펙 §6). **반출이 필요해지는 순간 이 결정을 다시 본다** |

> 이 항목이 늘어난 것은 새 기능이 아니라 **기존 데이터의 사본이 생긴 것**이므로 `/privacy` 문안·`POLICY_VERSION` 갱신 대상은 아니다(이용자에게 제공되는 서비스가 달라지지 않는다). 다만 **보관 기한·반출 금지를 어기면 그때는 고지 대상이 된다.**
