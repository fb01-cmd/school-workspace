# 효명고등학교 관리 시스템 — Claude 안내

## ⚠️ 이 파일에는 규칙 본문을 두지 않는다

모든 공통 규칙의 단일 원본은 저장소 루트의 **[`AGENTS.md`](./AGENTS.md)** 다. 이 파일은 **가리키기만 한다.**

> **왜 이 원칙이 다른 문서보다 엄격한가 (2026-08-20 실사고 후 명문화)**
>
> 이 파일은 **세션마다 자동으로 주입되고**, `AGENTS.md`는 **열어야 보인다.** 그래서 둘이 어긋나면 **자동으로 주입된 쪽이 이긴다 — 그게 낡은 쪽이어도.**
>
> 실제로 그렇게 됐다. 2026-08-20에 `AGENTS.md` ⑧이 개정됐는데 이 파일의 대응 줄은 안 고쳐졌고, **Claude 두 세션이 각자 독립적으로 낡은 줄을 따랐다.** 사용자가 에이전트 사이의 전달자 노릇을 하게 된 원인이다.
>
> 즉 **"단일 원본은 AGENTS.md"라는 선언만으로는 부족하다.** 규칙 본문이 이 파일에 한 줄이라도 남아 있으면, 그 줄이 사실상의 단일 원본이 된다. 이건 규율 문제가 아니라 **주입 순서 문제**라 규율로 못 고친다. **그래서 본문을 아예 두지 않는다.**

## 세션 시작 순서

**전체를 읽는 것은 두 개뿐이다** (근거: `AGENTS.md`의 「세션 시작 시 필수 읽기」).

1. **[`STATUS.md`](./STATUS.md)** — 지금 열린 항목 대장. "무엇이 남았나"는 여기서만 판단한다
2. **[`AGENTS.md`](./AGENTS.md)** — 규칙

[`development_roadmap.md`](./development_roadmap.md)와 [`project_notes.md`](./project_notes.md)는 **필수 읽기가 아니다.** 수십~수백 KB이므로 **날짜·키워드·§ 단위로 검색해 필요한 부분만** 읽는다.

## 협업 — 상세는 전부 `AGENTS.md`

- 분업(누가 무엇을) → `AGENTS.md` §1·§1-2·§1-3
- 남의 점검 결과를 받았을 때 → `AGENTS.md` §1-1
- 완료 정의·핸드오버·커밋 → `AGENTS.md` ①~④
- **작업을 마칠 때의 의무 출력** → `AGENTS.md` ⑧
- 모델 선택 → `AGENTS.md` §4

**CLI 호출과 다른 세션과의 연락은 Claude가 직접 한다. 사용자를 전달자로 쓰지 않는다.** 다른 에이전트에게 넘길 일이 있으면 Claude가 그 도구를 직접 부르고, 붙여넣을 프롬프트를 사용자에게 떠넘기지 않는다. (자세한 조건·권한·현재 사용 가능한 통로는 `AGENTS.md` §1-3)

## 사용자에게 답할 때

- **존댓말로 통일한다.** 여러 세션이 동시에 돌기 때문에 어투가 갈리면 누가 말하는지 헷갈린다 (2026-08-20 사용자 지적).
- 사용자는 비전문가다. 답변 맨 위에 **「알아야 할 것」 3줄 이내**를 전문용어 없이 두고, 경로·해시·식별자는 그 아래로 내린다. 사용자가 결정할 것이 있으면 그 3줄 안에 있어야 한다 (`AGENTS.md` ⑨-8).
- 질문을 받으면 **답을 첫 줄에** 쓴다. 과정과 조치는 그 뒤다 (`AGENTS.md` ⑨ 계열).
