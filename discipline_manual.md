# 생활지도 모듈 사용 설명서

> 대상 독자: 학생부 담당 교사, 담임 교사, 관리자. 전문 지식 불필요.
> 화면 위치: 관리자 페이지(`/admin`) → 왼쪽 사이드바 **"학생 생활지도" → 생활지도 종합 관리**
> 작성: 2026-07-25 Claude (Phase 6b 구현 직후). 시스템이 바뀌면 이 문서도 갱신할 것.

---

## 1. 한눈에 보는 개념

이 모듈은 종이/시트로 하던 생활지도 누적 관리를 그대로 옮긴 것입니다. 흐름은 다섯 단어로 요약됩니다:

```
항목 → 기록 → 회차 → 규칙 → 단계
```

| 용어 | 뜻 | 예시 |
|---|---|---|
| **항목** | 지도 사유의 종류 | 생활지도(교복), 흡연, 휴대폰 |
| **기록** | "언제, 누가, 무슨 항목으로" 1건의 적발 | 6/26 사복 착용(1교시) |
| **회차** | 같은 항목이 몇 번째 쌓였는지 (자동 집계) | 교복 2회차 |
| **규칙** | "n회차가 되면 어느 단계로 올라가는가" | 교복 2회차 → 생활지도교사 |
| **단계** | 처리 주체/수위 | 담임 → 생활지도교사 → 1단계 → 생활교육위원회 |

**핵심 원리**: 학생의 "현재 단계"는 어딘가에 저장된 값이 아니라, **기록들을 규칙에 대입해 그때그때 계산**됩니다. 그래서 기록을 무효화하면 단계가 자동으로 내려가고, 규정(규칙)을 바꾸면 모든 학생의 단계가 새 규정으로 즉시 재계산됩니다.

---

## 2. 누가 무엇을 할 수 있나 (권한 체계)

권한은 5종류입니다:

| 권한 | 할 수 있는 일 |
|---|---|
| `view` (열람) | 기록·현황·처리 이력 보기 |
| `record` (기록) | 기록 입력, 자기가 쓴 기록 무효화 |
| `resolve` (단계처리) | 처리함에서 조치 입력, 수동 단계 지정 |
| `manage_rules` (규정관리) | 규정 편집, 학년별 회차 리셋, 모든 기록 무효화 |
| `manage_permissions` (권한관리) | 권한 부여/회수, 담임 배정표 편집 |

권한이 주어지는 경로는 3가지이고, 서버가 매 요청마다 아래 순서로 판정합니다:

1. **학생 계정** → 무조건 차단 (어떤 경우에도 접근 불가)
2. **수퍼어드민**(admin 계정) → 모든 권한
3. **개별 부여(Grant)** → "권한 관리" 탭에서 지정한 대로 (예: 학년부장에게 1학년 전체 view+resolve)
4. **담임 기본권** → **승인된 교직원 프로필(조직도)에 담임으로 등록된** 교사는 **자기 반에 한해** 자동으로 view+record
5. **열람 설정** → 규정 편집기에서 "담임의 타반 열람 허용"을 켜면 담임은 다른 반도 열람(입력은 불가)

> 💡 즉 일반 교사는 ① 프로필에 담임으로 승인돼 있거나 ② Grant를 받아야 이 메뉴에서 뭔가 볼 수 있습니다. 아무것도 없으면 "권한 없음" 안내가 뜹니다 — 고장이 아니라 정상 동작입니다.
>
> ⚠️ 메뉴의 탭은 권한에 따라 보이고/숨겨지지만, 이것은 편의일 뿐입니다. 실제 차단은 항상 서버에서 이루어지므로 화면을 우회해도 데이터에 접근할 수 없습니다.

---

## 3. 메뉴(탭)별 사용법

### 📝 기록 입력 — 현장 적발을 기록 (record 권한)

1. **대상 학생 검색**: 이름이나 학번 입력 → 자동완성 목록에서 선택 (반드시 목록에서 학생 계정을 선택해야 함)
2. **지도 항목 선택**: 카드 중 하나 클릭 (규정 편집기에서 활성화된 항목만 보임)
3. **발생 일시**: 기본값은 지금. 나중에 입력하는 경우 실제 발생 시각으로 수정
4. **비고**: "1교시 사복 착용" 같은 구체적 상황 (권장)
5. 등록 → 이 기록으로 학생이 새 단계에 도달하면 **"단계 도달" 안내가 뜨고 처리함에 자동으로 사안이 생성**됩니다.

> 모바일(폰)에서도 동작하도록 만들어져 있어 현장에서 바로 입력할 수 있습니다.

### 📊 지도 현황 — 학생별 누적 상태 (view 권한)

- 학년/반 필터로 조회. **"전체"로 두면 자기 권한 범위만 자동으로 보입니다** (담임이면 자기 반만 — 오류가 아님)
- 학생 카드에 현재 단계 배지(빨강=단계 도달, 초록=정상)와 유효 기록 수 표시
- 카드 클릭 → 기록 타임라인. 여기서 **무효화** 가능:
  - 무효화 = 삭제가 아니라 "회차 계산에서 제외 + [무효화됨] 표시로 보존". 사유 입력 필수
  - 무효화 버튼은 **그 기록을 쓴 본인** 또는 **규정관리 권한자**에게만 보임

### 📥 단계 처리함 — 단계 도달 사안 처리 (resolve 권한, 주로 학년부장·학생부)

- **미처리 사안**: 규칙에 의해 새 단계에 도달한 학생들이 자동으로 쌓이는 대기열
- 각 사안에 **"조치 완료 처리"** → 실제 취한 조치 내용(학부모 상담, 위원회 상정 등)을 기록 → 완료 탭으로 이동
- **수동 단계 지정**: 회차 계산과 무관하게 특정 학생을 특정 단계로 직권 지정 (사유 필수)
  - 수동 지정은 자동 계산보다 **우선 적용**됩니다. 단, 지정 이후에 새 기록이 쌓여 더 높은 단계가 계산되면 다시 올라갑니다 (수동으로 낮춰줬는데 재발한 경우 대응)

### ⚙️ 규정 편집기 — 학교 규정을 데이터로 관리 (manage_rules 권한, 학생부)

규정은 코드에 박혀 있지 않고 여기서 편집하는 데이터입니다. 매년/수시로 바뀌어도 개발 없이 반영됩니다.

1. **지도 항목**: 항목 추가/이름·분류 수정/활성 체크. 항목을 없애고 싶으면 삭제 대신 **비활성**(과거 기록 표시는 유지되고 새 입력만 막힘)
2. **지도 단계**: 단계 이름과 순서(숫자). **순서가 클수록 상위 단계**
3. **자동 규칙**: "[항목 또는 카테고리] 누적 n회 → [단계]" 매핑. 현행 초기값:
   - 교복: 1회=담임, 2회=생활지도교사, 3회=생활교육위원회
   - 흡연·휴대폰: 1회=1단계, 2회=생활교육위원회
4. **열람 정책**: 담임의 타반 열람 허용 여부
5. **학년별 회차 리셋**: 새 학기 등에 그 학년의 회차 집계를 0부터 다시 시작
   - **기록은 절대 삭제되지 않습니다.** "이 시점 이후 기록만 세라"는 기준점만 갱신됩니다
   - 미저장 변경사항이 있는 상태에서 리셋을 누르면 자동으로 저장부터 하고 리셋합니다

> ⚠️ 규정을 바꾸면 **기존 기록에도 소급 재계산**됩니다 (예: "흡연 2회→위원회"를 "3회→위원회"로 바꾸면, 이미 2회인 학생의 단계도 즉시 내려감). 학기 중 규칙 변경은 신중하게.

### 🔑 권한 관리 — Grant 부여/회수 (manage_permissions 권한)

담임 기본권 외의 모든 권한은 여기서 부여합니다. 부여 시 3가지를 정합니다:

- **대상 교사** (자동완성 검색)
- **범위(Scope)**: 학교 전체 / 특정 학년 / 특정 반
- **권한(Rights)**: 위 5종 중 복수 선택
- **만료일**(선택): 학년도 말로 지정해 두면 자동 만료 — 연말 권한 회수 누락 방지

전형적인 부여 예:
| 대상 | 범위 | 권한 |
|---|---|---|
| 1학년부장 | 1학년 | view, resolve |
| 학생부 계원 | 전체 | view, record, resolve |
| 학생부장 | 전체 | 전부 (manage_rules, manage_permissions 포함) |

### 🏫 담임 현황 — 읽기 전용 (manage_permissions 권한)

**담임 정보는 여기서 편집하지 않습니다.** 이 플랫폼의 담임 단일 원본은 교직원 조직도(승인된 프로필)이고, 이 탭은 그걸 학년·반 순으로 모아 보여주는 현황판입니다.

- **담임을 바꾸려면**: 해당 교사가 프로필 카드의 **[조직 정보 신청]**에서 담임 학년·반을 제출 → 수퍼어드민이 **[프로필 승인 대기]**에서 승인. 승인되는 즉시 이 현황과 생활지도 권한에 자동 반영됩니다.
- 한 반에 두 명이 승인되면 **공동담임**으로 표시되고 둘 다 자기 반 권한을 갖습니다.
- 이렇게 하는 이유: 같은 정보(담임)를 두 곳에서 따로 관리하면 언젠가 어긋나고, 어긋난 쪽이 권한 판정에 쓰이면 사고가 되기 때문입니다. 조직도 하나만 최신으로 유지하면 명렬표·생활지도가 함께 맞습니다.

---

## 4. 신학년 세팅 순서 (권장)

1. **담임 프로필 갱신**: 새 담임 선생님들이 [조직 정보 신청]에서 담임 반을 제출하도록 안내하고, [프로필 승인 대기]에서 일괄 승인 (담임 현황 탭에서 빠진 반 확인)
2. **규정 편집기**에서 올해 규정 확인·수정 후 저장
3. **권한 관리**에서 학년부장·학생부 Grant 부여 (만료일 = 학년도 말 권장)
4. 필요 시 **학년별 회차 리셋** 실행 (진급 학년의 작년 회차를 0으로)

---

## 5. 자주 묻는 질문

**Q. 기록을 잘못 입력했어요. 삭제는 어디서 하나요?**
삭제 기능은 일부러 없습니다(징계성 데이터의 무결성·분쟁 대비). 대신 **무효화**를 쓰세요 — 회차 계산에서 빠지고, 무효화 사실·사유·처리자가 함께 남습니다.

**Q. 학생이나 학부모가 이 기록을 볼 수 있나요?**
아니요. 학생 계정은 서버에서 전면 차단되며, DB 직접 접근도 보안 규칙으로 막혀 있습니다. 교직원 중에서도 위 권한 체계를 통과한 사람만 봅니다.

**Q. 졸업하면 기록은 어떻게 되나요?**
졸업/제적으로 계정이 영구 삭제되는 시점에 **그 학생의 생활지도 기록도 자동 파기**됩니다(개인정보 보호 정책). 감사 로그에는 "파기했다"는 사실과 건수만 남고 내용은 남지 않습니다.

**Q. 담임인데 "전체 학년"으로 조회해도 우리 반만 나와요.**
정상입니다. 조회 범위는 항상 "요청한 범위 중 내 권한이 닿는 부분"으로 자동 축소됩니다.

**Q. 담임인데 "권한 없음"이 떠요.**
프로필이 아직 승인 전일 가능성이 큽니다. [조직 정보 신청]에서 담임 반을 제출했는지, 수퍼어드민이 승인했는지 확인하세요. 승인된 순간 자동으로 열립니다.

**Q. 리셋을 누르면 작년 기록이 사라지나요?**
아니요. 기록은 전부 보존되고, 회차 **집계의 기준 시점**만 리셋 시각으로 바뀝니다. 학생 상세 타임라인에서는 과거 기록도 계속 볼 수 있습니다.

**Q. 규칙에 없는 특수한 사안은요?**
처리함의 **수동 단계 지정**으로 직권 처리하세요(사유 기록 필수).

**Q. 누가 뭘 했는지 확인하고 싶어요.**
모든 기록 입력·무효화·단계 처리·권한 부여/회수·리셋·파기는 **작업 감사 로그**(사이드바 "추가 도구")에 남습니다. 각 로그에는 허용 근거(어떤 권한으로 실행했는지)까지 기록됩니다.

---

## 6. 작동 원리 요약 (관심 있는 분만)

- 현재 단계 = (마지막 리셋 이후 + 무효화 제외) 기록을 항목/카테고리별로 세서, 충족된 규칙들 중 **가장 높은 순서의 단계**. 수동 지정이 있으면 그것이 우선하되, 이후 새 기록으로 더 높은 단계가 계산되면 상위 단계 적용.
- 학번(5자리)이 학년·반의 근거입니다. 화면이 보내는 학년·반 값은 서버가 믿지 않고 학번에서 다시 계산합니다.
- 데이터는 모두 플랫폼 DB(Firestore)에 저장되며 클라이언트 직접 접근은 전면 차단, 서버 API만 통합니다. 상세 설계: `phase6_spec.md`, 데이터 목록: `personal_data_inventory.md`.
