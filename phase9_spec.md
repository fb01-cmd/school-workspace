# Phase 9 스펙 — 시간표·결보강 시스템 (컴시간 완전 대체)

> 작성: 2026-07-26 Claude (아키텍처/스펙 판단). 입력: `comcigan_analysis.md`(컴시간 설명서 2부 분석) + 사용자 확정 답변.
> 로드맵의 Phase 9를 Phase 7·8보다 앞당겨 착수한다 (사용자 우선순위).

## 0. 확정된 전제 (2026-07-26 사용자 답변)

1. **목표 = 컴시간을 아예 안 쓰는 것.** 기능을 최대한 이 플랫폼에 구현.
2. 사용자별 핵심 가치: **일과계(실무사)** = 시간표 작성 + 결보강 승인 / **일반 교사** = 교체수업 후보 원클릭 탐색 + 신청 / **학생** = 시간표 열람(현재 컴시간알리미 사용 중).
3. 컴시간의 고통: 비용(매년 인상), 학교 내 서버 PC 상주, 교외에서는 안드로이드 앱 열람만 가능.
4. NEIS 입력: 일과계 실무사가 교체 승인 후 거의 실시간으로 수기 반영 → **NEIS 연동은 자동화 대상이 아니라 "입력용 목록 제공"이 경계.**
5. 전 교사 필수 사용 중. 고교라 수동 교체수업 편성이 매우 어려움 → 후보 탐색 자동화가 최대 체감 가치.

## 1. 대전제 (아키텍처 결정)

- **① 대체 순서는 "매일 쓰는 것부터".** 열람(알리미) → 결보강(일과진행) → 자동 작성(주간시간표) 순. 자동 작성 엔진이 가장 어렵고, 앞 두 단계만으로도 서버 PC·알리미·일일 고통이 사라진다.
- **② 킬 스위치 = 컴시간 계약 갱신일.** 9c(자동 작성)까지 실사용 검증이 끝난 학기에 계약을 끊는다. 그 전까지 기초시간표는 컴시간(잔여 계약)으로 만들고 플랫폼으로 **가져와서** 쓴다.
- **③ 시간표 데이터 구조 = "기초시간표 + 주 단위 변경 오버레이".** 컴시간이 검증한 모델을 그대로 계승: 학기 기초시간표는 불변 원본, 매주의 결보강·교환은 그 위에 쌓이는 변경 레이어. 어떤 주의 실제 시간표 = 기초 + 그 주의 변경들. (생활지도의 "저장하지 않고 계산" 원칙과 동일 계열)
- **④ 자동 작성 솔버(CSP)는 일과계의 브라우저에서 실행한다.** Vercel 서버리스는 실행 시간 제한이 있어 수 분짜리 탐색에 부적합. 연 2회, 1명이 쓰는 기능이므로 웹워커(브라우저 백그라운드)에서 돌리고 결과만 서버에 저장 — 서버 비용 0, 시간 제한 없음, "파일은 보관하지 않는다" 원칙과도 충돌 없음.
- **⑤ 모든 접근은 기존 패턴대로**: API 라우트 + admin SDK + 서버 사이드 권한 판정. 학생은 열람 전용 API만. Firestore 클라이언트 직접 접근 금지.
- **⑥ 명단·교사 데이터는 기존 플랫폼 자산 재사용**: 교사 목록(GWS 디렉터리·프로필), 학급 목록(학교 설정), 알림(구글 챗 `sendGoogleChat`, 발신자 규칙 준수). 컴시간처럼 별도 등록부를 수동 관리하지 않는다.

## 2. 단계 구성

### Phase 9a — 시간표 도입 + 전 구성원 열람 (알리미 대체)

**목표**: 컴시간알리미 없이 웹(모바일 포함)에서 교사·학생이 시간표를 본다. 교외 접근 문제 해소.

- **9a-1. 데이터 모델 + 기초시간표 가져오기**
  - 입력 경로: **교사별 시수표 엑셀(컴시간 양식 호환)** + 컴시간 "엑셀로 인쇄"의 전체시간표 파일 파싱. 첫해는 컴시간이 만든 완성 시간표를 역파싱해 들여온다 (기존 UserSheetEditor의 웹 시트 복붙 패턴 재사용).
  - 검증: 시수표 합계 vs 배정 합계 대조, 중복(같은 교시 교사/학급 이중 배정) 검사 리포트.
- **9a-2. 열람 화면**
  - 교사: 내 주간시간표(변경분 강조 표시 — 컴시간의 빨간색 관례 계승), 다른 교사·학급 시간표 조회, 교시별 공강 교사 조회.
  - 학생: 학생 포털에 자기 반 시간표 (학번에서 반 도출 — 생활지도와 동일 패턴).
  - 전자칠판/인쇄용 학급 시간표 뷰 (로드맵 아이디어 흡수).

### Phase 9b — 결보강 워크플로 (일과진행 대체, 최대 체감 가치)

**목표**: 교사 원클릭 신청 → 일과계 승인 → 시간표 반영 → 알림 → NEIS 입력용 목록.

- **9b-1. 주 단위 운영**: 주 날짜 등록(휴업일 지정), 기초시간표 위 변경 오버레이, 정기고사 모드.
- **9b-2. 교체 후보 자동 탐색 (핵심 엔진)**: 교사가 "이 교시 수업 못 함"을 지정하면 —
  - 후보 A: **맞교환** — 상대 교사가 그 교시에 공강이고, 나도 상대의 어떤 교시에 공강인 쌍을 전수 탐색해 점수순 제시 (같은 학급 동일 요일 중복, 하루 시수 쏠림, 연속수업 파괴 등 컴시간의 검색조건을 감점 요소로).
  - 후보 B: **특별보강** — 교환 없이 대신 들어갈 공강 교사 목록 (시수누계 적은 순 제시 = 보강 부담 공평화).
  - 수학여행 등 대량 결강용 일괄 배정 도우미.
- **9b-3. 신청→승인 워크플로**: 교사가 후보 선택 → 신청 → 일과계 승인/반려(사유) → 승인 시 해당 주 시간표에 반영 + 관련 교사에게 구글 챗 알림 + 감사 로그. (컴시간 "수업교환 요청대장"의 대체)
- **9b-4. 운영 도구**: 시수누계/남은 시수 집계(교사·과목별), **NEIS 입력용 수업교환 목록**(기간 지정 조회·인쇄 — 실무사의 현행 실시간 입력 흐름 유지), 결재용 일람표 출력.

### Phase 9c — 자동 시간표 작성 (주간시간표 대체, 마지막)

**목표**: 학기 초 기초시간표를 플랫폼에서 작성 → 컴시간 계약 종료.

- **9c-1. 제약 입력 UI**: 교사별 시수표(엑셀 가져오기 + 웹 시트 편집), 이동/배정금지, 일괄배정(선택교육과정·재량활동), 연속수업, 동시수업(수준별·가상학급), 특별실, 복수교사 — `comcigan_analysis.md` §2의 제약 전 항목.
- **9c-2. 솔버 (브라우저 웹워커)**: 제약 충족 + 품질 점수(중복·하루 3과목·요일 균형·순배·연속3·점심 전후·오전오후) 최적화. 백트래킹 + 국소 탐색(담금질) 조합. 중간 결과 저장·재개, 여러 안 저장 후 비교 선택 (컴시간 "작업저장/작업열람" 대응).
- **9c-3. 수동 조정 UX**: 연쇄이동 미리보기(교시를 옮기면 밀려나는 교사들 시간표를 먼저 보여주고 확정) — 컴시간에서 가장 잘 만든 UX이므로 그대로 계승.
- 검증 기준: 실제 본교 데이터로 컴시간 결과와 나란히 비교, 일과계 승인 후 한 학기 병행 운영.

## 3. 데이터 모델 (Firestore, admin SDK 전용 — 초안)

```
timetable_settings/{domain}            ← 교시 수, 일과시간, 학년·반 구성, 현재 학기
timetable_subjects/{domain}/subjects   ← 과목 (정식명·단축명·복수교사 여부)
timetable_base/{domain}/entries        ← 기초시간표 배정 1건 = {teacherEmail, grade, classNum,
                                          subjectId, day, period, room?, flags(연속·동시·특별실·금지)}
timetable_weeks/{domain}/weeks/{weekId} ← 주 등록 {startDate, holidays[], examMode}
timetable_changes/{domain}/changes     ← 변경 오버레이 1건 = {weekId, type(swap|substitute|move|cancel),
                                          before{...}, after{...}, requestId?, appliedAt, appliedBy}
swap_requests/{domain}/requests        ← 교체 신청 {requester, absence{day,period,...}, candidate{...},
                                          type, status(PENDING|APPROVED|REJECTED), reason, decidedBy, decidedAt}
```

- 시수누계·공강·현재 주 시간표는 저장하지 않고 계산 (기초+오버레이 합성).
- 변경 이력은 불변(취소도 역방향 변경으로 기록) — 감사 추적 + NEIS 목록의 원천.
- 권한: 일과계 역할은 생활지도처럼 **grant 방식**(`manage_timetable`) — 실무사 계정에 부여. 교사는 자기 신청 CRUD + 전체 열람. 학생은 자기 반 열람 전용 API.

## 4. 명시적 범위 제외

- NEIS 자동 입력(API 없음 — 목록 제공까지만), 컴시간 데이터 실시간 연동(불가능 — 파일 가져오기만), 타교 겸임 교사 등 특수 케이스 1차 제외, 학부모 열람.

## 5. 구현 순서와 분업 (예정)

| 순서 | 작업 | 담당 |
|---|---|---|
| 1 | 9a-1 데이터 모델·권한 판정·가져오기 파서 스펙 확정 | Claude(스펙·모델) → Antigravity(파서·웹시트 구현) |
| 2 | 9a-2 열람 화면 3종 | Antigravity |
| 3 | 9b-2 후보 탐색 엔진 + 9b-3 승인 워크플로 서버 | **Claude 직접** (판정·데이터 무결성 핵심부) |
| 4 | 9b 화면(신청·승인·시수·NEIS 목록) | Antigravity |
| 5 | 9c 솔버 | Claude(알고리즘) + Antigravity(제약 입력 UI) |

## 6. 미결정 사항 (사용자 답변 필요)

1. 컴시간 **계약 만료일** — 9c 완료 목표일의 기준점. (킬 스위치 날짜)
2. 첫 도입 시점: 지금 학기 중에 9a(열람)부터 병행 도입? 아니면 새 학기 시작에 맞춰 일괄?
3. 교체 신청 승인 권한: 실무사 1인 grant로 충분한지, 교감 결재 단계가 필요한지 (결재용 일람표 인쇄로 갈음 가능한지).
4. 학생 열람 범위: 자기 반만 vs 전체 학급 (컴시간알리미는 어디까지 보여주고 있는지).
5. 현행 시간표 데이터 샘플: 컴시간 "엑셀로 인쇄" 전체시간표 파일 + 교사별 시수표 엑셀 1부씩 확보 부탁 (9a-1 파서의 실데이터 기준).
