# 📋 STATUS — 지금 열린 항목 대장 (단일 원본)

> **이 문서의 규칙 (AGENTS.md ④-3)**
> 1. **여기만 덮어쓴다.** append-only 예외다. **낡은 줄은 버그다** — 항목이 닫히면 그 행을 지운다(이력은 일지에 있다).
> 2. **"지금 무엇이 열려 있나"는 여기서만 판단한다.** 로드맵·노트의 "대기/잔여"는 작성 시점 기록이라 낡았을 수 있다.
> 3. **완료·착수·결정이 생기면 같은 커밋에서 갱신한다.** 행마다 **확인 방법**이 필수다 — 못 적으면 항목이 아니라 소망이다. 확인 방법은 **코드·실데이터 우선**.
> 4. 상세는 여기 쓰지 않는다 — 날짜+엔트리 제목으로 일지를 가리킨다.
>
> **[2026-08-20 02:40 전면 정리]** 완료된 12행을 지웠다(허브 배치 1·2·작성 폼 대조·실기기 확인 대부분·CLI 도입·협업 규칙 초안·로드맵 다이어트 2차·문서 회전 등). 남은 것만 아래에 둔다.

## ⏳ 8/21까지 — Claude 용량이 줄기 전에 (8/22 Max5 전환)

**기준: 「나중에 못 바꾸는 것」과 「Claude 아니면 안 되는 것」만 여기 둔다** (④-⑩·§1).

**2026-08-20 현재 비어 있다.** 마지막 두 건이 같은 날 닫혔다 — 쪽지 레버 ②' 스펙(`bf58c39`, 구현 재판정은 11월)과 수신 요약 문구(`50dd755`). 후자는 문구를 고르는 대신 **문장 저장을 그만두어** 결정 자체를 되돌릴 수 있게 만들었으므로 기한이 걸려 있지 않다.

> **비어 있다는 것이 곧 「할 일이 없다」는 뜻은 아니다.** 8/22 이후로 넘기면 비싸지거나 사라지는 항목이 더 있는지는 아래 세 표(사용자 판단 대기 · 날짜가 오면 확인 · 유령 방지)를 8/21에 한 번 더 훑어 판단한다.

> **✅ [2026-08-21 재훑기 완료 — 이 절은 닫힌다]** 위 지시대로 세 표를 훑었다. **결론: 8/21 안으로 당겨야 할 Claude 몫 작업은 없다.**
> - **9c(가장 유력했던 후보) — 아니었다.** 코드 실측 결과 4축이 전부 실재하고 백지 편성이 기준선을 이겼다(소프트 30 vs 39). 남은 Claude 몫은 **11월 실측 데이터가 있어야 하는 판단 2건**이라 지금 당길 수단이 없다. 실제 병목은 코드가 아니라 **NEIS 2·3학년 과목 등재명 데이터**였다(아래 표에 신규 등재). 잔여를 크게 보이게 한 것은 로드맵의 낡은 🔜 표기였고 같은 날 정정했다.
> - **나머지 표**: 사용자 판단 대기 2건은 눈·취향 결정이라 Claude 무관. 날짜표 9건 중 8건은 미래 데이터 의존. 유령 58건·경미 7건은 Antigravity 몫.
> - **당길 수 있는 유일한 항목** = 글씨 크기 2단계 다음 묶음 등재(아래 「결정 끝」 표). 실기기 통과를 선행조건으로 걸어 뒀으나, 다음 묶음(시간표·생활지도·수명주기)은 1묶음 결과와 무관하므로 **선행조건을 풀고 오늘 당겨 쓸 수 있다.**
> - **부수 발견**: 8/22 만료 규칙의 재질의가 어디에도 등재돼 있지 않았다 → 아래 표에 신규 등재.

## 사용자 판단 대기 (짧게 답하면 되는 것)

| 항목 | 내용 |
|---|---|
| 허브 소품 — 부분선택 막대 색 | 더 연하게 갈지. **실기기에서 눈으로 보고** 결정 — `docs/hub_batch1_directive.md` 「사용자 결정 대기」 A. (같은 표의 ⓑ 체크박스 히트영역 = **적용 결정 2026-08-20** → 아래 작업 큐로, ⓒ 내선번호 인라인 = **안 함 확정 2026-08-20**, 메뉴 이름 12-1 = 이미 반영됨 `page.tsx:978`) |
| 소품 우선순위 | TTL 콘솔 설정 · 프로필 승인 알림 배선 · 양해 왕복 실기기(상대 계정 필요) |
| 이연 대장 유령 3건 정정 | `deferred_backlog_2026-08-14.md`의 A1(백업 매뉴얼)·A2(이탈 경고)·A3(하드코딩)이 이미 해결돼 있음을 2026-08-21 코드로 확인 — 문서에 취소선 정정만 남음. "해" 한마디면 처리 |

## 결정 끝 — 작업 대기 (2026-08-20 사용자 결정)

| 항목 | 담당 | 확인 방법 |
|---|---|---|
| **글씨 크기 2단계 — 2묶음(생활지도·수명주기) 코드 측 종결, 화면 검증 대기** — 적용 `b81ee80`(생활지도 8파일)·`86805ae`(수명주기 13파일) + Claude 검수 보정 `8c8f145`(누락 입력 라벨 17곳). 기계 관문 4종 통과 | 남은 것: **360px(Claude — 로그인 필요, 아래 주 참조)** → 사용자(실기기) | 회귀 2종 = 9px이하 **0건** + 10px 파일이 **§7 목록 13개와 정확히 일치**. 둘 다 통과 |
| **화면 문구 다이어트 2차 — 시간표 폴더 45곳** — 1차(2026-08-21)에서 **의도적으로 뺐다.** 같은 날 창체·SLAT 축 설계(Fable)가 `src/components/admin/timetable/` 7파일을 동시에 고치고 있어 같은 파일을 건드리면 서로 덮어쓴다. 기준은 1차와 같다 — 제목·화면을 보면 아는 말은 삭제, 순서·자동 동작·되돌릴 수 없는 일은 존치. 목록 뽑는 법은 일지 2026-08-21 「화면 문구 다이어트」의 훑기 방법 | Claude(Sonnet) — Fable 작업이 커밋된 뒤 | `git status`에 timetable 미커밋 변경이 없는 것을 먼저 확인. 그 뒤 `npx tsc --noEmit` + `bash scripts/check_ui_removals.sh <시작커밋>`으로 **사라진 것이 전부 화면 문구인지**(핸들러·상태 0건) 확인 |
| **글씨 크기 2단계 — 3묶음(시간표) 범위 확정, 착수 대기** — 31파일/1,368곳. §3-1 격자 예외가 여기 몰려 있어(§7 13개 중 12개) **K-1(예외 없는 19파일/573곳) · K-2(예외 있는 12파일/795곳)**로 갈라 `NEXT.md`에 범위만 적어 뒀다. **과제 J의 Codex 검증 통과 후** Claude가 과제로 승격한다 | Claude(승격) → Antigravity(적용) | 위와 같음. K-2는 추가로 **격자 셀 안 10px 불변** + 잠정 12px 버튼 3건(`BaseRevisionTab` 내용 변경 · `DirectSubstituteTab` 가져오기 · `TeacherPortalSection` 취소) 14px 해소 |

## 창체·SLAT 고정 슬롯 — 설계 종결(2026-08-21 Fable), 구현 대기

설계 본문 = [`docs/fixed_slot_override_spec.md`](./docs/fixed_slot_override_spec.md). 결론 한 줄: 코호트 축은 그대로 두고 그 위에 **「○○학년도부터 바꾸기」 재정의 층**(학년별 전체 사본 + 작성 당시 교육과정 스탬프 + 지난 학년도 소급 금지)을 얹는다. 발견 경위·사용자 증언·정정 이력은 이 행의 2026-08-21자 이전 판(git)과 스펙 §0·§6에 있다.

| 항목 | 담당 | 확인 방법 |
|---|---|---|
| ✅ 구현 1·2 — 타입·해석 함수·검증·전개 확장 + 서버 API·자동 작성 배선 **(2026-08-21 Fable 완료)** | Claude | `npx tsx scripts/cohort_selftest.ts` **67/67 통과**(기존 35건 그대로 + 신규 32건 — §8 케이스 10종 포함) · tsc 0건 · build ✅ — 일지 2026-08-21 「창체·SLAT 학년도 재정의 층 구현 1·2」 |
| ✅ 구현 3 — 화면(해석 결과 보기·학년도별 변경 편집기) + `CurriculumCohortTab.tsx:22` 학년도 계산 교체 **(2026-08-21 Antigravity 완료)** | Antigravity | tsc 0건 · build 48/48 ✅ · `cohort_selftest` 67/67 ✅ + 화면에서 지난 학년도 변경 수정·삭제 버튼 비활성 확인 — 일지 2026-08-21 「과제 L 완결」 |
| ✅ 구현 4 — Codex 커밋 검증 **(2026-08-21 완료)** — 통과 13 · 실패 3(Claude 전건 재확인 → 전부 사실, 같은 날 수정: 문구 「코호트」 잔존 `9f7fd9d` · 비정규 학년 키 · 비활성 충돌 과잉거부 · 편집기 학년도 변경 시 미리 채움 미갱신) · 판정불가 1(자가 테스트 실행 — Claude가 70/70으로 해소) | Codex→Claude | 일지 2026-08-21 「과제 L 검증 종결」 · `cohort_selftest` 70/70 |
| **실기기 확인(사용자) — 푸시·배포 후**: 시간표 작성 → 창체·SLAT 배치 탭에서 ⓐ 맨 위 「지금은 어떻게 되나」에 학년별 격자·출처 표시 ⓑ 「+ ○○학년도부터 바꾸기」로 등록 → 저장 → 목록 카드 표시 ⓒ 적용 학년도를 지난 학년도로 입력하면 저장 거부 문구 ⓓ 기존 교육과정 카드·수정이 이전과 동일 | 사용자 | 위 ⓐ~ⓓ 눈으로 확인, 깨지는 곳은 화면 이름과 함께 신고 |

## 말로 묻는 시간표 해결사 — 스펙 종결(2026-08-21 Fable), 구현 대기

설계 본문 = [`docs/timetable_ask_fix_spec.md`](./docs/timetable_ask_fix_spec.md) (B6 「말로 선호도」는 §6으로 합류). 대원칙: AI는 질문 해석만, 수(手)는 엔진(체인 탐색 v3)이 만들고 검사기가 전수 채점.

| 항목 | 담당 | 확인 방법 |
|---|---|---|
| 구현 1·2·3 — 목표 어휘·체인 탐색·해석 프롬프트·S8 soft 금지 (스펙 §8) | Claude(Opus — 스펙 상세) | 스펙 §8 말미 자가 테스트 케이스 통과 + `fixfinder_selftest`·`ai_selftest` 회귀 0 |
| 구현 4 — 「물어보고 고치기」 카드 + E-4 제거 | Antigravity | tsc·build + 질문→해석 확인→계획→적용 왕복 |
| 구현 5 — S8 slotCost 실험 | Claude | **11월 리허설 전** — 백지 벤치마크 S4=0·기준선 이하 유지 시만 채택 (스펙 §6 관문) |
| 구현 6 — Codex 검증 | Codex | 스펙 §8 표 |

## 8/21 저녁 일괄분 — 배포 후 확인

| 항목 | 확인 방법 |
|---|---|
| **S7 순배** (감점 신설) | 자동 작성 → 감점 상세에 「순배」(0.5점) 항목 표시 + [해결안 찾기]에서 안이 나옴 |
| ✅ **보안 헤더** — **실서버 검증 완료 (2026-08-21 배포 직후 Claude 직접 확인)**: `portal.hmh.or.kr` 응답에 시행 4종 + CSP Report-Only 전부 존재, `/privacy`·`/login` 200, 보고 창구 204 | 사용자는 화면이 평소와 같은지만 (깨짐 없어야 정상 — CSP는 관찰 모드라 차단 0) |
| **쪽지 안읽음 배지** | 사이드바 「쪽지·업무」 옆 빨간 숫자(9+ 상한), 읽으면 줄어듦 |
| **업로드 상한** | 평소 파일은 그대로 통과 (10MB 초과 시에만 안내) |
| **개인정보 안내 5절** | "브라우저 보안 장치" 한 줄 추가 확인 |
| **CSP 시행 전환 판단** | Claude, **9월 초**(Codex 재점검과 같은 자리) — Vercel 로그의 `[csp-report]` 위반 보고를 보고 시행/유예 결정 |

## 결정 끝 — 학사일정 수정 시 기존 주 갱신 (2026-08-21 사용자 선택, 미착수)

| 항목 | 담당 | 확인 방법 |
|---|---|---|
| 학사일정을 고치면 **이미 만들어진 주(週)** 도 반영 (지금은 새로 만드는 주만) — backlog A10 | Claude(Opus) | 학사일정 수정 → 기존 주 문서의 휴업일·시수 반영 + 이미 배정된 보강과의 충돌 처리 방침 명시 |

## 결정 대기 — 실기기에서 나온 결함 (2026-08-21, 사용자 지시로 조치 보류·일괄 처리)

| 항목 | 진단 | 확인 방법 |
|---|---|---|
| 🟡 **고3의 「SL」은 SLAT이 아니다 — 등록부가 두 가지를 한 칸으로 뭉갠다** (2026-08-21 사용자 확인. **앞서 이 행을 ✅ 해소로 적었던 것은 틀렸다 — 그리드에 SL이 보인다는 이유만으로 판정했다**) | 사용자 원문: *"고3은 실제로 슬랫을 운영 안할거야. 저게 비어있는시간일거고, 집에 가든지 보충수업을 하든지 자습을 하든지 할건데, **교실별로 담당교사가 지정되어 시간표처럼 굴러가는 시간이 아니라서** 그냥 같이 뭉뚱그려서 SL로 표시해놨을거야. 그게 학교 입장에서 편하니까."* → 3학년 10반 그리드의 SL(수6·7)은 **표기상의 편의**이고 실제 수업이 아니다. 그런데 등록부는 `startAdmissionYear=2024`로 **고3까지 같은 교육과정**에 넣어 SLAT 슬롯을 준다. **의미가 다른 두 가지(실제 SLAT 수업 / 고3의 비어 있는 시간)를 같은 칸으로 표현하고 있다** | **기능상 지금은 같은 결과**다 — 어느 쪽이든 그 칸에 실수업이 안 들어간다. **깨지는 조건은 셋**: ⓐ 고3의 빈 시간을 고1·2의 SLAT과 **다른 요일·교시**로 옮길 때 ⓑ 고3 창체는 실제 운영인데(동아리 등) SL만 허구일 때 — **둘을 분리 못 한다** ⓒ 그 칸을 보충수업 등에 실제로 쓰려 할 때. **→ 설계는 끝났다(위 「창체·SLAT 고정 슬롯」 절) — 표현 수단은 스펙 §6-ⓑ가 두 가지로 마련했고(칸을 잡아 두되 이름 분리 / 칸을 비워 실사용), 어느 쪽으로 갈지가 이 행에 남은 사용자 결정이다.** 구현 완료가 선행 조건 |

## 실기기 확인 (사용자)

| 배포분 | 볼 것 |
|---|---|
| 실기기 4차~6차 일괄 수정 (최신 배포분) | ① 이름 뒤 영문 소멸 — 직권 탭·새로 남긴 감사 로그 (**`/m` 인사말은 2026-08-21 /m 폐지로 확인 불가 — 항목에서 뺀다**) ② 감사 로그 행위자 이름 통일(과거 행 포함, "이름없음" 소멸) ③ ~~`/m` 로그아웃 한 줄~~ (/m 폐지로 소멸) ④ 전출 표 이메일·"오후" 꺾임 해소 ⑤ 검색 드롭다운 "이름 (학번)" 표시(비번 초기화·그룹) ⑥ 홈 모바일 순서 할일→쪽지→시간표→급식 ⑦ 사이드바: 열면 본문 위에 겹침 + 배경 탭·메뉴 선택 시 닫힘 + 로고 탭 토글 ⑧ 상단 헤더 제목·권한 뱃지 겹침 해소(안내 pill은 폰에서 숨김) ⑨ 「내 조직 정보 신청」 모달 세로 줄·하단 잘림 해소 ⑩ 메뉴 제목 6종(북마크·비번 초기화·학기말 정리 등) 표시 ⑪ **쪽지·업무 모바일 개통** — 폰에서 열림 + 「수신자 선택」 버튼으로 조직도 전면 화면, 고르고 완료 → 발송까지. 본문이 360px에서 깨지는 곳은 그대로 신고 ⑫ 업무 상단 탭·칩 줄이 꺾이지 않고 옆으로 넘겨지는지 ⑬ 쪽지 목록 탭 → 내용이 전체 폭으로 열리고(머리줄 버튼은 아랫줄) X로 목록 복귀 ⑭ 업무·쪽지 전환 버튼 옆 설명 문구가 폰에서 사라짐 ⑮ 북마크 트리: 폰에서 수정·추가·삭제 버튼이 항상 보이고 폴더·링크 추가가 실제로 됨(순서 드래그는 폰 비지원 유지) |
| 글씨 크기 1단계+2단계 1묶음 (같은 배포분) | 쪽지·업무·조직도·모바일 업무에서 ⓐ 가로 스크롤 ⓑ 겹침 ⓒ 잘림 ⓓ 체감 개선. 완료 모달 열 때 이전 메모가 비워지는지(혼입 복원 확인) |

| **글씨 크기 2단계 2묶음 (생활지도·수명주기)** — 배포 `1a8bf6e`. ✅ **전입 처리 화면 통과**(2026-08-21 사용자: *"원래 목적인 글씨는 잘나와"*) · ⚠️ **졸업생 명단 표 깨짐 신고 → `c3f0639`로 조치**(재확인 필요) | **생활지도**: 학생 이름·학번, 징계 단계·발생일, 상태 뱃지. **수명주기**: 전출 표, 졸업 동의 목록·모달, **기간제 이관 마법사**(확인 문구·대상자 이름). 각 화면에서 ⓐ 가로 스크롤 ⓑ 겹침 ⓒ 1급 값 잘림 ⓓ 체감 개선 |
| **탭 재배치·개명·표 폭** (`3018932`, 2026-08-21 배포) | ⓐ 시간표 작성 메뉴를 열면 **「학기 & 권한 관리」가 먼저** 뜨는지 ⓑ 탭 순서가 학기관리 → **창체·SLAT 배치** → 주당 수업 시간 → 등록부 5종 → 자동 작성 → 개정 → 나이스 인지 ⓒ 「교육과정 고정 시간」 문구가 **어디에도 안 남았는지** ⓓ **표 10곳이 우그러지지 않고 옆으로 밀리는지**(졸업생 명단·졸업 동의·전출 표·이관 마법사·징계 요약 3곳 등) |

| **실기기 일괄 조치 6건** (`0be62ff`·`cef7efd`, 2026-08-21 배포) | ⚠️ **먼저 `Ctrl+Shift+R`** — PWA 캐시 때문에 옛 화면이 보일 수 있다. ⓐ `/student` **졸업 안내가 맨 위** ⓑ 쪽지 본문에 **역슬래시 없음**(옛 쪽지 포함, `3*4`·`snake_case`·`[참고]`로 신규 확인) ⓒ **표가 우그러지지 않고 옆으로 밀림** — 졸업생 명단·동의서 보관함·생활지도 요약·전출 대기·전입 결과·승급/입학 시트·이관 마법사·징계 권한 (⚠️ **페이지 몸통이 좌우로 흔들리면 반대 방향 실수이니 신고**) ⓓ **조직도 이름이 한 줄로** — 편집(수동 배치)·트리 뷰, 이메일은 말줄임이 정상 ⓔ 사이드바에 **「내 조직 정보 신청」 없음** + 하단 「내 정보 관리」로 여전히 들어가짐 + 홈 미등록 배너 유지 |

| **S4(같은 반 같은 날 동일 과목) 금기 강제** — 솔버 수정, **푸시·배포 후** 확인 | 시간표 자동 작성을 같은 계획으로 재실행 → 소프트 감점 상세에서 **S4 0건**(이전 5건), 미배정 0 확인. 편성 시간이 수십 초 늘어난 것은 정상(만석 교환 보수 추가) |
| **학급 간 교환(해결안 v2)** — 오래 이월됐던 항목 구현, **푸시·배포 후** 확인 | 감점 항목의 [해결안 찾기] → "두 학급이 같은 두 교시를 함께 맞바꿔…" 안이 나오는지 → 미리보기 → 적용 → 점수가 예고대로 바뀌고 **적용 취소(undo) 한 번에 두 학급이 함께 원상복귀**되는지. 「학급 간 교환(현재 미지원)」 문구가 안 남았는지 |
| **화면 문구 다이어트 1차** (`ff5a368`, 2026-08-21 배포. 시간표 폴더 제외 35파일 −127줄) | 큰 제목 밑 부제를 43곳 걷어냈다. ⓐ **홈·로그인·명렬표·북마크·업무 관리** 제목 밑이 비어 보이지 않고 간격이 자연스러운지 ⓑ 관리자 홈 **카드 7장**이 설명 없이도 무엇인지 알겠는지(그룹·조직단위 설정 두 장만 한 줄 남김) ⓒ **생활지도 탭 6종** 제목 밑 설명이 전부 사라졌는지 ⓓ **없어져서 곤란한 설명이 있으면 그 화면 이름과 함께 신고** — 되살릴 기준은 「순서·자동으로 벌어지는 일·되돌릴 수 없는 일」 |

(직전 배포분 `2b2b5e3`까지는 2026-08-20 전항목 통과 — 일지 「실기기 3차」)

> **⚠️ 배치 조치가 0건이다 (2026-08-21 실측).** `b81ee80`·`86805ae` 둘 다 **추가 줄 = 삭제 줄**(87/87, 216/216)이라 **클래스 교체만 했고 줄바꿈·세로 전환은 하나도 안 했다.** 과제 J 지시서 5번(*"안 들어가면 줄바꿈 허용 수준까지 고친다"*)이 수행되지 않았다는 뜻 — 구현자가 화면을 못 보니 구조적으로 그렇게 된다. **폭이 깨지는 곳이 있다면 실기기에서 처음 드러난다.** 우선 볼 곳 = 전입 등록 결과 표·졸업 대상 표·징계 요약 표·이관 마법사 단계 표시(표 열 승격 5곳 + wrap 없는 가로 배치 48곳).
>
> **⚠️ 360px 브라우저 확인이 막혀 있다 (2026-08-21).** 스펙 §4는 이 단계를 Claude 몫으로 두는데, 생활지도·수명주기는 **관리자 로그인 뒤에 있는 화면**이고 Claude는 자격 증명을 입력할 수 없다(안전 규칙). **사용자가 로컬 미리보기(`localhost:3000`)에 직접 로그인해 주면 그 뒤는 Claude가 360px에서 훑을 수 있다.** 그게 아니면 이 단계는 건너뛰고 실기기 확인이 유일한 화면 검증이 된다 — 그 경우 깨진 곳을 사용자가 처음 보게 되므로 왕복이 한 번 더 생긴다.

## 날짜가 오면 확인

| 항목 | 담당 | 시점 | 확인 방법 |
|---|---|---|---|
| **분업 기본값 자동 복귀 — 사용자 재질의** | Claude(질의)+사용자 | **8/22 (내일)** | `AGENTS.md` §0 「만료 처리」. 2026-08-13 사용자 결정으로 ⓐ구독 유지 여부 ⓑClaude 몫 최소 범위 ⓒ9c 선행 일정을 **사전에 정하지 않기로** 했고(*"당일까지 답을 못 줘"*), 같은 줄이 *"8/22이 지나면 그때 다시 묻는다"*로 끝난다. **그 재질의가 어디에도 등재돼 있지 않았다** — 8/21 재훑기에서 발견해 등재. ⓒ는 위 9c 실측으로 이미 답이 나와 있으므로 실제로 물을 것은 ⓐⓑ뿐 |
| **NEIS 2·3학년 과목 등재명 확보** | 사용자(일과계 수령) | **지금 요청 — 11월 검증 전 필수** | 9c-F의 CSV 내보내기(F-2)가 `phase9c_f_spec` B1 규칙으로 **차단된 상태**다. 등록 스크립트·차단 로직은 이미 있고(`scripts/register_neis_subjects.ts`) 받은 샘플이 **1학년 한 반분뿐**이라 2·3학년이 `neisName: ""`로 비어 있다. **데이터만 오면 끝나는 항목** — Claude가 못 푼다. 부수: 복수교사 NEIS 표기(F 스펙 §5-3)도 실사례 미확보, 3학년 이동수업 현황 파일 수령 미완 |
| 전출 교사 hjl@ 자동 삭제 | Claude | 8/24 이후 | `npx tsx --env-file=.env.local scripts/check_cron_health.ts` |
| **쪽지 월별 사본(레버 ②') 구현 재판정** | Claude+사용자 | **2026-11월** (무료 한도 복귀 판단과 같은 자리) | 그 시점 발송량·검색량 실측 + 무료 한도 여유 + 백필 대상 건수 → `docs/memo_monthly_mirror_spec.md` §8. **설계는 8/20 종결, 남은 건 착수 여부뿐** |
| **절약 모드 효과 실측** | Claude+사용자 | **2026-11월** (무료 한도 복귀 판단과 같은 자리) | **실사용 데이터로** 잰다 — 시간표 후보 조회(최대 비용원)의 캐시 적중률을 `x-tt-cache`·`x-tt-instance`로, 전체 조회량은 사용량 화면으로. 켜져 있는 상태는 2026-08-20부터(토글 기록은 `audit_logs`). 경위 = 노트 2026-08-20 「절약 모드 실측 방법 정정」 |
| **Codex 검증 역할 — 누적 10회 재확인** (구현 확장은 **비용 사유로 안 함 확정 2026-08-20**. 10회는 합격선이 아니라 **표본 수** — 이미 기본 검증자로 쓰면서 기록을 쌓는 중) | Claude | 9월 초 | `docs/codex_trial_2026-08.md` §4·§7·**§7-1**(재검토 지점·역전 조건). 현재 **6회**(8/20 4회 + 8/21 과제 L 검증·솔버 검증) — 과제 L 건은 실패 판정 3건이 전건 재확인 결과 전부 사실. 9월 재점검 때 **스펙 사전 검토 확대 아이디어**(로드맵 §2, 2026-08-21 사용자 발안)도 함께 판단 |
| 시트 브리지 일몰 | 사용자 | 9/10 | — |
| 9c 시간표: 컴시간 재계약 판단 | Claude | 11월 말 시한 | 백지 편성 소프트 **35점** vs 기준선 39점 (solve_blank, 2026-08-21 S4 금기 강제 후 재측정 — 30점이던 옛 수치는 S4 2건을 안은 값이라 비교 불가) |
| 소프트 가중 절대치 조정 | Claude | 11월 리허설 | questionnaire_result §3 (S6>S7 서열 고정) |
| Blaze 결제 fb01 복귀 | 사용자 | 11월 중순 전 | Firebase 콘솔 결제 계정 |
| 이관 마법사 첫 실전 동반 검증 | Claude+사용자 | 실제 기간제 발생 시 | 실행 후 감사 로그·주간시간표·역이관 대칭 (spec §7) |

## 유령 방지 트랙

| 항목 | 담당 | 확인 방법 |
|---|---|---|
| 상태 표기 재고 **51건** 점진 소각 (8/18 기준선 64 → 8/20 58 → **8/21 51**) | 전원 (지나가다 만나면) | `npm run check:docs` 재고 수치 |

## 경미 (원하면)

- git 작성자가 대부분 "antigravity"로 찍혀 이력 귀속이 흐리다 — Claude는 per-commit author로 자체 해소, 규칙화는 사용자 판단
- PII 엑셀 git 이력 blob 잔존 — 원하면 filter-repo (파일 자체는 삭제됨)
- 배정표 창체 추가를 "창체 없는 반만" 골라 넣는 옵션
- 날짜 유틸 `getTodayKSTISO` 사본 3곳 단일화
- 일지 엔트리 순서가 섞여 있다 — Antigravity는 위에, Claude는 아래에 붙여 08-19 → 08-18 → 08-19 → 08-20 순서가 됐다. 회전 경계 찾기가 번거로워진다
