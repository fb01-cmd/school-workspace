# Phase 5.9 — 조직도 고도화 스펙 (정렬 규칙 · 어드민 수동 배치 · 트리 뷰)

> 작성: Claude, 2026-07-27. 사용자 구두 요구 3건(수동 배치, 등록 모달 정렬, 트리 뷰)을 스펙화.
> 담임 단일 원본은 교직원 조직도(승인된 프로필)라는 기존 원칙(discipline_manual.md §담임 배정표)과 연동됨.

## §1. 조직도 정렬 규칙 (공식 기록 — 이전까지 미기록 상태였음)

사용자가 예전에 구두로 정한 규칙. 2026-07-27 재확인하여 여기에 단일 원본으로 기록한다.
**조직도·트리 뷰·부서별 명단 등 부서 내 인원을 나열하는 모든 화면에 공통 적용.**

1. **부서 내 최상단 = 부서장**(`isDeptHead`).
2. **나머지 인원 = 가나다순**(이름 기준).
3. **학년부(1학년·2학년·3학년) 예외**: 가나다순이 아니라 **부장 → 1반 담임 → 2반 담임 → … 반 번호 순**.
   - 담임 판별·반 번호는 승인된 프로필의 `homeroom: { grade, class }` 필드 사용 (AuthContext.tsx:18 — 이미 존재, 담임 배정표와 동일 소스).
   - 학년부 소속이지만 담임이 아닌 인원(부장 제외)은 담임들 **뒤에** 가나다순. *(Claude 판단으로 보충한 기본값 — 사용자 이견 시 이 줄만 수정)*
4. **부서 자체의 나열 순서** = 관리자가 정의한 `schoolSettings.departments` 배열 순서 그대로 (§3 참조). 직책도 `schoolSettings.positions` 배열 순서 그대로.

## §2. 어드민 수동 배치 (신규 기능 — 우선순위 최상)

**배경**: 현재는 교사 자기 등록 → 어드민 승인 흐름뿐. 학기 초에는 어드민이 **인사 배치표를 보고 직접 입력**하는 편이 압도적으로 빠르다. 향후 메신저 기능의 수신자 선택이 조직도에 의존하므로 조직도 데이터의 신속한 완성이 중요.

- **IA(배치)**: 기존 **ProfileApprovals(조직도/승인 관리) 화면 안에 "✍️ 수동 배치" 서브 탭**으로 추가. 새 메뉴를 만들지 않는다.
- **권한**: `super_admin` 전용.
- **대상 선택**: 공용 `AutocompleteInput` 사용 (자동완성 규칙 준수).
- **입력 항목**: 자기 등록 모달과 동일 스키마 — 부서(복수), 직책, 부서장 여부, 담임 여부 + 학년·반, 소속 없음(noDept).
- **동작**:
  - 신청·승인 단계 없이 **즉시 승인된 프로필로 저장**. 저장 시 `updatedBy: super_admin 이메일` 기록.
  - 해당 교사의 **기존 pending 신청이 있으면 자동 무효화**(수동 배치가 우선).
  - 감사 로그 필수 기록.
  - **교사에게 알림 발송 없음** — 학기 초 수십 명 연속 배치 시 알림은 스팸이 됨.
- **연속 입력 UX**: 저장 후 폼을 닫지 않고 대상 검색창으로 포커스 복귀 (배치표 보고 줄줄이 입력하는 시나리오). 표 형태 일괄 편집기는 v2로 미룸.

## §3. 등록 모달 부서·직책 정렬 (소품 — 즉시 처리 가능)

- **증상**: 조직 정보 등록 신청 모달(MyProfileModal)의 부서·직책 나열 순서가 관리자 의도와 다름.
- **원칙**: 나열 순서의 단일 원본 = `schoolSettings.departments` / `positions` **배열 순서**. 모달·승인 화면·수동 배치 폼·트리 뷰 전부 이 배열 순서를 **정렬 없이 그대로** 렌더링한다 (알파벳/가나다 재정렬 금지).
- **확인·수정 지점**:
  1. 관리자 설정 화면(OUConfiguration의 부서 관리)에 **순서 변경 수단(↑/↓ 버튼 정도면 충분)**이 없다면 추가 — 이것이 "어드민이 정렬한대로"의 실체.
  2. 소비처 어딘가에서 배열을 재정렬하고 있다면 제거.
  3. `DEFAULT_DEPARTMENTS` 폴백 순서도 실제 결재라인 순서(교장→교감→교목→교무기획부→… 스크린샷 참조)와 일치하는지 확인.

## §4. 조직도 트리 뷰 (신규 UI)

- **컨셉**: 관리 북마크 편집기(`BookmarkTreeEditor.tsx`)의 접이식 트리 느낌. 부서 = 폴더 노드, 교사 = 리프 노드.
- **정렬**: §1 규칙 전면 적용. 부서 순서는 §3의 배열 순서.
- **리프 표시**: 이름 + 직책 뱃지(부장 등) + 담임이면 `N-M 담임` 뱃지.
- **super_admin 전용**: 교사 노드에 ✏️ 버튼 → §2 수동 배치 폼을 해당 교사로 프리필해 열기 (조회하다 바로 고치는 흐름).
- **일반 교사**: 읽기 전용 조회. (노출 범위: 교직원 전체 — 조직도는 공유 운영 정보.)
- 드래그앤드롭 재배치는 v2 (수동 배치 폼이 있으므로 MVP에서 불필요).

## §5. 구현 순서 (Antigravity)

1. §3 모달 정렬 (독립 소품, 선행 가능)
2. §2 수동 배치 → 3. §4 트리 뷰 (§2의 프리필 진입 포함)
- 각 단계 tsc·build 자가 통과 후 커밋, §2·§4 완료 시 Claude 표적 리뷰 (프로필 쓰기 권한·pending 무효화 로직이 리뷰 대상).
