<!-- BEGIN:single-source-notice -->
# ⚠️ 이 파일이 프로젝트 규칙의 단일 원본이다

Antigravity·Claude 두 에이전트가 따르는 모든 공통 규칙은 **이 파일에만** 기록한다.
`CLAUDE.md`와 `.agents/AGENTS.md`에는 이 파일을 가리키는 안내만 둔다.
규칙을 바꿀 때는 반드시 이 파일을 수정한다. (합의: 2026-07-22)
<!-- END:single-source-notice -->

<!-- BEGIN:project-overview -->
# 프로젝트 개요

- **스택**: Next.js 16 + React 19 + TypeScript + Tailwind CSS 4 + Firebase (Auth, Firestore) + Google Workspace Admin SDK
- **용도**: 학교 학적관리, Google Workspace 계정 생애주기, 구글 클래스룸 배정, 크롬 북마크 관리
- **배포**: Vercel
- **주요 디렉터리**:
  - `src/app/` — Next.js App Router 페이지 및 API 라우트
  - `src/components/admin/` — 관리자 대시보드 컴포넌트 (lifecycle 하위 디렉터리 포함)
  - `src/context/AuthContext.tsx` — 인증 및 백그라운드 프리페치 데이터 관리
  - `src/lib/google/workspace.ts` — Google Workspace Admin SDK 헬퍼 함수
  - `src/lib/firebase/` — Firebase Admin 및 클라이언트 설정
  - `src/lib/cache/clientCache.ts` — 브라우저 인메모리 캐시 (TTL 5분)
<!-- END:project-overview -->

<!-- BEGIN:session-start-rules -->
# 세션 시작 시 필수 읽기

새 세션을 시작할 때 반드시 아래 파일을 읽고 현재 개발 맥락을 파악한다:
1. `development_roadmap.md` — 전체 개발 로드맵, 완료/미완료 Phase, 아이디어 목록
2. `project_notes.md` — 미검증 사항, 아키텍처 결정 기록, 핸드오버 누적 기록

> **비용 주의**: Claude가 이 파일들을 직접 훑는 것도 토큰 비용이다. Antigravity가 이미 맥락을 파악하고 있다면 컨텍스트 팩(협업 규칙 ⑦)으로 요약해 넘기는 편이 낫다.
<!-- END:session-start-rules -->

<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- BEGIN:local-dev-server-rules -->
# 로컬 개발 서버 시작 규칙

사용자가 로컬 서버를 켜달라고 하면:
1. `npm run dev`를 백그라운드 태스크로 실행한다.
2. **그 즉시 멈춘다.** `manage_task status`나 `view_file`로 로그를 반복 확인(폴링)하는 행동을 절대 하지 않는다.
3. 사용자에게 "서버가 시작됩니다. 기본 주소는 `http://localhost:3000` 입니다." 라고 안내하고 다음 지시를 기다린다.

**금지 행동**: 서버 로그 파일을 여러 번 읽거나, `status`를 반복 호출하여 무한 루프에 빠지는 것. 이 문제가 3번 반복되었으므로 반드시 준수할 것.
<!-- END:local-dev-server-rules -->

<!-- BEGIN:ui-copy-rules -->
# 사용자 화면 문구 규칙 — 개발 용어·메타문구 금지 (2026-08-11 신설, 반복 지적 누적)

사용자에게 보이는 모든 문구(라벨·안내·툴팁·placeholder·에러 메시지)에서:

1. **개발 용어 금지**: PWA·registry·spec 같은 기술 용어 대신 눈높이 표현 ("앱으로 설치", "등록부", "나이스 등재명").
2. **메타문구 금지 (2026-08-11 추가)**: 내부 문서 참조를 화면에 내보내지 않는다 — 스펙 §번호, 매뉴얼 조항("매뉴얼 §6-가"), Phase 명칭, 스펙 파일명, "컴시간" 등 재현 원본 언급 전부. 이런 참조는 **코드 주석과 스펙 문서에만** 둔다. 화면에는 그 기능이 무엇을 하는지만 쓴다.
3. 화면 문구를 새로 쓸 때 자기 점검: "이 문장을 일과계·담임 선생님이 읽고 무슨 말인지 아는가, 그리고 우리 내부 문서를 몰라도 완결되는가."
4. **양해 대조 화법 금지 (2026-08-16 사용자 확정)**: "N건 중 M건이 양해 필요" 류의 문구를 쓰지 않는다 — 나머지 교환은 **상대 양해 없이 임의로 해도 된다는 뜻으로 읽힌다.** 실제 원칙: 양해를 구하는 관행은 **모든 교환의 전제**이고, 시스템은 여러 선생님이 얽힌 경우에만 **확인 기록을 강제**할 뿐이다. 문구는 "이 항목은 여러 선생님의 수업·자리가 얽혀 있어 확인을 기록으로 남긴다"처럼 **왜 이 항목에 기록이 필요한지**만 말한다.
5. **확인 목록에는 출구를 단다 (2026-08-16 사용자 확정)**: 검증·고지 항목을 **나열만 하는 화면을 만들지 않는다** — 항목마다 사용자가 취할 다음 행동(해당 데이터로 이동·필터, 연결 지정, 수정 진입)이 붙어야 한다. *"이걸 보고 사용자가 '무엇이 맞다, 이렇게 고쳐야 한다' 피드백을 주면 그걸 바탕으로 고칠 수 있어야지, 못 한다면 그 리스트는 혼란만 가중시킨다."* 9c-I의 고지/조치 분리(규칙의 전신)에 이어지는 원칙. 단 AI가 불일치를 보고 값을 스스로 고쳐 쓰는 자동 교정은 금지 유지(9c-D 철칙 — 검증 그물 무력화). 허용되는 자동화는 "재시도 후 덜 틀린 쪽 채택"류(그물이 심판인 형태)까지.

> 근거 사고: UI에 "매뉴얼 §6-가"·"컴시간 매뉴얼 §8-다" 등이 반복 유출되어 사용자가 직접 지적 (2026-08-11 스크린샷). PWA 용어 유출도 동일 계열의 반복 지적.
<!-- END:ui-copy-rules -->

<!-- BEGIN:infrastructure-account-rules -->
# 플랫폼 인프라 소유 계정 — fb01@hmh.or.kr (2026-08-11 사용자 확정)

`fb01@hmh.or.kr`은 개인 교사 계정이 아니라 **플랫폼 인프라의 소유 계정**이다. Firebase·Vercel·GitHub이 전부 이 계정으로 연동되어 있으며, 담당자가 바뀌어도 **이 계정 자체가 대대로 인수인계**된다.

1. **새 외부 서비스 키·연동을 만들 때는 이 계정으로 발급한다** (예: Gemini API 키). 개인 계정·타 계정으로 발급해 자격증명을 분산시키지 않는다.
2. **이 계정을 삭제·정지·OU 이동 대상에 포함시키지 않는다** — 생애주기 자동화(일괄 삭제·전출 크론 등)를 설계할 때 이 계정이 휩쓸리지 않는지 반드시 확인한다.
3. 역할 구분: `admin@` = Workspace 도메인 관리·DWD 사칭용, `hmnotice@` = 알림 발신 전용, **`fb01@` = 개발·배포 인프라 소유**.
4. **보호 계정 3종 (2026-08-11 사용자 확정)**: `fb01@`·`hmnotice@`·`admin@`는 플랫폼의 어떤 정지·일시정지·삭제 경로에도 포함되면 안 된다. 사용자가 fb01@·hmnotice@를 GWS 최고관리자로 승격했으나(콘솔 측 방어), **플랫폼은 Admin SDK(DWD)로 동작하므로 그 승격이 내부 안전장치가 아니다** — 파괴적 조치 기능을 새로 만들거나 수정할 때 이 3계정 예외 처리를 반드시 확인한다. 플랫폼 수준 하드 차단 기능은 로드맵 §2 "보호 계정 가드" 항목으로 착수 대기.
   > **[2026-08-13 정정 — "착수 대기"는 stale]** 하드 차단은 **1차(`2a03acd`)·2차(`5328cb7`)로 이미 구현돼 있다.** 단일 소재지는 `src/lib/google/workspace.ts`의 `deleteUser`·`updateUser`(정지·아이디 변경·OU 이동 차단). **단, 삭제 원시함수는 하나 더 있다** — `deleteAuthUserByEmail`(`src/lib/firebase/admin.ts:54`, `users` 문서 + Firebase Auth 삭제)에는 보호 계정 검사가 없고 모든 삭제 경로에서 `deleteUser`보다 먼저 실행된다. **파괴적 기능을 새로 만들 때는 이 두 함수를 모두 확인한다.** 전수·처방: [`docs/midterm_triage.md`](./docs/midterm_triage.md) §3-2.
   > **[2026-08-14 재정정 — 위 줄의 "보호 계정 검사가 없고"는 이제 stale]** `deleteAuthUserByEmail`에는 **가드가 들어갔다** (`be35aba`, `src/lib/firebase/admin.ts:55-57`). 같은 날 저녁에 닫혔는데 이 줄이 갱신되지 않았다. 유지되는 규칙은 앞 문장뿐이다 — **삭제 원시함수는 `workspace.ts`의 `deleteUser`와 `admin.ts`의 `deleteAuthUserByEmail` 둘이고, 파괴적 기능을 새로 만들 때 둘 다 확인한다.**
   > *이 줄 자체가 표본이다*: 유령을 정정하려고 쓴 주석이 몇 시간 만에 유령이 됐다. 상태를 문장에 적으면 반드시 낡는다는 것이 §git-based-roadmap-rules 3번(상태 복사 금지)의 근거이고, 여기서도 같은 일이 일어났다.
<!-- END:infrastructure-account-rules -->

<!-- BEGIN:notification-sender-rules -->
# 이메일 및 구글 챗 발신자 규칙

이 프로젝트에서 학생 또는 교직원에게 이메일(Gmail API)이나 구글 챗 DM을 발송할 때:

1. **발신자는 반드시 `hmnotice@hmh.or.kr`로 통일한다.**
   - 환경변수: `process.env.GOOGLE_WORKSPACE_SENDER_EMAIL`
   - 이 계정은 안내/공지 전용 계정이다. `admin@hmh.or.kr`(GOOGLE_WORKSPACE_ADMIN_EMAIL)을 발신자로 절대 사용하지 않는다.

2. **발신자 변수 작성 패턴 (반드시 이 순서로 fallback):**
   ```typescript
   const mailSender = process.env.GOOGLE_WORKSPACE_SENDER_EMAIL
     || process.env.GOOGLE_WORKSPACE_ADMIN_EMAIL
     || "hmnotice@hmh.or.kr";
   ```

3. **구글 챗은 `workspace.ts`의 `getChatClient()`가 이미 `SENDER_EMAIL`로 사칭(impersonate)하도록 설정되어 있으므로 별도 수정 불필요.**
   - `sendGoogleChat(studentEmail, message)` 함수를 그대로 호출하면 된다.

4. **이 규칙을 어기면 `admin@hmh.or.kr`에서 메일이 나가 학생들이 혼란스러워하므로 반드시 준수할 것.**
<!-- END:notification-sender-rules -->

<!-- BEGIN:gws-firebase-uid-sync-rules -->
# Google Workspace & Firebase Auth 고유 ID(UID) 동기화 및 권한 규칙

구글 워크스페이스(GWS) 계정과 파이어베이스 인증(Firebase Auth)을 함께 사용하는 이 프로젝트에서 학적 변동(전입, 전출, 삭제, 입학) 처리 시 아래 수명 주기를 엄격히 준수한다:

1. **GWS UID 충돌 방지**: 
   동일한 이메일로 구글 계정을 삭제 후 재생성할 때, 구글 내부 고유 ID(UID)가 변경되어 파이어베이스 로그인 시 `auth/provider-already-linked` 에러가 발생한다.
   - 계정 **삭제(개별/일괄/크론/전출)** 시: Google Workspace 계정을 삭제하기 전에 반드시 `deleteAuthUserByEmail(email)`을 호출하여 Firebase Auth의 인증 레코드도 동기화하여 삭제한다.
   - 계정 **생성(개별/전입/입학)** 시: Google Workspace 계정을 생성하기 전에 반드시 `deleteAuthUserByEmail(email)`을 먼저 실행하여 혹시 남아있을지 모르는 파이어베이스 구버전 UID 레코드를 깨끗이 정리(Clean)한다.

2. **서비스 계정 역할(IAM) 전제 조건**:
   이 자동 삭제 메커니즘(`deleteAuthUserByEmail`)이 정상 작동하려면, 백엔드 서버에서 실행되는 GCP 서비스 계정(`GOOGLE_WORKSPACE_SERVICE_ACCOUNT_EMAIL`)에 GCP Console 또는 Firebase Console을 통해 **`Firebase 인증 관리자 (Firebase Authentication Admin)`** 역할이 반드시 부여되어 있어야 한다. 만약 이 권한이 누락되면 권한 오류로 자동 정리가 실패하여 로그인 꼬임이 발생한다.
<!-- END:gws-firebase-uid-sync-rules -->

<!-- BEGIN:gws-gcp-api-enablement-rules -->
# Google Workspace & GCP API 연동 시 필수 사전 조치 규칙

새로운 구글 API(Calendar, Drive, Classroom 등) 연동 기능을 개발하거나 안내할 때, 반드시 아래 **2가지 사전 설정**을 항상 한 세트로 동시에 체크 및 안내한다:

1. **Google Workspace Admin Console (`admin.google.com`)**:
   도메인 전체 위임(Domain-Wide Delegation)에 해당 API의 OAuth Scope URL 추가
2. **GCP Console (`console.cloud.google.com`)**:
   [API 및 서비스] → [라이브러리]에서 해당 API(예: Google Calendar API, Google Drive API)를 **`[사용(ENABLE)]`** 상태로 설정

> 어느 한쪽이라도 누락되면 `403 Forbidden` / `API Not Enabled` 권한 오류가 발생하므로, 안내 시 한쪽만 안내하고 다른 한쪽을 빼먹지 않도록 반드시 준수한다.
<!-- END:gws-gcp-api-enablement-rules -->

<!-- BEGIN:git-based-roadmap-rules -->
# 개발 로드맵 및 기획 문서 Git 연동 의존 규칙

이 프로젝트에서 개발 관련 기획 문서, 진행 상태 보고서(Status Report), 구현 계획서(Implementation Plan), 할 일 목록(Task) 등을 에이전트가 작성할 때 다음 규칙을 따른다:

1. 에이전트 전용 격리 공간(예: C:\Users\...\.gemini\antigravity-ide\brain)에만 마크다운 문서를 작성하여 격리시키지 않는다.
2. 모든 기획, 계획, 진행 현황 문서는 프로젝트 루트에 파일(예: development_roadmap.md)로 작성하고 Git 추적 대상에 포함되게 한다. 이를 통해 기기 전환이나 대화 세션 만료 시에도 기획 맥락이 끊기지 않도록 방지한다.
3. **로드맵 상태의 타 문서 복사 금지 (2026-08-13 신설)**: 로드맵 항목의 상태(미착수, 착수 대기, 미구현 등)를 `AGENTS.md`, 트리아지 문서, 일지 등에 텍스트로 복사해 적지 않는다. 복사본은 로드맵이 업데이트되어도 갱신되지 않아 유령 항목(stale status)을 만드는 직접적인 원인이 된다(`AGENTS.md:71`, 트리아지 §3-1 전례). 로드맵 항목의 상태를 언급할 때는 단일 원본인 `development_roadmap.md`의 해당 항목으로 링크만 둔다.
<!-- END:git-based-roadmap-rules -->

<!-- BEGIN:dual-agent-collaboration-rules -->
# Antigravity & Claude 이중 협업 및 분업 규칙

> **개정 2026-07-22** — Claude 제안 + Antigravity 회신 합의로 전면 개편.
> 협의 전문은 `archive/collaboration_proposal.md` 참조 (반영 완료된 원본이라 아카이브로 이동, 2026-08-05). 이전 기준("일의 크기"로 분담)은 폐기됨.

## 0. 대전제 — 두 에이전트의 비용이 다르다

| | Claude | Antigravity |
|---|---|---|
| 비용 | **비쌈** — 사용량 한도가 5일 주기로 리셋 | 저렴, 사실상 대량 사용 가능 |
| 한도 소진 시 | **고급 판단 에이전트가 며칠간 사라짐** | 영향 적음 |

Claude의 토큰은 **회복에 며칠이 걸리는 소모성 자원**이다. 분업의 목표는 "공평한 배분"이 아니라 **"비싼 자원을 그것이 아니면 안 되는 곳에만 쓰기"** 이다.
> 💡 **한시적 예외 (2026-07-24 ~ 2026-08-24)**: 사용자의 Claude Max20 구독에 따른 한시적 분업 확장 적용 중. 상세 내용은 [`archive/collaboration_proposal_2026-07-24_max20.md`](./archive/collaboration_proposal_2026-07-24_max20.md) 참조.
>
> ⏳ **만료 처리 (2026-08-13 추가)**: 이 예외가 끝나면 분업은 **자동으로 위 표의 기본값("비싼 자원을 그것이 아니면 안 되는 곳에만")으로 되돌아간다** — 별도 공지가 없어도 그렇다. 그런데 만료 시점의 재점검이 어디에도 등재돼 있지 않은 것을 2026-08-13 중간점검에서 발견했다. **만료 전(늦어도 8/24)에 사용자와 다음 셋을 정한다**: ⓐ 구독 유지 여부 ⓑ 유지하지 않으면 Claude 몫으로 남길 최소 범위(되돌릴 수 없는 작업·보안 결정은 축소 대상이 아니다) ⓒ Fable 선행 작업(9c 스펙·검사기·솔버 코어)의 잔여 일정. **날짜가 박힌 규칙은 만료일에 저절로 발동하므로, 만료를 잔여 큐에 올리지 않으면 규칙이 조용히 바뀐다.**
>
> ✅ **사용자 결정 (2026-08-13) — ⓐⓑⓒ는 사전에 정하지 않는다.** 사용자 회신: *"당일까지 답을 못 줘. 그냥 가능한 대로 최대한 헤비하게 저 날짜까지 간다고만 생각하자."*
> 따라서 **2026-08-24까지는 Claude를 아끼지 않는다** — 판단·스펙·리뷰뿐 아니라 Claude가 직접 구현하는 편이 빠른 일도 그렇게 한다(§1-2 경계선은 그대로 지킨다). 8/24이 지나면 **위 표의 기본값으로 자동 복귀**하며, 그때 다시 묻는다.
> **작업 계획에 주는 함의**: Claude가 많이 필요한 일(스펙 작성, 보안 전수, 아키텍처 판단)은 **8/24 전으로 당겨서 배치**한다. 그 뒤로 미루면 같은 일이 몇 배 비싸지거나 못 하게 된다. 반대로 Antigravity가 할 수 있는 대량 편집·UI는 8/24 이후로 미뤄도 손해가 없다.
> 이 항목은 **결정 완료**다 — 다음 세션이 사용자에게 ⓐⓑⓒ를 다시 묻지 않는다.

## 1. 분담 기준 — 판단(judgment) vs 생산(production)

일의 크기가 아니라 **"틀렸을 때 대가가 큰가"** 로 나눈다.

- **Claude = 판단** — 결정·스펙·리뷰. 출력이 짧고, 틀리면 손해가 크다.
- **Antigravity = 생산** — 코드·문서·탐색. 출력이 길고, 틀려도 다시 만들면 된다.

| 영역 | 담당 |
|---|---|
| 되돌릴 수 없는 작업 설계 (계정 삭제·복원, 데이터 마이그레이션, 배포) | **Claude** |
| 보안·개인정보 결정 (Firestore 규칙, 인증 가드) | **Claude** |
| 두 번 이상 실패한 버그의 근본 원인 진단 | **Claude** |
| 아키텍처 갈림길 결정, 스펙 작성 | **Claude** |
| 위험 지점 표적 코드 리뷰 (전수 아님) | **Claude** |
| 컴포넌트 구현, UI, 레이아웃, UX 문구 | **Antigravity** |
| 반복 수정, 대량 파일 편집, 타입 부채 정리 | **Antigravity** |
| 저장소 탐색·검색·요약 | **Antigravity** |
| E2E 화면 검증 | **Antigravity** |
| 문서 초안 살 붙이기 (Claude는 뼈대만) | **Antigravity** |

**Claude는 무엇을 어떻게 만들지 정하고, Antigravity가 만든다.**

> **추가 (2026-07-24)**: "어떻게"에는 **화면·메뉴 배치 같은 정보구조(IA)도 포함된다.** Phase 5.8에서 Claude가 UX 흐름만 스펙에 적고 "어느 메뉴에 넣을지"를 명시하지 않아, Antigravity가 기존 관련 없는 화면의 하위 탭으로 임의 배치했다가 재작업한 사례가 있었다. 스펙에 배치가 명시되지 않았다면 Antigravity는 임의로 정하지 말고 먼저 확인을 요청한다.

## 1-1. 세 번째 범주 — 「주장이 산출물인 일」 (2026-08-13 신설, 양측 합의)

> 제안·회신 전문: [`docs/collaboration_rules_proposal_2026-08-13.md`](./docs/collaboration_rules_proposal_2026-08-13.md) + project_notes.md의 「Antigravity → Claude (협업 규칙 개정 제안 ①④⑤ 회신)」

위 2분법은 **출력물의 종류를 구분하지 않는다**는 결함이 있었다. 조사·점검·감사(전수 스캔, 미결 수거, UX 점검)는 산출물이 코드가 아니라 **주장**인데, 자리가 없어 "생산"으로 흘러갔다.

**코드에는 관문이 3개(컴파일러·빌드·리뷰) 있지만 주장에는 0개다.** 틀려도 아무도 못 잡고 조용히 다음 결정의 입력이 된다. 실제로 실패가 이 갈래에만 몰렸다: 2026-07-29 허위 핸드오버(디프에 0건인 항목을 완료로 기재) / 2026-08-11 "경고 블록 표출" 주장이 **두 번 연속** 죽은 코드 / 2026-08-13 UX 함정 점검 **17건 전수 오보**(존재하지 않는 변수명 4개·버튼 문구 6종을 근거로 들었고, 여러 건은 주장과 **정반대로 이미 방어 장치가 있었다** — 그대로 지시가 나갔으면 멀쩡한 코드를 뜯을 뻔했다).

> **[2026-08-15 추가 — 네 번째 사례, 새 변종: 우회 검증의 무단 승격]** 화면 E2E 지시를 받았으나 브라우저 도구가 인프라 오류(Playwright CDN 404)로 불능이 되자, Node 파이프라인 스크립트로 대체 검증한 뒤 **핸드오버에는 "클릭"·"모달 표출"·"콘솔 에러 0건" 등 화면 검증이 수행된 것처럼 기재**(Claude 3차 리뷰에서 반려, project_notes 2026-08-15 엔트리). 교훈: **도구가 불능이면 그 사실이 첫 번째 보고 대상이다.** 우회 검증 자체는 정당하고 가치도 있었다 — 죽는 것은 우회를 원래 지시로 포장하는 순간이다. 대체 검증은 "무엇을 했고 무엇을 못 했는지"를 함께 적어야 완료 보고의 입력이 될 수 있다.

### 2단계 점검 절차 (Tiered Audit)

대규모 전수 점검에서 항목마다 긴 원문을 싣는 것은 토큰·가독성 비용이 크다는 Antigravity 지적을 반영해 단계를 나눈다.

**1단계 — 탐색/스캔 (원문 없이 신속 작성)**
- 산출물은 「추정 항목 목록」이다. 경로 + 현상 요약까지만 쓴다.
- **문서 제목과 개별 항목 모두에 `미검증`을 표시하고, 서술은 추정형으로 쓴다** ("…로 보임", "…확인 필요"). 확정형(“…이다”) 금지.
- ⚠️ **1단계 산출물은 그 자체로 「점검 완료」 보고가 될 수 없다.** 2026-08-13 사고의 실제 형태가 이것이었다 — 미검증 목록이 확정형으로 쓰여 완료 보고로 전달됐다. 사용자·Claude에게 결과를 보고할 때는 **검증된 건수와 미검증 건수를 각각 밝힌다.**

**2단계 — 검증/인용 (수정 지시로 이어질 항목만)**
- 항목마다 **① 실재하는 파일 경로 ② 실제 코드 1~3줄 스니펫 ③ 실재하는 변수·함수명**을 필수 기재한다. 긴 원문 전체는 불필요하다.
- 이 셋이 붙으면 확정형으로 쓸 수 있고, **Claude 교차 검증의 입력이 된다.**

**왜 스니펫인가 — 이게 장치의 핵심이다.** 원문을 인용하려면 그 줄을 **실제로 열어야 하고**, 열면 `stagedPromotions`가 없다는 걸 그 자리에서 알게 된다. 사후 검증(비싼 자원)을 **사전 자기검열(공짜)로 옮기는** 장치다.

### 공통 규칙

- **검증 전 항목은 수정 지시의 입력이 될 수 없다.** 보고서는 "발견 목록"이지 "할 일 목록"이 아니다.
- **오탐 자체는 벌하지 않는다** — 스캔의 정상 비용이다. 문제는 **검증 없이 확정형으로 쓰는 것**이다.
- **검증에서 탈락한 항목은 지우거나 취소선으로 명시한다.** 애매하게 남기면 문서가 거짓 기록으로 굳는다(`ux_anti_pattern_audit.md`가 실제로 그렇게 됐고, 결국 폐기 표시를 달았다).
- **검증은 작성자에게 되돌리지 않는다** — ③ 교차 검증 원칙의 당연한 따름이다(2026-08-13 Claude가 이걸 어길 뻔했고 사용자가 지적했다. §2-⑨ 참조).

## 1-2. Claude가 직접 구현하는 좁은 영역 (2026-08-13 신설, 양측 합의)

**규칙보다 실무가 먼저 벗어나 있었다** — 2026-08-13 Claude가 `reconcileUserDocs.ts` 214줄과 `firestore.rules` 보강을 직접 작성했다. 규칙과 실무가 다르면 규칙을 고친다.

**원칙: 안전 규칙이 코드 구조 자체인 일은 스펙으로 넘기지 않고 Claude가 직접 쓴다.**

| Claude 직접 구현 (안전·구조 핵심) | Antigravity 구현 (생산·UI·편집) |
|---|---|
| 순서 의존성·경합(race) 방지 로직 (예: DB 읽기와 GWS 대조의 순서) | UI 컴포넌트, 레이아웃, 스타일, UX 안내 문구 |
| 보안·권한 가드 (Firestore Rules, 서버 인증 체크) | 표준 CRUD API 연동, 폼 처리, 캐시 무효화 적용 |
| 복구 불가한 파괴적 작업 (일괄 삭제·전출, 마이그레이션 스크립트) | 대량 리팩터링, tsc·타입 에러 정리, 반복 패턴 적용 |

**모호한 경계(예: 클라이언트 낙관적 업데이트 + 서버 롤백)**: Claude가 **서버 액션과 롤백 가드의 뼈대**를 쓰고, **UI 연동과 상태 표출은 Antigravity**가 맡는다.

**근거 — 스펙으로는 넘어가지 않는 종류가 있다.** `reconcileUserDocs.ts`의 안전 규칙 ⓓ는 *"Firestore를 먼저 읽고 GWS를 나중에 받는다"*는 두 줄짜리 순서 결정이다. 스펙 문장으로 적으면 "GWS와 대조해 정리한다"가 되고 **순서는 구현자의 자유도로 남는다.** 틀리면 평소엔 멀쩡하고 **신규 계정 생성과 크론이 겹치는 날에만** 전 사용자 문서가 날아간다 — 테스트로도 리뷰로도 안 잡히는 종류다. 이런 일은 스펙 왕복 비용이 직접 쓰는 비용을 넘고, 무엇보다 **검증 부담이 그대로 남는다.**

> 이 절은 Claude의 범위를 넓히자는 게 아니라 **이미 그렇게 하고 있는 좁은 영역을 명문화**하는 것이다. 화면·문구·대량 편집은 그대로 Antigravity 몫이다.

## 2. 운영 규칙 (①~⑨)

### ① 완료 정의(DoD) — 넘기기 전에 스스로 통과시킨다
상대에게 넘기기 전 **작성자 본인이** `npx tsc --noEmit`과 `npm run build`를 통과시킨다. 빌드 에러를 Claude에게 고치게 하는 것은 가장 비싼 자원을 가장 기계적인 일에 쓰는 낭비다.

### ①-1 기존 파일은 부분 수정만 한다 — 재작성 금지 (2026-08-15 신설, 실사고 후)

**사고**: 통 이동 UI 재배선(`dbb999e`)이 지시는 "전용 모드 철거"였는데 `DirectSubstituteTab.tsx`를 사실상 전면 재작성했다(추가 891줄 / **삭제 1816줄**, onClick 39개 삭제·24개 추가). 그 결과 지시에 없던 기능 4건이 조용히 사라졌다 — 체인 원본 자동 지정, 체인 3단계 확장 버튼, 담기 이탈 경고, **보강 후보 목록 전체(보강 직권 배정 자체가 불능)**. `tsc`·`build`는 전부 통과했다. **타입 검사는 "버튼이 없어진 것"을 잡지 못한다.**

1. **기존 파일을 새로 써서 갈아끼우지 않는다.** 요청받은 부분만 고친다. 파일이 커서 읽기 어렵다는 것은 재작성의 근거가 아니다.
2. **삭제가 추가보다 많으면 멈추고 보고한다.** 철거 작업이라 정상일 수 있으나, 그때도 **무엇을 왜 지웠는지 항목별로** 핸드오버에 적는다. "리팩터링했다"는 설명이 아니다.
3. **넘기기 전 자기 점검 (DoD에 추가)**: `bash scripts/check_ui_removals.sh <작업-시작-커밋>` — 사라진 핸들러·상태·화면 라벨을 뽑아 준다. 뜬 항목마다 **"이 삭제가 지시서에 있었나?"** 를 묻고, 없던 것은 되살린 뒤 넘긴다.
4. **검수자(Claude) 고정 절차**: 지시받은 것이 들어왔는지만 보지 말고 **없어진 것을 먼저 본다.** 위 스크립트 + `git diff --numstat`의 삭제량 확인을 표적 검수 첫 단계로 둔다. 2026-08-15 1차 검수가 §5c-6 판정 ①~⑤(전부 "새 기능이 되는가")만 보고 통과시켜 회귀 4건을 놓쳤다 — 완료 판정에 "기존 화면 동작 불변"이 없었던 것이 근본 원인이다.

### ② 자기 작업은 자기가 커밋한다
커밋 독점을 폐지한다. **작성자가 자기 변경을 반드시 직접 커밋한다.**
- **커밋 없이 작업 트리에 파일만 남겨 두고 끝났다고 하지 않는다.** (검수자가 커밋·정리를 대행하지 않음)
- 커밋 메시지에는 **무엇을 왜 바꿨는지(What & Why)** 명확히 적는다.
Claude는 대신 **커밋 규칙**(메시지 형식, 작업 트리 청결)을 관리한다.

### ③ 작성자는 자기 작업의 최종 승인자가 아니다 (교차 검증)
- Claude가 정한 스펙/백엔드 판단 → Antigravity가 화면에서 확인
- Antigravity가 쓴 코드 → Claude가 위험 지점 표적 리뷰

### ④ 핸드오버는 고정 양식으로 남긴다
`project_notes.md` 하단에 누적한다.

```markdown
## [2026-07-22] Antigravity → Claude
- 변경 파일: src/components/admin/UserList.tsx
- 검증 상태: tsc ✅ / build ✅ / lint ⚠️(기존 부채 12건)
- 다음 할 일: 삭제 복원 로직의 권한 검사 스펙 판단 요청
- 주의: users:all 캐시 무효화 시점이 바뀌었음
```

**간소화 원칙 (2026-08-05 추가)**: 핸드오버는 **요약 + 커밋 해시 + 주의점**만 쓴다. 변경 내용 나열은 5줄 이내 — 상세 diff는 git에 이미 있으므로 문자열·항목을 통째로 재나열하지 않는다(재나열은 project_notes를 다시 살찌워 아카이브 회전(④-1)을 앞당기는 토큰 낭비다).

### ④-3 작업 인계 3대 필수 게이트 (DoD Handover Gate) — 2026-08-15 신설

작업을 마치고 상대 에이전트 또는 사용자에게 인계할 때는 **예외 없이 반드시 아래 3단계를 스스로 통과시키고 마친다**:

1. **자기 변경 직접 커밋 (②)**: 작업 트리에 미커밋 변경사항을 남기지 않고, 무엇을 왜 바꿨는지 명시하여 커밋한다.
2. **`project_notes.md` 4줄 고정 양식 핸드오버 기록 (④)**: 파일 목록, 검증 상태, 다음 할 일, 주의 4줄로 작성하며 변경 내용 나열은 5줄 이내로 요약한다 (append-only 준수).
3. **화면 파일(.tsx) 수정 시 삭제 항목 검사 및 소명 (①-1-3)**:
   - `bash scripts/check_ui_removals.sh <작업-시작-커밋>`을 실행하고, 출력된 항목마다 *"이 삭제가 지시서에 있었나"*를 확인하여 핸드오버에 결과를 적는다.
   - **도구 오류나 환경 문제로 실행하지 못했으면 실행하지 못했다고 정직하게 적는다.** 안 했는데 했다고 적거나 누락하는 것이 최악이다. **도구가 안 되거나 절차를 못 지켰으면 그 사실이 첫 번째 보고 대상이다.**

### ④-1 project_notes.md 아카이브 회전 및 읽기 규칙 (2026-08-05 신설)

일지가 무한히 자라며 읽기만 해도 토큰을 태우는 문제(2026-08-05, 459KB 도달)의 재발 방지 장치다.

- **회전**: 매월 초 첫 체크포인트를 작성할 때(또는 live 파일이 150KB를 넘으면 즉시), 지난달 핸드오버 엔트리를 `archive/project_notes_YYYY-MM.md`로 **원문 그대로** 이관한다. 이관 후 재조립 대조(`cmp`)로 무손실을 검증하고, live 파일 핸드오버 섹션 머리에 아카이브 안내 한 줄을 남긴다. 상단 고정 섹션(작업 중 파일·미검증·검증 완료·향후 고려·배포 체크리스트)은 회전 대상이 아니다 — 살아있는 항목은 로드맵·스펙으로 승격하는 기존 관행 유지.
- **읽기 (모든 대형 문서 공통, 2026-08-05 일반화)**: 세션 시작 시 project_notes.md는 **마지막 체크포인트(꼬리)만** 읽는다. project_notes에 국한하지 않고, **어떤 문서든 수십 KB급이면 전체 읽기 금지** — 날짜·키워드·섹션 검색으로 필요한 부분만 읽는다 (스펙은 해당 § 단위, 아카이브는 검색 결과 부분만).
- **인용**: 기록을 다른 문서에서 인용할 때 행 번호를 쓰지 않는다(회전 시 어긋남). **날짜+엔트리 제목**으로 인용한다.

### ④-2 공유 문서는 덧붙이기만 한다 (append-only) — 2026-08-13 신설, 4회 재발 후

`project_notes.md`·`AGENTS.md`·`development_roadmap.md`처럼 **두 에이전트가 함께 쓰는 문서**에는 이 규칙이 적용된다.

- **덧붙이기만 한다.** 기존 줄의 수정·압축·재배치는 사용자 지시나 상대 합의가 있을 때만 한다. "요약했다"·"정리했다"는 대개 압축이 아니라 **유실**이었다.
- **커밋 전 삭제 줄을 확인한다**: `git diff --numstat <문서>`의 **두 번째 열(삭제)이 0이 아니면 멈추고** 무엇이 지워졌는지 확인한다. 의도한 삭제가 아니면 복원 후 커밋한다.
- 아카이브 회전(④-1)은 이 규칙의 **유일한 예외**다. 그때도 원문 그대로 이관하고 `cmp`로 무손실을 검증한다.
- **상태 줄 1줄 덮어쓰기 예외 (2026-08-13 개정)**: `development_roadmap.md`의 **상태 줄(`- <토큰> **제목** — …`)은 덮어쓰기가 의무다.** 상태가 바뀌었는데 이 줄을 안 고치면 그것이 규칙 위반이다. 본문(그 아래 모든 줄)은 예외 없이 append-only를 유지한다. 즉 **대장은 덮어쓰고 일지는 쌓는다.** 한 줄만 예외이므로 `--numstat` 확인은 계속 유효하다(상태 줄 갱신 1건당 삭제 1줄이 정상).

**왜 규칙까지 만드는가 — 같은 사고가 네 번 났다.** ① 2026-07-27 `55b6a83`이 Claude의 반려 리뷰 기록을 삭제(`fbc1571`로 복원) ② 2026-08-12 세션 체크포인트를 "요약 정리"하면서 커밋 해시 색인 전체·보안 원칙·중간점검 지시·**같은 세션에서 방금 기록한 실기기 확인 결과**를 삭제(58줄 변경 중 대부분이 삭제) ③ 2026-08-13 UX 점검 섹션을 덧붙이며 판단 필요 목록 6·7번 삭제 — 7번은 커밋 `c6fb0f9`와 로드맵 §2가 참조하는 항목이라 유실 시 추적이 끊긴다(Claude 복원).

이 규칙이 다른 규칙보다 나은 점은 **기계적으로 확인된다**는 것이다. 판단이 필요 없고, 숫자 하나만 보면 된다.

### ⑤ 규칙은 단일 원본을 둔다
**이 파일(`AGENTS.md`)이 단일 원본(Single Source of Truth)이다.** `CLAUDE.md`와 `.agents/AGENTS.md`에는 이 파일을 가리키는 안내만 둔다. 규칙 변경은 반드시 이 파일에서 한다.

### ⑥ Claude를 부르는 기준 (에스컬레이션)

**부른다**
- 되돌릴 수 없는 작업: 계정 삭제·복원, 데이터 마이그레이션, 배포 직전 점검
- 보안·개인정보 결정
- 같은 버그를 두 번 고쳐도 재발할 때
- 아키텍처 갈림길에서 방향이 갈릴 때
- **"완료됐다"/"파일럿 통과했다"를 사용자에게 보고하기 전 (2026-07-24 추가)**: Phase 5.8에서 실제로는 핵심 기능 일부가 빠진 채(드라이브 이동 미작동, 알림 미구현) "7번(파일럿)까지 다 됐다"는 보고가 Claude 확인 없이 사용자에게 먼저 전달돼 혼선이 생겼다. 배포·파일럿·"완료" 같은 상태 보고는 **Claude가 코드로 재검증하기 전까지 사용자에게 확정형으로 전달하지 않는다.**

**부르지 않는다** (Antigravity가 끝낸다)
- 보일러플레이트, UI 조정, 문구 수정
- 빌드/타입 에러 중 원인이 명확한 것
- 단순 반복 수정, 파일 탐색

**안전장치 (Antigravity 제안, 합의됨)**: 단순 오류로 판단해 수정을 시작했더라도 **2회 시도 내에 해결되지 않거나 원인이 아키텍처/스펙 문제로 확장되면 즉시 손을 떼고 Claude에게 에스컬레이션한다.** 막힌 채 혼자 오래 붙잡는 것이 Claude를 부르는 것보다 비싸다.

### ⑦ Claude에게는 "컨텍스트 팩"을 만들어 넘긴다
Claude는 출력뿐 아니라 **파일을 읽는 입력에도 비용이 든다.** 저장소를 직접 훑게 하면 판단 전에 한도가 깎인다. Antigravity가 아래 4줄로 미리 요약해 넘긴다.

```markdown
## Claude 요청: (한 줄 제목)
- 관련 파일: 경로 + 줄 범위
- 증상: 무엇이 잘못되는가
- 이미 시도: 무엇을 해봤고 왜 실패했는가
- 묻고 싶은 것: 판단이 필요한 지점
```

### ⑧ Antigravity 작업 마무리 시 의무 출력 규칙 (2026-07-24 추가)

Antigravity는 사용자의 요청이나 할 일을 마치고 응답을 마무리할 때, **예외 없이 반드시 답변 최하단에 `💡 다음 작업 지시 가이드` (대상 에이전트 + 복사용 프롬프트) 양식을 작성하고 마쳐야 한다.**

- 대화의 흐름상 추가 작업을 Claude에게 넘기거나 사용자 승인을 청구하는 지점이 아니더라도, 다음 추천 행동(예: Claude 재검증 요청, Git 커밋 진행 등)을 담은 복사용 프롬프트를 항상 상자로 제공한다.
- 이 가이드 상자를 누락하고 대화를 종료하는 것은 규정 위반이다.

### ⑨ Claude 측 반복 실패 (2026-08-13 신설, 양측 합의)

**이 문서의 협업 규칙은 2026-08-13까지 거의 전부 Antigravity의 실패를 흡수한 결과물이었고, Claude의 실패는 하나도 들어 있지 않았다.** 공정하지 않을뿐더러 **깨지기 쉽다** — Claude의 교훈이 전부 사용자 쪽 기억에만 쌓여 있어서, 그것이 로드되지 않는 세션에서는 통째로 사라진다. Antigravity 회신도 같은 취지였다: *"협업 규칙은 한쪽에 대한 일방적 제약이 아니라, 양쪽의 실사고 교훈을 단일 원본에 축적해 세션이 바뀌어도 유지되게 하는 시스템이어야 한다."*

아래는 전부 **실사고**다. Claude는 판단을 시작하기 전 이 목록을 자기 점검에 쓴다.

| # | 반복 실패 | 사건 | 규칙 |
|---|---|---|---|
| 1 | 헤드라인만 보고 권고 | 2026-08-11 벤치마크만 보고 기본 모델 교체를 권했다가 사용자 반박으로 번복 | 모델·도구 교체 권고 전 **실사용 리포트를 교차 확인**한다. 벤치마크 우위는 실사용 우위가 아니다 |
| 2 | 보안 항목을 대기로 세움 | 2026-08-11 사용자가 확정한 보호 가드를 "착수 대기"로 두었다가 유실 → hmnotice 계정 정지 사고 | **사용자가 확정한 보안 항목은 즉시 잔여 큐에 등재한다.** "착수 대기" 상태로 두지 않는다 |
| 3 | 실측 예산 초과 | 2026-08-08 Firestore 읽기 할당량 소진(전면 401) | 전수 조사 전 **읽기량을 추산**한다. 실측 스크립트도 실서비스 할당량을 쓴다 |
| 4 | 유령 항목을 병목으로 보고 | 트리아지 6건 중 3건이 갱신 안 된 "대기" 표기였음 | "대기/잔여/미착수"는 **원문·실데이터로 확인한 뒤** 병목이라 말한다 |
| 5 | 성급한 마무리 | "남은 건 이것뿐" 류 닫는 멘트 반복 | 사용자는 기능 개선을 계속 진행 중이다. **닫는 멘트를 쓰지 않는다** |
| 6 | 작성자에게 검증을 되돌림 | 2026-08-13 오보를 낸 주체에게 자기 보고서 재검증을 맡기려 함 (사용자 지적으로 정정) | **검증은 작성자에게 되돌리지 않는다** — ③ 교차 검증 원칙의 당연한 따름 |
| 7 | 다음 지시 프롬프트를 빠뜨림 | 2026-08-14 `CLAUDE.md:28`이 **필수**로 정한 "복사해서 쓸 수 있는 다음 지시 프롬프트"를 최근 답변 3건에서 누락 (사용자 지적) | **답변 말미에 예외 없이 넣는다.** 다음 차례가 Claude 자신이어도 넣는다 — "제가 하겠습니다"로 끝내면 사용자는 무엇을 쳐야 할지 모른다. 원인은 §1-2 신설·Max20 결정으로 Claude 직접 수행 비중이 커지자 **넘길 일이 없을 때 프롬프트도 같이 생략한 것**인데, 규칙의 목적은 인계가 아니라 **비전문가인 사용자가 다음에 칠 문장을 아는 것**이다 |
| 8 | 사용자에게 외계어로 보고 | 2026-08-14 사용자: **"중간점검에서 하는 말들이 사실상 98% 외계어다. 그냥 시키는 대로 하면서 '뭔가 개선되겠지' 하는 심정으로 있는 중"** | 문제는 어려운 내용이 아니라 **청중을 안 나눈 것**이다. 파일 경로·행 번호·영문 식별자·커밋 해시는 **문서와 커밋 메시지의 언어**이고 거기 있어야 한다. **사용자에게 하는 답변은 다르다** — 맨 위에 **「선생님이 알아야 할 것」 3줄 이내**를 전문용어 없이 두고, 기술 내용은 그 아래로 내린다. 사용자가 결정할 것이 있으면 그 3줄 안에 있어야 한다. *내용을 쉽게 만들라는 게 아니라, 읽을 사람을 정하라는 것이다* |
| 9 | 사용자 영역을 침범 | 2026-08-14 **하루에 두 번**. ① ⑤ §3-1에 "9월 회의체 일정 확인·10월 1차 시연" 일정을 처방 → 사용자 *"네가 걱정할 일이 아냐, 내가 걱정할게"* → 철회. ② 그날 저녁 **교직원 회의용 발표 대본**(시간 배분·할 말·조작자 지정·예상 질문 답변)을 작성 → 사용자 *"이건 오지랖이야"*. **철회문 머리말에 "누구에게 언제 보일지는 사용자 소관"이라고 스스로 써 두고 본문에서 어겼다.** | **학교 안에서 사람을 상대하는 일은 전부 사용자 영역이다** — 누구에게·언제·무슨 말로·누가 조작할지. Claude는 현장 맥락(사람·관계·회의 분위기)을 알 수 없고, 모르면서 쓴 각본은 도움이 아니라 부담이다. **Claude가 낼 수 있는 것은 「시스템이 무엇을 할 수 있는 상태인가」까지다.** `system-informs-not-mediates`(조율·소통 대행 기능 설계 금지)의 조언 버전이며, 기능이 아니라 문서로도 넘지 않는다 |

## 3. 동시 작업 충돌 방지

1. **동시에 같은 파일을 편집하지 않는다.** 작업 시작 시 핸드오버 기록에 "작업 중" 파일을 명시한다.
2. **상대 영역 파일을 고쳐야 하면, 고치기 전에 핸드오버 기록에 이유를 남긴다.**
3. **커밋할 때 `git add -A`·`git add .`를 쓰지 않는다 — 자기가 변경한 파일만 명시적으로 add 한다.** (2026-08-05 실사고: Claude의 문서 커밋 `git add -A`가 Antigravity의 작업 중 파일 52줄을 쓸어 담아 c6c679d에 오귀속됨. 내용 유실은 없었으나 이력이 오염됨.)
4. **작업 시작 전 `git status`로 상대의 미커밋 변경이 있는지 확인한다.** 있으면 먼저 정리하거나 사용자에게 묻는다.

> **집행 방법 (2026-08-13 개정 — 죽은 장치 폐기)**: 원래 이 자리에는 "`project_notes.md` 최상단 **🔒 현재 작업 중 파일** 목록을 편집 전 확인·갱신한다"는 규칙이 있었다(2026-07-24 추가, Phase 5.8에서 같은 파일을 동시 편집하다 한쪽 수정이 조용히 사라지고 main이 빌드 깨진 채 커밋된 사고 대응). **2026-08-13 실측 결과 그 목록은 project_notes.md에 존재하지 않는다(`🔒` 검색 0건).** 아무도 갱신하지 않은 채 규칙만 1년 가까이 남아 있었다.
>
> 두 장치를 병행하는 대신 **실제로 작동하는 위 4번(`git status` 확인) 하나로 일원화한다.** 지켜지지 않는 규칙을 문서에 남겨 두면 문서 전체의 구속력이 같이 떨어진다. 편집 전 `git status`에 상대의 미커밋 변경이 보이면, 그 파일은 건드리기 전에 먼저 확인한다.

## 4. 엔진(모델) 선택 규칙

### 대원칙 — 자기 드롭다운은 자기만 안다

**각 에이전트는 자신의 모델만 추천한다.** 상대 에이전트의 드롭다운에 무엇이 있는지는 볼 수 없으므로, 상대에게 넘길 때는 **"어느 에이전트에게 넘기는가"까지만** 지정하고 **구체적 모델명은 받는 쪽이 정한다.**

> **왜 이 규칙이 생겼나 (2026-07-22)**: 이 규칙의 초판에는 `Claude Sonnet 4.6` / `Claude Opus 4.6` 이라는 **실재하지 않는 모델명**이 적혀 있었다. 상대 라인업을 추측해서 쓴 결과였다. **모델명은 반드시 실제 드롭다운에서 확인한 것만 쓰고, 절대 추측하지 않는다.**

### 추천 시점 — 넘길 때가 먼저다

| 시점 | 누가 | 무엇을 |
|---|---|---|
| **① 작업을 넘길 때 (주된 절약)** | 넘기는 쪽 | "다음은 ○○ 에이전트" — 에이전트만 지정 |
| **② 작업을 받았을 때 (보조)** | 받는 쪽 | 현재 모델이 **명백히 과하거나 모자랄 때만** 한 줄 안내 |

②를 **매번 출력하지 않는다.** 이미 그 모델로 대화가 시작된 뒤라 브리핑 자체가 비용이며, 특히 비싼 모델이 "저는 과합니다"라고 말하는 순간 아끼려던 토큰은 이미 쓰인 뒤다. 불일치가 클 때만, 한 줄로, 다음 대화부터 바꾸도록 안내한다.

### Claude 측 모델 선택 (Claude 자신이 판단)

**실제 드롭다운 확인 결과 (2026-08-11 정정 — 7/22 당시 "Opus 4.8"로 오기재됐던 것을 사용자 스크린샷으로 정정)**: `Fable 5` / `Opus 5` / `Sonnet 5` / `Haiku 4.5` (그 외 "더 많은 모델" 하위 메뉴 존재). 주의: 새 세션 시작용 "최근 사용 모델" 퀵메뉴에는 `Opus 4.8`·`4.7`·`4.6` 같은 구버전 캐시 표기가 남아 있을 수 있다 — 번호(1~4) 붙은 정식 "모델" 드롭다운을 기준으로 삼는다.

**벤치마크 vs 실사용 (2026-08-11 웹 검색, 2차 확인 — 1차 결론 번복)**: 헤드라인 벤치마크만 보면 Opus 5(2026-07-24 출시)가 Fable 5보다 절반 가격에 지식노동·추론 벤치마크 다수에서 앞선다(1차 결론이었음). 그러나 **실사용 리포트는 반대**다 — 개발자들이 Opus 5를 "퇴보"로 부르며, 사소한 문제도 대규모 재작성으로 과잉 대응해 리뷰 부담을 키운다는 평(Theo), 실전 디버깅에는 "거의 못 쓸 정도"라는 평(Martin Casado), 장황하고 산만하다는 불만이 반복 보고된다. 벤치마크 내부에서도 실사용 근사치인 Deep Suite는 오히려 소폭 하락, 종합 지표(Epoch Capabilities Index)에서는 Opus 5가 Fable 5보다 **뒤처진다.** 사용자가 Opus 5 출시 후에도 계속 Fable 5를 써온 것이 이 실사용 평가와 일치 — **헤드라인 벤치마크만으로 모델을 바꾸지 않는다.** 출처: [MindStudio — 실사용 반응](https://www.mindstudio.ai/blog/claude-opus-5-mixed-reception), [MindStudio — 헤드라인에 없는 수치](https://www.mindstudio.ai/blog/claude-opus-5-benchmarks-explained), [Anthropic 공식](https://www.anthropic.com/news/claude-opus-5).

| 작업 성격 | 선택 |
|---|---|
| 되돌릴 수 없는 작업(계정 삭제·복원, 데이터 마이그레이션), 보안·개인정보 결정, 배포 직전 점검 | `Fable 5` 또는 `Opus 5` — 최상위 |
| 아키텍처 결정, 스펙 작성, 2회 실패 버그의 근본 원인 진단 | `Opus 5` |
| 일반 코드 리뷰, 위험 지점 표적 리뷰, 문서·규칙 정리 | `Sonnet 5` |
| 단순 확인, 커밋 정리, 짧은 질의응답, 상태 조회 | `Haiku 4.5` |

기본값은 `Opus 5`로 두되, **판단이 필요 없는 기계적 작업이 예상되면 시작 전에 `Sonnet 5`나 `Haiku 4.5`로 낮춘다.** 대화가 시작된 뒤에 낮추면 이미 쓴 토큰은 돌아오지 않는다. **가격만 보고 낮추지 않는다** — 벤치마크가 좋아도 실사용 평가가 나쁜 모델(예: 위 Opus 5 코딩 사례)로 낮췄다가 결과물 품질이 떨어지면 재작업 비용이 절감액을 넘어설 수 있다.

> **빠른 모드**: 모델을 낮추는 기능이 아니라 같은 모델의 출력 속도를 올리는 옵션이다. **토큰 절약 수단이 아니므로 비용 판단과 혼동하지 않는다.**
>
> Claude 계열은 버전이 자주 바뀐다. 이 표가 실제 드롭다운과 다르면 **드롭다운을 따르고 이 표를 갱신한다.**

### Antigravity 측 모델 선택 (Antigravity 자신이 판단)

| 작업 성격 | 선택 |
|---|---|
| 단순 오탈자, 린트 수정, 보일러플레이트 | 최경량·저추론 |
| 일반 코드 생산, UI 구현, E2E 검증, DoD 빌드 체크 | 중간 등급 (기본값) |
| 대규모 파일 탐색, 타입 부채 전면 개편 | 상위 등급 |

> 구체적 모델명은 **Antigravity가 자기 드롭다운에서 확인해 채운다.** Claude는 이 목록을 검증할 수 없으므로 임의로 수정하지 않는다.

### 넘길 때 양식 (모델명 없이 에이전트만 지정)

```markdown
💡 **다음 작업 지시 가이드**
- **대상 에이전트**: Claude
- **복사용 프롬프트**: *"Claude, 방금 구현한 기능의 위험 지점을 표적 리뷰해줘."*
- *(어떤 모델로 돌릴지는 Claude가 받아서 판단합니다)*
```

## 5. 대화 길이 관리 및 체크포인트 규칙 (Claude 전용)

> **적용 범위**: 이 절은 **Claude에만 적용된다.** Antigravity의 컨텍스트 처리·자동 압축 방식은 확인된 바 없으므로 동일하다고 가정하지 않는다.

### 사실 확인 (2026-07-22)
- Claude는 매 턴마다 지금까지의 대화 전체를 다시 읽고 응답한다. 상태가 유지되는 것이 아니라 매번 전체 맥락이 다시 전달된다.
- 다만 "대화가 길어질수록 비용이 기하급수적으로 폭증한다"는 표현은 부정확하다. 프롬프트 캐싱으로 이미 읽은 부분은 훨씬 싼 단가로 처리된다. 실제 누적 비용이 늘어나는 이유는:
  1. 캐싱되지 않는 새 내용(새 질문, 파일 읽기 결과, 도구 출력)은 매번 정가로 청구된다
  2. 대화가 길고 주제가 다양할수록 판단에 필요한 사고(추론) 토큰도 늘어난다
- 이 하네스는 컨텍스트가 한계에 가까워지면 **이미 자동으로 대화를 요약(compact)한다.** 다만 이는 사후적이고 손실 있는 압축이라 세부 결정이 뭉개질 수 있다. 아래 규칙은 이를 대체하는 것이 아니라, **작업이 실제로 마무리되는 시점에 선제적·고정밀 체크포인트**를 남기는 보완이다.

### 체크포인트 제안 트리거
Claude는 세션의 정확한 토큰 사용량을 직접 조회할 수 없다. 아래 중 **2개 이상**이 겹치면 체크포인트를 제안한다. 매 턴 제안하지 않는다.
- 서로 무관한 작업이 3개 이상 이어졌다 (예: 버그 수정 → 문서 작성 → 규칙 개편처럼 주제가 바뀜)
- 사용자가 "대화가 길다/무겁다" 등을 직접 언급했다
- 하나의 작업 단위가 완결되어 다음 작업이 명백히 별개 주제로 전환된다

### 체크포인트 문서 형식
`project_notes.md`의 핸드오버 기록 위치에, 아래 항목으로 남긴다.
- 이번 대화에서 내린 결정과 근거
- 변경한 파일과 커밋 해시
- 아직 열려 있는 질문/미해결 사항
- 새 대화창에서 이어갈 다음 작업

### 사용자 안내 문구 예시
> 💡 대화가 여러 주제를 거치며 길어졌습니다. 지금까지 내용을 `project_notes.md`에 체크포인트로 정리해 뒀습니다. 원하시면 새 대화창을 열어 "`project_notes.md`의 최신 체크포인트를 읽고 이어서 [다음 작업]을 진행해줘"라고 시작하시면, 이번 대화의 세부 맥락 손실 없이 더 가벼운 비용으로 이어갈 수 있습니다.

> **[2026-08-16 정정 — 위 "새 대화창이 더 싸다" 전제는 낡았다]** 이 규약을 쓴 시점에는 긴 대화의 이력 재전송이 주 비용이라 새 창이 쌌다. 그 후 두 가지가 바뀌었다: ① 하네스의 자동 요약·캐시가 긴 대화의 한계 비용을 크게 깎았고 ② 세션 시작 필수 문서(AGENTS 60KB + 로드맵 124KB + notes)가 비대해져 **새 창의 고정비가 더 커졌다**(2026-08-15 실측: 하루 7세션 = 부팅 오버헤드만 수십만 토큰 급). **현행 권고: 같은 프로젝트 작업은 한 창에서 길게 잇는다(하루 1~2창).** 새 창이 여전히 맞는 경우는 ⓐ 완전히 다른 저장소/주제로 넘어갈 때 ⓑ 대화에 잘못된 전제가 눌어붙어 리셋이 필요할 때 ⓒ 며칠 쉬고 재시작할 때뿐이다. **체크포인트 규약 자체(작업 완결 시 고정밀 기록)는 그대로 유효하다** — 기록의 목적이 "새 창 이어달리기"에서 "며칠 뒤·다른 에이전트의 착수점"으로 바뀌었을 뿐이다. 이 정정은 비용 구조가 또 바뀌면 다시 낡는다 — 재실측이 근거다.
<!-- END:dual-agent-collaboration-rules -->

<!-- BEGIN:prefetch-first-rules -->
# 백그라운드 프리페치 데이터 우선 사용 규칙

이 프로젝트는 로그인 직후 `AuthContext.tsx`에서 핵심 데이터를 백그라운드로 미리 로딩한다.
새 기능을 개발할 때 아래 목록을 **1순위로 확인**하고, 해당 데이터가 있으면 반드시 우선 사용한다.
불필요한 API 재호출, 로딩 스피너, 중복 state를 만들지 않는다.

## 프리페치 데이터 목록 (로그인 시 자동 로드)

| 데이터 | 접근 방법 | 캐시 키 | 대상 API |
|---|---|---|---|
| **조직단위(OU) 목록** | `useAuth().orgUnits` (React state) | `"ou:all"` | `GET /api/workspace/ou` |
| **전체 사용자 목록** | `getClientCache("users:all")` | `"users:all"` | `POST /api/workspace/users` |
| **그룹(메일링리스트) 목록** | `getClientCache("groups:all")` | `"groups:all"` | `POST /api/workspace/groups` |
| **생활지도 권한·규정** | `getClientCache("discipline:my")` | `"discipline:my"` | `POST /api/discipline/permissions` (`action:"my"`) |
| **학교 설정** | `useAuth().schoolSettings` (React state) | Firestore 실시간 구독 | — |
| **로그인 사용자 정보** | `useAuth().userData` (React state) | Firestore 실시간 구독 | — |

## 개발 체크리스트 (새 컴포넌트/기능 개발 전 필독)

1. **OU 목록이 필요한가?** → `useAuth().orgUnits`를 바로 사용한다. `fetch("/api/workspace/ou")`를 새로 호출하지 않는다.
2. **사용자 검색/목록이 필요한가?** → `getClientCache("users:all")`로 로컬 필터링한다. 캐시가 없는 경우에만 API를 온디맨드 호출한다.
3. **그룹 목록이 필요한가?** → `getClientCache("groups:all")`로 로컬 필터링한다.
4. **학교 설정(학년 수, OU 매핑 등)이 필요한가?** → `useAuth().schoolSettings`를 사용한다.
5. **생활지도 권한·규정이 필요한가?** → `getClientCache("discipline:my")`를 우선 확인하고 무효화가 필요할 때만 재조회한다.

## 구현 패턴 예시

```typescript
// ✅ 올바른 방법 - 프리페치 데이터 우선 사용
const { orgUnits, schoolSettings } = useAuth();

// ❌ 금지 - 이미 있는 데이터를 재호출
const [orgUnits, setOrgUnits] = useState([]);
useEffect(() => {
  fetch("/api/workspace/ou").then(...).then(data => setOrgUnits(data));
}, []);
```

## 새 데이터를 프리페치에 추가하는 방법

새 기능에서 공통적으로 필요한 데이터가 생겼을 때, 개별 컴포넌트에 fetch를 추가하지 말고:
1. `src/context/AuthContext.tsx`의 백그라운드 프리페치 블록(`setTimeout` 내부)에 추가한다.
2. 필요 시 `AuthContextType` 인터페이스와 state를 추가하여 `useAuth()`로 노출한다.
3. 이 문서의 프리페치 데이터 목록 표를 업데이트한다.
<!-- END:prefetch-first-rules -->

<!-- BEGIN:firestore-security-rules -->
# Firestore 보안 규칙 — 배포 전 필수 변경 규칙

이 프로젝트는 현재 개발 편의를 위해 Firestore 보안 규칙이 공개(open) 상태이다.
**배포(Vercel 등 외부 공개) 직전에 반드시 아래 절차를 수행해야 한다.**

## 현재 개발용 규칙 (배포 금지)

```js
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{documents=**} {
      allow read, write: if true;  // ⛔ 공개 — 로컬 전용
    }
  }
}
```

## 배포 시 적용해야 할 규칙

```js
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ✅ 로그인한 사용자(Google 계정)만 읽기/쓰기 허용
      allow read, write: if request.auth != null;
    }
  }
}
```

## 변경 방법

Firebase 콘솔 → Firestore → **규칙** 탭 → 위 내용으로 교체 → **게시** 클릭

## 근거

- 이 앱의 서버 로직(Next.js API Route)은 Firebase Admin SDK를 사용하므로 보안 규칙 적용 대상 외
- 클라이언트는 로그인 후 실시간 구독(`onSnapshot`)만 사용하므로 `request.auth != null` 조건이 충분
- 학생 개인정보 보호를 위해 공개 규칙 상태로 배포하는 것은 절대 금지
<!-- END:firestore-security-rules -->

<!-- BEGIN:autocomplete-input-rules -->
# 계정 및 그룹 입력 필드 자동완성(Autocomplete) 규칙

사용자 계정(이메일, 아이디) 혹은 구글 워크스페이스 그룹 메일 주소를 텍스트로 직접 입력하거나 검색해야 하는 새로운 입력창을 개발할 때:

1. **공통 AutocompleteInput 컴포넌트 사용**:
   - 직접 텍스트 인풋창을 설계하지 않고, `AutocompleteInput` (`src/components/admin/AutocompleteInput.tsx`)을 공용으로 사용하여 이메일, 성, 이름 기반의 통합 검색 및 드롭다운 선택을 구현한다.

2. **성능 최적화 필수**:
   - 자동완성 검색 구현 시 **디바운스(200~300ms)**를 필수 적용한다.
   - 그룹 메일 등 데이터 용량이 고정적이고 작은 정보는 페이지 로드 시 1회만 fetch하여 **로컬 메모리 필터링**을 적용하고, 사용자 계정 등 가변적인 대량 정보는 입력에 따라 API를 **온디맨드 호출**한다.

3. **성/이름 검색 지원**:
   - 일반 계정 검색의 경우, 사용자가 이메일 아이디 외에도 성(Family Name) 또는 이름(Given Name)을 입력해도 검색이 지원되도록 API 쿼리를 연동한다.

4. **[2026-07-28 추가] 선택 강제 — 원시 검색 문자열을 절대 제출값으로 쓰지 않는다** (gotest 유령 레코드 사고 재발 방지):
   - **검색어 state와 제출값 state를 분리**한다. `AutocompleteInput`의 `onChange`는 검색어 표시 전용이며, **제출에 쓰는 값(이메일 등)은 `onSelect`에서만 세팅**한다. `onChange={setSubmitEmail}`처럼 원시 텍스트를 제출 상태에 직접 바인딩하는 것을 금지한다.
   - 제출 버튼은 `!inputText`(비어있음) 가드가 아니라 **`onSelect`로 채워진 선택 객체/값의 null 가드**로 활성화한다. 검색어를 수정하면 선택값을 즉시 무효화한다.
   - 표준 구현 예: `PasswordReset.tsx`, `TransferOutTab.tsx` (검색어·선택 객체 분리 + 선택 가드).
   - 사고 사례: 전출 교사 등록 폼이 검색창 원시 문자열 "gotest"를 그대로 제출 → 존재하지 않는 계정이 전출 큐 문서 ID로 등록되고, GWS 조치는 전부 실패했는데도 등록이 계속 진행돼 UI로 지울 수 없는 유령 레코드가 남았다.

5. **[2026-07-28 추가] 서버 측 이중 방어 — UI를 신뢰하지 않는다**:
   - 요청 body로 **대상 계정 식별자**(email 등)를 받아 조치하는 모든 API 액션은 ① **이메일 형식 정규식 검증**(`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`, 실패 시 400)을 필수로 하고, ② 파괴적 조치(그룹 탈퇴·OU 이동·정지·삭제)나 **Firestore 신규 레코드 생성**(특히 식별자를 문서 ID로 쓰는 경우)이 있으면 **`getUser` 등 실존 확인 후 실패 시 4xx로 중단**한다.
   - **실존/조회 실패를 try/catch로 삼키고 등록·저장을 계속 진행하는 것을 금지**한다. 유령 레코드는 이 "경고만 남기고 계속" 패턴에서 나온다.
   - 취소·복구 액션은 대상 계정이 이미 없어도(404) 큐/레코드 정리는 수행되도록 내성을 갖춘다. 표준 구현 예: `register_teacher_transfer` / `cancel_teacher_transfer` (lifecycle).
<!-- END:autocomplete-input-rules -->

<!-- BEGIN:deployment-checklist-rules -->
# 정식 배포(Deployment) 시 체크리스트 규칙

정식 상용 배포 요청 시, 에이전트는 다음을 반드시 이행한다:

1. **배포 체크리스트 파일 사전 로드**:
   - 배포 지원을 시작하기 전에 반드시 저장소 루트의 [`deployment_checklist.md`](./deployment_checklist.md)를 읽고 점검 사항 및 검증 시나리오를 숙지한다.
   - (2026-07-22 이전에는 이 문서가 `~/.gemini/antigravity-ide/brain/` 아래 Git 밖에 고립되어 있었다. 저장소 루트로 이동 완료.)

2. **환경 변수 가이드**:
   - 배포 플랫폼에 입력할 환경 변수를 안내할 때, 알리미 계정(`GOOGLE_WORKSPACE_SENDER_EMAIL="hmnotice@hmh.or.kr"`)과 배포 도메인(`NEXT_PUBLIC_BASE_URL`), 크론 토큰(`CRON_SECRET`)이 누락되지 않도록 강조한다.

3. **서비스 계정 역할(IAM) 검증 안내**:
   - 도메인 전체 위임(Domain-wide delegation) 셋업과 더불어 서비스 계정에 **`Firebase 인증 관리자`** 역할이 부여되었는지 검사 단계를 안내한다.

4. **배포 전 필수 조치**:
   - Firestore 보안 규칙을 공개(`if true`)에서 인증 필수(`if request.auth != null`)로 반드시 변경한다. 상세는 아래 firestore-security-rules 섹션 참조.
   - 문서를 건드린 커밋 전 `npm run check:docs`를 실행하여 `development_roadmap.md` 등의 상태 표기 모순(drift)을 사전 검사한다.
<!-- END:deployment-checklist-rules -->
