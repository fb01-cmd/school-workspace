# 미뤄 둔 것 전수 (2026-08-14, **2026-08-15 보강**) — 📕 **이력 문서 (대장 선언 철회 2026-08-21)**

> ## ⛔ 이 문서는 대장이 아니다 — 지금 열린 항목은 [`STATUS.md`](../STATUS.md)에서 본다
>
> 원래 이 자리에는 *"이 문서는 살아 있는 대장이다"* 라고 적혀 있었다. **2026-08-21에 걷었다** —
> `AGENTS.md` ④-3의 2026-08-21 신설 규칙(*"다른 문서는 열린 작업 목록을 자처하지 않는다"*)에
> 정면으로 걸린다. **선언이 둘이면 갱신되지 않는 쪽이 살아남는다.**
>
> **아래 항목들은 2026-08-14~15 시점의 조사 결과다. 그 뒤로 닫힌 것이 있다.** 코드로 확인된 것:
>
> | 항목 | 지금 상태 | 근거 (코드, 2026-08-21 실측) |
> |---|---|---|
> | **A2** 결보강 담기 이탈 경고(`beforeunload`) | 반영됨 | [`DirectSubstituteTab.tsx:898`](../src/components/admin/timetable/DirectSubstituteTab.tsx) |
> | **A3** `["교장","교감","교목"]` 하드코딩 단일화 | 반영됨 | [`departments.ts:26`](../src/lib/org/departments.ts) `POSITION_LIKE_DEPARTMENTS` + [`OrgChartBuilder.tsx:284`](../src/components/admin/OrgChartBuilder.tsx) |
> | **A4 → A7** 나이스 일괄 입력 CSV 내보내기 배선 | 반영됨 | [`manage/route.ts:1520`](../src/app/api/timetable/manage/route.ts) `case "neis_csv"` + [`neis.ts:353`](../src/lib/timetable/neis.ts) |
>
> **이 문서 전체가 종결됐다는 뜻은 아니다.** A2·A3·A7 셋만 코드로 확인했고, **나머지 항목의 현재 상태는
> 이 문서로 판정하지 않는다** — B·C 갈래에는 아직 열려 있는 것이 섞여 있을 수 있고(예: 시트 브리지 일몰 9/10,
> 9c 시한 11월 말, Blaze 결제 복귀), 그 판정의 단일 원본은 `STATUS.md`다. 여기서 항목을 골라 착수하지 않는다.
>
> **B1(Max20 만료 처리)은 전제가 바뀌었다** — 실제 전환일은 2026-08-22이고, "8/24"는 낡은 날짜다.

*(이하 2026-08-14·15 조사 당시 기록 — 현재 상태가 아니다.)*

> 🔔 ~~**이 문서는 살아 있는 대장이다.**~~ **[2026-08-21 선언 철회 — 위 머리말 참조]** 아래 8/14 조사 뒤에, 스펙 문서 **안쪽**에 묻혀 있던 이월 항목 47건을 추가 발굴해 문서 끝에 **「2026-08-15 보강」** 절로 붙였다. A4 판정 정정(§F-0) 포함 — 새 항목을 고를 때는 그 절까지 함께 볼 것.

> 사용자 요청 *"뒤로 미뤄둔 거 상당히 많아. 일단 다 끄집어내 보자."*
> 기억이 아니라 **저장소에서 긁어모았다** — 로드맵 상태 대장(`npm run roadmap`) + 오늘 만든 문서 9종의 보류·미결 표시.
>
> **핵심은 목록이 아니라 분류다.** 40개를 나열하면 아무것도 안 골라진다. **"왜 미뤄졌는가"** 로 갈라야 지금 집을 수 있는 것이 보인다.

## 0. 한눈에

| 갈래 | 개수 | 뜻 |
|---|---|---|
| **A. 지금 할 수 있음** | **6** | 막는 것이 없다. 우선순위에 밀렸을 뿐 |
| **B. 사용자 결정 대기** | **3** | 방향이 정해져야 열린다 |
| **C. 사건 대기** | **11** | 특정 시점·데이터가 있어야 의미가 생긴다 |
| **D. 아이디어 (착수 미정)** | **13** | 하기로 정한 적 없다 |
| **E. 안 하기로 닫음** | **4** | 다시 열지 않는다 |

---

## A. 지금 할 수 있음 — 막는 것이 없다

| # | 항목 | 크기 | 출처 |
|---|---|---|---|
| **A1** | ~~**운영 매뉴얼에 백업 절차·데이터 유실 시나리오 등재**~~ **[2026-08-21 유령 판정 — 이미 해결]** `operations_handbook.md` §6(백업과 데이터 유실 대응, 6-1 백업·6-2 비전문가 복원 절차·6-3 한계)이 실재 | 작음 | 중간점검 ③ 처방 5 |
| **A2** | ~~결보강 담기 이탈 경고(`beforeunload`)~~ **[2026-08-21 유령 판정 — 이미 해결]** `DirectSubstituteTab.tsx`에 beforeunload 2곳 실재 | 작음 | UX 스캔 §6-6 |
| **A3** | ~~`OrgChartBuilder`의 `["교장","교감","교목"]` 하드코딩을 단일 소재지로~~ **[2026-08-21 유령 판정 — 이미 해결]** 해당 하드코딩 grep 0건 | 작음 | UX 스캔 §6-5 |
| **A4** | 나이스 내보내기 **실제 구현** (형식·매핑 오늘 확보) — 🔴 **[2026-08-15 정정] "기구현 유령"으로 닫은 것은 오판.** 탭은 있으나 CSV 내보내기는 비활성 스텁이다(§F-0). **미착수로 되돌리고 A7로 승계** | 중간 | 질의 결과 §4-2 |
| **A5** | 쪽지 쓰기 개편 v1.1 (발송 확인창 제거·조직도 우선 동선) — 스펙 확정됨 | 중간 | 로드맵 🔜 |
| **A6** | 공동교육 학생 계정 포털 접속 차단 — 스펙 확정, 관문·UI 모두 기구현 확인됨 | 확인만 | 로드맵 🔜 / 트리아지 §3-1 |

> **A1이 가장 급하다.** 백업을 만들어 놓고 매뉴얼에 안 적었다 — 지금 상태로는 **후임자가 백업이 있는 줄도 모른다.** `operations_handbook.md`에 "백업"이 1회 나오는데 그건 전출 교사 데이터 이야기이고, **데이터 유실 장애 시나리오는 아예 없다.**
>
> **A6은 실제로 남았는지 재확인이 먼저다.** 트리아지 §3-1에서 "기구현"으로 판명된 항목인데 로드맵 상태 줄은 아직 🔜다.

---

## B. 사용자 결정 대기 — 내가 정할 수 없다

| # | 항목 | 시한 |
|---|---|---|
| **B1** | **Max20 만료 처리** — 8/24 이후 분업이 자동으로 기본값 복귀 | **8/24** |
| **B2** | Phase 8(교사 업무 Task) 착수 여부 — 2026-08-08 방향 논의만 됨 | 없음 |
| **B3** | Phase 7(학부모 소통) 착수 조건 | 없음 |

> B1은 이미 결정됐다 — *"8/24까지 최대한 헤비하게"*. **그 뒤를 정하는 것이 남았고, 시한이 열흘 안이다.**

---

## C. 사건 대기 — 그 일이 일어나야 의미가 생긴다

| # | 항목 | 무엇을 기다리나 |
|---|---|---|
| C1 | 결보강 후보가 배정금지(`kind:"move"`)를 읽도록 | 9c 착수 |
| C2 | 학년별 고정 슬롯 등록부 (**교육과정 코호트 축**) | 9c 백지 편성 |
| C3 | 미배정 사유 진단 | 미배정이 실제로 발생 |
| C4 | 전환일 리허설 단계 2·3 (150·600 요청) | 하루 요청 1,000 돌파 |
| C5 | 캐시 히트율 실측 | 채택 직전 |
| C6 | `next` 16.3.1 상향 | 11월 이후(9c 시한 후) |
| C7 | `xlsx` 취약점 | 9c 완성 시 경로 소멸 |
| C8 | 2·3학년 나이스 과목명 | 파일 추가 수령 |
| C9 | 복수 교사 나이스 표기(5-3) | 그런 수업이 실제로 생길 때 |
| C10 | 화이트라벨 세팅 문서 | 타 학교 확장 착수 |
| C11 | 기초시간표 개정판 주차별 적용 | 개정판이 실제로 생길 때 |

> **C는 손대면 안 되는 것들이다.** 지금 만들면 검증할 데이터가 없어 추측이 된다 — 오늘 여러 번 확인한 함정.

---

## D. 아이디어 — 하기로 정한 적 없음

알림 센터 · 쪽지 서식/이미지/이모지/GIF · 기간제 교사 인수인계 · 아이폰 PWA 안내 문안 · 대시보드 미처리 건수 카드 · 대시보드 최소 구성 · 효명고 알리미(0원) · 교사 승인 안내 메일 · 학적 변동 타임라인 · 담임 전용 비번 초기화 · 크론 발송 모니터링 · 학부모 연락처 DB · Phase 10 라이센스 대여

**타 학교 도입용 위자드**는 로드맵에 **3번 중복 등재**돼 있다(정리 대상).

---

## E. 안 하기로 닫음 — 다시 열지 않는다

- HWP 뷰어 (성숙도 검증 후 별건)
- Tenor GIF (외부 위탁 추가 → 재고지 상향 트리거)
- 전체 부서(효명고) 신설 (현 관행 수용)
- 공동교사 원클릭 탈퇴 (비가역 정리라 보류)

---

## 0-1. 🔴 추론 밀도로 다시 세운 순위 (2026-08-14 사용자 요청)

사용자: *"우선순위를 고려할 때 **하드하게 돌려야 하는 순위**로도 고려해줘. **Fable급으로 돌려야 하는 건 미리 명시**를 해주고."*

**A 목록에는 Fable급이 없다.** A1~A6은 전부 문서·소규모 코드·기구현 확인이라 Opus나 Antigravity로 충분하다. Fable을 여기 쓰면 낭비다.

### F1. 🟣 **Fable급 — 신학기 백지 편성 경로** (오늘 유일한 Fable 대상)

> **✅ [2026-08-14 당일 종결]** 실험 통과 — 하드 0·미배정 0·결정론·소프트 38(컴시간 39보다 낮음).
> `compileSectionsFromHours` 신설 + `scripts/solve_blank.ts`. **"그리드는 알려주는데 시수표는
> 모르는 정보" 5종 목록 확보** → [`phase9c_blank_run_2026-08-14.md`](./phase9c_blank_run_2026-08-14.md) §3
> = 시수표 입력 화면·C2 코호트 등록부의 사양. 아래 원문은 착수 전 분석으로 보존.

`solver.ts:97-98`이 스스로 적어 뒀다:
> *"현행 그리드 + 등록부 → 섹션 목록. **신학기(그리드 없는) 경로는 시수표+등록부 컴파일러를 Phase C-2에서 추가한다**"*

**즉 9c의 마지막 큰 조각이 통째로 비어 있다.** 오늘 재현 실험이 성공한 것은 **현행 그리드에서 역산**했기 때문이고(§3-2), 그건 *"답이 있는 문제를 다시 푼 것"* 이다. **컴시간 없이 짜는 능력은 아직 증명되지 않았다.**

- 시수표 입력 화면도 없다(`HoursRequirement`를 쓰는 컴포넌트 0건).
- 11월 말 검증 시한의 **본체가 이것**이다.

**왜 Fable급인가** — 그리드가 없으면 다음을 전부 다른 출처에서 구성해야 하고, 그 매핑 설계가 이 작업의 전부다:

| 그리드가 주던 것 | 백지에서는 어디서 오나 |
|---|---|
| 학급별 과목·교사·시수 | 시수표 (입력 필요) |
| 동시수업 묶음 | 등록부 (있음) |
| 특별실 점유 | 등록부 (있음) |
| 연속 블록 | 등록부 (있음) |
| **창체·SLAT 고정 슬롯** | **없음** ← C2(교육과정 코호트 등록부)가 여기서 필수가 된다 |
| 학년별 요일 운영 교시수 | 설정에서 역산 or 입력 |

### F1을 오늘 검증 가능한 형태로 쪼개는 법 (핵심 아이디어)

**시수표를 새로 입력받을 필요가 없다.** `deriveHoursFromGrids`가 이미 있다(`validate.ts:118`).

```
현행 그리드 → deriveHoursFromGrids → 시수표
                                        ↓
                        compileSectionsFromHours (신설)   ← 그리드를 보지 않는다
                                        ↓
                                   솔버 → 검사기
```

**그리드에서 시수표만 뽑은 뒤 그리드를 버리고** 시수표만으로 섹션을 만들어 돌린다. 결과를 오늘의 기준선(하드 0·소프트 38)과 비교하면:

- **성공하면** → 백지 편성 능력이 증명된다. 11월 시한의 핵심 위험이 사라진다.
- **실패하면** → **무엇이 부족한지가 정확히 드러난다.** "그리드는 알려주는데 시수표는 모르는 정보"의 목록이 나온다 — 그게 곧 시수표 입력 화면의 사양이고, C2 등록부의 요구사항이다.

**어느 쪽이든 값지다.** 그리고 이건 **비교 기준이 있는 실험**이라 오늘 안에 판정이 난다.

### 순위 요약 — 2026-08-14 당시 (이력)

> 아래 등급표는 그날의 배분이다. F1은 같은 날 종결됐고(위 ✅), 모델 배분 규칙의 현재 원본은 `AGENTS.md` §4다.

| 등급 | 항목 | 왜 |
|---|---|---|
| **🟣 Fable** | **F1 신학기 백지 편성 (설계 + 시수표 컴파일러 + 대조 실험)** | 9c 마지막 큰 조각. 추론 밀도 최고. **8/24 전에 해야 한다** |
| 🔵 Opus | A4 나이스 내보내기 구현 | 형식은 확정됐고 재현 정확도가 관건 |
| 🔵 Opus | A1 운영 매뉴얼 백업 등재 | 판단 조금, 분량 적음 |
| ⚪ Antigravity | A2·A3·A5·A6 | 소규모 코드·확인 |

> **Fable 잔량이 75%이고 금요일이다.** 리셋까지 시간이 남았으니 **오늘 F1에 쓰는 것이 가장 값이 맞다.** A 목록은 Fable을 안 써도 되고, C·D는 지금 하면 추측이 된다.

---

## 1. ~~그래서 지금 뭘 하나~~ — 2026-08-14 당시의 권고 (이력)

> **이 순서로 지금 착수하지 않는다.** A2·A3·A4(→A7)는 위 머리말의 표대로 코드에 반영돼 닫혔고,
> 지금 무엇을 할지는 `STATUS.md`와 `docs/handoff/NEXT.md`가 정한다.

**A1 → A6 확인 → A2·A3 → A4** 순을 권한다.

- **A1**은 오늘 만든 백업이 실제로 쓰이게 만드는 마지막 한 걸음이다. 지금 안 하면 "만들었지만 아무도 모르는 기능"이 된다.
- **A6**은 확인만 하면 로드맵 한 줄이 닫힌다(비용 거의 0).
- **A2·A3**은 각각 작고 독립적이라 Antigravity가 병렬로 할 수 있다.
- **A4**(나이스 내보내기)는 오늘 형식을 다 파악했으므로 **정보가 가장 신선한 지금** 만드는 것이 싸다. 미루면 다시 파악해야 한다.

**B1은 시한이 있다** — 8/24. 나머지 B·C·D는 지금 건드리지 않는다.

---

# 2026-08-15 보강 — 스펙 문서 **안쪽**에 묻힌 이월 항목 스윕

> 계기: 사용자가 시간표 감점 화면에서 "학급 간 교환(현재 미지원)" 문구를 보고 *"이것도 로드맵에 있는 거 맞지? 이런 식으로 미착수된 게 정말 많을 텐데"* 라고 물었다. 확인해 보니 **그 항목이 이 대장에 없었다.**
>
> **구멍의 모양**: 8/14 조사는 "로드맵 상태 대장(`npm run roadmap`) + 그날 만든 문서 9종"을 훑었다. 그런데 이월 항목의 상당수는 **오래된 스펙 문서 안쪽의 "v2 이월 / 범위 제외 / 별건" 절**에 적혀 있다. 그 절들은 어느 그물에도 안 걸린다 — `check:docs` 드리프트 검사도 `development_roadmap.md`만 본다. 로드맵 이행률은 100%인데 대장은 새고 있었다.
>
> **방법**: docs/ 42개 문서 + 루트 `phase9b_spec.md`를 세 갈래로 나눠 전수 발굴하고, **후보마다 실코드를 대조**해 유령을 걸러냈다(문서 표기를 신뢰하지 않는다 — 이 저장소의 반복 사고). 8/14 대장에 이미 오른 21건은 제외했다.
>
> **결과**: 새 발굴 후보 89건 → **진짜 미착수·부분 구현 47건 / 유령(이미 구현됨) 31건 / 판정 불가 2건.** 유령 비율이 3분의 1이다.

## 🔴 F-0. 먼저 — 8/14 대장 **A4 판정을 정정한다**

**A4(나이스 내보내기)를 오늘 "기구현 유령"으로 닫은 것은 틀렸다.** Claude 직접 확인:

- 변환기는 완성돼 있다 — `src/lib/timetable/neis.ts:310` `buildNeisTimetableCsv` + 자가 테스트.
- 그러나 **서버 액션이 없다** — `manage/route.ts`에서 `buildNeisTimetableCsv` 참조 **0건**.
- 화면 버튼은 **하드 비활성 스텁**이다 — `NeisBatchExportTab.tsx:507` 주석이 스스로 "CSV 일괄 생성 **스텁**", 버튼은 `disabled` + 툴팁 *"나이스 양식 서식 확정 후 지원 예정입니다"*.
- **그런데 양식은 2026-08-14에 이미 확정됐다** (`neis.ts:277-291` 주석 = 실물 1건 바이트 단위 분석 완료). **막고 있던 조건이 이미 풀렸는데 화면은 여전히 잠겨 있다.**

오늘 A4를 닫은 근거는 `NeisBatchExportTab`의 **존재**였는데, 그 탭의 사전검증·매핑은 구현됐고 **정작 내보내기만 스텁**이다. 유령 판정도 틀릴 수 있다는 사례로 남긴다 — 화면이 있는 것과 기능이 되는 것은 다르다.

→ **A4는 미착수로 되돌리고, 갈래 A(지금 할 수 있음)의 최우선으로 올린다.**

## F-1. 갈래 A — 지금 할 수 있음 (막는 것이 없다)

| # | 항목 | 크기 | 근거 |
|---|---|---|---|
| **A7** | **나이스 일괄 입력 CSV 내보내기 배선** (변환기·양식 확정 완료, 서버 액션+버튼만) | 작음 | 위 F-0 |
| **A8** | **정리·삭제 스크립트에 "지우기 전 자동 백업" 붙이기** — 헬퍼(`scripts/lib/firestoreBackup.ts:203`)가 있는데 호출은 2개 스크립트뿐. `delete_remaining_graduates`·`cleanup_voided_test_records` 등은 백업 없이 지운다 | 작음 | Claude 확인 |
| **A9** | 데이터베이스 색인 설정을 파일로 보관 (`firestore.indexes.json` 없음 — 재해 복구 시 수동 재구성) | 작음 | |
| **A10** | 학사일정을 고쳐도 **이미 만들어진 주**는 자동 갱신되지 않음 (없는 주만 생성) | 작음 | |
| **A11** | ~~학사일정 구독 주소 **재발급** 수단 없음 (유출 시 갈아끼울 방법이 없다)~~ **[2026-08-21 사용자 결정으로 닫음]** 원문: *"학사일정은 공개된 자료라 학교 관련자가 아닌 사람이 구독해도 문제없어. 학교 홈피만 들어가도 다 나와. 누가 수정만 못하면 돼."* — 유출이 위협이 아니므로 재발급 수단이 불필요. 수정 불가 조건은 확인됨(`src/app/api/calendar/ics/route.ts`는 **GET 전용** — 쓰기 경로 없음) | — | 닫힘 |
| **A12** | 안 읽은 쪽지 수를 **사이드바·홈**에도 (지금은 쪽지함 안에만) | 작음 | |
| **A13** | "새 기능마다 데이터 읽기량 1줄 적기" 규칙이 `AGENTS.md`에 없음 (준수 사례 1건뿐) | 작음 | |
| **A14** | 엑셀 가져오기 **파일 크기 상한** 없음 (확장자 검사만) | 작음 | |
| **A15** | **문서 정정 31건** — 아래 F-4 | 작음 | |

## F-2. 갈래 B — 사용자 결정 대기

| # | 항목 | 왜 지금 결정할 수 있나 |
|---|---|---|
| **B4** | ~~**서로 다른 학급 사이의 수업 맞바꾸기**(감점 개선 v2)~~ **[2026-08-21 구현 완료 — `swap_pair`, fix_assist_spec §7 참조]** | 이번 질문의 발단. 지금 감점 39건 중 30건이 "해결안 없음"인 구조적 원인. **Fable 불요 — Opus급 판정**(초안 대상 + 검사기 안전망). 다만 "솔버를 다시 돌리는 것"과 어느 쪽이 나은지 먼저 |
| **B5** | ~~**순배 규칙(S7)**~~ **[2026-08-21 구현 완료 — 검사기 0.5점+해결안 표적, 솔버 반영은 실측 후 검사기 전용 확정]** | 선생님 질문지 회수 완료("있으면 좋음", S6>S7 서열 확정) → **대기가 이미 풀렸고 착수 판단만 남음** |
| **B6** | ~~말로 입력할 때 **선호도 점수까지** 조정~~ **[2026-08-21 스펙 합류 — `timetable_ask_fix_spec.md` §6(S8 soft 금지), 구현 대기는 STATUS]** | 위와 같은 이유로 대기 풀림 |
| **B7** | **웹 보안 헤더(CSP) 적용 여부** | 미적용 확인(전수 grep 0건). ⚠️ **공개 안내문은 정직하다** — CSP를 시행 중이라 적지 않고 "미확인이라 기재하지 않음"으로 비워 뒀다(`phase11_privacy_spec.md:78`). 즉 **거짓 고지가 아니라 보안 강화 미착수** 건이다 |

## F-3. 갈래 C·D·E — 요약 (상세는 각 스펙 문서)

- **C(사건 대기) 15건**: 생활지도 브리지 일몰(**9/10 — 날짜 확정**) · 미배정 사유 진단(미배정이 실제 발생해야) · 학년별 점심시간 · 전환일 부하 실측 · 백업 자동화·암호화 · 타 학교 세팅 문서·학교별 DB 분리 · 5자리 학번 계정 차단 · 교육과정부장 3파일 → 시수표 자동 생성 · 양해 개방 확장 3건(다른 겹침 종류 / 체인 조율 / 통 이동 교사 개방) 등
- **D(아이디어) 13건**: 쪽지 첨부·답장·업무 타입·미열람 재알림 · 급식/공지 푸시 · 오프라인 실행 · 폰 관리화면 서랍 · AI 제안 클릭 시 칸 강조 · 다단계 해결안 · 전후 비교 시트 · 이동수업 손편집 · 작업기록 압착
- **E(닫음) 4건**: 양해 사후 검증(승인 알림이 대체) · 프레임워크 상향(11월 후) · 엑셀 라이브러리 취약점(9c 완성 시 경로 소멸) · 증분 백업(규모 미달)

## F-4. 유령 31건 — 문서만 낡은 것 (읽는 사람을 잘못 이끈다)

문서는 "미구현/이월/대기"인데 **코드에는 이미 있는** 것들. 정정 대상이며, 기능 작업이 아니다.

- **시간표(10)**: 시수표 엑셀 업로드·성명 매칭 화면 · 학년×요일 교시 수 입력 · 계획 힌트 엔진 전달(9c-I-2) · 초안 채택 · 징검다리 체인 · 복수교사 시수 검사(H10) · 특별실 등록부 화면 · 해결안 카드 화면 · 작업기록 열람 · 채색 성능 실측 / 부수: AI 4종 전부 배선됨
- **인프라(11)**: 쪽지 365일 자동 파기 · 쪽지 회수 · 공동교육 계정 차단 · 졸업 서명/감사기록 자동 파기 · 생활지도 이메일 위조 방지 · 고지 확인 현황 화면 · 구글 권한 16개 문서화 · `.env.example` · 운영 매뉴얼 백업 절 · 백업 아카이브 개인정보 등재 · 조직도 내선번호
- **점검·리뷰(10)**: 보호 계정 삭제 차단 · 클래스룸 정리 관리자 우회 제거 · 통 이동 직권 화면(오늘 완료) · 창체·SLAT 고정 슬롯 선점 · 로드맵 상태 줄 100% · 8월 Fable 전수 스캔 4종 · 명단 편집 부분 실패 시 작업물 보존 · 진급 부분 실패 표시 · 신입생 등록 후 캐시 갱신 · 잡일 묶음 6건

**판정 불가 2건**: 웹 푸시 키의 Vercel 등록 여부(저장소 밖) · 직권 화면 캐시가 가리키는 대상(원문 모호, 어느 쪽이든 병목 없음으로 닫힘).

## F-5. 재발 방지 — 구조를 고치지 않으면 또 샌다

이번 누락은 사람의 부주의가 아니라 **그물의 모양** 때문이다. 로드맵은 100% 정합인데 대장이 샜다.

1. **스펙 문서의 "v2 이월 / 범위 제외 / 별건" 절을 대장의 입력으로 승격한다.** `check:docs`(현재 `development_roadmap.md` 전용)에 docs/ 전체의 이월 표시를 세어 대장 등재 여부를 대조하는 검사를 붙이는 것이 근본 처방.
   - **검사기의 사각 하나 더**: 현행 검사는 ✅와 "착수 대기"가 **같은 줄**에 있을 때만 모순으로 본다. 그런데 이 저장소의 로드맵은 「상태 줄 + 서술 문단」 짝 구조라, **상태 줄은 ✅인데 바로 아래 서술 문단 괄호가 "착수 대기"인** 형태가 그냥 통과한다(공동교육 계정 차단 항목이 실제로 그랬다 — 8/15 정정). 서술 문단(`*` 줄, 현재 19개가 검사 제외)까지 짝지어 보게 넓힐 것.
   - ⚠️ **정정 시 주의**: 그 `*` 줄은 **중복이 아니라 본문**이다. 상태 줄과 겹쳐 보인다고 지우면 설계 근거가 통째로 날아간다(이번 스윕에서 실제로 "중복 유령 줄"로 오판될 뻔했다 — 「정리가 유실을 낳는다」 계열).
2. **"기구현 확인"으로 항목을 닫을 때는 화면 존재가 아니라 동작으로 판정한다** — A4가 정확히 이 함정이었다(탭은 있고 버튼은 스텁).
3. **막고 있던 조건이 풀리면 그 항목을 자동으로 A갈래로 올린다.** A7(나이스 CSV)·B5·B6은 전부 "대기 사유가 이미 소멸했는데 표기만 남은" 것들이다. 대기 항목에는 **무엇을 기다리는지**와 함께 **그것이 충족됐는지 확인할 방법**을 적어야 한다.
