# Phase 9c-H — 신학기 편성 입력 2종 스펙 (2026-08-14, Claude Opus 5)

> 근거: [`phase9c_blank_run_2026-08-14.md`](./phase9c_blank_run_2026-08-14.md) §3(시수표가 모르는 것 5종) · [`phase9c_questionnaire_result_2026-08-14.md`](./phase9c_questionnaire_result_2026-08-14.md) §2-2(코호트 축)
> 대상: **① 시수 조정 화면** · **② 교육과정 코호트 고정 슬롯 등록부(C2)**
> Antigravity가 바로 구현할 수 있는 수준으로 저장 모델·API·화면까지 적는다.

---

## 0. 착수 전 설계 판단 2건

### 0-1. 🔴 **엑셀 업로드 경로를 만들지 않는다** (원 지시에서 축소)

백지 실험 §3 부수는 *"시수표 입력이 성명 기반이면 성명→이메일 매칭 단계가 필요하다"* 고 적었고, 원 지시도 *"엑셀 업로드 재사용, 성명→이메일 매칭 포함"* 이었다. **그 전제를 재검토했다.**

**실측**: 현행 2026-2 그리드에서 파생한 시수표는 **432행 전부 이메일로 식별된다**(가상 교사 60행 제외 372행). 동명이인 문제가 **구조적으로 존재하지 않는다.**

**다음 학기 시수표의 출처는 무엇인가**가 갈림길이다:

| 경로 | 언제 | 식별자 | 필요한가 |
|---|---|---|---|
| **전 학기 그리드에서 파생** | 2027-1부터 매년 | **이메일** | ✅ 이것만 있으면 된다 |
| 컴시간 엑셀 업로드 | 전 학기가 없을 때 | 성명 | ❌ 우리 학교엔 2026-2가 있다 |

**우리 학교는 이미 2026-2 그리드를 갖고 있다.** 2027-1을 짤 때 거기서 파생하면 되고, 그 뒤로는 매년 직전 학기에서 파생된다. **업로드가 쓰일 순간이 없다.**

- 업로드가 필요한 경우는 **타 학교 신규 도입**뿐이고, 그건 로드맵 "타 학교 도입용 위자드"의 일이다(D 갈래, 착수 미정).
- 업로드를 빼면 **성명→이메일 매칭·동명이인 확인 UI가 통째로 사라진다.** 오늘 부서장 명단에서 겪은 함정과 같은 종류를 애초에 만들지 않는 것이다.

> **판정: 파생 전용. 업로드는 만들지 않는다.** 필요해지는 날(타 학교 도입) 그때 만든다.

### 0-2. 저장은 **파생 시점의 전체 사본**이다 (차분 아님)

편집분만 차분으로 저장하면 원본(전 학기 그리드)이 나중에 바뀔 때 조용히 어긋난다. **파생하는 순간 전체를 복사해 두고 그 사본만 편집한다.** 원본이 뭘 하든 영향받지 않는다.

---

# ① 시수 조정 화면

## 1-1. 무엇을 하는 화면인가

> **"작년 시수표를 불러와서 바뀐 것만 고친다."**

학교가 2월에 실제로 하는 일이다 — 백지에서 입력하지 않는다. 교사 전출입, 학급 수 변경, 과목 개폐만 반영하면 새 학기 시수표가 된다.

## 1-2. 저장 모델

```
timetable_hours_plans/{domain}/plans/{planId}
```

```ts
export interface HoursPlan {
  id: string;
  label: string;              // "2027학년도 1학기 시수" — 사람이 붙임
  sourceTermId: string;       // 파생 원본 학기 (예: "2026-2")
  derivedAt: number;          // 파생 시각 — 이후 원본이 바뀌어도 이 사본은 불변
  rows: HoursPlanRow[];
  gradeDayPeriods: Record<number, Record<number, number>>;  // 부족정보 #5
  status: "draft" | "ready";  // ready = 솔버에 넘길 수 있음
  createdBy: string;
  updatedBy: string;
  updatedAt: number;
}

export interface HoursPlanRow {
  id: string;                 // 행 고유 id (편집 추적용)
  grade: number;
  classNum: number;
  subjectName: string;
  teacherEmail: string;       // "" = 가상 교사(창체·SLAT) — ②가 위치를 준다
  teacherName: string;        // 표시용 스냅샷. 판정은 email로만 한다
  hours: number;              // 주당 시수
  /** 부족정보 #2 — 이 행이 동시수업 그룹 몫인가 */
  simulGroupId?: string | null;
  /** 부족정보 #4 — 이 시수 중 특별실을 쓰는 시간 수. null = 전 시수(보수) */
  venueHours?: number | null;
}
```

**왜 `rows`를 문서 안에 배열로 두나**: 실측 432행이고 학급이 늘어도 수백 행 규모다. 하위 컬렉션으로 쪼개면 읽기가 432번이 되고 편집마다 트랜잭션이 필요하다. **한 문서 1MB 한도 안에 충분히 들어간다**(432행 × ~150바이트 ≈ 65KB). 다만 **저장 시 크기를 검사**해 900KB를 넘으면 거부한다.

## 1-3. API — `/api/timetable/manage`에 action 추가

기존 라우트를 쓴다(새 라우트를 만들지 않는다 — 인증·권한 가드가 이미 거기 있다).

| action | 본문 | 하는 일 |
|---|---|---|
| `hours_plan_derive` | `{ sourceTermId, label }` | 그 학기 그리드 → `deriveHoursFromGrids` → 새 plan 생성. `gradeDayPeriods`는 `deriveGradeDayPeriods`로 함께 채운다 |
| `hours_plan_list` | `{}` | 목록 (rows 제외한 요약만) |
| `hours_plan_get` | `{ planId }` | 전체 |
| `hours_plan_save` | `{ planId, rows, gradeDayPeriods, status }` | 전체 교체. 서버에서 sanitize |
| `hours_plan_delete` | `{ planId }` | 삭제 |

**권한**: 기존 라우트의 일과계·super_admin 가드를 그대로 탄다.

**`hours_plan_save` 서버 검증 (필수)** — 클라이언트 값을 믿지 않는다:

1. `rows` 길이 상한 **2000**, 문서 직렬화 크기 상한 **900KB**
2. `hours`는 1~40 정수
3. `grade`·`classNum` 양의 정수
4. `teacherEmail`이 비지 않았으면 **도메인 계정으로 실재 확인**(`users` 또는 GWS) — 없으면 거부. *오늘 부서장 건에서 배운 것: 사람 이름·주소를 그대로 믿지 않는다*
5. `venueHours`는 `0 ≤ venueHours ≤ hours`
6. `simulGroupId`는 실재하는 동시수업 그룹 id
7. 같은 `(grade, classNum, subjectName, teacherEmail)` 중복 행 금지

## 1-4. 화면 구성

**위치**: 「시간표 작성」 메뉴 안, 초안 만들기 **앞 단계**. (IA는 `TimetableCreationSection`에 탭 추가)

```
[시수 조정]
 ┌─────────────────────────────────────────────────────┐
 │ 어느 학기에서 가져올까요?  [2026학년도 2학기 ▾] [가져오기] │
 └─────────────────────────────────────────────────────┘

 2027학년도 1학기 시수   (2026-2에서 2026-08-14 가져옴)     [저장]

 ┌ 학년별 하루 교시 수 ────────────────────────────────┐
 │        월  화  수  목  금                              │
 │ 1학년   7   7   7   7   6                              │   ← 부족정보 #5
 │ 2학년   7   7   7   7   6                              │
 │ 3학년   7   7   7   7   6                              │
 └─────────────────────────────────────────────────────┘

 [학년 1▾] [반 전체▾] [교사 검색____]        총 432행 · 변경 12행
 ┌──────┬──────────┬────────────┬──────┬────────┬────────┐
 │ 학급  │ 과목      │ 선생님       │ 시수 │ 동시수업│ 특별실 │
 │ 1-1  │ 공통국어2 │ 지상인 ▾    │  4   │  —     │  —    │
 │ 1-1  │ 과학탐구  │ 이시내 ▾    │  2   │  —     │ 2시간 │
 │ 1-1  │ 창체      │ (담당 없음)  │  2   │  —     │  —    │  ← 회색, 편집 불가
 └──────┴──────────┴────────────┴──────┴────────┴────────┘
                                              [+ 행 추가]
```

**동작 규칙**

- **선생님 칸은 드롭다운**이다. 자유 입력을 만들지 않는다 — 이메일로만 고르게 하면 동명이인 문제가 생길 수 없다.
- **가상 교사 행(창체·SLAT)은 읽기 전용**으로 표시하고 *"위치는 「교육과정 고정 시간」에서 정합니다"* 안내를 붙인다. ②로 연결.
- **변경된 행은 색으로 표시**하고 상단에 건수를 센다. 432행 중 뭘 고쳤는지 안 보이면 못 쓴다.
- **행 추가·삭제** 지원(신규 과목·전출 교사).
- **저장 전 자체 점검**: 학급별 시수 합 vs 그 학년 운영 교시수 합을 비교해 **어긋나면 경고**(막지는 않는다 — 컴파일러의 `class-slot-mismatch`와 같은 계산).

**문구 규칙**: 화면에 "시수표", "컴파일", "섹션" 같은 말을 쓰지 않는다. *"선생님별 주당 수업 시간"*, *"가져오기"*, *"저장"* 으로 쓴다.

---

# ② 교육과정 코호트 고정 슬롯 등록부 (C2)

## 2-1. 왜 「학년」이 아니라 「교육과정」인가

질의 결과 §2-2에서 확정했다. 교육과정 개정은 1학년부터 들어오므로 **두 교육과정이 2년간 공존**하고, **「학년」은 해마다 뜻이 바뀌는 축**이다. "1학년: 금 5·6교시"로 적어 두면 **매년 3월에 한 칸씩 밀어야 하고 안 밀면 조용히 틀린다.**

→ **등록 단위는 교육과정, 학년은 역산으로 파생한다.**

```
학년 = 학년도 − 입학년도 + 1
```

입학년도 축은 이미 있다(학생 계정이 `{입학년도}{번호}@`).

## 2-2. 저장 모델

```
timetable_curriculum_cohorts/{domain}/cohorts/{cohortId}
```

```ts
export interface CurriculumCohort {
  id: string;
  label: string;                 // "2022 개정 교육과정" — 사람이 붙임
  startAdmissionYear: number;    // 이 교육과정이 처음 적용된 입학년도. 예: 2025
  fixedSlots: CohortFixedSlot[];
  active: boolean;
  createdBy: string;
  updatedBy: string;
  updatedAt: number;
}

export interface CohortFixedSlot {
  displayName: string;   // "창체" / "SLAT" — 화면·그리드 표시용
  day: number;           // 1=월 … 5=금
  period: number;
}
```

**설계 규칙 3가지**

1. **이름을 키로 삼지 않는다.** `displayName`은 표시용일 뿐이고 판정은 `(day, period)`로 한다. 교육과정이 바뀌면 이름도 바뀐다.
2. **연속 2시간은 두 줄로 적는다** (`금5`, `금6`). 블록 개념을 넣지 않는다 — 솔버는 이 슬롯을 **점유 불가**로만 다루고 배치하지 않으므로 연속성이 의미 없다(질의 2-2 부연: 사람이 나중에 채운다).
3. **학년을 저장하지 않는다.** 저장하는 순간 3월 함정이 돌아온다.

## 2-3. 학년 역산 — 단일 소재지

`src/lib/timetable/cohort.ts` 신설. **이 함수 밖에서 학년을 계산하지 않는다.**

```ts
/** 특정 학년도에 어느 학년이 어느 교육과정을 따르는가 */
export function cohortForGrade(
  cohorts: CurriculumCohort[],
  schoolYear: number,
  grade: number
): CurriculumCohort | null {
  const admissionYear = schoolYear - grade + 1;
  // 적용 시작 입학년도가 그 이하인 것 중 가장 최근 것
  return cohorts
    .filter((c) => c.active && c.startAdmissionYear <= admissionYear)
    .sort((a, b) => b.startAdmissionYear - a.startAdmissionYear)[0] ?? null;
}

/** 컴파일러 입력용 — 학급 단위 FixedBlock 전개 */
export function expandCohortFixedBlocks(
  cohorts: CurriculumCohort[],
  schoolYear: number,
  classList: Array<{ grade: number; classNum: number }>
): FixedBlock[] { /* … */ }
```

> `compileSectionsFromHours`는 학년 축을 모른다(백지 실험 §3-1이 그렇게 설계했다). **전개는 이 함수가 하고 컴파일러는 학급 단위 결과만 받는다.** 그 경계를 지켜야 코호트 축 변경이 솔버로 번지지 않는다.

## 2-4. API

| action | 본문 |
|---|---|
| `cohort_list` | `{}` |
| `cohort_save` | `{ cohort }` — 신규·수정 겸용, 전체 교체 |
| `cohort_delete` | `{ cohortId }` |

**서버 검증**: `startAdmissionYear` 1900~2200 정수 · `fixedSlots` 상한 50 · `day` 1~5 · `period` 1~9 · 같은 `(day, period)` 중복 금지 · `displayName` 30자.

## 2-5. 화면

**위치**: 「시간표 작성」 메뉴 안, ①과 같은 층.

```
[교육과정 고정 시간]

 ┌ 2022 개정 교육과정 ──────────── 2025년 입학생부터 ── [수정] [삭제] ┐
 │        월    화    수    목    금                                    │
 │  1교시  ·     ·     ·     ·     ·                                   │
 │  …                                                                   │
 │  5교시  ·     ·     ·     ·    창체                                  │
 │  6교시  ·     ·   SLAT   ·    창체                                  │
 │  7교시  ·     ·   SLAT   ·     ·                                    │
 │                                                                       │
 │ ℹ️ 2026학년도 기준: 1학년 · 2학년 이 교육과정을 따릅니다             │  ← 역산 결과 표시
 └───────────────────────────────────────────────────────────────────┘

 [+ 교육과정 추가]
```

**동작 규칙**

- **격자를 눌러 켜고 끈다.** 켜면 이름을 묻는다(기본값 "창체").
- **역산 결과를 항상 보여준다** — *"2026학년도 기준: 1·2학년"*. 이게 이 화면의 핵심 안전장치다. 등록자가 **자기가 어느 학년을 건드리는지 눈으로 확인**할 수 있어야 한다.
- 교육과정이 둘 이상이면 **어느 학년이 어디에 걸리는지 한눈에** 보이게 나란히 놓는다.
- **문구에 "코호트"를 쓰지 않는다.** 화면에는 *"교육과정"*, *"○○년 입학생부터"* 로만 쓴다.

---

## 3. 구현 순서와 몫

| # | 항목 | 몫 | 선행 |
|---|---|---|---|
| 1 | `cohort.ts` (모델·역산 함수·셀프테스트) | **Claude** | — |
| 2 | ② 등록부 API + 화면 | Antigravity | 1 |
| 3 | ① 시수 조정 API + 화면 | Antigravity | — (병렬 가능) |
| 4 | `compileSectionsFromHours`에 코호트 전개 결과 연결 | **Claude** | 1·2 |
| 5 | 백지 실험 재실행 — 이번엔 **그리드 대역 없이** 등록부만으로 | **Claude** | 4 |

**5번이 진짜 완주 시험이다.** 백지 실험은 창체 위치를 그리드에서 베껴 왔다(§3-1의 "대역"). 등록부로 대체하고도 통과하면 **컴시간 의존이 완전히 끊긴다.**

## 4. 하지 않는 것

- **엑셀 업로드·성명 매칭** — §0-1.
- **시수표 차분 저장** — §0-2.
- **코호트에 학년 저장** — §2-2 규칙 3.
- **창체·SLAT를 솔버가 배치** — 질의 2-2 부연. 점유 불가 슬롯일 뿐이다.
- **특별실 주당 시수 필드**(부족정보 #4의 정식 요구사항) — 백지 실험이 보수 처리로 통과했다. 급하지 않다. ①의 `venueHours`를 **선택 입력**으로 열어 두되 비면 지금처럼 보수 처리한다.
