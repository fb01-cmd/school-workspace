# AGENTS.md 다이어트 — 개정안 — 📕 **적용 완료 (2026-08-20)**

> ## ✅ 이 개정안은 전부 적용됐다. 승인을 기다리는 문서가 아니다
>
> **종결: 2026-08-20.** 근거 커밋 `684b16b` · `4ae8c65` · `9b2052a` · `adc4d2d`(다이어트 1~4단계) · `f6d6522` · `74260b2`.
> 적용 확인(코드가 아니라 규칙 파일 본문): `AGENTS.md:437`(H2 — append-only 제외 채택) · `:273`(G1 보고 형식 고정) ·
> `:274`(G2 대상을 커밋 해시로 고정) · `:278`(G3 검증 과제는 읽기 전용). 문서 안쪽에도 이미 *"처리 완료 `74260b2`"* ·
> *"✅ 사용자 확정"* 표시가 붙어 있다.
>
> 원래 이 자리에는 **"제안이며 사용자 승인 전에는 적용하지 않는다"**는 취지가 적혀 있었다. **2026-08-21에 걷었다** —
> 승인도 적용도 이미 끝난 뒤였고, 그 줄 때문에 이 문서 전체가 **결정을 기다리는 안건**으로 읽혔다.
>
> **지금 열려 있는 항목은 [`STATUS.md`](../STATUS.md)에서 본다.** 이 문서는 그 대장이 아니다.
>
> 참고: 아래 머리말의 *"현재 638줄"* 은 **2026-08-20 작성 시점의 값**이다. 다이어트 이후에도 규칙이 더 붙어
> 실측은 **679줄을 넘는다**(`wc -l AGENTS.md`로 확인). 줄 수로 무언가를 판단하려면 그때 다시 센다.

> **작성**: 2026-08-20 Claude (Opus 5)
> **발단**: 사용자 지적 — *"규칙이 너무 많아지면 오히려 일을 못 해. 지금 협업 규칙을 새로 만들면서 기존 규칙들 중 삭제하거나 조정할 것들도 좀 찾아보면 좋겠어. 함께"*
> **방법**: Claude·`agy`·Codex **셋이 각각 독립으로** AGENTS.md를 감사하고, 겹치는 것부터 채택
> **현재**: 638줄

*(이하 작성 당시 기록 — 현재 상태가 아니다.)*

## 채택 기준

1. **겹치면 채택** — 둘 이상이 독립으로 같은 것을 짚었거나, 기계적으로 확인되는 것
2. **⚠️ 되돌릴 수 없는 작업·보안·개인정보 규칙은 건드리지 않는다** — 감사 지시에도 못 박았다
3. **"지켜지지 않으니 지운다"는 함부로 쓰지 않는다** — 안 지켜지는 이유가 ⓐ 잊혀서인지 ⓑ 지킬 수 없어서인지 ⓒ 필요 없어서인지 갈라 본다. ⓐ면 오히려 강화 대상이다

---

## ⚠️ 0. 긴급 — 다이어트가 아니다. 지금 사고를 낼 수 있는 것 (Codex 발견, Claude 전건 확인)

**Codex가 규칙 문서 안에서 「따르면 해로운 규칙」 3건을 찾았다.** 셋 다 Claude가 코드로 재확인했다. **이건 줄 수 문제가 아니라 내용이 틀린 문제이므로 다이어트보다 먼저 처리한다.**

### ✅ H1 (처리 완료 `74260b2`). Firestore 보안 규칙 절을 따르면 **보안이 후퇴한다**

| | 내용 |
|---|---|
| AGENTS.md의 주장 | *"이 프로젝트는 현재 개발 편의를 위해 Firestore 보안 규칙이 **공개(open) 상태**이다"* |
| **실제** | `firestore.rules` **230줄**. 기본 전부 거부 + **@hmh.or.kr 도메인 게이트** + 역할별 분리 + 학생은 자기 것만 + 서버 전용 컬렉션 쓰기 금지 (2026-07-24 Claude 설계) |
| AGENTS.md가 시키는 것 | 배포 전에 `allow read, write: if request.auth != null` 로 **교체**하라 |
| **그렇게 하면** | 도메인 게이트 소멸(**외부 Gmail 계정도 접근**) · 역할 분리 소멸(**학생이 교직원 데이터 열람**) · 서버 전용 컬렉션 보호 소멸 |

**즉 이 절은 낡은 게 아니라 위험하다.** 배포 담당자가 규칙 문서를 믿고 따르면 **실제 규칙보다 훨씬 약한 것으로 갈아끼운다.**

**처방**: 이 절을 **통째로 들어내고** 한 줄로 바꾼다 — *"Firestore 보안 규칙의 단일 원본은 저장소의 `firestore.rules`다. 문서에 규칙 본문을 복사하지 않는다. 완화는 보안 검수 없이 하지 않는다."*

### ✅ H2 (처리 완료 `74260b2`). 「계정 생성 **전** Auth 삭제」가 **오늘 고친 것과 정반대다**

AGENTS.md: *"계정 생성 시 Google Workspace 계정을 생성하기 **전에** 반드시 `deleteAuthUserByEmail(email)`을 먼저 실행한다."*

**2026-08-20에 그 순서를 뒤집었다**(`e6241da`·앞선 커밋). 이유: `deleteAuthUserByEmail`은 이름과 달리 **Firestore `users` 문서(=role의 원본)까지 지우는데**, 생성 앞에 두면 **이미 살아 있는 계정에도 무조건 실행**돼 중복 실패 시 **멀쩡한 사용자만 권한을 잃는다.**

**규칙 문서는 아직 옛 순서를 지시한다.** 누가 이 줄을 따라 새 생성 경로를 만들면 **오늘 고친 것을 되돌린다.**

> **이건 Claude의 실수다.** 코드는 고치고 **규칙 문서를 갱신하지 않았다.** 오늘 실수 목록의 7번째이고, 성격이 가장 나쁘다 — 규칙과 코드가 어긋나면 규칙을 따르는 쪽이 사고를 낸다.

**처방**: `:109`를 **생성 성공 뒤**로 정정하고, 왜 그 순서여야 하는지 한 줄을 붙인다.

### 🟡 H3. 규칙끼리 연결이 끊겼다 — `check:docs`가 유령 검사기를 부르지 않는다

④-4(`:319`)는 *"문서 커밋 전 `check_ghost_markers.ts`를 실행한다"* 이고, 배포 절(`:637`)은 *"`npm run check:docs`를 실행한다"* 인데, **`check:docs`는 `check_roadmap_status_drift.sh`만 돌린다.** 두 규칙이 따로 논다.

**처방**: `check:docs`가 둘 다 부르게 한다. **규칙을 늘리지 말고 명령을 합친다.**

### 🟡 H4. 안 지켜지는 규칙 — `project_notes.md` 회전

④-1(`:292`)은 *"150KB를 넘으면 즉시 회전"* 인데 **현재 220KB**다. 이건 「잊혀서」에 해당하므로 **지우면 안 되고 자동화해야 한다** — 위 `check:docs`에 크기 검사를 얹는다.

---

## A. 기계적 결함 — 이견 없음, 바로 고침

| # | 문제 | 처리 |
|---|---|---|
| A1 | **`④-3`이 두 개** — `:278`(작업 인계 게이트)와 `:309`(STATUS 대장) | `:309`를 **`④-5`** 로. `:278`은 B2에서 흡수돼 사라진다 |
| A2 | 섹션 제목이 **「운영 규칙 (①~⑨)」** 인데 `:383`에 **⑩**이 있다 | 제목을 `①~⑩`으로 |
| A3 | ④ 계열 순서가 **④ → ④-3 → ④-1 → ④-2 → ④-3 → ④-4** | 번호순으로 재배열 |

---

## B. 중복 제거 — Claude·`agy` 독립 합치

### B1. `git add -A` 금지가 두 곳에 (`:242` ①-0 · `:401` §3-3)

같은 규칙이 각각 다른 사고(2026-08-17 / 2026-08-05)를 근거로 두 번 적혀 있다.

**처방**: **§3-3 한 곳으로 합친다**(동시 작업 충돌 방지가 이 규칙의 본적이다). ①-0은 삭제하고 사고 사례 두 건을 §3-3에 나란히 남긴다. **줄었지만 근거는 안 줄었다.**

### B2. 「작업 인계 3대 필수 게이트」(`:278`)가 ②·④·①-1-3의 재나열

세 줄이 전부 *"②를 해라 / ④를 해라 / ①-1-3을 해라"* 다. **새 내용은 한 줄뿐이다** — *"도구가 안 되거나 절차를 못 지켰으면 그 사실이 첫 번째 보고 대상이다."*

**처방**: 게이트 절 전체를 지우고, **그 한 줄을 ④(핸드오버) 끝에 붙인다.** 나머지는 각 규칙이 이미 말하고 있다.

> **주의**: 이 절이 만들어진 이유(2026-08-15, 인계 때 셋을 빼먹음)는 유효하다. **묶음으로 상기시키는 효과**를 잃지 않게, ④ 머리에 *"인계 = 커밋(②) + 기록(④) + 삭제 검사(①-1-3), 못 한 것은 못 했다고 적는다"* 한 줄을 둔다.

### B3. 「⑤ 규칙은 단일 원본을 둔다」(`:327`)가 문서 최상단(`:1-7`)과 중복

최상단이 이미 *"이 파일이 프로젝트 규칙의 단일 원본이다"* 를 선언한다.

**처방**: **⑤ 삭제.** 최상단에 *"규칙 변경은 반드시 이 파일에서 한다"* 한 줄만 흡수.

### B4. ⑨ 표(`:364`)의 항목 절반이 본문 독립 조항과 중복

`#6`(교차 검증) → `③`(`:261`) · `#7`(다음 지시 프롬프트) → `⑧`(`:357`) · `#10`(미래 사건 금지) → `⑩`(`:383`)이 각각 정식 조항으로 따로 있다.

**처방**: **표는 남기되 중복 항목의 본문을 「→ ③ 참조」식 한 줄로 줄인다.** 표 자체는 지우지 않는다 — 이 표는 **Claude가 판단 전에 훑는 자기 점검 목록**이라 목적이 다르다(`:368`).

---

## C. 충돌 해소 — `agy` 발견, Claude 확인

### ⭐ C1. 세션 시작 규칙이 서로 어긋난다 (토큰을 직접 태우는 충돌)

| 위치 | 내용 |
|---|---|
| `:25-32` | *"새 세션 시작 시 `development_roadmap.md`·`project_notes.md`를 **반드시 읽는다**"* |
| `:293` (④-1) | *"어떤 문서든 수십 KB급이면 **전체 읽기 금지** — 검색으로 필요한 부분만"* |
| `:313` (④-3) | *"무엇이 열려 있나는 **`STATUS.md`에서만** 판단한다"* |

**앞을 지키면 뒤를 어긴다.** 게다가 **「필수 읽기」 목록에 `STATUS.md`가 없다** — 열린 항목의 단일 원본으로 정해 놓고 정작 읽으라는 목록에서 빠졌다.

**처방 — 필수 읽기를 이렇게 다시 쓴다.**
1. **`STATUS.md`** — 전체를 읽는다 (짧고, 지금 열린 것의 단일 원본)
2. `AGENTS.md` — 규칙
3. **`development_roadmap.md`·`project_notes.md`는 「필수 읽기」에서 뺀다** — 필요할 때 **검색으로 해당 부분만**. 이것이 ④-1의 이미 정해진 규칙이다

> 이 항목이 이번 다이어트에서 **실효가 가장 큰 하나**다. 규칙 줄 수보다 **매 세션 낭비되던 읽기**를 줄인다.

### C2. 「대화 길이 관리」에 정반대 주장이 나란히 (`:467-490`)

본문은 *"길어지면 체크포인트 남기고 새 창"*, 끝의 2026-08-16 정정은 *"새 창이 싸다는 전제는 낡았다"*.

**처방**: **정정 내용으로 본문을 교체**하고 낡은 본문은 지운다. 체크포인트 양식은 **핸드오버(④)와 같은 것**이므로 ④를 가리키게만 한다.

---

## D. 역할이 끝난 것

| # | 위치 | 내용 | 처리 |
|---|---|---|---|
| D1 | `:404-406` | 폐기된 「🔒 작업 중 파일 목록」 회고 — *"규칙만 1년 가까이 남아 있었다"* 는 기록 | **삭제.** 이미 폐기 선언이 끝났고 `git status` 규칙으로 일원화됐다. **교훈 한 줄**(*"지켜지지 않는 규칙을 남기면 문서 전체의 구속력이 떨어진다"*)만 다이어트 원칙으로 승격 |
| D2 | `:152-162` | Max20 한시 분업 확장 — **2026-08-22 만료** | **만료 후 1줄로 압축.** 지금 지우지 않는다(아직 유효) |
| D3 | `:428-430` | 모델 선택의 외부 인용·블로그 링크 3개 | **줄인다** — 규칙이 아니라 배경 일화. 선택 표만 남긴다 |

---

## E. 새 협업 규칙을 어디에 넣나 — **얹지 않고 흡수시킨다**

**초안을 통째로 AGENTS.md에 붙이면 638줄이 800줄이 된다.** 그러면 이번 작업의 목적을 어긴다.

| 초안 절 | 어디로 |
|---|---|
| §2 역할표 | **기존 §1 분담 기준 표에 열 하나 추가** (별도 절 신설 안 함) |
| §3 차등 재확인 | **§1-1(주장이 산출물인 일)의 2단계 점검에 흡수** — 같은 주제다 |
| §4 프롬프트 5줄 | **새 절 1개** (이건 실무 체크리스트라 독립이 맞다) |
| §5 권한·안전 | **§3 동시 작업 충돌 방지에 흡수** |
| **§6-1 (Claude도 같은 규율)** | **⑨ 표에 행으로 추가.** 새 절을 만들면 Claude 실패가 두 곳에 흩어진다 |
| §9 토큰 분산 측정 | **AGENTS.md에 넣지 않는다** — 한시적 실험이므로 `codex_trial` 문서에만 |

**결과 예상**: 새 규칙은 **순증 20~30줄**, 위 A~D로 **80~120줄 감소**. 총량은 줄어든다.

---

## F. ~~사용자 판단이 필요한 것~~ → **✅ 2026-08-20 사용자 확정: 3건 전부 권장안**

| # | 항목 | 상황 | **확정 (= 권장안)** |
|---|---|---|---|
| **F1** | **⑧ Antigravity 의무 출력**(`:357`) — *"답변 최하단에 다음 작업 지시 가이드를 예외 없이"* | **CLI(`agy --print`)로 부르면 그 상자가 의미 없다.** 받는 사람이 Claude이고, 사용자가 복사할 일이 없다 | **조건을 단다** — *"사용자에게 답할 때"* 로 한정. CLI 호출 응답에는 면제 |
| **F2** | **④-4 유령 탐지기**(`:319`) — 문서 커밋 전 `check_ghost_markers.ts` 실행 | **오늘 아무도 안 돌렸다.** 기준선 64건 소각도 진행 없음 | **판단 필요**: 잊혀서면 강화(커밋 훅), 실효가 없으면 폐기. **제 추정은 「잊혀서」** — 그렇다면 지우면 안 된다 |
| **F3** | **「Antigravity가 두 번 막히면 Codex에게 진단을 묻는다」** (초안 §2-1) | **`agy`가 반대했다** — *"원인 진단과 스펙 수정은 Claude 영역이고, 진단이 갈리면 혼선만 난다"* | **절충 제안**: Codex는 **사실 수집까지만**(무엇이 어디서 어떻게 실패하는가), **진단과 처방은 Claude.** 그러면 갈릴 것이 없다 |

---

## G. `agy`가 제 초안에서 찾은 구멍 — 반영할 것

| # | 지적 | 처리 |
|---|---|---|
| **G1** ⭐ | **Codex 보고 형식이 정해져 있지 않다** — 장황하면 Claude가 읽는 비용이 분산 효과를 상쇄한다 | **채택.** 보고 규격을 못 박는다: **항목당 `[통과/실패/판정불가] file:line — 한 줄`.** 서술형 보고서 금지 |
| **G2** ⭐ | **검증 대상 커밋이 고정돼 있지 않다** — 작업 중 워크트리를 읽으면 오탐이 쏟아진다 | **채택.** 검증은 **커밋 해시를 지정해서** 시킨다. 시키기 전 `git status`가 깨끗한지 확인 |
| G3 | *"실기기 검증 불가"* 가 **로컬 검증 의무 면제**로 읽힌다 | **채택.** *"실기기(브라우저·모바일) 확인은 불가. 단 `tsc`·`build`·`check_ui_removals`는 DoD 의무"* 로 명시 |
| G4 | *"건드릴 파일만 열거"* 는 연쇄 수정 때 작업을 멈추게 한다 | **부분 채택.** 구현 과제는 **디렉터리 단위 허용**, 대신 **커밋 시 변경 파일 명시**(§3-3와 같은 원리). 검증 과제는 읽기 전용 유지 |
| G5 | *"분산된다는 전제가 틀렸다"* | **이미 초안 §9-3에 역전 조건이 있다.** 다만 **표 맨 앞으로 올려** 눈에 띄게 한다 |

---

## H2. Codex만 찾은 것 — 추가 채택

| # | 지적 | 처리 |
|---|---|---|
| ⭐ | **AGENTS.md를 append-only(④-2) 대상에서 뺀다** — *"이 규칙 때문에 잘못된 규칙을 고치지 못하고 정정문만 누적됐다"* | **채택.** 이것이 638줄의 **근본 원인**이다. `:74-75`가 실례다 — 정정문이 정정문을 낳고 원문은 그대로 남아 있다. **규칙 문서의 이력 보존은 git이 한다.** `project_notes.md`·`development_roadmap.md`는 append-only 유지 |
| ⭐ | **번호 대신 주제별 제목으로** — 재번호는 또 깨진다 | **채택.** A1·A3의 상위 해법이다 |
| | 프리페치(`:514` 캐시 우선)와 자동완성(`:603` 온디맨드 호출)이 **충돌** | 채택 — 합친다 |
| | 배포 절이 발신자·IAM·Firestore 내용을 **재복사**해 이미 낡음 | 채택 — `deployment_checklist.md`를 가리키는 관문만 남긴다 |
| | 「매 답변마다 다음 지시 프롬프트」(⑧)는 **의례**이고 `:378`에서 반복 | **F1과 같은 항목.** 사용자 판단 |
| | DoD가 문서만 고친 커밋에도 `tsc`·`build`를 요구한다 | 채택 — **범위 명시**(코드 변경 시에만) |

---

## H. 건드리지 않는 것 (셋 다 동의)

인프라 소유 계정 · 알림 발신자 · UID 동기화 · API 사전 조치 · **§1-1 2단계 점검** · §1-2 Claude 직접 구현 영역 · 프리페치·이름 표시 · **Firestore 보안 규칙** · 자동완성 선택 강제·서버 이중 방어 · 배포 체크리스트 · **①-1 재작성 금지**(2026-08-20에도 실사례가 나왔다) · ④-2 append-only · ④-3 STATUS · ⑩ 미래 트리거 금지

---

## 적용 순서 (승인 후)

1. **A(기계 결함) + C1(세션 시작)** — 효과가 가장 크고 위험이 가장 낮다
2. B(중복 제거) · D(역할 종료)
3. E(새 규칙 흡수) + G(초안 보강)
4. F는 사용자 회신 후

**각 단계마다 커밋을 끊는다.** ④-2(append-only)의 예외 작업이므로 무엇을 왜 지웠는지 커밋 메시지에 남긴다.
