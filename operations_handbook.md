# 운영·인수인계 핸드북 (Operations Handbook)

> 2026-07-24 Claude가 골격 작성, Antigravity가 세부 [TODO] 구현 완비.
> 이 문서의 독자는 **이 시스템을 만든 사람이 없어도 학교가 운영을 이어가야 하는 상황의 후임 담당자(비개발자 가능)** 다.
> 전문용어보다 "무엇을 눌러야 하는지"를 우선한다.
>
> ⚠️ **보안 주의**: 비밀값(키·시크릿)은 절대로 이 문서에 직접 기록하지 않으며, "어디에 있는지"와 "무슨 용도인지"만 기재합니다.

## 1. 시스템 한눈에 보기

- **웹사이트**: Vercel에서 호스팅되는 Next.js 앱. 주소: [https://portal.hmh.or.kr](https://portal.hmh.or.kr) (구 주소 `school-workspace-eight.vercel.app`도 계속 접속 가능, `hmh.or.kr`·`www.hmh.or.kr`은 여기로 자동 리디렉션)
- **데이터베이스**: Firebase Firestore (프로젝트: `school-sync-hub`)
- **로그인**: Firebase Auth — `@hmh.or.kr` 구글 계정만 허용
- **실제 데이터 원본**: Google Workspace (계정·클래스룸·캘린더·드라이브)
- **소스 코드**: GitHub `fb01-cmd/school-workspace` (main 브랜치 push 시 Vercel 자동 배포)
- **기능별 사용 설명서**: 생활지도 모듈은 [`discipline_manual.md`](./discipline_manual.md), 명단 자동 공급(API 키·마스터 시트)은 [`roster_feed_manual.md`](./roster_feed_manual.md) 참조 (비개발자용)

## 2. 핵심 계정과 열쇠 (값이 아니라 위치)

| 무엇 | 어디에 | 용도 |
|---|---|---|
| 관리자 계정 | `fb01@hmh.or.kr` (Workspace 최고관리자) | 콘솔 접근, 시스템 소유 |
| 알림 발신 계정 | `hmnotice@hmh.or.kr` | 학생·교사 안내 메일 및 구글 챗 발신자 |
| GCP 서비스 계정 | `school-sync-hub-admin@school-sync-hub.iam.gserviceaccount.com` (GCP IAM) | 서버가 Workspace API를 호출하는 백엔드 신원 (도메인 전체 위임 적용) |
| 환경변수 (비밀값 묶음) | 로컬 `.env.local` / Vercel 프로젝트 [Settings] → [Environment Variables] | 아래 §3 목록 참조 |
| Firebase 콘솔 | console.firebase.google.com → `school-sync-hub` | DB·인증·보안 규칙 관리 |
| GCP 콘솔 | console.cloud.google.com → `school-sync-hub` | 서비스 계정, API 활성화, 결제 및 할당량 관리 |

## 3. 환경변수 목록 (이름과 용도만)

| 환경변수 이름 | 구분 / 노출 범위 | 주요 용도 |
|---|---|---|
| `NEXT_PUBLIC_FIREBASE_API_KEY` | 클라이언트 (공개) | Firebase 웹 SDK 인증 및 Firestore 연결 API 키 |
| `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` | 클라이언트 (공개) | Firebase Auth 인증 도메인 (`school-sync-hub.firebaseapp.com`). **단, 배포 환경에서는 코드(config.ts)가 이 값을 무시하고 접속 도메인 자신을 사용** — 로그인 리디렉션 유실 방지용 same-origin 프록시 구조(`next.config.ts`의 `/__/auth` rewrite)와 세트 |
| `NEXT_PUBLIC_FIREBASE_PROJECT_ID` | 클라이언트 (공개) | Firebase 프로젝트 ID (`school-sync-hub`) |
| `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` | 클라이언트 (공개) | Firebase Storage 버킷 URL |
| `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID` | 클라이언트 (공개) | Firebase Cloud Messaging 발신자 ID |
| `NEXT_PUBLIC_FIREBASE_APP_ID` | 클라이언트 (공개) | Firebase 웹 앱 식별 ID |
| `NEXT_PUBLIC_BASE_URL` | 클라이언트/서버 | 서비스의 기본 프로덕션 주소 (`https://portal.hmh.or.kr`) |
| `GOOGLE_WORKSPACE_SERVICE_ACCOUNT_EMAIL` | 서버 (비밀) | GCP 서비스 계정 이메일 |
| `GOOGLE_WORKSPACE_SERVICE_ACCOUNT_PRIVATE_KEY` | 서버 (비밀) | GCP 서비스 계정의 RSA 개인키 (`\n` 줄바꿈 포함 문자열) |
| `GOOGLE_WORKSPACE_ADMIN_EMAIL` | 서버 (비밀) | Google Workspace 최고관리자 계정 (`admin@hmh.or.kr`) |
| `GOOGLE_WORKSPACE_SENDER_EMAIL` | 서버 (비밀) | 메일 및 구글 챗 공지 전용 발신 계정 (`hmnotice@hmh.or.kr`) |
| `CRON_SECRET` | 서버 (비밀) | Vercel Cron API 자동 호출 시 외부 무단 호출을 막기 위한 인증 시크릿 |

## 4. 매일 자동으로 일어나는 일 (크론)

매일 **자정(KST)** Vercel 크론이 `/api/workspace/lifecycle/cron`을 호출해서:

1. **전출·자퇴 학생**: 정지 예정일 지난 계정 일시정지 → 삭제 예정일 지난 계정 영구삭제
2. **졸업생** (12월~6월에만): 3학년/졸업생 명단 동기화, 미서명자 안내 발송(졸업생 매일, 재학생 월요일), 정지일·삭제일 도래 시 집행
3. **전출 교사**: 백업 기한 미설정자 주간 독촉, 기한 경과 시 정지, 정지 30일 후 영구삭제

모든 집행 결과는 어드민 화면의 **작업 감사 로그**에 남는다.
⚠️ 크론은 `CRON_SECRET`이 Vercel에 등록돼 있어야 작동하며, 미설정 시 실행을 거부한다(안전장치).

## 5. 문제가 생겼을 때 보는 곳 (순서대로)

1. **어드민 → 작업 감사 로그**: 최근에 무슨 자동/수동 작업이 실행됐는지 확인
2. **Vercel 대시보드 → 프로젝트 → Logs**: 서버 API 에러 및 크론 실행 로그 확인
3. **Firebase 콘솔 → Firestore**: 데이터(사용자 role, 태스크 상태 등)의 실제 값 확인
4. **Google 관리 콘솔** (`admin.google.com`): 계정·클래스룸·그룹의 실제 상태 확인

### 대표 장애 시나리오별 대응 절차

1. **"로그인이 안 되거나 화면이 튕겨요" (auth/popup-blocked 또는 redirect 오류)**
   - **원인**: 브라우저 팝업 차단 또는 OAuth 승인 도메인 누락.
   - **대응**:
     1. 브라우저의 팝업 차단을 해제하거나 리디렉션 로그인(`signInWithRedirect`)으로 자동 전환되도록 사용 안내.
     2. Google Cloud Console (`console.cloud.google.com`) → [API 및 서비스] → [사용자 인증 정보]에서 OAuth 2.0 클라이언트 ID의 '승인된 JavaScript 원본' 및 '승인된 리디렉션 URI'에 배포 주소(`https://portal.hmh.or.kr` 및 `https://portal.hmh.or.kr/__/auth/handler`)가 포함되어 있는지 확인.

2. **"크론(매일 자정 자동 처리)이 안 돌았거나 실행 오류가 나요"**
   - **원인**: Vercel 환경변수에 `CRON_SECRET` 누락 또는 GCP API 한도 초과/권한 이슈.
   - **대응**:
     1. Vercel 대시보드 → [Settings] → [Environment Variables]에 `CRON_SECRET`이 정확히 설정되어 있는지 확인 (미설정 시 503 HTTP 에러로 닫힘).
     2. Vercel 대시보드 → [Logs] 메뉴에서 `/api/workspace/lifecycle/cron`의 00시 실행 기록과 상세 오류 로그 확인.

3. **"계정이 실수로 정지/삭제되었거나 전출 취소가 필요해요"**
   - **원인**: 유예기간 만료 또는 크론 자동 집행.
   - **대응**:
     1. 관리자 대시보드 → **[계정 생애주기 관리]** 메뉴 접속.
     2. **졸업생 관리** 탭 또는 **교사 전출 관리** 탭에서 대상 계정을 찾은 뒤 **[복원 / 정지 해제]** 또는 **[전출 취소]** 버튼 클릭.
     3. 구글 워크스페이스 상에서 정지가 즉시 해제되고 지정된 연동 그룹에 자동 재가입 처리됨.

4. **"API 호출 시 403 Forbidden / API Not Enabled 에러가 떠요"**
   - **원인**: GCP API 미활성화 또는 Domain-Wide Delegation Scope 누락.
   - **대응**:
     1. GCP Console (`console.cloud.google.com`)에서 해당 API(예: Google Calendar API, Google Drive API, Google Classroom API)가 **[사용(ENABLE)]** 상태인지 확인.
     2. Google Workspace Admin Console (`admin.google.com`) → [보안] → [API 제어] → [도메인 전체 위임]에 서비스 계정 이메일과 해당 OAuth Scope URL이 등록되어 있는지 확인.

5. **"시간표 그리드/뷰 데이터가 이전 값으로 꼬이거나 즉시 갱신되지 않아요" (캐시 장애 대응)**
   - **원인**: 시간표 그리드 조회 캐시(In-memory / Redis)의 만료 전 갱신 누락.
   - **대응**:
     1. 시간표 캐시 킬스위치 환경변수인 `TIMETABLE_VIEW_CACHE`를 `off`로 지정 (`TIMETABLE_VIEW_CACHE=off`).
     2. 캐시가 즉시 우회되고 실시간 재계산 결과가 화면에 표시되는지 확인 후 원인 해결 후 다시 제거/원복.

## 6. 백업과 데이터 유실 대응

학교 포털의 **모든 데이터**(학생 명단, 생활지도 기록, 시간표·시수 계획, 쪽지, 교직원 조직도, 작업 기록)는 클라우드 데이터베이스 한 곳에 보관됩니다. 만약 데이터가 실수로 지워지거나 꼬였을 때 되돌릴 수 있는 유일한 방법이 **백업**입니다.

---

### 6-1. 백업 — 무엇을, 언제, 어디에 저장하는가

1. **무엇을 저장하나**
   - 시스템 내의 모든 데이터(약 1,300여 개 문서 전체)를 빠짐없이 그대로 저장합니다.
   - 학생·교직원 명단, 생활지도 기록, 학기별 시간표와 시수표, 교직원 조직도 및 부서 배치, 보낸 쪽지함, 관리자 작업 감사 로그가 모두 포함됩니다.

2. **언제 저장하나**
   - **정기 백업 (수동)**: **한 달에 1번** 및 **학기 경계(2월 봄방학, 8월 여름방학)** 에 관리자가 명령어 한 줄로 실행합니다.
   - **작업 직전 자동 보관**: 대량의 데이터를 일괄 수정하거나 정리하는 관리 스크립트가 실행될 때, 시스템이 스스로 변경 직전의 데이터를 자동 스냅샷으로 보관합니다.

3. **어디에 저장되나**
   - 관리자 컴퓨터의 `~/school-backups/` 폴더에 저장됩니다. (웹 서버나 공개 저장소 안이 아니라 내 컴퓨터 안 안전한 곳입니다.)
   - **보관 수량**: **최근 백업 3개 + 학기말 보관본 1개**만 남기고, 오래된 백업은 디스크 용량과 보안을 위해 정리합니다.

> ⚠️ **개인정보 취급 주의사항**
> 백업 파일 안에는 학생과 교직원의 이름, 생활지도 기록, 쪽지 등 민감한 개인정보가 그대로 들어 있습니다.
> - 백업 파일을 카카오톡, 이메일, 개인 클라우드(구글 드라이브 등)로 **전송하거나 업로드하지 마세요.**
> - 내 컴퓨터를 벗어나는 순간 개인정보 유출 사고가 됩니다. 다른 사람에게 인계할 때도 파일을 전달하는 것이 아니라 **후임자가 본인 컴퓨터에서 직접 새 백업을 만들도록** 안내합니다.

#### 백업 실행 방법 (명령어 2줄)

```bash
# 1) 백업 만들기 (약 1~2분 소요, 시스템 중단 없음)
npm run backup

# 2) 만들어진 백업이 정상인지 안전 검증 (실제 쓰기 없음)
npm run backup:verify -- --from=~/school-backups/<방금_생성된_폴더명>
```
- 검증 결과에 `왕복 검증 통과`가 나오면 언제든 이 백업으로 안전하게 되돌릴 수 있는 완벽한 상태입니다.

---

### 6-2. 데이터 유실 시 단계별 복원 절차 (비전문가 후임자 가이드)

데이터가 지워졌거나 잘못 변경되었을 때는 당황하지 말고 아래 6단계를 차례대로 진행하세요.

#### 1단계: 즉시 추가 작업을 멈추기
- "다시 채워 넣어야지" 하고 화면에서 새 데이터를 임의로 입력하거나 수동으로 만지지 마세요. 복원할 때 데이터가 섞이거나 충돌할 수 있습니다.

#### 2단계: 유실 원인과 시점 파악하기
- 관리자 포털 화면의 **[작업 감사 로그]** 메뉴로 이동합니다.
- 최근에 어떤 관리자 작업이나 자동 처리가 실행되었는지, 몇 시에 삭제/변경이 발생했는지 확인합니다.

#### 3단계: 가장 가까운 정상 백업 폴더 찾기
- 터미널에서 저장된 백업 목록을 확인합니다:
  ```bash
  ls -l ~/school-backups/
  ```
- 유실 사고가 발생하기 직전에 만들어진 가장 최신 백업 폴더 이름을 확인합니다 (예: `2026-08-14T2130_full`).

#### 4단계: 미리보기(예행 연습) 실행 — 쓰기 없이 안전하게 확인
- 실제로 데이터베이스를 건드리지 않고, **무엇이 복구될지 목록만 먼저 확인**합니다.
  ```bash
  # 전체 데이터를 복원할 경우 (미리보기)
  npx tsx --env-file=.env.local scripts/restore_firestore.ts --from=~/school-backups/<폴더명> --all

  # 특정 항목(예: 생활지도 기록만) 복원할 경우 (미리보기)
  npx tsx --env-file=.env.local scripts/restore_firestore.ts --from=~/school-backups/<폴더명> --path=discipline_records/hmh.or.kr/records
  ```
- 화면에 **「신규 N건 / 기존 M건」** 요약이 표시됩니다. 여기서 **신규(New)** 로 잡히는 숫자가 바로 복원되어 다시 살아날 데이터입니다.

#### 5단계: 실제 복원 실행
- 미리보기 결과가 맞다면, 명령어 끝에 `--apply`를 붙여 실제로 복원을 실행합니다:
  ```bash
  npx tsx --env-file=.env.local scripts/restore_firestore.ts --from=~/school-backups/<폴더명> --all --apply
  ```
- **안전 원칙**: 이 복원 도구는 **사라진 데이터만 쏙쏙 다시 채워 넣으며, 현재 정상적으로 존재하는 데이터는 절대 삭제하거나 해치지 않습니다.**
- **기존 데이터 덮어쓰기 주의 (`--overwrite`)**: 기본은 사라진 것만 안전하게 채워 넣습니다. 지금 있는 값을 백업 시점 값으로 강제로 덮어쓰려면 맨 뒤에 `--overwrite`를 붙여야 합니다. **하지만 이는 현재 살아있는 값까지 과거 값으로 덮여 사라지므로**, 특별한 확신이 없으면 절대 붙이지 마세요.

#### 6단계: 화면에서 확인
- 관리자 포털에 접속하여 사라졌던 데이터가 정상적으로 돌아왔는지 확인합니다.

#### 복원 시 알아 두실 점
- **되돌리기는 절대 지우지 않습니다.** 백업에 없는 것은 그대로 둡니다. 그래서 안전합니다.
- **백업이 없거나 너무 오래됐으면 더 손대지 말고 상의하세요.** 원인을 모른 채 임의로 시스템을 만지면 복구가 더 어려워집니다.
- **계정 자체가 지워졌다면** 이는 데이터베이스 문서 문제가 아니므로 §5 시나리오 3(구글 계정 20일 내 복원)을 먼저 진행합니다.

> 💡 **실사용 기록 (2026-08-14)**: 실제로 관리자 계정의 조직 정보를 정리하다 되돌린 사례가 있습니다. 그때 작업 직전 변경 전 89건이 자동으로 보관되었고, 이 복원 절차를 통해 손실 없이 안전하게 복구했습니다.

---

### 6-3. 복원해도 돌아오지 않는 것 (한계와 예외)

백업 복원을 진행할 때 아래 사항은 복원 대상에 포함되지 않거나 별도의 처리가 필요하므로 미리 알아두어야 합니다:

1. **구글 워크스페이스 계정(이메일) 자체**
   - 데이터베이스 백업을 복원하면 포털 안의 사용자 정보는 살아나지만, 구글 계정(로그인 ID) 자체가 재생성되지는 않습니다.
   - 구글 계정이 삭제된 경우라면 구글 관리 콘솔(`admin.google.com`) 또는 포털 [계정 생애주기 관리] 메뉴에서 **「삭제된 사용자 복원(20일 이내)」** 을 먼저 실행해야 합니다.

2. **구글 드라이브 파일 및 클래스룸 과제물**
   - 교사나 학생의 개인 구글 드라이브 파일, 클래스룸에 제출된 과제 파일 등은 구글 서버에 직접 저장되는 파일입니다.
   - 이 파일들은 구글 드라이브의 휴지통 정책을 따르며, 데이터베이스 백업 복원으로는 되살아나지 않습니다.

3. **마지막 백업 이후에 새로 발생한 변경분**
   - 백업이 생성된 시점과 데이터 유실 사고 발생 시점 사이에 새로 작성된 쪽지나 새로 등록된 생활지도 기록은 백업 파일에 들어있지 않으므로 복구되지 않습니다. (정기 백업을 매월 잊지 않고 해야 하는 이유입니다.)

4. **백업 이후에 새로 만들어진 정상 데이터의 강제 삭제 (완전 롤백)**
   - 복원 도구는 안전을 최우선으로 하여 **'사라진 것만 추가'** 합니다. 백업 시점 이후에 새로 입력된 다른 정상 데이터를 과거로 되돌리기 위해 강제로 삭제하지는 않습니다.

---

## 7. 정기 운영 캘린더

| 시기 | 할 일 |
|---|---|
| **월 1회 + 학기 경계** | **데이터 백업** — 위 §6-1. 가장 중요하고 가장 잊기 쉽다 |
| 2월 | 학기말 클래스룸 정리 시즌 — 교사 알림 배너 자동 노출, 교사 셀프 정리 |
| 12월~6월 | 졸업생 계정 생애주기 시즌 (크론 자동, §4 참조) |
| 3월 | 신입생 입학 처리, 진급 처리 (생애주기 메뉴) |
| 수시 | 전출·자퇴·전입 처리 |
| 연 1회 | **서비스 계정 키 로테이션**: GCP Console → IAM 및 관리자 → 서비스 계정에서 새 JSON 키 발급 → Vercel 및 `.env.local`의 `GOOGLE_WORKSPACE_SERVICE_ACCOUNT_PRIVATE_KEY` 환경변수 갱신 후 재배포 (구 키는 만료 삭제) |

---

## 8. 후임자 인수인계 체크리스트

- [ ] Workspace 최고관리자 권한 이양 (또는 공동 관리자 추가)
- [ ] GitHub 저장소 소유권/협업자 이전
- [ ] Vercel·Firebase·GCP 프로젝트 멤버 추가
- [ ] `.env.local` 비밀값 안전한 방법으로 전달 (문서·메일 금지)
- [ ] 이 핸드북 + `product_overview.md` + `deployment_checklist.md` 함께 읽기
- [ ] **백업을 후임자 컴퓨터에서 한 번 직접 돌려 보기** (§6-1) — 명령이 실제로 도는지 눈으로 확인. 설명만 읽고 넘기면 정작 필요한 날 못 돌린다
- [ ] **기존 백업 파일은 넘기지 않는다** — 개인정보다. 후임자가 자기 환경에서 새로 만든다
- [ ] AI 에이전트 협업 방식은 `AGENTS.md` 참조 (후임자가 원할 경우)

