# 🚀 효명고등학교 관리 시스템 — 복구된 개발 로드맵 및 아이디어 보관소

이 문서는 이전 대화 세션들(`2af17987-3499-43a7-854b-92319a40b0b2`, `f6bb0e2e-851a-4d9f-ac71-3a888b7f0015` 등)의 기록을 역추적하여 유실될 뻔한 개발 계획, 아키텍처 의사결정 맥락, 보안 지적 사항 및 브레인스토밍 아이디어들을 하나로 통합 정리한 공식 로드맵 파일입니다.

---

## 📌 1. 전체 개발 로드맵 (Phase 1 ~ Phase 8)

> 📦 **완료 Phase의 상세(설계 결정·리뷰·배포 이력)는 [`archive/roadmap_completed_phases.md`](./archive/roadmap_completed_phases.md)로 이관됨 (2026-08-05, 원문 그대로).** 아래는 요약.

### Phase 1 ~ 5.8 (완료 ✅ — 요약)

* **Phase 1. 학적 관리 핵심 기능** ✅
* **Phase 2. 교직원 계정 생애주기 관리** ✅
* **Phase 3. 졸업 동의서 터치 서명 및 보관 강화** ✅
* **Phase 3.5. UX 성능 개선 및 삭제 계정 복원** ✅ (2026-07-16)
* **Phase 4. 구글 클래스룸 로스터 강제 배정** ✅ (2026-07-16)
* **Phase 5. 크롬 관리 북마크 즉시 배정 및 실시간 히스토리 로그** ✅ (2026-07-16)
* **Phase 5.5. 교직원 조직도 및 내 프로필 관리** ✅ (2026-07-16)
* **Phase 5.8. 학기말 클래스룸·캘린더·드라이브 일괄 정리** — 배포 승인 ✅ (2026-07-24). **후속 과제 4건 잔존**: 이메일 에스컬레이션 크론, 캘린더 restore, 관리자 읽기전용 대시보드, restore의 super_admin 소유자 검증 우회. 실사용 시점(2027-02) 전까지 처리하면 됨.

### Phase 5.9. 조직도 고도화 — 정렬 규칙·어드민 수동 배치·트리 뷰 (완료 ✅ 2026-07-27 종결)

* 전체 종결 — 정렬 규칙 기록·수동 배치(조직도 빌더 v2.1 스테이징 커밋)·모달 정렬·트리 뷰·후속 4건까지. 스펙: [`org_chart_spec.md`](./org_chart_spec.md), 종결 기록: archive/project_notes_2026-07.md [2026-07-27]. (2026-08-05 상태 정정 — "구현 📋"은 낡은 표기였음)

### Phase 6a. 명단 공급 허브 & 6b. 생활지도 기록 모듈

* **Phase 6a-1. 읽기 전용 명단 API 및 키 관리 UI** ✅ (2026-07-25) · **Phase 6a-2. 명렬표 마스터 시트 자동 갱신** ✅ (2026-07-25) — 상세는 아카이브 참조.

* **Phase 6b. 생활지도 기록 모듈 (완전 종결 ✅ 2026-07-26)**: 보안 핵심부(Claude — 엔진·권한 판정·API 4종, 41케이스 통과) + 화면 6종(Antigravity, Claude 표적 리뷰로 치명 5건 수정) + 규칙 게시 자동화 + 파기 크론 + 프로덕션 E2E 25/25. 담임 단일 원본은 승인 프로필로 일원화(배정표 화면은 파생 뷰). 사용 설명서: [`discipline_manual.md`](./discipline_manual.md), 종결 기록: archive/project_notes_2026-07.md [2026-07-26]. (2026-08-05 상태 정정 — "화면 6종 미구현 📋"은 낡은 표기였고, 이 오기가 8/5 중복 착수 지시의 원인이 됨)

### Phase 7. 학부모 소통 모듈 — 상용 알림 플랫폼 대체 (검토 단계 📋 2026-08-06 재정의)

> **재정의 배경 (2026-08-06 사용자)**: 당초 "카카오 알림톡 연동"이었으나, 실제 목적은 **학부모 알림** — 학교는 이알리미 → 슈퍼스쿨(작년) → 리로스쿨(올해)로 갈아타며 연 수백만 원대 상용 플랫폼을 쓰는 중. 이를 저비용으로 대체할 수 있다면 가치 있음.
> **비용 사실 (2026-08-06 확인)**: 알림톡(전화번호 발송, 채널 친구 불필요) 딜러사 단가 건당 7~13원 — 900가구 월 8회 발송 시 월 7만원대·연 70~80만원. 채널 채팅 메시지는 15원/건, 채널 소식·1:1 채팅은 무료. 학생 대상 푸시는 웹 푸시(0원)로 이미 대체 확정.
> **설계 방향 (하이브리드)**: 알림톡은 "가정통신문 도착 + 링크" 초인종으로만 최소 발송, 본문 열람·설문·동의서 서명(터치 서명 기구현 재사용)·열람 확인은 플랫폼 웹(0원)에서. §2 "학부모 연락처 데이터베이스"가 기반.
> **전제 (착수 조건)**: ① 학교의 리로스쿨 실지출액 확인(행정실) ② 실사용 기능 범위 확인(가정통신문+설문 위주면 현실성 높음) ③ 비용 부담 주체는 학교 예산(사용자 개인 부담 금지 — 무료 원칙) ④ 학부모 전화번호 수집·보관·파기 설계(Phase 11 인벤토리 확장, Claude) ⑤ 카카오 비즈니스 채널·발신프로필·템플릿 심사(교육기관 가능). 시점: 개학 안정화·9c 이후.

### Phase 8. 교사 업무 Task 관리 시스템 (미구현 📅 — 2026-08-08 방향 논의, 사용자 결정 대기)

* **배경·목적 (2026-08-08 사용자 구술)**: 현재 교사 업무는 쿨메신저(구식·별도 서버컴·유료) 기반 — 메시지와 업무 상태가 분리돼 있지 않아 "업무 놓친 것 없나" 메신저를 뒤적이는 구조를 근본 대체. 이 플랫폼 착수 전의 원 구상이 바로 이것(당시엔 캘린더·태스크 부가기능으로 계획).
* **사용자 구상 흐름**: 조직도에서 대상 지정 → 업무명·기한·메시지·리소스 첨부(Drive 자동 업로드+링크) → 전송 → 수신자 수락 시 본인 캘린더 등록 + 플랫폼 할일 목록 편입 → 완료 체크가 발신자에게 실시간 반영(미수락·미완료자 파악).
* **Claude 의견 (2026-08-08, 채택 여부는 사용자 결정)**: ① **플랫폼 네이티브 구현 권장** — 어려운 재료(조직도·알림·권한·감사·Drive·설치 앱)가 전부 기구현, 애드온(Apps Script)은 재구축+UI 제약+모듈 결합 불가. 컴시간 대체와 동일 패턴. ② **Firestore 용량 무해** — 인간 생성 데이터라 증폭 구조 없음(쓰기 수백/일, 읽기 ~5천/일 상한, capacity review Phase 8 판정 '하' 유지). 첨부는 Drive 링크만 저장. ③ **캘린더 = 단방향 출력 채널로 격하, 구글 Tasks 동기화는 제외 권고** — 체크 상태가 두 곳에 살면 실시간 반영이 깨지고 폴링(진짜 읽기 낭비)이 필요해짐. 상태 단일 원본 = 플랫폼. ④ 승부처는 보드가 아니라 **발신자 화면의 사람별 수락/완료 실시간 현황**(재촉 대상 즉시 파악 — 좁은 구독이라 용량 무해). ⑤ 스펙에서 결정할 실무 설계: 수락 전 상태(거절·대리·재촉), 지시 권한 범위(전 교사 자유 vs 부장 이상), 알림 피로 설계, 캘린더 이벤트 취소 동기화.

* **단계별 구현 계획 (2026-08-09 Claude 제안 — 쪽지 아이디어(§2)와 통합, 착수 시점은 사용자 결정)**: 원칙 = "쪽지가 기반, 업무는 그 위의 구조화, 캘린더는 마지막 출력" — 각 단계가 단독 완결 가치를 내며 다음 단계의 기반이 됨 (9a→9b→9c 패턴). 순서 근거: ⓐ 업무 관리의 난부(도달·수신확인·알림) = 쪽지 인프라 그 자체 ⓑ 채택 곡선 — 쪽지는 메일 사본으로 비채택자에게도 성립하지만 업무 수락은 플랫폼 진입이 전제이므로, 쪽지로 유입을 만든 뒤 업무를 얹는 것이 전환 마찰 최소.
  - **1단계 — 쪽지 MVP (도달+수신확인)**: 조직도/그룹 다중 선택 → 제목·내용·Drive 링크 → 발송. 원본·읽음 상태 = 플랫폼(Firestore), 발송 시 hmnotice@ 메일 사본("확인은 플랫폼에서" 링크) + 웹 푸시. 보낸 쪽지함에 수신자별 읽음 표시. 보존 기한 + 파기 크론. 이 단계만으로 쿨메신저 핵심 대체.
  - **2단계 — 쪽지 완성**: 첨부는 **"링크 공유"가 아니라 "사본 수수" 의미론** (2026-08-09 사용자 지적 반영 — HWP 등 로컬 편집 파일은 링크로는 "실제로 가져가지"를 못함, 학교 업무 지배 패턴 = 양식 배포→로컬 한글 작성→제출): 첨부 파일은 Drive에 저장하되(파일 Firestore 저장 금지) 수신자 기본 동작 = **내려받기**(각자 사본). 플랫폼 내 답장·스레드, 대시보드 "안 읽은 쪽지 N건" 카드(기존 건수 카드 아이디어 부분 실현).
  - **3단계 — 업무 지시 (Phase 8 본체)**: 쪽지에 "업무" 타입 — 기한·수락/거절·완료 상태 머신, 발신자 현황판(사람별 수락/완료 실시간+재촉), 기한 임박 크론. **+ 제출형 업무 (2026-08-09 추가)**: 완료 = 작성본 파일 업로드 → 업무별 Drive 제출함 폴더 자동 생성(담당자만 접근) + **제출 파일명 자동 정규화("2학년3반_홍길동_양식명.hwp")** — 취합(파일명 난장판·미제출 파악)이 담당자 최대 고통이므로 이것이 쿨메신저 대비 킬러. 전송·알림은 1단계 재사용이라 신규는 상태 관리·현황판·제출함뿐. 실무 설계 결정(의견 ⑤)은 이 단계 스펙에서.
  - **(후속 연계) HWP 뷰어**: §2 "HWP 미리보기/편집 임베드"(rhwp)의 1차 자리 = 제출함 검토 — 담당자가 HWP 제출본을 내려받지 않고 브라우저에서 훑기. 편집은 성숙도 검증 후 별건. 당장의 원칙은 "로컬 한글 왕복을 잘 받쳐주기"(교사 도구를 바꾸지 않고 수수·취합 고통 제거).
  - **파일 흐름·저장 구조 (2026-08-09 문답으로 확정)**: 수신자는 Drive를 경유하지 않는다 — 플랫폼에서 로컬 내려받기 → 한글 작성 → 플랫폼 업로드 제출 (쿨메신저 첨부와 동일 감각, Drive는 불가시). 저장소는 **교사 개인 Drive가 아니라 플랫폼 전용 공유 드라이브(조직 소유)** — 개인 Drive는 전출·퇴직 계정 삭제 시 첨부 소멸+소유권 꼬임(생애주기 모듈과 충돌). 폴더는 사람이 관리하지 않는 **플랫폼 자동 생성 창고**: `쪽지첨부/YYYY/MM/`, `업무/YYYY/{업무ID}/양식|제출함/`, 접근 권한 = 서비스 계정만("창고는 잠그고 수수는 플랫폼 창구로만" — 폴더 배치 실수·권한 꼬임 구조적 차단). 예외 1: 담당자에게 해당 업무 제출함 폴더 열람 자동 부여(통째 내려받기 취합). 보존 기한 경과 시 연도 폴더 단위 파기 크론 + 개인정보 인벤토리 등재. **(2026-08-09 추가 문답)** ① 드라이브는 기존 명단 시트용 공유드라이브 재사용이 아니라 **업무 전용 공유드라이브 신설** 권고 — 명단 드라이브는 외부 열람용으로 권한이 열려 있어 "서비스 계정만" 잠금 원칙과 충돌(권한 규칙 혼합 = 제출물 개인정보 유출 구멍). ② 파일 수 개념 확정: 양식 원본 1개 + 제출 시마다 1개(다운로드는 로컬로만, Drive 사본 없음) → 업무당 = 양식 수 + 제출 인원수. ③ 재제출은 최신본 교체 권고 — 이전 본은 Drive 파일 버전 이력이 무료 보관, 제출함은 사람당 1개 유지.
  - **4단계 — 캘린더 출력+전환 마감**: 수락 시 수신자 캘린더 이벤트 등록·취소/변경 동기(단방향, DWD 스코프 1 추가), /m "내 할일" 노출, 쿨메신저 공식 퇴역.
  - 용량: 전 단계 인간 생성 데이터 — capacity review 판정 '하' 유지, 단계별 스펙에 읽기 계수 명기. 분업: 스펙·모델·상태 머신·권한 = Claude / 화면 = Antigravity.
* **식은 눈 재검토 (2026-08-09 Claude, 사용자 요청)**: **뼈대 유지 판정** — 단계 순서·상태 단일 원본·사본 수수·창고 잠금 원칙 모두 존치. 보강·정정 6건: ① **[보안, 1단계 스펙 필수]** 현행 전역 규칙(`request.auth != null`)로는 학생 포함 전 로그인 사용자가 쪽지 컬렉션을 클라이언트에서 직독 가능 — 수신자·발신자 검사 규칙(또는 API 경유 강제) 필수. ② **[2단계 기술 리스크]** Vercel 함수 요청 본문 4.5MB 한도 — 첨부 업로드는 API 경유 불가 크기 존재, Drive 업로드 세션 URL 발급→브라우저 직접 업로드 등 우회 경로를 2단계 스펙에서 결정(무료 범위 내 해결 가능). ③ **[사실 정정 → 사용자 확정 2026-08-09]** Drive 리비전은 기본 30일/100개 초과 시 자동 삭제 — **(a) 최신본만 남음 채택**, 재제출 UI에 "이전 제출본은 교체되며 30일 후 복구 불가" 경고 표시. ④ **[1단계 설계 결정 → 방향 확정 2026-08-09]** 메일 사본 이중 소음은 **수신자 Gmail에 필터+라벨 자동 설치**로 해소 (사용자 발상, Claude 실행 가능 확인): Gmail API `users.settings.filters`(생성·삭제)·`users.labels`로 "쪽지 메일 사본 → 받은편지함 건너뛰기(INBOX 제거) + '효명고' 라벨" 필터를 DWD 사칭으로 각 교사 계정에 설치 가능. 필터 조건은 from:hmnotice@ 전체가 아니라 **쪽지 사본만 식별**(제목 접두사 등)해야 타 알림 메일 오배송 방지. 신규 DWD 스코프 2건: `https://www.googleapis.com/auth/gmail.settings.basic`, `https://www.googleapis.com/auth/gmail.labels`. **잔여 결정 1건**: 필터 설치 대상 — 전 교사 일괄이면 미채택 교사는 메일이 받은편지함에 안 떠서 도달 붕괴(메일 사본의 존재 이유 상실) → Claude 권고 = **플랫폼 첫 로그인(채택) 시 설치** (채택자는 조용한 보관용, 미채택자는 받은편지함 도달 유지). 1단계 스펙에서 확정. ⑤ **[문구 조정 → 사용자 확인 2026-08-09]** 플랫폼 인지 그룹은 점진 개발 중임을 이미 앎 — 포지셔닝은 실사용 홍보 시점에만 챙기면 되는 저우선. ⑥ ~~[일정] 개학 첫 1~2주 회피 권장~~ **철회 (2026-08-09 사용자 정정)**: 사용자 휴직 중 + 개학 시점 실사용 인구 사실상 제로 — 착수 시점에 일정 제약 없음. 경미: 파기 크론은 정책만 1단계 결정, 구현은 후행 가능.

### Phase 9. 시간표·결보강 시스템 — 컴시간 완전 대체 (스펙 확정 ✅, Phase 7·8보다 앞당겨 착수)

* **2026-07-26 사용자 확정**: 컴시간(주간시간표+일과진행+알리미) 완전 대체가 목표. 스펙: [`phase9_spec.md`](./phase9_spec.md), 도메인 조사: [`comcigan_analysis.md`](./comcigan_analysis.md).
* 단계: 9a 열람(알리미 대체) → 9b 결보강 워크플로(일과진행 대체, 교체 후보 원클릭) → 9c 자동 시간표 작성(브라우저 CSP 솔버). 킬 스위치 = 컴시간 계약 갱신일.
* **9a-1 종결 (2026-07-30 ✅)**: §6 미결정 4/5 확정 (재계약 결판 2026-12 이전 → 9c 검증 시한 11월 말 / 2학기 개학 오픈 / 실무사 승인+교무부장 알림 / 학생 자기 반만). 상세 스펙: [`phase9a_spec.md`](./phase9a_spec.md). 백엔드+가져오기(xlsx 업로드 파서)+열람 탭+학생 카드 구현, Claude 표적 리뷰 승인. 실파일 5단계 리허설 완주 — `2026-2` 학기 active, 30학급 그리드 실측 확인 (리허설 중 발견된 room undefined 블로커는 `ce034bb`로 수정). 노출 원칙(spec §8-1): 일반 교사·학생 비노출 유지, 9b 오픈 게이트까지.
* **9b 스펙 확정 대기 (2026-07-30 📝)**: [`phase9b_spec.md`](./phase9b_spec.md) 작성 완료 (설명서 PDF 2종 정독 기반 — 컴시간 기능→범위 매핑, weeks/changes/requests 모델, 주간 합성, 교체 후보 엔진(맞교환·특별보강·감점 규칙), 신청→승인 워크플로, NEIS 목록·시수 집계, 구현 분업 6단계). **사용자 확인 4건(spec §12) 답변 후 구현 착수** — 순서 1·2(합성·엔진·승인 서버)는 Claude 직접, 화면은 Antigravity.

#### (구) Phase 9 원안 — AI 기반 웹 시간표 및 교체수업 편성 시스템 (스펙으로 대체됨)
* **제약 충족 알고리즘(CSP)**: 교사 가용 시간, 교실 제한, 과목 연속 수업 차단 규칙을 반영한 최적의 시간표 생성.
* **결강 자동 보강**: 결강 발생 시 당일 공강 교사 목록을 우선순위 규칙에 따라 최적 추천.
* **구글 캘린더 연동**: 확정된 시간표를 개인 캘린더에 자동 동기화.

### Phase 11. 보안·개인정보 정책 + 최초 로그인 동의 (완료 ✅ 2026-08-06 — 실기기 확인 통과·종결, [`docs/phase11_privacy_spec.md`](./docs/phase11_privacy_spec.md))

* **종결 요약 (2026-08-06)**: 산출물 A(/privacy 공개 페이지)·B(최초 로그인 고지 모달+ack API+policy_acks)·C(수퍼어드민 고지 현황 탭)·D(records 실이메일 Directory 검증 — 합성 경로 소멸)·E(서명 증빙 3년·감사 로그 5년 파기 크론) 전부 배포. 실기기 확인 6항목(고지 모달→확인→재접속 억제, /privacy 전문·모달 억제, 상시 링크 3곳, 기록 입력, 고지 현황 탭 날짜 표시[장애 복구 검증 포함]) 전 항목 통과.

* **배경**: 실사용 전 사용자 고지·동의 체계가 필요. 플랫폼 완성 과정에서 범위가 늘겠지만 **현재 개발분까지만이라도 우선** 갖춘다.
* **범위(초안)**: ① 개인정보 처리 안내 문서 — 현재 기능 범위(계정·학적, 생활지도[민감 최상], 시간표·수업교환, 클래스룸 정리, 명단 API, 감사 로그) 기준, [`personal_data_inventory.md`](./personal_data_inventory.md)를 단일 원본으로 삼아 파생. ② **고지 vs 동의 구분 설계** — 학교 고유 사무(법령 근거) 처리는 고지 중심, 동의는 법적으로 필요한 최소 항목만 (과잉 동의 요구는 오히려 리스크). ③ **최초 로그인 동의/고지 화면** — 버전 관리(정책 개정 시 재고지), 교사/학생 역할별 문안 분리. ④ 관리자 화면에 동의 이력 조회.
* **분업**: 정책 문안·고지/동의 구분·스펙 = Claude (보안·개인정보 결정 영역), 동의 화면 UI = Antigravity.

### Phase 10. Teaching & Learning 라이센스 대여 자동화 (미구현 💡)
* **배경**: 학교당 라이센스 보유량(15개) 제한으로 인해 교사들이 필요할 때마다 정보부 관리자에게 수동 요청하여 할당받던 비효율을 해결.
* **구글 라이센스 API 연동**: Google License Manager API (`licensing.v1`)를 연동하여 `licenseAssignments`를 백엔드에서 자동 제어.
* **실시간 캘린더 예약**: 날짜별 가용 수량(15개 - 예약 확정 수)을 시각적 캘린더에 표시하여 교사가 원하는 기간(시작일~종료일)을 선택해 셀프 대여 신청.
* **크론 연동 스케줄링**: 매일 자정 크론이 돌 때, 시작일인 교사에게 자동으로 라이센스를 할당하고 만료일이 지난 교사에게서는 라이센스를 자동 회수 처리.

---

## 📌 2. 유실될 뻔한 기타 아이디어 메모

* **플랫폼 쪽지(사내 메신저) — 쿨메신저 대체, "플랫폼 원본 + 메일 단방향 사본" (2026-08-09 사용자 발상 + Claude 방향 의견, 미확정)**: 발상 = "메신저 쪽지는 메일의 로컬화"이므로 Gmail을 활용해 메신저처럼 보이게. Claude 의견: Gmail을 원본 백엔드로 쓰는 안(갈래 A)은 수신확인 불가(Gmail 열람 감지 불가)·교사 명의 사칭 발송 권한(DWD) 문제로 비추천. **갈래 B 추천 — 쪽지 원본·읽음 상태는 플랫폼(Firestore, 인간 생성 데이터라 용량 무해·보존 기한+파기 크론), 발송 시 hmnotice@ 명의 메일 사본 자동 발송**(발신자 규칙 정합). 노림수: ① 비채택 교사도 메일로 도달(쿨메신저 전환기 소외 없음 — 학부모 모듈 초인종 패턴과 동일) ② 수신확인 완전(원본 열람=플랫폼) ③ 메일 사본의 "확인은 플랫폼에서" 링크가 채택 유입 동선. 트레이드오프: 메일 사본에 직접 답장하면 플랫폼 밖 일반 메일(감수 — 답장 유도=채택 유도). Phase 8 업무 관리와의 관계: 업무 지시 = 구조화된 쪽지(수락·기한·완료 상태) — 쪽지 전송·도달·수신확인 레이어를 먼저 깔면 Phase 8이 그 위에 올라가는 구조 가능. "상태 단일 원본=플랫폼, 구글 서비스=출력 채널" 원칙(Phase 8 캘린더 격하 결정과 동일 계열).

* **로그인 화면 개인정보 안내 박스 (✅ 구현 완료 2026-08-05)**: 로그인 버튼 아래에 처리 안내 요약 + `/privacy` 전문 링크 박스 표시 — 인증 전 사전 고지 보강. 단 "로그인 시 동의 간주" 문구는 넣지 않는다(이 시스템은 고지+명시적 확인 구조가 원칙 — 간주 방식은 증빙·재고지·비자발적 이용자 문제로 기각, project_notes 2026-08-05 판단 기록 참조).
* **앱 설치 안내 상설 메뉴 (2026-08-05 사용자 확정, 구현 완료 ✅)**: 관리자 화면에 전 교사용 메뉴 "앱으로 설치하기"를 상설 — `docs/pwa_installation_guide.md`의 3단계(설치 → 컴퓨터 로그인 시 앱 실행 → 작업표시줄 고정)를 화면화 (`PWAInstallGuideTab.tsx`). 스크린샷 3장은 사용자 제공 대기(자리 표시로 선구현). UI 문구에 PWA 등 개발 용어 금지.
* **대시보드 할 일 건수 카드 (2026-08-06 원칙 신설의 후속)**: 대시보드 카드 나열 원칙(project_notes [2026-08-06] 메뉴 재구성 핸드오버 ④)에 따라, 단순 바로가기 카드를 "대기·미처리 건수" 카드로 승격 — 프로필 승인 대기 N건, 미처리 생활지도 사안 N건, 수업교환 신청 대기 N건 등. 건수 표시가 붙기 전까지 기존 카드는 잠정 유지.
* **직권 배정 목표 지향 체인(징검다리) 교체 (2026-08-06 사용자 제안 → 같은 날 구현 착수 확정 — 관찰 대기 폐기)**: 체인을 "담기 순서로 우연히 성립"시키는 수준을 넘어, **일과계가 A 교사의 수업을 옮기고 싶은 빈칸(목적지)을 직접 클릭하면 그 수업이 그 칸에 도달할 수 있는 중간 교환 체인을 시스템이 역방향으로 탐색·구성**해 담기 목록으로 제안하는 방식. 즉 "후보 중에서 고르기"가 아니라 "목적지를 찍으면 경로를 만들어주기". ~~§14-3 승격 조건(2학기 초 빈도 관찰)~~ → **사용자 결정으로 즉시 구현 대상 승격 — 스펙 확정 ✅ 2026-08-06 [`docs/pre_opening_3features_spec.md`](./docs/pre_opening_3features_spec.md) §C (개학 후 착수)**. 기반은 이미 있음 — direct_projected 가상 합성 + direct_commit_batch 순차 승인(먼저 비운 자리를 뒤 항목이 쓰는 성질).
* **학사일정 등록 → 주차 자동 파생 (2026-08-06 사용자 결정 — 스펙 확정 ✅ [`docs/pre_opening_3features_spec.md`](./docs/pre_opening_3features_spec.md) §B, 첫 주는 수동 등록·개학 후 첫 주 내 구현)**: 학사일정(휴업일·재량휴업·단축·고사)을 플랫폼에 학기 초 1회 등록(공휴일 자동)하면 주차별 운영이 자동 파생 — 9b의 주 운영 수동 등록(컴시간 답습)을 대체. 일과계 설정은 "몇 주 앞까지 교사 공개"뿐. 향후 학교 구글 캘린더로 역배포 여지(플랫폼=학사일정 단일 원본).
* **동시수업(분반 이동수업) 교체 불가 지정 (2026-08-06 일과계 실무 요구 — 스펙 확정 ✅ [`docs/pre_opening_3features_spec.md`](./docs/pre_opening_3features_spec.md) §A, 개학 전 필수 D-4~D-1)**: 중국어·일본어 등 제2외국어와 일부 과학 수업은 **여러 반을 묶어 동시에 진행하는 분반 이동수업**이라 단일 반 교체가 불가능 — 부득이하게 옮기려면 묶인 반 전체를 통째로 옮기는 대작업이 됨. 한셀 원본 시간표에는 셀 배경색으로 구분돼 있었으나 xlsx 변환 과정에서 색 정보 유실. 요구: 이런 수업을 **교체수업 후보 산출에서 제외(맞교환·특별보강·직권·체인 전부)**하고 그리드에서 구분 표시. 데이터 확보 확정: 8/7 일과계 목록 수령 → Claude 1차 등재 + 직접 지정 등록부 UI(컴시간 "동시수업 등록부" 계승).
* **특별실(장소) 점유 제약 — 교체 후보 산출에 반영 (✅ 구현 완료 2026-08-07 — 스펙 `docs/pre_opening_3features_spec.md` §F, 엔진+18건 등재 ff72f14, 등록부 UI·배지 20e9dfe 리뷰 승인, 실기기 통합 확인 통과)**: 체육관(다윗관)·탁구장·화학실·생명과학실 등 특별실은 **한 교시에 한 수업만 쓸 수 있는 공유 자원** — 기초시간표는 이 충돌이 없게 짜여 있으나(실측: 2·3학년 체육은 전 교시 최대 1반, 1학년만 다윗관 2반 동시), 교체로 수업이 다른 교시로 이동하면 교사·학급이 비어 있어도 **장소가 이미 점유돼 물리적으로 겹칠 수 있음**. 3학년 이동수업 파일 비고 실례: 3학년 6·7반 지구과학Ⅱ는 화학실을 빌려 쓰므로 화학 수업과 같은 교시 금지. 현 엔진은 장소를 모름 → 일과계가 눈으로 걸러야 함. 최대 구멍은 체육(전 반 수행, 구장 1개). 설계 방향: 수업→특별실 배정표(레지스트리, 동시수업 등록부와 유사) + 후보 산출 시 대상 교시의 동일 특별실 점유 검사. **2026-08-07 일과계 답변으로 확정된 사항**: ① 모든 특별실 수용력 = 1반(다윗관 포함) ② 실무는 특별실 수업 교체를 아예 배제하지만, 사용자 방향 = **시스템이 장소 충돌 없는 후보만 산출하면 교체 허용** → 충돌 후보 하드 제외 방식(경고 아님)으로 설계 ③ 1학년 체ⅠA/B는 장소 분리(다윗관 1시간·탁구장 1시간)라 동시 2반이 겹침 아님 — 실측 페어링(체ⅠA↔체Ⅲ, 체ⅠB↔체Ⅱ 고정 짝)과 부합. 실측 보강: 전 학년 전 교시 체육 최대 2반(구장 2개×1반 전제 성립). ④ (2026-08-07 추가 답변) 체육 구장은 학년 전담제 — 2·3학년은 각자 한 구장 전담, 1학년만 A/B로 양쪽 사용. 실측 짝 패턴(체ⅠA↔체Ⅲ만 공존, 체ⅠB↔체Ⅱ만 공존)과 완전 부합하며, 파일 비고 "3학년=탁구장"을 앵커로 **체Ⅱ=다윗관·체Ⅲ=탁구장·체ⅠA=다윗관·체ⅠB=탁구장 확정** → 체육 장소 매핑은 과목명→구장 4줄 표로 충분(구판 비고의 "2학년 10+10 분할"은 폐기). **⑤ (2026-08-07 오후) 배정표 수령 완료 ✅ — 컴시간 특별실 시간표 스크린샷 7장(일과계 송혜원 제공, 저장소 루트 png·커밋 금지)**: 컴시간 등재 특별실은 4실뿐 — 다윗관(101) 30시수 = 체ⅠA(1학년 전 반 각1)+체Ⅱ(2학년 전 반 각2) / 탁구장(102) 30시수 = 체ⅠB(각1)+체Ⅲ(3학년 전 반 각2) / 정보실(103) 27시수 = 인공Ⅱ(2-6·8·9 각4, 최희라)+인공Ⅲ(3-6~10 각3, 이혜원) / 생명과학실(104) 16시수 = 1학년 과탐 반별 1시수(실험 시간 — **슬롯 특정**, 나머지 과탐 시수는 일반 교실)+2-9 지구 전3시수+3-6 지Ⅱ 전3시수. 체육 매핑 ③·④ 재확인 ✓. 구판 비고의 "3-6·7 지구과학Ⅱ=화학실"은 폐기(실배정 생명과학실, 화학실은 컴시간 미등재). 209지구·306지Ⅱ는 동시수업 차단에 이미 포함. 스펙 유의점: 과탐처럼 특정 시수만 특별실인 과목은 슬롯 수준 등재 필요 + 교체 시 실험실 시간이 수업을 따라가는지 실무 확인. 데이터 완비 → 사용자 지시로 당일 앞당겨 구현 완료. 잔여는 운용 감시뿐 — 기초 개정(§E)으로 과탐 실험 교시가 바뀌면 등록부 slots 갱신(감지망: venue_list의 baseConflicts·판정 0셀 경고, 스펙 §F 유의점).
* **대시보드 최소화 — 교사·학생 메인 홈은 당장 "본인 주간시간표 + 급식 메뉴"만 (2026-08-07 사용자 지시)**: 개학 시점의 교사·학생 대시보드는 복잡한 카드 나열 대신 **본인 주간시간표와 오늘/이번 주 급식 메뉴 두 가지만** 보이게 단순화. 급식은 알리미 0원 경로 항목의 나이스 급식식단정보 API 재사용(학생·교사 공용). 기존 "대시보드 할 일 건수 카드" 아이디어는 폐기 아님 — "당장은" 최소 구성으로 가고 건수 카드는 추후 재검토. 학생 포털엔 StudentTimetableCard 기마운트라 급식 카드 추가가 핵심, 교사 대시보드는 구성 교체 필요.
* **효명고 알리미 (2026-08-06 사용자 제안 → 같은 날 네이티브 앱은 접음 — 무료 원칙 위배: Apple 연 $99, Google Play도 1회 $25)**: 대신 **기존 학생 포털(설치형 웹앱)에 같은 기능을 얹는 0원 경로**로 보존 — ① 오늘의 급식 카드(나이스 교육정보 개방 포털 급식식단정보 API — 공공 무료, 인증키만 발급) ② 자기 반 시간표 변경 시 **웹 푸시 알림**(안드로이드는 완전 지원, 아이폰도 홈 화면에 설치한 웹앱이면 iOS 16.4+부터 지원 — 스토어·연회비 전혀 불요). 학년·반 설정은 학생 계정 학번에서 자동 유도 가능해 최초 설정조차 생략 여지. 착수는 개학 안정화 후.
* **기초시간표 개정 — 개학 첫 주 수정분을 다음 주부터 적용 (2026-08-06 사용자 요구, 실무 관행 반영)**: 개학 첫 주에는 학기 시간표 자체를 바꾸는 교사들이 있어, 일과계가 **기초시간표를 주간 변경과 같은 방식으로 수정**할 수 있어야 함. 단 실무 관행상 **수정분 반영은 그 다음 주부터** — 첫 주에 이미 출장 등으로 주간 교체를 한 사람들과 꼬이는 것을 방지. 설계 방향: 기초시간표 개정판(revision)에 적용 시작 주(effectiveFrom)를 달아 주간 합성이 주차별로 맞는 판을 선택, 기존 changes와 안 맞는 지점은 기구현 integrityWarnings가 안전망. 스펙·구현은 개학 첫 주 내 (§B 학사일정과 같은 사이클).
* **Firestore users 문서 생애주기 정비 — 삭제 연동 + 유령/중복 정리 (2026-08-07 사용자 지적 → 같은 날 구현 완료 ✅ 커밋 23516e0)**: ①~④ 전부 반영(삭제 관문 동반 정리·sync-user 자가 치유·유령 7건 정리 실행·429 백오프) + 잔존 졸업생 126명 삭제 재실행 완료(성공 126/실패 0, 잔존 0 재검증). 상세는 project_notes [2026-08-07] 삭제 흐름 정비 체크포인트. 원 기록: `users/{uid}` 문서는 로그인 시 생성(sync-user)만 되고 **어떤 삭제 흐름(개별·일괄·크론·전출)에서도 지워지지 않음** — 실측: 총 20문서 중 유령(GWS에 없는 계정) 7건, 같은 이메일(24343@) 5중복(재생성 때마다 새 uid 문서). 그 결과 고지 현황 미확인자 목록에 삭제된 계정·중복 행이 표출되고 집계가 부풀며, 퇴교자 PII가 무기한 잔존(최소 보유 원칙 위배 소지). 개학 후 실사용이 쌓이면 커지는 구조라 선정비 필요. 설계 방향: ① 계정 삭제 경로에 `users` 문서(이메일 매칭 전건) 동반 삭제 헬퍼 — `deleteAuthUserByEmail`과 같은 자리에서 호출 ② sync-user가 같은 이메일·다른 uid 구문서를 자가 정리(재생성 중복 자연 치유) ③ 기존 유령 7건 일회성 정리 스크립트(실측 스크립트 `scripts/inspect_stale_user_docs.ts` 기반) ④ 고지 현황 list는 이메일 dedup(최신 문서 기준). policy_acks 이력 문서는 증빙용이므로 보존. **일괄 삭제 429 재시도 수정과 같은 "삭제 흐름 정비" 묶음으로 한 세션에서 처리 권장.**
* **교사 승인 알림**: 수퍼어드민이 일반 교사를 승인(`role: "teacher"`, `isApproved: true`) 처리하면, 즉시 해당 교사에게 사용 가능 안내 메일을 발송.
* **학적 변동 타임라인**: 특정 학생 검색 시, 이 학생이 언제 전입했고, 몇 학년에 진급했으며, 언제 전출 또는 졸업했는지 전체 생애주기 이력을 타임라인 뷰로 시각화.
* **교사 전용 비밀번호 초기화**: 담임 교사가 정보부에 연락할 필요 없이 본인 반 학생의 임시 비밀번호를 웹앱에서 즉시 초기화하여 부여.
* **크론 발송 모니터링 대시보드**: 매일 밤 구동되는 크론 배치에서 메일 및 구글챗 발송에 성공/실패한 통계를 차트와 테이블로 관리자에게 제공.
* **학부모 연락처 데이터베이스**: 학부모 전화번호/메일을 별도로 관리하여 향후 카카오 알림톡 서비스 발송 대상자로 연동.
* **범용 위자드 툴**: 효명고 플랫폼 완성 후 타 학교가 최초 도입 시, 1·2·3학년 전체 OU 및 초기 학생 정보를 쉽게 세팅할 수 있는 단계별 웰컴 마법사 메뉴 제공.
* **수업교환 사전 양해 워크플로 (3단계)**: 현장 관행("먼저 물어보고 오케이 받으면 신청")을 플랫폼에 담는다 — ① 양해 요청 이미지 클립보드 복사 + 교체안 임시저장(즉시 구현) ② 본인 구글 챗 DM 발송(준비 완료 상태로 보류) ③ 플랫폼 내 동의→자동 신청(오픈 게이트 후). 상세 스펙: phase9b_spec.md §13 (2026-08-04 사용자 제안).
* **공동교사 원클릭 탈퇴 (2026-07-26 사용자 건의, 보류 → 2026-08-05 감사에서 보관소 승격)**: 학기말 정리에서 비소유 클래스룸은 알림 제외로 처리했지만(구현 완료), 공동교사 상태 자체를 클릭 하나로 탈퇴하는 기능은 비가역 작업이라 보류됨. 필요성이 다시 제기되면 이중 확인 + 대상 제한을 갖춘 별도 스펙으로 설계 (archive/project_notes_2026-07.md의 [2026-07-26] 고아 폴더 세션 "보류: 건의 2번" 항목 참조).
* **PWA 설치형 웹앱 + 컴퓨터 자동 시작 (2026-08-05 복원 — 사용자가 이전에 요청했으나 기록 유실됐던 항목)**: ① 플랫폼을 PWA(manifest + 아이콘 + 최소 service worker)로 만들어 브라우저 탭이 아닌 **독립 창의 설치형 앱처럼** 실행. ② 교무실 컴퓨터 **부팅 시 자동 실행**. ①은 구현 완료(055a441, Claude 리뷰 승인 fafe6ce). ②의 배포 방식은 **2026-08-05 최종 확정 — 강제 설치 폐기, 자발적 설치+안내문으로 일원화** (당초 하이브리드안을 같은 날 실측 2건으로 재조정. 관리 브라우저 기기 2대뿐이라 기기 정책 전면 배포가 애초 불가한 점은 동일):
  - **(1) 강제 설치 폐기 근거**: ⓐ 타 교사 Windows PC 실측 — 설치된 앱 설정에 "로그인 시 앱 실행" 토글 존재 확인(스크린샷) → 콘솔 정책 없이도 자동 시작 목표 달성 가능. ⓑ 클래스룸 전례 — 콘솔 강제 설치+고정 정책이 교사 Windows 바탕화면에 아이콘을 로그인마다 재생성해 실제 민원 발생 → 같은 리스크를 재현할 강제 설치는 채택하지 않는다.
  - **(2) 최종 흐름 (교사 셀프 3단계)**: 앱 안의 "앱으로 설치" 버튼 1클릭 → 설치된 앱 설정에서 "로그인 시 앱 실행" 켜기 → 작업표시줄 고정(선택). "로그인 시 앱 실행"은 Windows·Mac 크롬 전용(크롬북엔 없음) — 교사 PC 대상이라 무방.
  - **(3) 남은 작업 → 종결 ✅ (2026-08-07)**: 교사 안내문·스크린샷 대기를 **사용자 제작 iorad 따라하기 튜토리얼(2754580)**로 갈음 — 앱 내 "앱으로 설치하기" 페이지도 이 튜토리얼 임베드로 교체(자리 표시 3박스 소멸). 알림 켜기 단계 포함이라 웹 푸시(알리미) 권한 선확보 효과.
  - **연관 후속 (종결 ✅ 2026-08-05)**: 기존 클래스룸 아이콘 민원 — 원인은 예상대로 학생 크롬북용 고정 정책의 OU 과적용이었고, 담당자가 콘솔에서 적용 OU를 학생으로 한정해 해소 완료.
  - **(4) 후속 구멍 — "앱으로 실행"만 누른 사람의 튜토리얼 재진입 경로 (2026-08-07 사용자 지적 → 스펙 확정 ✅ 2026-08-08 [`docs/tutorial_reentry_spec.md`](./docs/tutorial_reentry_spec.md), Antigravity 구현 인계)**: 이미 앱으로 실행 중인 사람은 설치 단계는 지났지만 **작업표시줄 고정·로그인 시 자동 실행 등 후속 단계를 따라갈 진입로가 없음**. 앱 내부(설치 여부와 무관하게 항상 보이는 위치)에 "앱 사용 설정 따라하기" 같은 눈높이 문구의 튜토리얼(iorad 2754580) 진입점 필요 — 기존 "앱으로 설치하기" 페이지는 미설치자 관점 동선이라, 설치·실행 상태(display-mode: standalone)에서도 자연스럽게 보이는 링크/카드로 보완. UI 구현은 Antigravity, 배치 IA는 Claude 스펙으로.
* **모바일 사용성 — 동일 도메인 모바일 전용 경로 `/m` 확정 (2026-08-08 사용자·Claude 합의 ✅ → 같은 날 스펙 확정 [`docs/mobile_m_spec.md`](./docs/mobile_m_spec.md), Antigravity 구현 인계 중 — 중복 스펙 작성 금지)**: 전 화면 반응형 커버 폐기, 서브도메인안 폐기(웹 푸시·설치 앱·로그인 세션이 origin에 묶여 이중화됨 — Claude 반대 근거 수용). **확정안**: portal 동일 도메인에 `/m` 전용 경로 — 폰 로그인·알림 랜딩 시 자동 분기, 교사용 구성은 '오늘·내일 내 시간표(변경 강조) + 오늘 급식 + 알림 켜기 + PC 화면 링크'뿐, 학생은 기존 학생 포털이 /m 역할. 관리 업무는 PC 원칙. 사이드바 서랍화(기존 1단계)는 보조 수단으로 격하. 착수 시 Claude가 /m 스펙(화면·분기 규칙) 작성 → Antigravity 구현. 이전 진단 기록: 실기기 진단 — ① 햄버거·사이드바 토글은 기구현이나 **기본값이 열림**(useState(true))이라 폰 첫 화면부터 사이드바가 화면을 점유 ② 열린 사이드바가 오버레이가 아니라 **본문 옆에 끼어드는(in-flow) 구조**라 본문이 세로 한 글자 폭으로 뭉개짐 ③ 본문 콘텐츠의 반응형 클래스가 admin 페이지 전체에 3개뿐. 단계 제안: **1단계(즉효·소규모)** 사이드바 드로어화 — 모바일 기본 닫힘 + 열면 fixed 오버레이(배경 딤·메뉴 선택 시 자동 닫힘). 이것만으로 본문 전체 폭 확보, 교사 모바일 3대 동선(홈 카드·내 시간표·알림 랜딩)은 카드 세로 흐름이라 사실상 해결. **2단계** 교사 모바일 다듬기 — 홈 카드 여백/폰트, 주간 표 오늘 요일 강조/스와이프, 헤더 축소. **3단계(선언적 결정)** 관리 화면(전체관리·생애주기·직권 배정 등 광폭 테이블)은 모바일 최적화 **대상 제외** — "관리 업무는 PC" 원칙 명시, 모바일은 가로 스크롤로 기능만 보장. 학생 포털은 세로 카드 구조라 대체로 무난 추정(폰 확인 1회 권장).
* **반 간(같은 구장) 체육 맞바꿈 (2026-08-08 발상, ⏸ 실무 확인 대기)**: 현 교환 모델은 같은 반 안의 두 수업 자리 교환뿐이라, 물리적으로 유효한 "다른 반 체육끼리 구장을 유지한 채 교시 맞바꿈"을 표현 못 함. **사용자가 체육 결보강 실무 처리 방식을 학교에 확인 중** (체육도 교실 수업이 있어 구장 고정 가정이 과할 수 있음 — project_notes 2026-08-08 보류 질문 참조). 답에 따라 §F 체육 매핑 완화 / 모델 확장 / 현상 유지 결정.
* **징검다리 체인 탐색 실패 사유 표시 (2026-08-08 발상, 소형)**: 현재는 "N단계 안에서는 경로가 없습니다"만 출력 — 실패의 지배적 원인(예: 목적지 점유 수업이 특별실·구장 §F 하드 제외로 이동 불가 0건)을 서버가 알고 있으므로 한 줄 사유를 함께 반환·표시하면 일과계가 헛된 재시도(3단계 확장 등)를 안 하게 됨. 실증 사례: 1-9반 한국 월2→목2, 목2 체ⅠB(탁구장)가 전 후보 슬롯 구장 충돌로 이동 불가 → 어떤 깊이로도 원천 불가.
* **공동교육 학생 계정 역할 분류 점검 (2026-08-07 지나가다 발견)**: 비학번 이메일 학생 계정(`/학생/공동교육`의 s10@, `/학생/공동교육과정(26)`의 ch_01@ 등)은 sync-user의 "5자리 숫자 이메일=학생" 휴리스틱에 안 걸려 로그인 시 **teacher 역할로 분류**될 수 있음. 이 계정들이 실제로 포털에 로그인하는지, 교사 화면 접근 권한이 문제가 되는지 점검 필요 — 대응은 OU 기반 역할 판정(orgUnitPath가 /학생 계열이면 student) 추가가 자연스러움. 착수는 개학 안정화 후.
* **HWP 미리보기/편집 임베드**: 메신저·업무 기능에서 드라이브의 HWP/HWPX 파일을 다운로드 없이 브라우저에서 열람·편집. 자체 제작 아님 — 오픈소스 rhwp(MIT, Rust+WASM, `@rhwp/editor` npm 임베드) 활용. 전부 클라이언트 사이드 처리라 서버·저장소 비용 0, 문서 내용 외부 전송 없음. 순서는 뷰어 먼저 → 편집은 실제 학교 공문으로 완성도 검증 후. (2026-07-24 논의, 파일 처리 원칙과 연동 — project_notes 참조)
* **생활지도 종합 현황 — 부장 분석 뷰 (2026-08-08 사용자 요구 → 같은 날 스펙 확정 ✅ [`docs/discipline_analytics_spec.md`](./docs/discipline_analytics_spec.md), Antigravity 구현 인계)**: 지도 현황 탭이 학생별 카드 나열뿐이라 학생부장 실무(반별 건수→내용→문제 학생 드릴다운, 구 시트 운용의 강점)가 불가 → 기존 탭 상단에 종합 섹션(학년×반 히트맵·항목 분포·단계 인원·상위 학생) + 시트뷰·xlsx 내보내기. 서버 변경·추가 Firestore 읽기 0(기존 list 클라이언트 집계), 신규 권한 없음(스코프 자동 축소 재사용).

---

## 📌 3. 연동 서비스(교육과정 선택 시스템) 보안 감사 권장 사항

> **과거 세션(`b2a82b0f-a881-444c-9c03-0113f51b2067`)에서 도출된 교육과정 선택 시스템(`course-selection-app`) 연동 시의 핵심 보안 조치 권장사항입니다.**

1. **Firebase API Key 및 도메인 바인딩**:
   * Firebase API Key 자체는 노출되어도 무방하나, Firebase Console에서 **HTTP 리퍼러 제한**을 추가하여 허용된 도메인(`*.vercel.app`, `localhost:3000`) 외의 비정상적인 Firestore 접근을 반드시 원천 차단할 것.
2. **Firestore Rules 보완**:
   * 데이터 노출 방지를 위해 `submissions` 및 `users` 컬렉션의 읽기/쓰기 조건에 `request.auth.token.email.matches('.*@hmh\\.or\\.kr')` 규칙을 적용하여 도메인 외의 탈취 방어선 마련할 것.
3. **이메일 API Endpoint 무단 사용 방지**:
   * `/api/email` 엔드포인트 호출 시 Firebase ID Token 검증 및 `Authorization: Bearer <Token>` 인증 헤더를 필수로 요구하고, 수신 주소도 반드시 학교 도메인으로 제한하여 외부 스팸 발송용 프록시로 악용되지 않도록 방어할 것.
4. **XSS(크로스 사이트 스크립팅) 예방**:
   * Q&A 게시판 및 이메일 내용 템플릿에 학생이 입력한 텍스트를 그대로 삽입할 때 `escapeHtml` 헬퍼 함수를 필수 연동할 것.
