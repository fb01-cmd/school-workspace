# 🚀 효명고등학교 관리 시스템 — 복구된 개발 로드맵 및 아이디어 보관소

> ⚠️ **지금 열린 항목의 단일 원본은 [`STATUS.md`](./STATUS.md)다** (2026-08-18 신설, AGENTS.md ④-3).
> 이 파일의 "대기/잔여/미착수/남은 선행" 표기는 **작성 시점의 일지 기록**이라 낡았을 수 있다
> (2026-08-18 하루 유령 4건 전례). 열린 일을 판단할 때는 STATUS.md만 본다.

이 문서는 이전 대화 세션들(`2af17987-3499-43a7-854b-92319a40b0b2`, `f6bb0e2e-851a-4d9f-ac71-3a888b7f0015` 등)의 기록을 역추적하여 유실될 뻔한 개발 계획, 아키텍처 의사결정 맥락, 보안 지적 사항 및 브레인스토밍 아이디어들을 하나로 통합 정리한 공식 로드맵 파일입니다.

---

## 📌 1. 전체 개발 로드맵 (Phase 1 ~ Phase 8)

> 📦 **완료 Phase의 상세(설계 결정·리뷰·배포 이력)는 [`archive/roadmap_completed_phases.md`](./archive/roadmap_completed_phases.md)로 이관됨 (2026-08-05, 원문 그대로).** 아래는 요약.
> 📦 **완료 엔트리 본문 24건은 [`archive/roadmap_2026-08.md`](./archive/roadmap_2026-08.md)로 이관됨 (2026-08-18 로드맵 다이어트, 원문 그대로·블록 무손실 검증).** 상태 줄 아래 §R## 포인터 참조.

### Phase 1 ~ 5.8 (완료 ✅ — 요약)

- ✅ **Phase 1. 학적 관리 핵심 기능** — 학적 관리 핵심 API 및 UI 구현 완료 · src/app/api/workspace/users/route.ts
* **Phase 1. 학적 관리 핵심 기능** ✅
- ✅ **Phase 2. 교직원 계정 생애주기 관리** — GWS 교직원 생애주기 관리 완결 · src/lib/google/workspace.ts
* **Phase 2. 교직원 계정 생애주기 관리** ✅
- ✅ **Phase 3. 졸업 동의서 터치 서명 및 보관 강화** — 터치 서명 및 PDF 보관 처리 완결 · src/components/admin/lifecycle/GraduationConsentsTab.tsx
* **Phase 3. 졸업 동의서 터치 서명 및 보관 강화** ✅
- ✅ **Phase 3.5. UX 성능 개선 및 삭제 계정 복원** — 브라우저 인메모리 캐시 및 삭제 계정 복원 모달 배포 · src/lib/cache/clientCache.ts
* **Phase 3.5. UX 성능 개선 및 삭제 계정 복원** ✅ (2026-07-16)
- ✅ **Phase 4. 구글 클래스룸 로스터 강제 배정** — 클래스룸 학생 수동/일괄 배정 로직 구현 완결 · src/app/api/workspace/classroom/route.ts
* **Phase 4. 구글 클래스룸 로스터 강제 배정** ✅ (2026-07-16)
- ✅ **Phase 5. 크롬 관리 북마크 즉시 배정 및 실시간 히스토리 로그** — 관리 북마크 설정 및 감사 로그 기록 완결 · src/app/api/workspace/bookmarks/route.ts
* **Phase 5. 크롬 관리 북마크 즉시 배정 및 실시간 히스토리 로그** ✅ (2026-07-16)
- ✅ **Phase 5.5. 교직원 조직도 및 내 프로필 관리** — 조직도 조회 및 내 프로필 수정 기능 완결 · src/components/admin/MyProfileModal.tsx
* **Phase 5.5. 교직원 조직도 및 내 프로필 관리** ✅ (2026-07-16)
- ✅ **Phase 5.8. 학기말 클래스룸·캘린더·드라이브 일괄 정리** — 일괄 정리 기능 배포 완결(후속 4건 2027-02 전 처리) · src/app/api/workspace/classroom/cleanup/route.ts
* **Phase 5.8. 학기말 클래스룸·캘린더·드라이브 일괄 정리** — 배포 승인 ✅ (2026-07-24). **후속 과제 4건 잔존**: 이메일 에스컬레이션 크론, 캘린더 restore, 관리자 읽기전용 대시보드, restore의 super_admin 소유자 검증 우회. 실사용 시점(2027-02) 전까지 처리하면 됨.

### Phase 5.9. 조직도 고도화 — 정렬 규칙·어드민 수동 배치·트리 뷰 (완료 ✅ 2026-07-27 종결)

- ✅ **Phase 5.9. 조직도 고도화** — 정렬 규칙·수동 배치·트리 뷰 및 후속 과제 종결 · archive/org_chart_spec.md
* 전체 종결 — 정렬 규칙 기록·수동 배치(조직도 빌더 v2.1 스테이징 커밋)·모달 정렬·트리 뷰·후속 4건까지. 스펙: [`org_chart_spec.md`](./org_chart_spec.md), 종결 기록: archive/project_notes_2026-07.md [2026-07-27]. (2026-08-05 상태 정정 — "구현 📋"은 낡은 표기였음)

### Phase 6a. 명단 공급 허브 & 6b. 생활지도 기록 모듈

- ✅ **Phase 6a. 명단 공급 허브** — 읽기 전용 명단 API 및 명렬표 시트 자동 갱신 완결 · src/app/api/roster/feed/route.ts
* **Phase 6a-1. 읽기 전용 명단 API 및 키 관리 UI** ✅ (2026-07-25) · **Phase 6a-2. 명렬표 마스터 시트 자동 갱신** ✅ (2026-07-25) — 상세는 아카이브 참조.

- ✅ **Phase 6b. 생활지도 기록 모듈** — 엔드투엔드 기능 및 화면 6종 종결 · discipline_manual.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R01 (2026-08-18 이관)
- ✅ **6b 후속 — 기존 시트 실대체 전환 브리지** — 단방향 브리지 크론 통합 가동 완료 · 170cf17
* **6b 후속 — 기존 시트 실대체 전환 브리지 (2026-08-09 사용자 요청 확정, 스펙 미착수)**: 학생부장이 플랫폼 사용 공지 예정(기관 채택 모델 실사례 1호). 배경: 기존 시트 운용은 사용자가 개별 권한·범위보호(체크박스 외 셀 잠금)·참조/함수를 수작업 관리 — 인수인계 불가 수준의 노동, 대체 시 사용자가 최우선 수혜. 권한 관리는 학생부장이 플랫폼에서 직접, 담임 튜토리얼 불요(작업 단순). **전환 후 ~1개월 단방향 브리지 크론 (2026-08-09 사용자 확정 — 양방향에서 축소)**: 시트 체크박스 → 플랫폼 가져오기(**추가 전용** — 실수로 시트에 계속 적는 교사 구제)만 수행, 플랫폼→시트 역방향 거울은 **하지 않음**. 두 데이터는 차이가 나지만 원본=플랫폼이고 차이 사유는 전부 추적 가능하므로 감수(사용자 판단). 이 축소로: 시트 **읽기 권한만** 필요(보호 범위 편집자 등재 불필요), 에코 방지 불필요, 매 실행 전량 대조 후 누락분만 추가하는 무상태·멱등 설계 가능. 핵심 설계 1건 = **중복 판정 규칙**(같은 학생·항목·날짜를 시트와 플랫폼 양쪽에 기록한 경우 재수입 제외). 일몰(~1개월) + 킬 스위치 + 일몰 시 시트 처리(읽기전용/안내 배너)는 유지. 접근 경로 = 서비스 계정 DWD admin@ 사칭 — 선행: 시트를 admin@에 공유(뷰어면 충분), Sheets API 스코프·GCP enable 확인. ~~다음 단계 = Claude가 실제 시트 구조 열람 후 매핑 스펙 작성~~ → **매핑 스펙 확정 ✅ (2026-08-09, 아래)**, 구현 = Antigravity.

  > **[2026-08-13 상태 정정 — 위 제목의 "스펙 미착수"는 stale]** 같은 줄 뒤쪽에 이미 "매핑 스펙 확정 ✅ (2026-08-09)"이 붙어 있고, 바로 아래에 매핑 스펙 본문이 있다. 스펙뿐 아니라 **구현도 끝나 크론이 실전 가동 중**이다 — 8/13 실측에서 브리지가 죽어 있던 이틀간의 시트 기록 3건을 실제로 수입했다(체크 174→177, `170cf17`). 현재 스케줄은 `/api/cron/daily-sync`에 통합(Vercel Hobby 크론 2개 한도 대응). **일몰 예정일 9/10.** 제목 괄호는 2026-08-09 최초 작성 시점의 표기이며 그 뒤 갱신되지 않았다. (근거·경위: [`docs/midterm_triage.md`](./docs/midterm_triage.md) §1-0)

  **브리지 매핑 스펙 (2026-08-09 Claude — 시트 3부 실측 완료, `scripts/inspect_discipline_sheets.ts` 재실행 가능)**
  - **시트 구조 (실측)**: 학년별 스프레드시트 3부(ID는 `scripts/discipline_sheet_migration.ts`의 `SHEETS` 상수와 동일). 각 부 = `종합 현황` 탭(수식 요약, 브리지는 무시) + 학급 탭 `{학년}-{반}` (1~10반). 학급 탭 데이터 범위 `A4:J80`(실제 행 30~40이라 여유 충분): A=학번(반 내 번호), B=이름, C~I=체크박스 7열(교복1·2·3회 / 흡연1·2회 / 휴대폰1·2회), J=비고. 열↔항목 매핑은 이관 스크립트 `COLS` 상수 그대로 재사용 (`item_uniform` nth1-3 / `item_smoking` nth1-2 / `item_phone` nth1-2).
  - **과거 이관 여부 = 불필요 (종결)**: 1학기 누적분 173건은 2026-08-06 이관 완료(커밋 260e101). 2026-08-09 실측 결과 시트 총 체크 수 = **173건으로 이관 시점과 정확히 일치**(방학 중 추가 기입 0). 브리지는 과거를 다루지 않으며, 가동 시점 이후 시트에 **새로 늘어난 체크만** 대조로 자연 검출된다.
  - **중복 판정 규칙 (핵심 — 날짜 대조가 아니라 건수 대조)**: 시트 체크박스에는 날짜가 없으므로 (학생·항목·날짜) 매칭은 성립 불가. 대신 학생×항목 단위 **건수 비교**로 판정한다:
    - `시트건수(학생, 항목)` = 해당 항목의 체크된 회차 중 **최대 회차**(예: 교복 1·2회 체크 → 2). 회차가 접두열이 아니면(2회만 체크 등) 경고 로그 남기고 최대 회차 채택.
    - `플랫폼건수(학생, 항목)` = `discipline_records`에서 해당 학생·항목·**현재 학년(grade 일치)** 기록 수 — **voided 포함**. (취소는 플랫폼의 의도적 결정이므로 시트 체크로 되살리지 않는다 = 플랫폼 우선 원칙. 이관분 173건도 이 비교에 자연 포함되어 재수입이 원천 차단됨.)
    - `시트건수 > 플랫폼건수`이면 차액만큼 기록 추가, 아니면 아무것도 안 함(플랫폼이 더 많아도 삭제 없음 — 추가 전용). 무상태·멱등: 실행 후 건수가 같아지므로 재실행 시 0건.
  - **추가되는 기록의 형태**: 정규 생성 경로(POST create)와 동일 필드로 직접 기입 — `occurredAt` = ~~비고의 `M/D` 파싱 성공 시 그 날짜, 실패 시~~ **항상 크론 실행일 (2026-08-09 검수에서 정정 — 비고 날짜 파싱 금지)**: 행의 비고는 1학기 기존 기록의 날짜일 수 있어(전환기의 흔한 형태 = 1학기 비고 있는 행에 2학기 새 체크 추가), 파싱하면 새 기록이 과거 일자로 박히고 리셋 마커 이후만 집계하는 엔진(`computeStudentStatus`의 `occurredAt > markerMs` 필터)에서 단계 판정이 조용히 누락될 수 있음. 매일 실행이라 실행일 오차 ≤1일, 비고에 `[날짜 근사]` 표기(비고 원문은 note에 보존되므로 정보 손실 없음), `recordedBy` = `admin@hmh.or.kr`, note에 `[시트 자동 가져옴]` 태그. 학생 대조는 이관과 동일: Workspace Directory familyName(학번 5자리) ↔ `{학년}{반2}{번2}`, 이름까지 일치해야 수입(불일치는 제외 목록으로 보고, 실이메일은 Directory에서 — 합성 금지). **단계 자동 판정도 정규 경로와 동일하게 수행**: 기록 추가 후 `computeStudentStatus` 재계산 → 새 단계 도달 시 auto 단계 이벤트 생성(처리함 큐 공급). 정규 생성 경로에 DM 발송은 원래 없으므로 알림 문제 자체가 없음(스크립트는 관례상 `_force_notify_mock` 첫 import 유지).
  - **운영**: 매일 1회 크론(Vercel cron 또는 GitHub Actions) · 킬 스위치 = `discipline_config`의 플래그 1개(끄면 즉시 no-op) · 일몰 = 가동일 +1개월(예: 9/10)에 크론 제거 + 시트에 "플랫폼으로 이전됨" 안내 배너·읽기 전용 전환(학생부장 공지와 동시). 실행 로그는 감사 로그(`writeAuditLog`)에 요약 1줄(대조 N건·추가 M건·제외 목록).
  - **비용/권한**: Sheets API 읽기 ~33콜/일(무료 할당 내), Firestore 읽기 ~기록 전수 200~300건/일(무시 가능). DWD 스코프는 기존 승인된 `https://www.googleapis.com/auth/spreadsheets`(전체) 문자열을 그대로 사용 — `spreadsheets.readonly`는 별도 승인이 없어 **실측에서 unauthorized_client 확인됨**, 새 스코프 추가하지 말 것. 코드에서 쓰기 호출을 안 하는 것으로 읽기 전용을 보장.
  - **스펙 보강 (2026-08-09 Claude 계획 리뷰에서 확정 — 구현 필수)**: ① 단계 자동 판정은 재구현 금지 — `records/route.ts` create의 auto 이벤트 블록(리셋 마커 이후 동일 단계 기존 이벤트 중복 방지 + causeRecordIds 계산 포함)을 `server.ts` 공유 헬퍼로 추출해 정규 경로·브리지가 같은 코드 사용. ② 킬 스위치 기본값 = 꺼짐: `sheetBridgeEnabled` 필드 부재 시 no-op(전환 공지일에 플래그 켜는 순간부터 가동). ③ 크론 엔드포인트는 GET+CRON_SECRET(기존 lifecycle 크론의 fail-closed 패턴)만 — 수동 실행용 POST는 두지 않음(CLI 스크립트가 담당). ④ 폭주 가드 2종: 학급 탭 머리글(1~2행)이 기대 라벨과 불일치하면 해당 탭 스킵+경고 / 1회 실행 추가 예정 건수 상한 30건 초과 시 전체 중단+감사 로그(정상 상황에서 비정상 신호). 경미: 비접두열 체크 경고 로그, Directory suspended 학생 제외+보고, 테스트 스크립트 첫 import `_force_notify_mock`, Vercel 크론은 기존 1개+신규 1개=Hobby 한도 2개 정확히 소진(추후 크론 필요 시 통합 검토).

- ❓ **Phase 7. 학부모 소통 모듈** — 가정통신문 알림톡+웹 연동 하이브리드 소통 모듈 (착수 조건 검토 중)
### Phase 7. 학부모 소통 모듈 — 상용 알림 플랫폼 대체 (검토 단계 📋 2026-08-06 재정의)

> **재정의 배경 (2026-08-06 사용자)**: 당초 "카카오 알림톡 연동"이었으나, 실제 목적은 **학부모 알림** — 학교는 이알리미 → 슈퍼스쿨(작년) → 리로스쿨(올해)로 갈아타며 연 수백만 원대 상용 플랫폼을 쓰는 중. 이를 저비용으로 대체할 수 있다면 가치 있음.
> **비용 사실 (2026-08-06 확인)**: 알림톡(전화번호 발송, 채널 친구 불필요) 딜러사 단가 건당 7~13원 — 900가구 월 8회 발송 시 월 7만원대·연 70~80만원. 채널 채팅 메시지는 15원/건, 채널 소식·1:1 채팅은 무료. 학생 대상 푸시는 웹 푸시(0원)로 이미 대체 확정.
> **설계 방향 (하이브리드)**: 알림톡은 "가정통신문 도착 + 링크" 초인종으로만 최소 발송, 본문 열람·설문·동의서 서명(터치 서명 기구현 재사용)·열람 확인은 플랫폼 웹(0원)에서. §2 "학부모 연락처 데이터베이스"가 기반.
> **전제 (착수 조건)**: ① 학교의 리로스쿨 실지출액 확인(행정실) ② 실사용 기능 범위 확인(가정통신문+설문 위주면 현실성 높음) ③ 비용 부담 주체는 학교 예산(사용자 개인 부담 금지 — 무료 원칙) ④ 학부모 전화번호 수집·보관·파기 설계(Phase 11 인벤토리 확장, Claude) ⑤ 카카오 비즈니스 채널·발신프로필·템플릿 심사(교육기관 가능). 시점: 개학 안정화·9c 이후.

- 🔜 **Phase 8. 교사 업무 Task 관리 시스템** — 3단계(업무 지시) 스펙 확정 ✅(2026-08-19), 코어 구현은 STATUS 작업 큐 · docs/phase8_tasks_spec.md
### Phase 8. 교사 업무 Task 관리 시스템 (미구현 📅 — 2026-08-08 방향 논의, 사용자 결정 대기)

<!-- 서술 -->
* **배경·목적 (2026-08-08 사용자 구술)**: 현재 교사 업무는 쿨메신저(구식·별도 서버컴·유료) 기반 — 메시지와 업무 상태가 분리돼 있지 않아 "업무 놓친 것 없나" 메신저를 뒤적이는 구조를 근본 대체. 이 플랫폼 착수 전의 원 구상이 바로 이것(당시엔 캘린더·태스크 부가기능으로 계획).
<!-- 서술 -->
* **사용자 구상 흐름**: 조직도에서 대상 지정 → 업무명·기한·메시지·리소스 첨부(Drive 자동 업로드+링크) → 전송 → 수신자 수락 시 본인 캘린더 등록 + 플랫폼 할일 목록 편입 → 완료 체크가 발신자에게 실시간 반영(미수락·미완료자 파악).
<!-- 서술 -->
* **Claude 의견 (2026-08-08, 채택 여부는 사용자 결정)**: ① **플랫폼 네이티브 구현 권장** — 어려운 재료(조직도·알림·권한·감사·Drive·설치 앱)가 전부 기구현, 애드온(Apps Script)은 재구축+UI 제약+모듈 결합 불가. 컴시간 대체와 동일 패턴. ② **Firestore 용량 무해** — 인간 생성 데이터라 증폭 구조 없음(쓰기 수백/일, 읽기 ~5천/일 상한, capacity review Phase 8 판정 '하' 유지). 첨부는 Drive 링크만 저장. ③ **캘린더 = 단방향 출력 채널로 격하, 구글 Tasks 동기화는 제외 권고** — 체크 상태가 두 곳에 살면 실시간 반영이 깨지고 폴링(진짜 읽기 낭비)이 필요해짐. 상태 단일 원본 = 플랫폼. ④ 승부처는 보드가 아니라 **발신자 화면의 사람별 수락/완료 실시간 현황**(재촉 대상 즉시 파악 — 좁은 구독이라 용량 무해). ⑤ 스펙에서 결정할 실무 설계: 수락 전 상태(거절·대리·재촉), 지시 권한 범위(전 교사 자유 vs 부장 이상), 알림 피로 설계, 캘린더 이벤트 취소 동기화.

<!-- 서술 -->
* **단계별 구현 계획 (2026-08-09 Claude 제안 — 쪽지 아이디어(§2)와 통합, 착수 시점은 사용자 결정)**: 원칙 = "쪽지가 기반, 업무는 그 위의 구조화, 캘린더는 마지막 출력" — 각 단계가 단독 완결 가치를 내며 다음 단계의 기반이 됨 (9a→9b→9c 패턴). 순서 근거: ⓐ 업무 관리의 난부(도달·수신확인·알림) = 쪽지 인프라 그 자체 ⓑ 채택 곡선 — 쪽지는 메일 사본으로 비채택자에게도 성립하지만 업무 수락은 플랫폼 진입이 전제이므로, 쪽지로 유입을 만든 뒤 업무를 얹는 것이 전환 마찰 최소.
  - **1단계 — 쪽지 MVP (도달+수신확인)** — **스펙 확정·구현·시연 통과 ✅ (2026-08-13, [`docs/memo_spec.md`](./docs/memo_spec.md))**: 조직도/그룹 다중 선택 → 제목·내용·Drive 링크 → 발송. 원본·읽음 상태 = 플랫폼(Firestore), 발송 시 웹 푸시 (~~hmnotice@ 메일 사본~~ **2026-08-09 전면 철회** — 기관 채택 모델 확정으로 존재 이유 소멸, 아래 재검토 최종 결론 참조). 보낸 쪽지함에 수신자별 읽음 표시. 보존 기한 + 파기 크론.
  - **2단계 — 쪽지 완성**: 첨부는 **"링크 공유"가 아니라 "사본 수수" 의미론** (2026-08-09 사용자 지적 반영 — HWP 등 로컬 편집 파일은 링크로는 "실제로 가져가지"를 못함, 학교 업무 지배 패턴 = 양식 배포→로컬 한글 작성→제출): 첨부 파일은 Drive에 저장하되(파일 Firestore 저장 금지) 수신자 기본 동작 = **내려받기**(각자 사본). 플랫폼 내 답장·스레드, 대시보드 "안 읽은 쪽지 N건" 카드(기존 건수 카드 아이디어 부분 실현).
    - **[2026-08-15 사용자 발상] 첨부 Drive 폴더 체계화**: 쪽지 첨부가 본인 Drive에 쌓이므로 **전용 폴더를 자동 생성**하고 그 하위를 체계적으로 나눠 업로드하게 한다. 분류 축은 미정 — 후보: 기간별(월/주) vs 용도별 vs 파일형식별. 스펙 작성 시 결정 (사용자 원문: "별도로 폴더가 생성되고 그 안에 월단이든 주간이든 폴더를 만들고,(혹은 용도별? 혹은 파일형식별?) 체계적으로 업로드").
    - **[2026-08-18 사용자 관찰] GIF 첨부 미지원**: v1 화이트리스트(PNG·JPG·WEBP — attachment spec §0-2 실수요 기반 확장 구조)의 의도된 범위이나 사용자가 인지. 붙이려면 서버 화이트리스트+GIF 바이트 서명 추가와 **클라이언트 원본 통과 분기**(캔버스 리사이즈는 GIF 움직임을 죽이므로 3.5MB 이하 원본 통과·초과 거부)가 필요 — 소규모. 착수는 사용자 결정.
      - **[2026-08-18 종결 ✅]** 같은 날 사용자 결정으로 소품 큐에서 앞당겨 구현 완결 — 위 설계(richtext spec §9) 그대로: 서버 화이트리스트·GIF87a/89a 바이트 서명(attachment_logic)·클라 원본 통과 분기(client_attachments)·accept 확장·인벤토리 "GIF 포함". 셀프테스트 GIF 4케이스 포함 전판 통과. 한계 명시 = 목록 미리보기는 Drive 정지 썸네일이라 안 움직이고, 클릭해 열면 재생(글자 사이에서 움직이는 것은 v1.1 인라인 이미지 §13의 몫).
    - **[2026-08-18 사용자 격상] 답장 — "아주 중요한 기능이 없네"**: 이 줄에 "플랫폼 내 답장·스레드"로 나열만 돼 있고 일정이 없던 것을, 이미지 첨부 완성 직후 사용자가 **핵심 누락 기능으로 격상**. 쪽지 2단계의 다음 착수 후보 = 답장(스펙은 Claude 몫 — 스레드 데이터 모델·수신자 규칙·알림 연동이 갈림길). 메신저 대체라는 정체성상 왕복이 안 되면 반쪽이라는 취지.
      - **[2026-08-18 스펙 작성 완료 ✅]** [`docs/memo_reply_spec.md`](./docs/memo_reply_spec.md) — 핵심 결정: 답장 = 발신자 1인 서버 강제(전체 답장 제외 — "쪽지는 업무용" 원칙)·memos 문서 재사용(threadId/replyTo 필드 2개, 규칙·색인 무변경)·스레드 뷰는 받은/보낸 목록 로컬 그룹핑(규칙상 스레드 쿼리 불가가 제약)·알림·첨부·회수 전부 자동 승계. 다음 = §8 순서 1 서버부(Claude).
      - **[2026-08-18 종결 ✅]** 서버부(777eb80, selftest 9케이스)·UI(ac20204, 검수 통과)·**실기기 확인 통과(사용자)** — 스펙 §8 순서 1~3 전부 완료. 답장 기능 완결.
  - **3단계 — 업무 지시 (Phase 8 본체)**: 쪽지에 "업무" 타입 — 기한·수락/거절·완료 상태 머신, 발신자 현황판(사람별 수락/완료 실시간+재촉), 기한 임박 크론. **+ 제출형 업무 (2026-08-09 추가)**: 완료 = 작성본 파일 업로드 → 업무별 Drive 제출함 폴더 자동 생성(담당자만 접근) + **제출 파일명 자동 정규화("2학년3반_홍길동_양식명.hwp")** — 취합(파일명 난장판·미제출 파악)이 담당자 최대 고통이므로 이것이 쿨메신저 대비 킬러. 전송·알림은 1단계 재사용이라 신규는 상태 관리·현황판·제출함뿐. 실무 설계 결정(의견 ⑤)은 이 단계 스펙에서.
  - **(후속 연계) HWP 뷰어**: §2 "HWP 미리보기/편집 임베드"(rhwp)의 1차 자리 = 제출함 검토 — 담당자가 HWP 제출본을 내려받지 않고 브라우저에서 훑기. 편집은 성숙도 검증 후 별건. 당장의 원칙은 "로컬 한글 왕복을 잘 받쳐주기"(교사 도구를 바꾸지 않고 수수·취합 고통 제거).
  - **파일 흐름·저장 구조 (2026-08-09 문답으로 확정)**: 수신자는 Drive를 경유하지 않는다 — 플랫폼에서 로컬 내려받기 → 한글 작성 → 플랫폼 업로드 제출 (쿨메신저 첨부와 동일 감각, Drive는 불가시). 저장소는 **교사 개인 Drive가 아니라 플랫폼 전용 공유 드라이브(조직 소유)** — 개인 Drive는 전출·퇴직 계정 삭제 시 첨부 소멸+소유권 꼬임(생애주기 모듈과 충돌). 폴더는 사람이 관리하지 않는 **플랫폼 자동 생성 창고**: `쪽지첨부/YYYY/MM/`, `업무/YYYY/{업무ID}/양식|제출함/`, 접근 권한 = 서비스 계정만("창고는 잠그고 수수는 플랫폼 창구로만" — 폴더 배치 실수·권한 꼬임 구조적 차단). 예외 1: 담당자에게 해당 업무 제출함 폴더 열람 자동 부여(통째 내려받기 취합). 보존 기한 경과 시 연도 폴더 단위 파기 크론 + 개인정보 인벤토리 등재. **(2026-08-09 추가 문답)** ① 드라이브는 기존 명단 시트용 공유드라이브 재사용이 아니라 **업무 전용 공유드라이브 신설** 권고 — 명단 드라이브는 외부 열람용으로 권한이 열려 있어 "서비스 계정만" 잠금 원칙과 충돌(권한 규칙 혼합 = 제출물 개인정보 유출 구멍). ② 파일 수 개념 확정: 양식 원본 1개 + 제출 시마다 1개(다운로드는 로컬로만, Drive 사본 없음) → 업무당 = 양식 수 + 제출 인원수. ③ 재제출은 최신본 교체 권고 — 이전 본은 Drive 파일 버전 이력이 무료 보관, 제출함은 사람당 1개 유지.
  - **4단계 — 캘린더 출력+전환 마감**: 수락 시 수신자 캘린더 이벤트 등록·취소/변경 동기(단방향, DWD 스코프 1 추가), /m "내 할일" 노출, 쿨메신저 공식 퇴역.
  - 용량: 전 단계 인간 생성 데이터 — capacity review 판정 '하' 유지, 단계별 스펙에 읽기 계수 명기. 분업: 스펙·모델·상태 머신·권한 = Claude / 화면 = Antigravity.
<!-- 서술 -->
* **식은 눈 재검토 (2026-08-09 Claude, 사용자 요청)**: **뼈대 유지 판정** — 단계 순서·상태 단일 원본·사본 수수·창고 잠금 원칙 모두 존치. 보강·정정 6건: ① **[보안, 1단계 스펙 필수]** 현행 전역 규칙(`request.auth != null`)로는 학생 포함 전 로그인 사용자가 쪽지 컬렉션을 클라이언트에서 직독 가능 — 수신자·발신자 검사 규칙(또는 API 경유 강제) 필수. ② **[2단계 기술 리스크]** Vercel 함수 요청 본문 4.5MB 한도 — 첨부 업로드는 API 경유 불가 크기 존재, Drive 업로드 세션 URL 발급→브라우저 직접 업로드 등 우회 경로를 2단계 스펙에서 결정(무료 범위 내 해결 가능). ③ **[사실 정정 → 사용자 확정 2026-08-09]** Drive 리비전은 기본 30일/100개 초과 시 자동 삭제 — **(a) 최신본만 남음 채택**, 재제출 UI에 "이전 제출본은 교체되며 30일 후 복구 불가" 경고 표시. ④ **[최종 2026-08-09: 메일 사본 층 전면 철회 (사용자 확정)]** 당일 두 차례 중간 결론(필터·라벨 자동 설치 → 미채택자 전용 얇은 층)을 모두 폐기. 근거 = **기관 채택 모델**: 플랫폼 전환은 개인별 채택 곡선이 아니라 실무협의회 결정 → 교직원 회의 공지 → 연수 → 일괄 전환이므로, 메일 사본의 두 목적(미사용자 도달 보장, "확인은 플랫폼에서" 유입 동선)이 모두 존재 이유를 잃음. 쪽지는 **순수 플랫폼 네이티브**(Firestore 원본 + 웹 푸시 + 읽음 확인)로 확정 — 메일 발송·필터·식별 표식·DWD 추가 스코프 전부 불요, 설계 단순화. 유일한 잔존 후보: 미열람 N시간 후 지연 메일 알림(선택적 후행, 필요성 입증 시에만). ⑤ **[문구 조정 → 사용자 확인 2026-08-09]** 플랫폼 인지 그룹은 점진 개발 중임을 이미 앎 — 포지셔닝은 실사용 홍보 시점에만 챙기면 되는 저우선.
<!-- 서술 -->
* **개발 가치 자체 재검토 (2026-08-09 사용자 발제, 진행 중 — 결론 미확정)**: "어떻게"가 아니라 "만들 가치가 있나"를 재검토. **Claude 의견 = 보류 권고**: ① 쪽지는 플랫폼 최초의 네트워크 상품(송·수신자 모두 필요) — 메일 사본은 수신측 채택 문제만 풀고 송신자 부재(사용자 휴직·채택 ~0)는 못 품 ② 비용(필터 설치 민원 리스크·파기 크론·유지보수)은 즉시, 가치는 빨라야 복직 후 — 재고는 낡음 ③ 제출형 업무의 무료 대체재로 구글 Forms 파일 업로드 존재(제출자명 자동 부착·시트 집계) — 플랫폼 delta는 명단 대조 미제출·자동 재촉·조직도 연동으로 축소 ④ 기회비용: 진짜 데드라인은 9c(11월 말). 반론: 도달·수신확인 인프라는 업무 기능의 전제라 언젠가 필요 — 단 스펙·구현은 복직 무렵이 신선. **사용자 측 변수 대기**: 복직 예정 시점 / 휴직 중 업무 연락 유무 / 현장 협력자 유무 / 쿨메신저 계약 시한. 답에 따라 보류의 형태(무기 연기 vs 복직 역산 일정) 결정.
<!-- 서술 -->
* **재검토 결론 (2026-08-09, 전제 정정 후)**: 사용자 정정 — **플랫폼 개시·확산은 복직과 무관하게 진행 중**(현재도 학교 컨택 유지), 생활지도 메뉴가 기존 시트를 곧 실대체, 일과계 교사 시연 후 상당히 긍정적(교무부장·교감·교장 라인 미접촉), 쿨메신저·컴시간 모두 연말까지 계약(우위 입증 시 전환 의향 환경). → **Claude 판정 수정: 보류 철회, 개발 가치 있음.** 근거: ① 채택 기반은 쪽지 자신이 아니라 도구형 모듈(생활지도·시간표)이 만들고 쪽지는 그 로그인 인구 위에 얹힘 — 원래 "쪽지로 유입" 논리의 역전이자 더 견고한 구조(쪽지가 채택을 견인할 부담 소멸) ② **연말 계약 갱신 = 자연 전환 창** — 갱신 판단 전 "시연 가능한 쪽지"가 목표 상태 ③ Forms 대체재 할인(제출형 delta는 명단 대조·재촉·조직도 연동)과 9c 우선순위는 유지. **권고 대기열**: 당장은 9 계열·생활지도 시트 대체 실전이 우선 / **쪽지 1단계 MVP = 구현 완료 및 실기기 시연 통과 ✅ (2026-08-13)** / 스펙은 Claude·구현은 Antigravity라 9c(11월 말 시한)와 자원 충돌 작음. 미확정 잔여: 쿨메신저 갱신 의사결정 라인(교감·교장 등)에 닿을 경로.
<!-- 서술 -->
* **[2026-08-19 변수 4개 최종 확정 (사용자 — 유령 11건째 정리와 함께)]**: ① 복직 = **2027년 3월** ② 휴직 중에도 계속 활동(테스트 지속 — 본인이 첫 사용자) ③ 현장 협력자 **있음** ④ 쿨메신저 계약 = **2026년 12월 말**. ②·④는 8/9 재검토 결론에 이미 있던 답의 재확인(변수 대기 문구가 그 뒤 갱신되지 않아 유령화 — 11건째), ①·③이 신규 확정. 함의: **12월 말 갱신 전 "시연 가능한 업무 지시(3단계)"가 진짜 데드라인**, 복직 3월 = 2027 새 학년도와 전환 시점 자연 정렬. 3단계 스펙 착수 관문 해제.
<!-- 서술 -->
* **[2026-08-19 3단계 스펙 확정 ✅]** [`docs/phase8_tasks_spec.md`](./docs/phase8_tasks_spec.md) — 미뤄둔 실무 결정 4가지를 닫음: 상태 머신(수락/거절 사유 필수/대리 없음)·발신 자격(전 교직원 — 쪽지와 동일)·알림 피로(발신자향 푸시 제거, 현황판+원장 집약)·재촉(수동·업무당 24시간 1회). 모델 = tasks 신설 컬렉션(수신자별 상태 한 문서 — 좁은 구독), 제출함 = 업무 전용 공유드라이브 + 파일명 정규화(소속_이름_업무명), 대용량은 Drive 세션 업로드 2경로제(10MB), 알림 센터 [수락] 창구는 설계 예약의 이행. 일정 = 코어 8월(Claude)→화면 9~10월(Antigravity)→11월 실기기→12월 초 시연(쿨메신저 갱신 판단 재료).
<!-- 서술 -->
* **[2026-08-19 코어 구현 완결 ✅ (스펙 §11 순서 1 — 당일)]**: 순수 로직(src/lib/tasks/logic.ts — 상태 머신·파일명 정규화·기한 KST 판정·재촉 24h, 셀프테스트 31케이스 전판) + 전용 공유드라이브 계층(drive.ts — 프로비저닝·업로드/교체·권한 부여/회수·재개 업로드 세션) + API(/api/tasks 9액션 2상 발송, /api/tasks/file 프록시) + firestore.rules tasks 블록 + 알림(notifyTask·원장 4종) + daily-sync 6번째 작업(D-1 리마인드+파기) + 인벤토리 2행. 배선·화면층 실측(규칙 실배포·공유드라이브 첫 생성·세션 업로드 CORS)은 화면 완성 후 배포 시점에 — STATUS 작업 대기 2번.
<!-- 서술 -->
* **[2026-08-19 실기기 피드백 수집 중 — 사용자 지시 "고치지 말고 적어둬, 한번에 다 가자"]** (수정은 수집 종료 후 Antigravity 일괄):
  1. **수신자 선택 조직도 순서 엉망** — 부서가 가나다순으로 나열됨. 조직도 고도화(Phase 5.9)의 정렬 규칙이 단일 원본인데 이 모달이 안 씀 → 기존 정렬 재사용으로 고칠 것.
  2. **무관한 부서(1학년)가 기본 펼침** — 쪽지 v1.1 확정 동선은 "내 소속 부서만 펼친 트리". 같은 규칙 적용.
  3. **검색 placeholder "이름 또는 내선번호로 검색"** — 내선으로 사람을 찾는 실무는 없음(내선은 공용이라 식별자 불신이 기존 결정). 이름 검색만으로 문구·로직 정리.
  4. **보내다 만 초안이 "보낸 업무"에 유령으로 표시** — 2상 발송의 1상(prepare)만 하고 중단한 문서(수신 0명)가 발송 일시까지 달고 목록에 나옴(실측: 01:07 초안). 처방 = ⓐ 목록에서 수신 0 문서 제외 또는 "보내다 만 업무" 구분 표시(Antigravity) ⓑ 고아 초안 24시간 후 자동 정리를 taskSweep에 추가(Claude — 코어 설계 누락분).
  5. **내 할 일 목록 불안정 (치명 — 실측 확정)** — PC·모바일 공통으로 쿼리(recipientEmails 배열 포함 + dueAt 정렬)가 복합 색인을 요구하는데 색인이 없어 서버 조회가 실패한다(admin SDK 동일 쿼리로 FAILED_PRECONDITION 재현, 색인 API 생성은 서비스 계정 권한 밖 403). 구독 오류가 콘솔에만 찍히고 화면은 빈 목록(조용한 실패). 사용자 실관찰(모바일에서 새로고침 후 표시·수락/완료 됨)은 SDK 캐시 경유로 추정 — 수정 후 재확인으로 갈음. 처방 = 색인을 만들지 말고 **배열 포함 단독 쿼리 + 클라이언트 정렬**(색인 관리 대상 자체를 없앰) + 구독 실패를 화면에 눈높이 표기(조용한 실패 금지). PC·모바일 두 곳.
  6. **본문 URL 자동 링크 — "이건 꼭 필요해" (사용자 확정, 쪽지·업무 공용)** — 붙여넣은 https 주소가 클릭되는 링크로 자동 표시돼야 함(구글 문서도구 링크로 업무하는 실무 多). richtext spec §10에서 v1.1 후보로 미뤄뒀던 bare URL 자동 링크를 실수요로 승격 — **코어(파서 autolink, https만·기존 안전 규칙 동일) = Claude**, md1·평문 두 렌더 경로 모두 적용(업무 본문·쪽지 본문·업무 지시 내용). 렌더는 기존 MemoRichBody 링크 노드 재사용.
  7. **수락·완료·완료취소 반영 지연 (PC·모바일 공통 — 증상 상세 확보)** — 완료 체크를 누르는 순간 잠깐 바뀌었다가 원래 아이콘으로 되돌아가고, 새로고침해야 반영(완료취소도 동일). 5번과 한 뿌리로 추정: 전이 후 화면이 죽은 구독의 스냅샷을 기다리다 이전 상태로 롤백되는 모양새 — 처방 = 전이 API 성공 응답(next 상태)을 즉시 로컬 반영(낙관 갱신) + 5번 구독 복구. 두 건을 같이 고치고 같이 재확인. **증상 추가(실측)**: 이미 수락한 업무의 카드가 "도달(확인 필요)"+수락 버튼 활성으로 남아, 누르면 "이미 수락한 업무입니다" 오류 — 낡은 화면 상태가 만드는 잘못된 행동 유도(같은 수정으로 해소 확인할 것). 상태 칩·버튼 활성이 statuses 실시간 값을 따르는지 일괄 점검.
  8. **"지시"라는 말 전면 금지 (사용자 — "어감이 매우 안 좋아")** — 업무 화면 전체에서 "지시" 단어 소탕(전수 검색), 라벨 "지시 및 안내 내용" → **"내용"**. 사용자 대면 언어는 "업무 보내기/전달/내용"으로 통일. 문서·코드 내부 명칭은 유지하되 화면 노출 0. (알림·푸시 문구도 점검 대상)
  9. **업무 내용에도 서식 편집기 (쪽지의 즉시 반영 편집기 재사용)** — 코어는 이미 준비됨(tasks.contentFormat md1 지원) — 작성 화면에 MemoEditorToolbar+직렬화기 배선, 상세 표시(PC·모바일·현황판)는 contentFormat 분기로 MemoRichBody 재사용. **단 GIF는 업무엔 불요(사용자)** — 업무 파일 화이트리스트에서 gif 제거(코어, Claude), 서식·이모지는 유지.
  11. **"미완료자 재촉" → "리마인드 알림" (사용자 — "매우 공격적이야")** — 명칭·화면 언어 전면 교체(버튼·안내·429 문구, Antigravity). 동작도 정비(코어, Claude): 현재는 푸시만 가고 종모양 알림 원장에 기록이 없음(설계 빈틈 — 푸시를 놓치면 흔적 0) → **원장(task-due 계열) 기록 추가 + 푸시는 부드러운 문구("기한이 다가오는 업무가 있어요" 톤) 1회 + 24시간 1회 제한 유지**. 감사 로그 액션명도 "업무 리마인드"로.
  12. **철회 확인창에 진행 상황 경고** — 철회는 수락·완료자 포함 전원 회수(제출물은 보존)가 맞는 동작이나, 확인창이 이를 안 알려줌. "이미 수락 N명·완료 N명이 있습니다. 철회하면 전원의 할 일에서 사라지며, 제출물은 보존 기간까지 남습니다"를 표시(Antigravity — 현황판 집계 재사용, 서버 무변경).
  13. **어드민 홈에 쪽지·할 일 부재 (실측 — 역할별 하드코딩 두 벌)** — 교사 홈엔 내 할 일·받은 쪽지가 상단인데, 수퍼어드민 홈은 관리 카드 위주로 업무 카드가 8번째(맨 아래·스크롤 밖)이고 쪽지 패널은 아예 없음. 어드민도 수신자다 — **내 할 일·받은 쪽지를 어드민 홈 상단(관리 카드 위)에 배치**(Antigravity, 기존 두 컴포넌트 재사용).
  14. **상태 칩 "도달 (확인 필요)" → "수락 전"** (사용자 — 간결하게). 같은 칩이 쓰이는 곳 전부(내 할 일 카드·현황판 표·모바일) 일괄 교체(Antigravity).
  15. **작성 모달 명칭 "업무 등록"으로 확정 (사용자)** — "새 업무 지시 보내기" 등 발신 중심 명칭을 전부 "업무 등록"으로(셀프 용도까지 품는 말·"지시" 소탕과 정합). **+ 셀프 등록 빠른 진입 — 채택 확정 (2026-08-19 사용자, 보정 1건: 셀프는 확인형 전용)**: [내 할 일 추가] 미니 입력(업무명+기한, 내용 선택) — 수신자 자동 본인·생성 즉시 수락 상태·**유형 선택 없음(확인형 고정 — 자기에게 제출하는 제출형은 개념 불일치, 사용자 판단. 서버도 셀프 경로에서 확인형 강제)**. D-1 리마인드·향후 캘린더 연동 자동 수혜, 셀프 업무는 현황판에서 접고 내 할 일에 "내가 등록" 표시. 코어(셀프 등록 경로) = Claude, 화면 = Antigravity.
  16. ~~"교직원 조직도" 메뉴를 관리자 전용으로~~ → **같은 날 사용자 발안으로 철회·대체**: 내선 등 정보 열람 수요를 짚은 뒤, 쿨메신저·경기도교육청 메신저의 실동선("켜면 조직도가 떠 있다")을 재현하는 **통합 허브**로 방향 전환 — 아래 별도 항목. 배치에서는 조직도 메뉴 현상 유지(이동·숨김 안 함).
  10. **쪽지 일반 파일 첨부 (사용자 요청으로 소품 큐에서 배치 편입)** — 업무 코어가 만든 재료 재사용으로 소형화: 쪽지 첨부 화이트리스트를 이미지 전용에서 업무 파일 목록(한글·오피스·PDF·압축 등, GIF 포함 유지)으로 확장 + 비이미지는 리사이즈 우회 + 4MB 초과는 업무의 세션 업로드 경로 이식(대용량). 저장·권한·파기는 기존 쪽지 첨부 모델(hmnotice Drive·수신자 reader·동반 파기) 그대로. **서버부 = Claude, 첨부 UI 확장 = Antigravity.** 인벤토리 쪽지 첨부 행 문구 갱신 동반(이미지→파일).
  17. **알림 패널의 [업무 수락하기] 버튼이 낡은 상태로 잔존 (2026-08-19 실기기 재확인 중 신규 — 모바일 실측, PC 동일 구조)** — 이미 수락한 업무의 task-assigned 알림에 수락 버튼이 그대로 남고, 누르면 "이미 수락한 업무입니다" 오류. 원인 실증: NotificationCenter의 버튼 숨김이 `acceptedTaskIds`(이 패널에서 이 세션에 수락한 것만 기억하는 로컬 Set)뿐 — 업무 화면에서 수락했거나 재접속하면 버튼 영구 잔존. 알림 원장은 불변 기록이라 버튼 상태를 원장에 쓸 수 없고 실측이 필요. 처방(Claude): ⓐ 패널에서 task-assigned 알림이 보일 때 해당 업무 문서를 1회씩 읽어(표시분만, 보통 수 건 — 읽기 예산 무해) 내 상태가 PENDING 아니면 버튼 대신 "수락함 ✓" 표기, 철회·비대상이면 버튼 숨김 ⓑ 클릭 시 "이미 수락" 400은 alert 오류가 아니라 버튼을 완료 표기로 바꾸는 신호로 처리(7번 낙관 갱신의 알림판 버전). 공용 컴포넌트라 PC·모바일 한 번에 해소. 담당 = Antigravity(배선), 수정은 일괄 원칙대로 다음 배치.
  18. **주소 입력 시점 링크 변환 — 쿨메신저 동선 (2026-08-19 실기기 재확인 중 사용자 발견·제안)** — 사용자 실측: 렌더 자동 링크(6번)는 https:// 로 시작해야만 먹고 `hmh.or.kr` 같은 스킴 없는 주소는 안 먹음(의도된 안전선 — 렌더에서 느슨하게 풀면 "3.4버전" 류 오탐). 쿨메신저 관찰: *"URL을 그냥 붙이면 반응 없고, 붙인 후 엔터를 치면 하이퍼링크를 받더라"* → **처방 = 렌더가 아니라 편집기가 입력 시점에 변환**: 엔터/공백 직후 직전 토큰이 주소꼴(`https://…` 또는 `www.…`/`도메인.tld[/경로]` — 점 포함·공백 없음·TLD 영문 2자+, 숫자 TLD 제외로 버전 문자열 오탐 차단)이면 md1 정식 링크 `[원문](https://…)`로 치환(스킴 없으면 https:// 부여). 파서 안전선(https만) 무변경 — 명시 링크 문법을 편집기가 대신 써주는 것. 적용 = 쪽지·업무 공용 편집기(MemoEditorToolbar 직렬화기). 담당 = Antigravity(편집기 배선, 치환 규칙은 이 항목이 스펙), 수정은 일괄 원칙대로 17번과 같은 배치.
  19. **어드민 홈 급식 카드가 최상단 잔존 (2026-08-19 실기기 재확인 중 — 13번 마무리 부족분)** — 13번으로 내 할 일·받은 쪽지 카드는 들어왔으나 급식 카드 아래에 있음. 교사 홈과 달리 어드민 홈만 급식이 최상단(스크린샷 실측). 처방 = 어드민 홈 카드 순서를 내 할 일·받은 쪽지 → 급식 순으로(사용자: "사소한건데 좀 내려줘"). admin/page.tsx super_admin 분기 순서 조정만, 담당 = Antigravity, 17·18번과 같은 배치.
  20. **~~보낸 업무 현황판 진입 시 페이지 크래시~~ → 즉시 수정 완료 ✅ (2026-08-19 실기기 재확인 중 발견, 당일 핫픽스 `0044163`)** — React #310(렌더 간 훅 개수 불일치): 92fadca가 추가한 셀프 접기 `useState`(15번)가 로딩·빈 목록 조기 return **뒤**에 선언돼, 로딩이 끝나는 렌더에서 훅 순서 위반 → 전면 사망. 훅을 컴포넌트 최상단으로 이동, 배치 파일 8종 전수 스캔 결과 동일 유형 추가 0건(걸린 것은 전부 일반 헬퍼 함수). 크래시는 일괄 원칙 예외로 즉시 배포. **Antigravity 교훈: 훅은 어떤 조기 return보다도 위에 — 조건부 렌더 아래에 useState를 추가하면 조건이 바뀌는 순간 화면 전체가 죽는다.**
  21. **사이드바 "효명" 텍스트 배지를 교표로 (2026-08-19 사용자 — "아주 사소한거")** — 어드민 사이드바 상단의 보라 배경 "효명" 글자 배지를 효명고 교표 이미지로 교체. **자산 실재 확인: public/icon-192.png가 교표**(바르게·굳세게·참되게 1953 문양, PWA 아이콘과 동일)라 신규 파일 불요 — 흰 원형/라운드 칩 배경 위에 얹으면 보라 사이드바에서 시인성 확보(현 배지 형태 유지). 담당 = Antigravity, 17·18·19번과 같은 배치.
  22. **GWS 이름 변경이 플랫폼 사본에 전파 안 됨 (2026-08-19 실기기 재확인 중 사용자 신고 — 데이터는 당일 정정 완료, 구조 수정 대기)** — 사용자가 며칠 전 GWS에서 이름 뒤 영문 삭제("이현준(Hyun-Joon Lee)"→"이현준")했으나 알림·업무에 옛 이름 계속 표기. 실측: 코드 하드코딩 아님 — **teacher_profiles.name·users.name 사본 2곳이 낡아 있었고**(check_name_diff 전수 대조: 불일치 전 교직원 중 이 1건뿐) 전이 알림이 live 프로필을 읽으므로 낡은 사본이 그대로 찍힘. 당일 조치: 두 문서를 GWS 실물로 갱신 완료. **주의: 기존 업무·알림·쪽지에 이미 찍힌 이름은 발송 당시 스탬프라 그대로 남는 게 정상**(원장 불변 원칙 — 새로 생기는 것부터 새 이름). 구조 처방(다음 배치, 코어=Claude): 이름 동기화를 자동화 — 후보 ⓐ daily-sync에 check_name_diff 로직 통합(불일치 시 사본 갱신, 도구 기존재) ⓑ 로그인 시 대조. ⓐ가 자연스러움(전 교직원 커버·로그인 무관).
  23. **조직도·수신자 픽커에 이름/아이디 혼재 (2026-08-19 실기기 재확인 중 사용자: "지맘대로" — 데이터는 당일 정정 완료)** — 원인 실측: teacher_profiles.name이 **빈 문서 18건**이 아이디(이메일 앞부분)로 폴백 표시. GWS에는 전원 실명 존재 → 당일 GWS 실명으로 전량 채움(신석우·박영림·박상은·김은호·문치성 등 18건, GWS 이름 없으면 건드리지 않는 원칙·건너뜀 0). 화면은 프로필 캐시(5분) 만료 후 자동 반영. **구조 처방은 22번과 한 묶음**: daily-sync 이름 동기화가 "낡은 이름"(22번)과 "빈 이름"(23번)을 모두 대조·갱신하도록 명세 — 코어(Claude), 다음 배치.
  24. **업무 등록 모달 상태가 발송 후에도 잔존 (2026-08-19 실기기 재확인 중)** — 발송 완료 후 [업무 등록]을 다시 누르면 1단계(내용·기한)가 아니라 **2단계(양식·수신자)가 직전 발송 정보 그대로** 열리고, 스크린샷상 [발송 중…] 버튼 상태까지 남아 있음. 원인 추정: TaskComposerModal이 닫혀도 언마운트되지 않고 내부 상태(step·필드·수신자·taskId·submitting)가 유지. 처방(Antigravity): **발송 성공 시와 모달 닫기 시 모두 전체 상태 초기화**(1단계 복귀·필드 비움·submitting 해제) — 작성 중 임시보관은 지원하지 않는 게 4-a(고아 초안 24h 스윕) 설계와 정합, 재개 UI가 없는 반쯤 초안을 UI가 기억하면 이중 발송·혼선만 남는다. 17·18·19·21·22+23과 같은 배치.
  25. **제출함을 데스크톱용 구글 드라이브에서도 열기 (2026-08-19 사용자 발상 — "고민 좀")** — 담당자의 제출함 접근은 폴더 단위 reader라 웹에선 "공유 문서함"으로 열리지만, **데스크톱용 드라이브는 내 드라이브·공유 드라이브만 표시**해서 공유 문서함 항목이 안 보임. 해법 판정(Claude): ⓐ **바로가기(shortcut)가 정답** — 웹 드라이브에서 제출함 폴더에 [정리 → 바로가기 추가 → 내 드라이브] 한 번이면 데스크톱 드라이브 탐색기에도 그 경로로 보임(개발 0, 지금도 가능). 유의: 철회·파기로 권한이 회수되면 바로가기는 깨진 채 남음(사용자가 지우면 됨). ⓑ 플랫폼 지원 = **현황판 [제출함 폴더 열기] 옆에 눈높이 안내 한 줄**("컴퓨터의 드라이브 폴더에서도 보려면: 바로가기 추가" 툴팁/도움말 — Antigravity 소형, 다음 배치). ⓒ **자동 바로가기 생성은 비채택** — DWD로 교사 개인을 사칭해 내 드라이브에 쓰는 구조라 권한 표면 확대(쪽지 개인 Drive 미러 기각과 같은 계열 ⓑ 사유), 클릭 한 번을 아끼자고 넘을 선이 아님. ⓓ 공유드라이브 멤버십 부여도 비채택 — 남의 업무 제출물까지 보임(§0-4 최소 권한 위배).
  26. **제출형: 파일 선택 즉시 제출 → [첨부 확인 후 제출 버튼] 2단계로 (2026-08-19 사용자)** — 현재는 파일을 고르는 순간 업로드+제출 완료. 사용자: *"더 좋은 프로세스인 건 맞지만, 사람들은 심리적으로 첨부 상태를 보고 전송 버튼을 눌러 최종 제출하고 싶어 해."* 처방 = **화면만 변경**(서버 무변경): 파일 선택 → 로컬 대기 표시(파일명·크기·바꾸기/빼기) → [제출] 버튼 클릭 시 그때 업로드+제출(4MB 초과 세션 경로도 동일 흐름). 재제출도 같은 2단계. 담당 = Antigravity, 다음 배치.
  27. **업무 완료 시 간단 코멘트 (2026-08-19 사용자)** — 거절 사유처럼, 완료(확인형 done)·제출(제출형 submit) 때 선택적 한 줄 코멘트를 남겨 발신자가 "제출 특이사항"을 현황판에서 바로 보게. 처방: statuses[email].note 자리를 거절 사유와 공유(대칭 설계 — 상태머신 done/submit에 선택 note 수용·500자 제한, 제출은 FormData·세션 finish에 note 동반), **발신자향 task-status 원장에는 기존 note 동반 로직이 이미 있어 코어가 note만 허용하면 알림에 자동으로 실림**. 현황판 표에 코멘트 열 표시 + 완료/제출 UI에 선택 입력. **코어(상태머신·API) = Claude, 화면 = Antigravity**, 다음 배치.
  28. **쪽지 "링크 첨부" 섹션 제거 (2026-08-19 사용자 — "본문에 링크가 먹으니 없어도 될 것 같아")** — 6번(본문 URL 자동 링크) 배선으로 별도 링크 첨부(최대 5개·이름 선택)가 중복이 됨. 이름 붙인 링크도 편집기 🔗 버튼(md1 명시 링크)이 대체. 처방: **작성 UI에서 링크 첨부 입력만 제거** — 기존 쪽지의 links[] 표시("첨부 링크" 박스)는 하위호환으로 유지(과거 데이터), 서버 모델·검증도 존치(제거는 UI 입력뿐이라 서버 무변경). 담당 = Antigravity, 다음 배치.
  29. **보내기 비활성 사유 미표시 (2026-08-19 실기기 — "왜 안 눌러지는지 알려줘야 오해를 안 하지")** — 한도 초과 첨부(12MB)가 카드에 ⚠️ 사유를 달고 남아 있으면 [보내기]가 비활성되는데, 버튼 쪽엔 아무 설명이 없어 "왜 안 되지"가 됨. 처방(Antigravity): 비활성 사유를 버튼 바로 위에 눈높이 한 줄로 — "올릴 수 없는 첨부 1개가 있습니다. 빼면 보낼 수 있어요" (실패 카드 X 제거 시 즉시 활성화). 실패 카드를 자동으로 지우지 않는 현행은 유지(무엇이 왜 안 됐는지 사용자가 봐야 함 — 조용한 실패 금지 원칙의 작성기 버전). 버튼 비활성엔 언제나 사유 표기를 동반한다는 일반 원칙으로 다른 비활성 버튼도 같은 배치에서 1회 점검. 다음 배치.
    - **[같은 날 재시도 관찰 — 증상 확장]**: 두 번째 시도에선 "업로드에 실패한 이미지가 있습니다. 삭제하거나 다시 시도해 주세요" 배너가 떴는데(1차엔 부재) **이번엔 보내기 버튼이 활성**으로 보임 — 1차와 정반대. 추정: 실패 등록 경로가 2개(ⓐ 파일 선택 시 크기 사전 차단 → 카드 ⚠️만 ⓑ 업로드 시도 후 실패 → 배너)이고 배너·버튼 비활성이 **서로 다른 상태에서 따로 파생**됨. 처방 확정: 실패 첨부 존재 여부를 **단일 상태**로 판정해 카드 ⚠️·배너·버튼 비활성이 항상 함께 움직이게 + 실패 카드가 있는 채로 발송되면 실패분이 조용히 빠진 채 나가는지(오해 경로) 코드 확인 후 차단. 문구도 "이미지"가 아니라 "파일"로(일반 파일 확장 반영).
  30. **초과 파일 = 발신자 개인 드라이브 업로드 + 링크 발송 (2026-08-19 사용자 제안 — Gmail 동선) → 자동화는 비채택, 안내 문구만 채택** — 판정(Claude): 현행 첨부가 **이미 "드라이브 업로드 + 참조 발송 + 권한 자동 부여" 구조**라 제안과의 차이는 소유자(hmnotice/공유드라이브 vs 개인)뿐. 개인 소유 전환은 ⓐ 용량 이득 0(Education 저장은 조직 단일 풀 — 개인 드라이브도 같은 풀 소모) ⓑ 365일 파기(처리방침 약속)·회수를 플랫폼이 이행 불가 ⓒ 발신자 전출·퇴직 시 파일 소멸 → 수신 쪽지에 깨진 링크(업무 제출함을 공유드라이브로 정한 사유와 동일) ⓓ 권한 자동 부여에 개인 드라이브 쓰기 권한 필요(25-ⓒ·쪽지 미러 기각과 같은 선). **채택분**: 한도 초과 거부 시 눈높이 안내 한 줄 — "10MB가 넘는 파일은 내 드라이브에 올린 뒤 링크를 본문에 붙여 주세요"(6번 자동 링크가 이 동선의 전제, 29번 사유 표시와 같은 자리 — Antigravity, 다음 배치). 파일 한도 자체 상향은 별도 결정으로 열어 둠(사용자가 숫자 주면 기록). **→ [같은 날 확정·반영 ✅] 사용자 "한도는 좀 풀자, 30MB" — TASK_FILE_MAX_BYTES 10→30MB(쪽지 세션 상한 승계로 동반 상향, 서버 경유 4MB 경계는 기술 한계라 불변), 문구 6곳·셀프테스트 갱신, 당일 배포(`7ab99ba`).**
  31. **쪽지 제목 선택화 (2026-08-19 사용자)** — *"업무는 타이틀이 필요하지만, 메신저 서비스는 딱히 제목을 안 붙인다. 없애지는 말고 제목 없이 내용만으로도 전송되게. 대신 알림 등에서 (제목 없음)으로 표기."* 업무(제목 필수)와 쪽지(제목 선택)의 성격 구분 정확 — 채택. 처방: **코어(Claude)** = memo/logic.ts validateMemoContent의 제목 필수 검사(:80 "제목을 입력해 주세요") 해제(빈 문자열 허용·상한 검사 유지), 서버 발신 표면(푸시 제목·알림 원장 title·답장 RE: 조립)에 빈 제목 폴백 "(제목 없음)" 일괄 — 폴백 문자열은 단일 상수로. **화면(Antigravity)** = 작성 UI 제목 placeholder "제목 (선택)", 받은/보낸 목록·상세·검색 결과의 빈 제목 표기 "(제목 없음)" (제목 검색은 빈 값 무해 확인). 착수는 **배치 2와 분리** — 배치 2가 이미 진행 중이라 끼워넣지 않고, 배치 2 검수 시 코어를 Claude가 동반 구현 후 화면 소형 후속.
  32. **[내 할 일 추가]에 서식·첨부 (2026-08-19 사용자 — "구현돼 있는 거라 가져오는 건 어렵지 않을 듯")** — 판단(Claude) 2갈래: ⓐ **리치텍스트 = 채택, 소형** — 쪽지 편집기 배선 재사용 + bodyHasMd1Formatting 스탬프, self_add는 contentFormat을 이미 수용(검수 때 md1 무조건 스탬프를 제거한 건 "서식 도구 없는 입력의 승격 금지"였고 도구가 붙으면 정당). 화면 = Antigravity ⓑ **첨부 = 조건부** — 셀프 업무는 폴더 미생성 설계(가벼움이 취지)라 첨부를 붙이려면 **첫 첨부 시점에만 양식 폴더를 지연 생성**(lazy — 코어 소형, Claude)하는 설계가 필요. 무첨부 셀프 업무의 가벼움은 유지. 착수 = 배치 3(배치 2 안정화 후).
  33. **내 할 일 전체·완료 탭의 무한 스크롤 문제 (2026-08-19 사용자 — "500개면 500줄 스크롤되는 거야?")** — 맞음: 현재 전체보기는 페이지 장치 없는 단일 목록이라 볼륨이 곧 스크롤 길이. 기한창(STATUS 3번)은 읽기 비용 해법이지 렌더 줄 수 해법이 아님을 구분. 처방 = 쪽지함 패턴 이식: **전체·완료 탭에 20~30개 단위 [더 보기] 페이지네이션 + 길어지면 월 구분선**(기본 "진행할 일" 탭은 처리 시 빠지는 구조라 자연 유지 — 대상 아님). 검색은 볼륨 신호 후 쪽지 검색 이식 후보. 담당 = Antigravity(화면), 착수 = 배치 3(32번과 함께).
  - **[2026-08-19 실기기 재확인 결과 ✅ — 22/23 통과 (사용자, 체크리스트 아티팩트 결과문)]**: 1군 치명(5 목록·7 즉시 반영·20 크래시 해소)·픽커 3종·문구류·서식·URL 링크·일반 파일 첨부·대용량 세션 업로드(12MB 실측 포함)·셀프 등록·리마인드·철회 경고 전부 실기기 통과. 유일 미체크 = 4번(수신자 없이 닫기 → 보낸 목록 유령 없음)인데 **현황판 초안 제외 필터는 코드 실재 확인(TaskStatusBoard 초안 제외)** — 다음 실사용에서 자연 확인으로 갈음, 잔존 시 신고. 재확인 중 신규 수집 17~30번은 배치 2로.
  - **[2026-08-19 코어 몫 6건 구현 완결 ✅ (Claude)]**: 4-ⓑ 고아 초안 24h 자동 정리(taskSweep 3단계·isOrphanDraft 순수 판정) · 6 URL 자동 링크(richtext.ts autolinkBlocks/parsePlainAutolink — https만·2048자·꼬리 문장부호 제외, 렌더 후처리라 저장 형식·stripMd1·md1 스탬프 판단 무영향) · 9 업무 파일 화이트리스트 gif 제거(쪽지는 유지) · 10 쪽지 일반 파일 서버부(확장자 화이트리스트=업무 목록+GIF, 서버 경유 상한 3.5→4MB, 세션 업로드 attach_session_start/finish ≤10MB — staging 등재 후 기존 발송·권한·파기 흐름 동일, 인벤토리 행 갱신 동반) · 11 리마인드 동작 정비(종 알림 원장 task-due 기록 신설 + 푸시 "기한이 다가오는 업무가 있어요" + 감사 로그 "업무 리마인드" + 429·대상 없음 문구 교체) · 15 셀프 등록 self_add(확인형 강제·수신자 자동 본인·즉시 수락·selfAssigned 표시·폴더 미생성, 양식 업로드는 폴더 없는 업무 거부 가드 동반). 검증 = 셀프테스트 3종 확장 전판 통과(richtext ⑫ 12케이스·tasks +11케이스·첨부 +10케이스) / tsc / build 전판. **화면 몫 전체 인계 = [`docs/phase8_feedback_screen_handoff_2026-08-19.md`](./docs/phase8_feedback_screen_handoff_2026-08-19.md) (5·7 치명 선두, 항목별 파일 앵커·완료 판정 포함).**
<!-- 서술 -->
* **[2026-08-19 사용자 아이디어 2건 — 배치와 별개 기록 ("아이디어는 풀고 에러 더 잡아볼게")]**:
  - **쪽지 → 업무 승격**: 받은/보낸 쪽지에서 "업무로 만들기" — 구글이 메일에서 캘린더 일정을 뽑아내는 동선처럼, 쪽지 제목·내용·첨부를 업무 작성 화면에 프리필. 작성 2상 구조(prepare)와 정합이라 소형. 착수는 배치·안정화 뒤.
  - **업무 수락 → 구글 캘린더 자동 등록 (4단계 상세의 사용자 확정판)**: 제목=업무 제목, 설명=업무 내용, 첨부는 Drive 링크 동봉, **완료하면 캘린더에서 삭제**. 업무 화면 복귀 딥링크는 사용자가 보류 — 단 기술적으로는 포털 로그인 관문 뒤라 보안 문제 아님(4단계 스펙 때 재론 가능으로만 기록). **묶음 설계 제안(사용자 "같이 고민해보자")**: 이 4단계 + 기존 §2 「교사별 개인 시간표 Calendar API 직접 동기화(B안)」는 같은 기반 — DWD calendar 스코프 1회 승인(선배포 금지 절차) + 캘린더 쓰기·동기 엔진 공유. 스펙을 한 장으로 묶어 스코프·엔진을 1회에 확보하는 것이 정공법. 시점 = 배치 안정화·9c 이후.
  - **조직도·쪽지·업무 통합 허브 (2026-08-19 사용자 발안 — 방향 확정, 다음 사이클)**: 하나의 큰 메뉴 — **좌측에 조직도 상주**(클릭·검색으로 사람을 모음) → 모인 대상으로 [쪽지 보내기]/[업무 등록] 중 선택. 쿨메신저 실동선("시작하면 조직도")의 재현 = 채택 논리 정면 + "사람 먼저 → 행동 선택" 실무 순서 복원 + 내선 등 정보 확인 자연 해소. 설계 원칙 3(Claude 의견, 사용자에게 전달됨): ① 기존 쪽지 쓰기·업무 등록 진입은 병행 유지(허브는 추가 진입점) ② 배치(에러 수정)와 분리 — 안정화 후 IA 스펙(Claude 소형)→화면(Antigravity) ③ PC 전용(모바일은 현행). 16번의 대체물.
<!-- 서술 -->
* **최종 정정 (2026-08-09, 사용자 — 채택 모델 확정)**: 위 결론의 "채택 곡선·유입" 계열 논리는 전면 폐기. **채택 = 기관 결정(실무협의회 → 교직원 회의 일괄 전환 + 연수)**이며, 개발 지속의 목적은 "완성되어 굴러가는 미려한 모델 + 대체 가치(특히 비용 우위)"로 학교의 공식 대체 결정을 끌어내는 것. 따라서 쪽지의 가치 = **쿨메신저 대체 데모의 완결성**이지 유입 장치가 아님. §2 쪽지 아이디어의 "갈래 B 메일 사본" 노림수(비채택자 도달·소외 없음·유입 동선)도 같은 이유로 철회 — 메일 층 없는 순수 네이티브로 확정(④ 최종 참조). 가을 스펙 착수·연말 시연 목표는 유지(기관 결정을 끌어낼 데모 완성 시점으로서). ⑥ ~~[일정] 개학 첫 1~2주 회피 권장~~ **철회 (2026-08-09 사용자 정정)**: 사용자 휴직 중 + 개학 시점 실사용 인구 사실상 제로 — 착수 시점에 일정 제약 없음. 경미: 파기 크론은 정책만 1단계 결정, 구현은 후행 가능.

### Phase 9. 시간표·결보강 시스템 — 컴시간 완전 대체 (스펙 확정 ✅, Phase 7·8보다 앞당겨 착수)

- ✅ **Phase 9. 시간표·결보강 시스템 개요** — 컴시간 완전 대체 추진 스펙 확정 · archive/phase9_spec.md
* **2026-07-26 사용자 확정**: 컴시간(주간시간표+일과진행+알리미) 완전 대체가 목표. 스펙: [`phase9_spec.md`](./phase9_spec.md), 도메인 조사: [`comcigan_analysis.md`](./comcigan_analysis.md).
<!-- 서술 -->
* 단계: 9a 열람(알리미 대체) → 9b 결보강 워크플로(일과진행 대체, 교체 후보 원클릭) → 9c 자동 시간표 작성(브라우저 CSP 솔버). 킬 스위치 = 컴시간 계약 갱신일.
- ✅ **Phase 9a-1. 알리미 대체 열람** — 30학급 그리드 및 열람 탭/카드 구현 완결 · archive/phase9a_spec.md
* **9a-1 종결 (2026-07-30 ✅)**: §6 미결정 4/5 확정 (재계약 결판 2026-12 이전 → 9c 검증 시한 11월 말 / 2학기 개학 오픈 / 실무사 승인+교무부장 알림 / 학생 자기 반만). 상세 스펙: [`phase9a_spec.md`](./phase9a_spec.md). 백엔드+가져오기(xlsx 업로드 파서)+열람 탭+학생 카드 구현, Claude 표적 리뷰 승인. 실파일 5단계 리허설 완주 — `2026-2` 학기 active, 30학급 그리드 실측 확인 (리허설 중 발견된 room undefined 블로커는 `ce034bb`로 수정). 노출 원칙(spec §8-1): 일반 교사·학생 비노출 유지, 9b 오픈 게이트까지.
- ✅ **Phase 9b. 결보강 워크플로** — 교체 후보 엔진 및 신청/승인 워크플로 스펙 확정 · phase9b_spec.md
* **9b 스펙 확정 (2026-07-30 ✅)**: [`phase9b_spec.md`](./phase9b_spec.md) 작성 완료 (설명서 PDF 2종 정독 기반 — 컴시간 기능→범위 매핑, weeks/changes/requests 모델, 주간 합성, 교체 후보 엔진(맞교환·특별보강·감점 규칙), 신청→승인 워크플로, NEIS 목록·시수 집계, 구현 분업 6단계). **2026-07-30 당일 사용자 확인 4건(spec §12) 전부 확정되어 구현 진행됨** — 순서 1·2(합성·엔진·승인 서버)는 Claude 직접, 화면은 Antigravity.
- 🔜 **Phase 9c. 자동 시간표 작성 (CSP 솔버)** — 브라우저 CSP 솔버 및 스펙 4종 확정 (11월 말 시한) · docs/phase9c_spec.md
* **9c 자동 시간표 작성 (브라우저 CSP 솔버 — 스펙 확정 🔜, 11월 말 검증 시한)**: 컴시간 재계약 판단 전 11월 말 검증 시한이 걸린 최대 미결 작업. 결정론 솔버 코어(CSP) + AI 보조 결합. 스펙: [`docs/phase9c_spec.md`](./docs/phase9c_spec.md), 서브 스펙 3종([`phase9c_d_spec.md`](./docs/phase9c_d_spec.md), [`phase9c_e_spec.md`](./docs/phase9c_e_spec.md), [`phase9c_f_spec.md`](./docs/phase9c_f_spec.md)).
  - **[2026-08-14 추가] 학기 전환 스펙 확정** — 사용자 지적("등록부는 현 학기용인데 신학기 것은 어디서?")으로 드러난 신학기 준비 통로 부재를 나이스형 모델(학기 데이터 분리 + 사전 작성·검증 + 전환)로 설계. 컴시간 매뉴얼(§3·§4 통째 사본)·나이스 실무(사용자 증언) 레퍼런스 대조 완료. 초안 학기 생성·학기 선택·등록부 승계 복사·시수 계획 소속·자동 작성 채택(draft_adopt)·전환 관문 6부. 임시 회귀는 안 만들기로 사용자 확정(생기부류 법정 기록 없음). 상세 = [`docs/term_transition_spec.md`](./docs/term_transition_spec.md).
  - **[2026-08-15 추가] 9c-I 시수 계획 → 자동 작성 연결 스펙 확정 (Claude)** — 9c 마지막 큰 조각. 백지 편성 엔진(`compileSectionsFromHours`·코호트 전개, src에서 미사용이던 것)을 자동 작성 화면으로 잇는 배선: 새 서버 조립 함수 + `hours_plan_solve_input` action + 워커 백지 분기 + 초안 스냅샷 2필드. 조립 순서의 단일 원본은 `scripts/solve_blank.ts`(실물 통과 실증). 이중 계상 방지·H1 무력화 방지(초안 hoursSnapshot=계획) 등 함정 6건 명문화. 구현은 Antigravity. 행 단위 동시수업·특별실 힌트 전달은 9c-I-2로 분리(스펙 §10-1). 상세 = [`docs/phase9c_i_spec.md`](./docs/phase9c_i_spec.md).
  - **[2026-08-15 사용자 피드백 — 확인 모달 이슈 문구 성격 오전달]** 실사용 확인 중 사용자 지적: 노란 이슈들은 "확인이 필요한 점"이 아니라 **"시스템이 이렇게 처리하기로 했다"는 선 보고**인데 문구가 조치 요청처럼 읽힘 — *"일과계 피드백을 받아볼 게 아니면 확인이 필요하다고 접근하면 안 되지"*. 처방 2갈래: ① (경미, Antigravity) 모달에서 고지성 이슈(`simul-assumed`·`venue-slot-limited`·`fixed-standalone`)와 조치성 이슈(`fixed-missing`·`fixed-mismatch`·`simul-unsolved`·`class-slot-mismatch`)를 분리해 "이렇게 처리합니다 N건"/"짜기 전에 살펴볼 점 N건"으로 표기 ② (근본) 9c-I-2가 되면 고지성 이슈 대부분이 발생 자체가 사라짐 — 계획 행에 이미 있는 `simulGroupId`·`venueHours`를 엔진이 받게 하는 것이므로 ①보다 ②의 우선 착수도 검토 가치. (①은 같은 날 반영 완료 `e72e257`)
  - **[2026-08-15 추가] 9c-I-2 스펙 확정 (Claude)** — 그리드 로더가 스탬프하는 `lesson.simul`·`lesson.room` 실증을 파생이 계획에 자동 기록(수작업 태깅 불요) + 컴파일러 태그 우선·학급 단위 폴백 + `venueHours` 정밀 분할. 힌트 없으면 종전 동작 그대로(하위호환 회귀 관문). 엔진은 Claude(Fable) 직접, 화면은 Antigravity. 상세 = [`docs/phase9c_i2_spec.md`](./docs/phase9c_i2_spec.md).
  - **[같은 날 구현 완료 ✅]** 엔진(Claude, `20c6164`) + 화면(Antigravity, `a22228a`) 당일 완결. 실측 = 폴백 바이트 동일 · 자동 채움 태그 25행·venueHours 60행 · 확인 모달 이슈 **20건→0건** · 품질 하드 0·미배정 0·소프트 34점(현행 39).

- ⏸ **(구) Phase 9 원안 (CSP·결강보강·캘린더)** — phase9_spec.md 및 9a/9b/9c 단계별 스펙으로 대체되어 보류
#### (구) Phase 9 원안 — AI 기반 웹 시간표 및 교체수업 편성 시스템 (스펙으로 대체됨)
<!-- 서술 -->
* **제약 충족 알고리즘(CSP)**: 교사 가용 시간, 교실 제한, 과목 연속 수업 차단 규칙을 반영한 최적의 시간표 생성.
<!-- 서술 -->
* **결강 자동 보강**: 결강 발생 시 당일 공강 교사 목록을 우선순위 규칙에 따라 최적 추천.
<!-- 서술 -->
* **구글 캘린더 연동**: 확정된 시간표를 개인 캘린더에 자동 동기화.

### Phase 11. 보안·개인정보 정책 + 최초 로그인 동의 (완료 ✅ 2026-08-06 — 실기기 확인 통과·종결, [`docs/phase11_privacy_spec.md`](./docs/phase11_privacy_spec.md))

- ✅ **Phase 11. 보안·개인정보 정책 + 최초 로그인 동의** — 개인정보 처리방침·고지 모달·파기 크론 전부 배포 완결 · docs/phase11_privacy_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R02 (2026-08-18 이관)

- 💡 **Phase 10. Teaching & Learning 라이센스 대여 자동화** — 라이센스 API 연동 및 캘린더 예약/크론 스케줄링 미구현 아이디어
### Phase 10. Teaching & Learning 라이센스 대여 자동화 (미구현 💡)
<!-- 서술 -->
* **배경**: 학교당 라이센스 보유량(15개) 제한으로 인해 교사들이 필요할 때마다 정보부 관리자에게 수동 요청하여 할당받던 비효율을 해결.
<!-- 서술 -->
* **구글 라이센스 API 연동**: Google License Manager API (`licensing.v1`)를 연동하여 `licenseAssignments`를 백엔드에서 자동 제어.
<!-- 서술 -->
* **실시간 캘린더 예약**: 날짜별 가용 수량(15개 - 예약 확정 수)을 시각적 캘린더에 표시하여 교사가 원하는 기간(시작일~종료일)을 선택해 셀프 대여 신청.
<!-- 서술 -->
* **크론 연동 스케줄링**: 매일 자정 크론이 돌 때, 시작일인 교사에게 자동으로 라이센스를 할당하고 만료일이 지난 교사에게서는 라이센스를 자동 회수 처리.

---

## 📌 2. 유실될 뻔한 기타 아이디어 메모

- 🚧 **2026-08-18 사용자 피드백 덤프** — 실기기 사용 피드백 수집 및 우선순위별 순차 처리 진행 중 (2/7/8/9 완결)
* **[2026-08-18 사용자 피드백 덤프 — "우선순위 정해서 차례로 하자"]**: 이미지 첨부 종결 직후 실기기 사용 중 막 던지기로 수집 시작. 전부 모이면 우선순위 확정 후 차례 처리.
  - **① 알림 종 위치**: 종 아이콘을 **화면 최상단 최우측에 고정**하는 게 좋겠다 — 지금은 학교 이름 카드 안에 있어 패널이 화면 중간에 팝업되어 불편하고, **모바일에서는 패널이 왼쪽으로 잘림**(스크린샷 실측). "아직 모바일 UI 신경 쓸 단계가 아닌 건 알지만"이라는 단서 포함.
  - **② 첨부 실패 사유가 안 보임 (2026-08-18 실기기 → ✅ 당일 수정)**: 3.5MB 초과 거부 시 사유 문구가 카드 안 80px 말줄임에 갇혀 "왜 안 되는지"가 안 보였다. 사유를 카드 하단 전체 폭 줄로 이동(Claude 직접 수정).
  - **③ 대용량 첨부 — "3.5MB 넘으면 드라이브에 넣어서 보내주자" (2026-08-18 사용자 발상)**: **전제 교정 — 첨부는 이미 전부 hmnotice@ Drive에 저장된다**(attachment spec §1). 3.5MB 한도는 저장소가 아니라 **업로드 통로**(Vercel 함수 요청 본문 4.5MB) 제약. 따라서 해법 = 서버가 Drive **업로드 세션 URL을 발급**하고 브라우저가 그 URL로 직접 올리는 우회(위 「쪽지 완성」 식은 눈 재검토 ②에서 이미 예약된 경로, DWD 키 노출 없음). **판단: 일반 파일 첨부(HWP 등 — 진짜 대용량 실수요) 단계와 한 묶음으로 구현.** 이미지 v1은 리사이즈가 실사진을 전부 3.5MB 안으로 넣어 주므로 현행 유지 — 한도에 걸리는 건 노이즈 테스트 이미지 같은 예외뿐.
  - **④ 주 운영 목록에 지난주가 계속 남음 (2026-08-18 실기기)**: 8/18(2학기 2주차) 시점에 시간표 운영 > 주 운영 목록 맨 위에 지난주(8/10 개학주)가 그대로 있다. 처방 결정 필요 — 단순 숨김이 아니라 **지난 주 접기/분리**가 후보(지난 주 기록은 시수 집계·나이스 입력 근거라 아예 없애면 안 될 수 있음 — 스펙 판단 시 확인). 기본 화면은 "이번 주부터"가 되어야 한다는 취지.
    - **[2026-08-18 처방 확정 — 실측 후 판단]** 조사 결과(`WeekManageTab.tsx`·`server.ts`): 목록은 `listWeeks`가 `startDate` **오름차순**으로 주는 것을 **아무 필터 없이 전부** 그린다(`WeekManageTab.tsx:250`). 지난 주가 위에 쌓이고 이번 주는 학기가 갈수록 아래로 밀린다(학기당 ~25행). 행당 동작은 `⚙️ 휴업·시수 수정` **하나뿐**이고 삭제 액션은 아예 없다.
      **① 지난 주 문서는 절대 지우지 않는다 (확인 완료).** 사용자가 우려한 그대로다 — `computeHourTotals`(`server.ts:6498`)는 `startDate <= endDate`인 **과거·현재 주만** 골라 시수를 합산하고, 나이스 내보내기(`listNeisRows`, `server.ts:6478`)도 과거 구간 주를 직접 읽는다. 다만 **둘 다 Firestore를 자체 조회**하지 이 화면 목록을 읽지 않는다. 즉 **화면에서 감추는 것은 안전하고, 문서를 지우는 것은 사고**다. 이 구분을 구현 지시에 못 박는다.
      **② 처방 = 지난 주 기본 숨김 + 토글.** 새 패턴을 만들지 않는다 — **바로 옆 탭 `CalendarManageTab`이 이미 같은 것을 한다**(`showPastEvents`, 헤더에 "지난 일정 N건 숨김", 하단 `▼ 지난 일정 N건 보기` 토글, 펼쳤을 때 흐리게, 빈 목록 탈출구). 같은 섹션·같은 사용자이므로 그 동작을 그대로 옮긴다.
      **③ 함께 고칠 것 — 이번 주 표시가 아예 없다.** 현재 모든 행이 동일 스타일이라 어디가 이번 주인지 알 수 없다(`WeekManageTab.tsx:254`). 지난 주를 숨기면 이번 주가 맨 위로 오지만, **"맨 위 = 이번 주"는 규약일 뿐 표시가 아니다.** 이번 주 행에 배지를 단다. 사용자 요구("기본 화면은 이번 주부터")의 나머지 절반이 이것이다.
      **④ 숨김이지 편집 금지가 아니다.** 펼치면 과거 주도 그대로 수정 가능해야 한다 — 뒤늦게 발견한 휴업일을 나이스 입력 전에 고치는 것은 실수요다(`updateWeek`에 날짜 가드가 없는 것은 의도된 상태로 유지).
      **⑤ 과거 판정 기준** = `주 시작일 + 6일 < 오늘(KST)`. 주말이 지나야 지난 주다 — 금요일이 끝났다고 그 주를 접으면 주말에 정정할 창이 사라진다.
      **⑥ 학기 말 대비**: 전부 과거가 되면 목록이 비어 보이므로, 빈 상태에 안내 + 지난 주 보기 버튼을 둔다(CalendarManageTab의 탈출구와 동일).
      담당 = **Antigravity**(패턴 이식·UI). 서버 변경 0.
  - **⑤ 대시보드에 쪽지 편입 (2026-08-18 사용자)**: 어드민 홈 대시보드에도 모바일처럼 받은 쪽지가 보였으면. 사용자 원안 = 시간표·급식 너비를 반으로 줄이고 남은 반에 받은 쪽지를 세로로 길게. 반응형 우려는 기우 — 넓을 때 2단 그리드(좌: 시간표+급식 스택, 우: 받은 쪽지), 좁을 때 자동 세로 쌓임이 표준 패턴. 1단계 스펙 §4-1이 2단계로 미뤄 둔 "대시보드 쪽지 카드"의 확장 실현. 착수 순서는 답장 UI 뒤.
  - ~~**⑥ 알림 "모두 읽음" 처리 (2026-08-18 사용자)**~~ → **취소 (같은 날 사용자 확인)**: 벨 패널을 열면 자동 전체 읽음 처리되는 현행 설계가 정상 동작함을 사용자가 실기기로 확인("벨 누르고 닫아보질 않아서 몰랐어"). 기능 추가 불요 — 요청 철회.
  - **⑦ 쪽지 화면 시각 구분 부족 (2026-08-18 사용자 → ✅ 당일 개선 완료)**: *"어디까지가 이전 쪽지고 다음 쪽지인지, 어디가 제목이고 어디가 내용이고 어디가 다른 정보들인지 구분이 잘 안돼. UI를 깔끔하게 가져가려고 의도한 건 알겠는데 그 결과로 구분과 정리가 안 되는 문제."* 실측 증상 — 데스크톱 받은쪽지함 목록 행들이 발신자+제목 두 줄로 거의 동일해 경계가 약하고, 상세 영역은 제목·메타(발신자/시각)·본문·이력이 같은 톤이라 위계가 없음. 모바일 목록도 동일 계열. 처방 완료: 목록 행 간 구분선 강화(`border-slate-200/80`), 발신자 보조 톤·제목 주 톤 대비, 안 읽음 강조 유지, 상세 패널 메타 정보 한 줄 정리·본문 카드 구획(`text-[15px]` 가독성 확보)·주고받은 이력 및 읽음 현황 보조 박스 분리. Antigravity 구현 및 빌드 검증 완결.
  - **⑧ 쪽지 즐겨찾기 (2026-08-18 사용자 → ✅ 당일 구현 완료)**: 별표(☆/★)를 누르면 낙관적 갱신으로 즐겨찾기 토글되며, 상단 탭의 [즐겨찾기] 쪽지함에서 별표한 쪽지를 모아볼 수 있게 완결(등호 쿼리 2개 1회 fetch, `orderBy` 미사용으로 복합 색인 불요, 클라이언트 `createdAt` 정렬 및 `hiddenBy` 필터 적용, 빈 목록 안내 문구 지원).
  - **⑨ 쪽지 검색 (2026-08-18 사용자 → ✅ 당일 구현 완료)**: 탭 줄 옆 검색 입력창을 통해 제목·본문·발신자 이름(스탬프+현재 표시이름)·수신자 요약을 전량 조회(`limit(300)` 페이지네이션) 후 5분 in-memory `clientCache` 기반 다중 키워드 AND 검색(`memoMatchesSearch` 순수 로직 적용, 검색 로딩/0건 문구 처리, 검색어 비우면 원래 목록 복귀).
    - **[2026-08-18 ⑧⑨ 스펙 및 서버·UI 전면 완결 ✅]** [`docs/memo_star_search_spec.md`](./docs/memo_star_search_spec.md) — ⑧ starredBy 맵(값 `true` — 등호 쿼리용)·star 액션·즐겨찾기 전용 등호 쿼리 2개(복합 색인 0 실측 통과)·별표 토글 UI·즐겨찾기 탭 완결. ⑨ 검색 시 전량 페이지 조회+로컬 필터+5분 캐시, 제목·본문·발신자 이름 다중 키워드 AND 매칭 UI 완결. selftest, tsc, check_ui_removals, build(40/40) 검증 완료.
    - **[2026-08-18 사용자 발상 — 검색 비용 레버 ③: 시트 창고 색인]**: 하루 1회 크론으로 쪽지 기록을 공유드라이브 스프레드시트에 옮기고 검색은 거기서(첨부는 Drive 링크 동봉, 리치텍스트 검색 포기 수용). Claude 판단 = **성립하나 지금 미채택**: 시트를 서비스 계정 전용 잠긴 창고 + 서버 창구 검색으로 하면 개인정보 유지 가능("창고 잠금" 원칙)하지만, ⓐ 검색·비용 문제는 기간 드롭다운(스펙 §2-4a)으로 기해결 ⓑ 안 써도 매일 고정비 + 회수·삭제·파기를 시트에도 전파해야 하는 제2 원본 부담 ⓒ 하루 지연으로 최근분 이중 검색 복잡도. **실사용 검색량이 한도를 위협할 때의 레버 순서: ① 캐시 연장 ② 경량 색인 문서 ③ 이 시트 창고.**
      - **[2026-08-18 저녁 정정 — 사용자 반문 "하루 한 번 옮기는 건데 읽기 1회 아닌가"]**: 사용자 지적이 옳다. 위 ⓑ의 "매일 고정비"는 **오해를 부르는 표현이었고 정정한다** — 이관 크론의 Firestore 읽기는 전일 신규분만(하루 50~200 reads 추정)이라 **검색 1회(§2-2 300~600, 드롭다운 후 75~150)보다도 싸다**. 시트 측 검색은 시트 전량이 Sheets API 1콜이라 Firestore 읽기 0에 수렴하는 것도 맞다. 따라서 **기계 비용은 이 안의 반대 근거가 아니다.** 실제 부담은 ⓑ' **파기·회수·삭제의 제2 원본 전파 의무** — 365일 파기는 처리방침에 고지된 약속이라 시트 사본에도 이행해야 하고(미이행 = 실제 위반), 회수·삭제분이 시트 검색에 계속 잡혀도 안 된다. **완화 설계 확인**: 시트에 불변분만(memoId·제목·본문·발신자·수신요약·createdAt) 담고 가변 상태(reads·hiddenBy·starredBy·recalledAt)는 Firestore에 유지 → 검색은 시트 매칭 후 **매칭분만 원본 재확인**(수 건 수준이라 읽기 무시 가능). 이 형태면 전파 의무가 **파기 시 행 삭제 1건**으로 축소되고, purge.ts가 이미 삭제 대상 memoId를 알고 있어 접합점도 있다. **결론 불변(미채택) — 단 근거는 ⓐ 하나로 좁혀진다**: 기간 드롭다운으로 필요 자체가 사라졌다. ⓒ(당일분 이중 검색)는 잔존하나 경미. 레버 순서는 유지.
      - **[2026-08-18 사용자 발상 — 레버 ③': 개인 Drive 시트 미러(사용자별 사본)]**: 공용 창고가 아니라 **교사 각자의 개인 Google Drive에 자기 쪽지 이력 시트를 생성**해 발송·열람 시 누적하고, 검색은 각자 자기 사본에서. 회수·삭제 시 해당 행도 삭제. Claude 판단 = **미채택(레버에도 넣지 않음)**. **성립하는 부분**: 검색 Firestore 읽기가 진짜 0이 되고(각자 Sheets API), 시트 내용이 본인 수·발신분으로 자연 스코핑돼 공용 창고보다 격리는 오히려 낫다 — 사용자의 비용 직관은 이번에도 정확. **채택 못 하는 이유 3**: ⓐ **소유권 역전 = "창고는 잠그고 수수는 플랫폼 창구로만" 원칙 정면 위배** — 파일 주인이 개인이라 임의 공유·반출을 플랫폼이 막을 수 없다. "어차피 캡처 가능"은 반론이 안 됨(캡처=1건 vs 시트=수신 전량 기계가독 파일, 유출 단위가 다르다). ⓑ **회수·파기 전파가 ×N으로 증폭** — 80명 공지 회수 시 80개 시트 수정, 그러려면 플랫폼이 **전 교직원 개인 Drive 상시 쓰기 권한**(DWD 각 사용자 사칭)을 보유해야 함 = 권한 표면 대폭 확대, 대외 설명 곤란. ⓒ **계정 생애주기 충돌** — 개인 Drive 소유물은 전출·퇴직 삭제 시 소멸·소유권 꼬임(§업무 파일 흐름에서 이미 같은 이유로 개인 Drive를 기각하고 공유드라이브로 확정한 전례). **살릴 알맹이**: 발상의 핵심인 "사용자별 소형 색인을 따로 둔다"는 방향은 옳고, **위치만 플랫폼 안으로 옮기면 = 레버 ② 경량 색인 문서**(교사별 요약 문서 1건: memoId·제목·발신자·createdAt, 본문 제외 → 검색이 300~600 reads에서 **1 read**로). 절감 효과는 개인 Drive안과 사실상 동등하면서 ⓐⓑⓒ가 전부 소멸. 즉 이 발상은 레버 ②의 재발명이며, **레버 ② 착수 시 설계 근거로 이 항목을 참조할 것**.
      - **[2026-08-18 사용자 발상 — 레버 ②' 승격안: 월별 개인 사본(중량 색인)]**: 사용자 정정 — "경량이 아니라 **중량**, 웬만한 정보를 다 담고 **달 주기로 끊는다**". Claude 판단 = **이건 색인이 아니라 사용자별 월 단위 미러(fan-out on write) — 지금까지 나온 비용 안 중 가장 크고, 레버 ②를 흡수한다.** 아이디어 승격하되 **착수는 조기 경보 신호 후**(아래 이유). **효과(추산)**: 검색만이 아니라 쪽지 기능 전체가 싸진다 — 받은쪽지함 열기 목록 구독→**1 read**, 검색(3개월)→**3 reads**(완결 월은 불변이라 캐시 영구, 실효 1), 지난 달 재열람 0. 부수 이득 = **firestore.rules 단순화**("내 사본만 읽는다" — 현행의 쪽지별 자격 증명 구조가 통째로 사라짐, 스레드 전용 쿼리 금지 제약도 무의미해짐). **새로 생기는 것 3**: ⓐ **쓰기 증폭 = 비블로커** — 쪽지 1건이 수신자 수만큼 쓰기(80명 공지=80 writes), 무료 20,000 writes/일 대비 여유 충분(실측 전 추산). ⓑ **1 MiB 월 상한이 이번엔 하드월** — 색인과 달리 실데이터라 "넘치면 발췌"가 불가, **월 분할(달을 둘로 쪼개는) 규칙 필수**. 실사용 추산은 월 45~75KB로 한도의 5~7%라 벽은 멀지만 조용한 쓰기 실패라 가드 필수. ⓒ **읽음 확인은 사본으로 불가** — 수신자가 발신자 사본에 쓸 권한이 없어야 하므로 **원본 memo 문서는 존치**. 역할 분리 확정: **원본 = 가변 상태(reads·hiddenBy·starredBy·recalledAt·파기 대상), 월 사본 = 불변 내용(제목·본문·발신자·첨부 참조)**. **착수 보류 사유(단일)**: 개선이 아니라 **데이터 모델 변경**이라 회수·삭제·파기·실시간 수신·보안 규칙을 동시에 건드린다. 쪽지는 2026-08-18 대량 완결 직후 정상 가동 중이고 **비용 문제는 아직 발생하지 않았다** — 잘 도는 것을 예방적으로 재작성하지 않는다. **착수 트리거 = 「Firestore 사용량 조기 경보」가 무료 한도 50% 경보를 울리는 시점**(두 항목을 이 줄로 연결). 그때 레버 ①(캐시 연장) 다음 수단으로 ②' 채택, 레버 ②(경량 색인)·③(시트 창고)는 이 안에 흡수·폐기 검토.
  - **💡 Firestore 사용량 조기 경보 (2026-08-18 사용자 고민 "종합 플랫폼이라 뭘 더 붙일지 모른다, 최악을 대비" → Claude 처방)**: 한도 소진은 현재 사후 통보(그날 전면 오류)다. daily-sync 크론이 전일 읽기 사용량(Cloud Monitoring 지표)을 확인해 무료 한도의 50%·80% 초과 시 관리자에게 알림(알림 센터 원장) — 최악의 경우를 "갑작스러운 사고"에서 "몇 주 전부터 보이는 추세"로 바꾸는 계기판. 신규 크론 슬롯 불요(기존 통합), 착수 시점 사용자 결정.
    - **[2026-08-18 구현 완결 ✅ — 단 GCP 권한 1건 대기]** `src/lib/ops/usage_logic.ts`(순수 판정)·`usage_alert.ts`(수집·발송)·daily-sync 4번째 작업·`scripts/verify_usage_alert.ts`. 검증 = verify_usage_alert ✅ 26케이스 / tsc ✅ / build ✅(40/40). **설계 확정 5**: ① **자료원 = Cloud Monitoring** — 앱 자가 계수는 이 시스템에서 성립 불가(읽기 다수가 브라우저 onSnapshot 구독이라 서버를 지나지 않아 최대 비용원을 통째로 놓친다). ② **판정 단위 = 마지막 완결 「태평양」 날짜** — 무료 할당량은 태평양 자정에 초기화되므로 KST로 자르면 두 할당 주기가 섞여 비율이 왜곡된다. KST 03시 크론의 보고 대상은 KST 기준 이틀 전처럼 보이는데 정상(추세 관측이 목적, 실시간 차단 아님). 서머타임 25시간 날짜까지 실측 통과. ③ **세 지표(조회·저장·삭제) 중 최대치로 판정** — 읽기만 보면 레버 ②'(월별 사본)처럼 쓰기를 늘리는 개선의 부작용을 놓친다. ④ **잔소리 방지** — 단계 상승 시에만 발송, 같은 단계 유지는 7일 1회, 50% 아래 회복 시 상태 초기화(재상승하면 다시 알림), 같은 날 재실행 무발송. ⑤ **권한 없으면 조용한 no-op** — 가짜 경보를 내지 않고 `available:false`로 구분(사용량 0과 절대 혼동 금지), 권한이 생기면 **재배포 없이** 스스로 켜진다. **잔여(사용자 콘솔 1회)**: 서비스 계정 `school-sync-hub-admin@school-sync-hub.iam.gserviceaccount.com`에 **모니터링 뷰어**(roles/monitoring.viewer) 부여 + Cloud Monitoring API 사용 설정 — 2026-08-18 실측에서 403 확인됨. 부여 후 `npx tsx --env-file=.env.local scripts/verify_usage_alert.ts` 2부에 실제 사용량이 찍히면 가동 확인.
    - **[2026-08-18 사용자 지적 → 주·보조 역전 확정]** 사용자: *"하루 한 번 리셋되는데 다음 날 새벽 크론으로 몇 프로라고 경고하는 게 무슨 의미가 있어… 차라리 사용자 통계를 관리자가 바로 볼 수 있는 게 낫지 않을까."* **지적이 옳다** — 일일 경보는 어제를 보고 오늘 아침에 알리므로 도착 시점에 어제의 위험은 끝났고 오늘 것은 안 보인다. 게다가 실제 위험은 평균이 아니라 **급증**인데 하루 지연 경보는 그 날을 통째로 놓친다. **→ 「사용량 보기」 화면을 주 도구로 승격, 일일 경보는 백스톱으로 강등**(폐기하지 않음 — 화면은 "아무도 안 보면 모름", 알림은 "하루 늦음"이라 서로의 구멍을 메운다. 알림 클릭 시 화면으로 이동해 둘을 잇는다). 화면이 나은 근거: ⓐ **여는 순간이 곧 실시간** — 무료 플랜 크론은 하루 1회 상한에 슬롯 2개 소진(vercel.json)이라 폴링으로는 실시간 불가, 사람이 여는 것은 주기 제약이 없다 ⓑ 임계값 튜닝·알림 피로 없음 ⓒ 급증은 선 넘김보다 **모양**(며칠째 몇 배)이 정확 ⓓ 시간대별 그래프로 폭주 지점 진단까지 한 화면. 스펙 = [`docs/usage_dashboard_spec.md`](./docs/usage_dashboard_spec.md) (오늘 진행 중·최근 30일·시간대별 / Firestore 읽기 0 / 지연·초기화시각 정직 표기 / super_admin 전용 / 권한 없으면 0이 아니라 안내). **착수 순서상 권한 부여가 먼저** — 권한 없이 서버를 쓰면 검증 없이 넘기는 코드가 된다.
    - **[2026-08-18 파생 공백 — 절약 레버 부재]**: 급증을 봐도(화면이든 알림이든) **당길 것이 없다.** 관리자가 그 자리에서 켜서 당일 읽기를 줄이는 스위치(레버 ① 캐시 TTL 연장의 수동 토글)가 없으면 경보·화면은 "곧 터진다"를 알려줄 뿐 막지 못한다. **경보+레버가 한 세트여야 보호 장치가 된다.** 착수 = 사용량 화면 다음, 담당 Claude(무효화 일관성이 위험 지점 — 시간표 읽기 다이어트 캐시와 같은 계열). **→ 스펙 확정 ✅ 2026-08-18** [`docs/saving_mode_spec.md`](./docs/saving_mode_spec.md) — 요점: ① 레버는 **실측된 비용원에만** 건다, 1순위는 쪽지가 아니라 **시간표 후보 클릭**(클릭당 240+ 읽기, 8/8·8/15 사고 둘 다 이 구조) ② 손잡이 3개 일괄(시간표 advisory 캐시 연장·클라 캐시 5분→60분·검색 기본 3개월→1개월), 목록 실시간 구독 축소는 **미실측이라 보류**(사용량 화면으로 재고 후 판단) ③ 승인·커밋 fresh 경로는 캐시 연장 대상 밖(읽기 다이어트에서 세운 선) ④ 전달은 설정 문서 1건 onSnapshot — 로그인 시 프리페치로는 접속 중인 교사에게 안 닿아 위급 레버 자격 미달 ⑤ **24시간 자동 해제**(켜둔 채 잊는 것 방지, 반복 점등은 구조 개선 신호) ⑥ **자동 발동 금지** — 자동 완화는 원인 규명을 가린다(급증은 대개 버그 신호), 사람이 보고 사람이 켠다 ⑦ 정직성: 좁아진 범위가 드롭다운에 실제로 보여야 함. **순서 1(순수 로직·토글 API·rules)은 GCP 권한 없이 지금 착수 가능**, 효과 실측만 사용량 화면 뒤.
- 🔜 **읽기 다이어트 배치 — course-selection 대화창 인계 4건 (2026-08-19)** — Phase 8 실기기 재확인 후 착수, Claude 검증 완료(4/4 코드 실증)
* **[2026-08-19 인계 — course-selection 대화창 읽기 실측 조사]**: 배경 = Firebase 읽기 평균 14,671/일(한도 29%)·최대 94,680(189%), 목표 인원(교사 70+학생 800) 도달 시 약 66,000/일로 한도 초과 추정, **11월 중순 Blaze 크레딧 만료 → 무료 한도 복귀 전 정리 필요**. 인계 주장 4건을 이쪽에서 코드 실증 완료(2026-08-19 Claude — 검증 전 항목은 수정 입력 불가 원칙):
  1. **AuthContext.tsx:149-204 [실증 ✅ — 효과 최대]**: users 문서 onSnapshot 콜백 안에서 settings·teacher_profiles 구독을 매번 해제→재생성. users에 `unreadNotifCount`가 있어(notifications/server.ts:98 증분 실증) **알림 1건마다 발화 → 읽기 1회면 될 것이 3회**. 전 사용자·전 화면 효과. 처방 = 하위 구독 재생성을 domain/email 변경 시로 한정. 담당 Claude(구독 구조 = 경합 위험 지점).
  2. **TaskStatusBoard.tsx 보낸 업무 구독 [실증 ✅]**: 구독 콜백이 `setSelectedTaskId`를 하는데 그 값이 useEffect 의존성 배열에 있음 — 진입 시 목록 전량 2회 + 항목 클릭마다 목록 전체 재구독. 상세는 별도 단일 문서 구독이 이미 담당하므로 순수 낭비. 처방 = 의존성에서 selectedTaskId 제거, 초기 선택은 ref로.
  3. **tasks 구독 3곳 limit 부재 [실증 ✅]**: TasksSection·MobileTasksSection·TaskStatusBoard 전부 limit 없음(memos는 전부 limit(50)). 보존 365일이라 시간이 갈수록 악화. 단서: 내 할 일은 색인 회피로 orderBy를 뺐으므로 limit을 붙이면 임의 부분집합이 됨 — limit+orderBy(복합 색인 1건 등록)로 갈지, 기한 창(where dueAt 범위)으로 갈지 배치 착수 시 결정. **현황판(where senderEmail==+orderBy createdAt)은 색인 없이 성립함을 admin SDK로 실측(2026-08-19)** — 인계문의 "복합 색인 검토"는 내 할 일 쪽만 해당.
  4. **teacher_profiles 전체 구독 4곳 캐시 우회 [실증 ✅ — TaskStatusBoard:71 등]**: 기존 `getClientCache("teacher_profiles:all")` 5분 캐시 경로로 통일하면 탭 왕복마다 89 읽기 소멸.
  - **★ 통합 허브 설계 선행 조건**: "좌측 조직도 상주"는 teacher_profiles 89건 구독이 탭 전환형 → **상시형**이 된다는 뜻. **4번을 먼저 처리하지 않고 허브를 만들면 교사 전원에게 상시 비용원이 생긴다 — 허브는 반드시 캐시 경로 위에 얹는다** (스펙 후보 조건으로 STATUS에 연결).
  - 기타(비용 무관, 같은 조사에서): ⓐ admin/page.tsx의 `case "memo"` 분기 도달 불가 — :1090 삼항이 renderContent()를 우회하고 :1097에서 `<MemoSection />`을 prop 없이 렌더 → **대시보드 쪽지 패널에서 특정 쪽지 클릭해도 initialMemoId 미전달 (실증 ✅, 소형 수정은 Antigravity)** ⓑ ~~.claude/worktrees/bold-perlman-05adb1 껍데기~~ → **머지 확인 후 즉시 정리 완료(2026-08-19, worktree remove + 브랜치 삭제)**.
- 💡 **교육과정 선택 앱 ↔ 학교 플랫폼 통합 (2026-08-19 사용자 발안 — 상세 논의는 이 대화창에서 진행하기로)** — course-selection 앱 개선 중 나온 두 플랫폼 통합 구상. 저쪽 대화창에서 진행 시 문제가 있어 논의 장소만 이관된 상태, 구체 방향·범위는 미전달(기록만). 논의 시작 시 위 읽기 다이어트 ★허브 조건과 같은 비용 관점 선행 검토 필요.
* **[2026-08-19 방향 1차 — 사용자 "로그인만 공유되면 링크만 넣으면 끝"]**: 통합 수준 3단계 정리(Claude) — ① **구글 로그인 공유**(양쪽 다 학교 계정 구글 로그인 → 브라우저 구글 세션 덕에 비밀번호 없이 진입, 학생 800명 전원 학교 계정 보유라 대상 완전 일치) + 플랫폼 내 링크 = 사용자가 기운 안, 개발 최소 ② 완전 무클릭 SSO = Firebase Auth 세션이 origin 단위 격리라 도메인이 다르면 같은 프로젝트여도 자동 공유 불가 — 커스텀 토큰 전달 소형 개발 필요, 체감 차이는 클릭 1회뿐 ③ 한 지붕 흡수 = 통합 본편(미논의). **선행 확인 1건 = 그 앱의 현재 로그인 방식·호스팅** — 이미 구글 로그인이면 ①은 링크 추가 수준.
* **[2026-08-19 실사 완료 ✅ — 1단계 성립 확정 (Claude 번들 실측)]**: 앱 = https://course-selection-app.vercel.app/ (Next.js·Vercel). 공개 번들에서 확인 — ⓐ **Firebase 프로젝트 별도**: `course-selection-f4447` (플랫폼 school-sync-hub와 분리 = 읽기 한도 별도, 읽기 다이어트와 무간섭. 유지 권장) ⓑ **도메인 제한 이중 확인**: 로그인에 `hd:"hmh.or.kr"`(구글 화면 필터) + 로그인 후 `email.endsWith("@hmh.or.kr")` 불일치 시 즉시 signOut("(@hmh.or.kr)으로만 로그인할 수 있습니다") — 클라이언트 강제까지 실재(서버측 rules 강제 여부는 외부에서 미확인, 저쪽 대화창 확인 항목). ⓒ fb01@ Firebase 소유자 초대 수령(사용자 조치) — 인프라 소유 계정 원칙 정합. **서브도메인 course.hmh.or.kr 성립(사용자 질문에 답)**: Vercel 커스텀 도메인 무료, 절차 3 = 그 앱 Vercel 프로젝트 Domains에 course.hmh.or.kr 추가 + DNS CNAME(course→cname.vercel-dns.com) + **Firebase Auth 승인 도메인에 course.hmh.or.kr 추가(빠뜨리면 새 주소에서 구글 로그인 거부 — 함정)**. 서브도메인이어도 세션 자동 공유는 아님(②의 origin 격리 동일) — 얻는 것은 학교 도메인 신뢰·주소 통일. 장기 정리 후보: Vercel 프로젝트도 fb01 팀으로 이전(현재 fb01 팀 목록에 없음 실측 — 소유 분산 상태). 플랫폼 내 링크 위치는 Phase 8 안정화 후 소형 결정(학생 포털 카드 + 교사 사이드바 외부 링크가 1안).
- 💡 **교사 보조 AI 에이전트 — 본인 Gemini API 키 지참(BYOK)** — 사용자 발안 2026-08-19, 착수는 "플랫폼이 어느 정도 정리된 나중" 명시 (기록만)
* **[2026-08-19 사용자 발안 — "일단 던져봐"]**: 학생 제외, **일반 교사의 업무를 도와주는 에이전트 성격의 간단한 툴**. 토큰 비용 문제는 **각자 부담**으로 푼다 — 로그인된 계정이 **본인의 Gemini API 키**를 가져다 쓰는 구조. 키 입력 창구를 **개인정보 관리**에 두고, 교사가 AI 스튜디오에서 키를 발급해 등록. Claude 초견(기록 시점): ① 비용 구조가 무료 원칙(실서비스 운영 경로 0원)과 정합 — 플랫폼 고정비 0, 쓰는 사람만 자기 키로 ② 단 **키 보관·전달이 보안 결정 사항** — 유출 시 과금 피해가 교사 개인 지갑으로 가므로 Firestore 평문 저장 금지, 서버 암호화 보관 또는 브라우저 로컬 보관 중 택일 + 본인 외 접근 차단이 스펙의 중심(착수 시 Claude 스펙 필수) ③ 무엇을 돕게 할지(쪽지·업무 초안 작성, 제출물 요약 등)는 착수 시점의 실수요로 정한다. 시점 = 플랫폼 안정화 후(사용자 "나~중에" 명시) — Phase 8 배치·9c와 무관, 지금은 기록만.
- 💡 **교사별 시수표 자동 생성 (교육과정부장 3파일 → 시수표)** — 일과계 수작업 구간 자동화 + 배정표 오류 자동 검출, 사용자가 일과계에 직접 언급 (2026-08-14)
* **교사별 시수표 자동 생성·배정표 오류 검출 (2026-08-14 사용자 발상 — 일과계 대화에서 직접 제안)**: 일과계 실무 흐름 실측(송혜원 님 증언) = 교육과정부장이 주는 3개 파일(**① 과목별 배정표 ② 이동수업현황 ③ 창체수업 담당교사**)을 일과계가 **손으로 편집·대조**해 교사별 시수표(.xls)를 만들어 컴시간에 입력. 사용자가 대화에서 직접 제안: *"3파일을 넣으면 자동으로 교사별 시수표가 나오게"* + 일과계가 배정표 오류 상존을 증언하자 *"그 오류도 자동으로 잡게 만들어볼게요"* — **사용자가 일과계에 이미 약속한 항목**. 일과계가 짚은 함정 2건 = ⓐ 배정표 자체에 오류가 있는 경우가 있음 ⓑ 1학기에는 담임 교체로 배정표의 담당반이 바뀜. 선행 = 9c-H 시수표 업로드 파서(출력 형식이 곧 이 기능의 목표 형식). **→ 서버부 2026-08-16 완결**(경로 ⓓ: PDF 가명 AI 추출 + 검증 그물 + 작업 3단계 라우트 + 모델 사다리 — `verify_hours_assignment` 전 항목·매칭 347/347). 잔여 = UI(Antigravity)·3학년 파일. UI도 같은 날 완결(실기기 확인 통과). 개선 아이디어: 창체(진로) 추가를 전부/전무가 아니라 **배정표에 창체가 없는 반만 골라 추가**하는 옵션(이중 계상 원천 차단, 경미). 실물 시수표 분석 = [`docs/phase9c_h_spec.md`](./docs/phase9c_h_spec.md) §0-1a-③.
  - **[2026-08-14 저녁 갱신] 3파일 실물 수령·전수 분석 완료 — 자동 생성 성립 판정.** 교차 검증 실측 전건 일치(창체 담당↔시수표 진로, 수학 부서 합 127 삼중 일치 등), 자동 오류 검출 목록 8종 도출, 실측 함정 1건 확인(창체 파일 내부 제목이 전 학기 것 — 양식 재활용). 병목은 배정표가 HWP 표라는 입력 형식이며 권장 경로는 한글 표 복사→붙여넣기(TSV). 산출은 9c-H `HoursPlan`의 네 번째 진입 경로로 얹는다. 상세 = [`docs/hours_source_files_analysis_2026-08-14.md`](./docs/hours_source_files_analysis_2026-08-14.md). 남은 선행 = 3학년 이동수업 현황 수령 + 입력 경로·착수 시점 사용자 결정.
  - **[2026-08-18 갱신 — "3학년 이동수업 현황 수령" 선행은 이미 충족 (사용자 확인)]** 이 현황 엑셀들은 일과계가 받아서 시간표를 짜는 표준 입력 재료이고 필요한 정보가 전부 그 안에 있는데, **3학년 것 포함 8/16에 이미 수령·커밋돼 있다**(docs/이동수업_현황_원본_2학년_2026-2.xlsx · 원본_3학년_2026-1학기표.xlsx — 성명 없음 실측, 과목·인원뿐). 게다가 **현재 배정표 만들기 실행에 두 엑셀이 실제 입력으로 투입되어 돌아가는 중**이라 "수령 필요"는 갱신 안 된 낡은 줄이었다 (2학기 배정표·창체 파일도 8/16 docs/ 수령 완료). ※ 같은 날 앞선 정정이 이유를 "시스템 역추출 대체"로 잘못 적었다가 사용자 재정정 — 역추출은 별개 용도(현행 학기 검증)의 기능이다. **"입력 경로·착수 시점 사용자 결정"도 낡은 줄** — 경로는 8/16 ⓓ(PDF 저장→업로드→가명화 AI 추출)로 사용자 확정·같은 날 서버부+UI 구현 완결·실기기 통과이며(윗줄 8/16 괄호 참조) 실사용 중이다. **이 항목의 선행은 전부 소진 — 남은 것은 경미 개선 아이디어(창체 없는 반만 골라 추가)뿐.**

- ⏸ **참조 학기 우선순위를 전전학기(전년도 같은 시기)로** — 규칙·서버부 구현 완료(rankReferenceTerms), 실적용은 1년치 데이터 축적 후(2027-2 준비 시점)로 대기
* **참조 학기 우선순위 — 전전학기 먼저, 없으면 전 학기 (2026-08-17 사용자 발상)**: 사용자 원문 — *"교육과정은 1년 단위로 간단 말이지. 비슷한 과목은 전 학기보다는 전전학기(전년도 같은 시기)에 더 많아. 교육과정이 같다면 사실 아예 과목명은 다 같을 거야. 1년치 데이터가 쌓인 이후에는 먼저 전전학기를 참조하고, 그래도 뭐가 없을 때 전 학기를 참조하도록."* 예: 2027-1 준비의 1차 참조 = 2026-1 (2026-2가 아니라).
  - **적용 지점 후보 (착수 시 스펙에서 확정)**: ① 학기 전환의 사전·등록부 승계 원본 ② 시수 계획 "이전 학기에서 가져오기" 기본 선택 ③ 이동수업 현황 시스템 역추출(deriveSimulStatusFromSystem)의 "previous" 근거 학기 ④ 관문 후보 랭킹은 확정 이력이 학기 무관이라 이미 자동 수혜 — 변경 불요.
  - **선행 조건**: 같은 시기 학기 데이터 1년치 축적. 플랫폼 첫 운영이 2026-2라 **첫 적용 가능 시점 = 2027-2 준비**(그때 2026-2가 전전학기가 됨). 2027-1 준비는 지금 구조(전 학기 참조)가 유일한 선택지였으므로 소급 수정 불요.
  - 다음 단계: 착수 시점 사용자 결정 → Claude 스펙(참조 학기 결정 규칙 단일 소재지) → 구현 분담 판단.
  - **[2026-08-17 사용자 재확정 — "데이터는 없지만 구현은 지금"]** 플랫폼은 데이터가 도착하면 저절로 발효되는 완성품이어야 한다는 취지로 즉시 착수. 규칙 단일 소재지 = `utils.rankReferenceTerms`(순수 함수, 전년도 같은 학기 → 과거 최신순, `verify_reference_term.ts` 5케이스). 서버 자동 선택 지점 배선 = 배정표 불러오기의 이동수업 실증 근거 학기(`pickReferenceTermWithGrids` — 그리드 실재하는 첫 후보, 없으면 운영 학기 폴백 = 현행 동작 보존). 사용자가 2026-1 데이터를 넣으면 2027-1 준비부터 즉시 발효. 잔여 = UI 기본 선택 2곳(시수 계획 "이전 학기에서 가져오기"·등록부 승계 fromTermId — Antigravity, utils 함수 재사용).
  - **[2026-08-18 상태 정정 — 실적용 시점 대기]** 순수 규칙 및 서버 자동 선택(rankReferenceTerms, pickReferenceTermWithGrids)은 구현 완료되었으나, 플랫폼 첫 운영(2026-2) 특성상 전전학기 데이터가 1년 뒤에 쌓이므로 실적용(2027-2 준비 시점)까지 ⏸ 대기 상태로 전환.
  - **[2026-08-18 종결 확인 — 윗줄 끝의 "UI 기본 선택 2곳" 문구는 소진]** UI 배선 자체는 `89e44b4`(8/17)에 이미 반영돼 있었고(STATUS 등재 근거였던 "components 미사용 grep"이 이 커밋을 놓친 유령 행), `7293ff5`(8/18)가 폴백 3곳 보정(대상 학기 자신이 폴백으로 잡히던 구멍 제거)으로 완결 — Claude 검수: diff 대조·tsc·verify_reference_term 5케이스 전판 통과. 이 항목에 사람 손이 갈 일은 이제 없음 — 2027-2 준비 때 전전학기 데이터가 쌓이면 자동 발효.
- ✅ **과목 이름 단일 사전 원칙** — 단일 소재지 subjectDict 모듈·관문 UI·런타임 폴백 제거 전면 완결 · docs/subject_dictionary_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R24 (2026-08-18 이관)
- ✅ **활성 학기의 편성용 등록부를 열람 전용으로 잠그기** — 서버 가드+해제 다이얼로그 완결·실기기 확인 통과(해제 기록 3건 실측) · docs/registry_lock_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R03 (2026-08-18 이관)
- ✅ **알림 센터 (처리·변경된 것 목록 + 배지)** — 서버·화면·알림 입구 통일까지 완결, 실기기 확인 통과(2026-08-18) · docs/notification_center_spec.md
- 💡 **알림 파기 TTL 콘솔 설정** — `notifications` 컬렉션 그룹 `expireAt` TTL 정책 1회 설정 (Firestore 콘솔 작업 — 사용자 콘솔 접속 시 Claude가 URL·단계 안내, 그전까지 문서는 쌓이되 180일 내라 무해)
- 💡 **프로필 승인 알림 배선** — 알림 센터 행정 처리 유형(admin-action)의 발생 지점 연결 (승인 엔드포인트 특정 후 소규모 — notification_center_spec 구현 노트 ③)
* **알림 센터 — "나와 관련해 처리·변경된 것" 목록 + 새 알림 배지 (2026-08-13 사용자 재발상 — 예전에 떠올렸다가 기록 없이 유실됐던 것을 다시 던짐)**: 다른 플랫폼이 다 갖고 있는 그 메뉴. 내게 일어난 일을 시간순 목록으로 보여주고, 안 읽은 것이 있으면 메뉴가 활성 표시된다. **Claude 판정: 편의 기능이 아니라 안전망이다 — 필요하다는 데 동의.**
  - **핵심 근거 — 지금 이 플랫폼의 통지 수단은 웹 푸시뿐이고, 푸시는 휘발성이다.** 잠금화면에서 쓸어 넘기거나 알림을 끈 사람은 **영영 모른다.** 그런데 이 시스템이 사람에게 통지하는 것 중에는 *놓치면 안 되는 것*이 있다 — 특히 **결보강으로 내 수업이 바뀐 것**, 내 교체 신청의 승인·거절, 내 프로필 승인, 전출·계정 처리. 지금 구조에서 이걸 놓치면 복구 경로가 없다. 알림 센터는 그 **원장(ledger)** 이고 푸시는 초인종이다(쪽지가 이미 쓰는 패턴을 전면화).
  - **설계 시 결정할 것(착수 전 Claude 스펙)**: ⓐ **읽기 비용** — 배지(안 읽은 수)를 목록 실시간 구독으로 구현하면 로그인마다 읽기가 붙는다. `count()` 집계나 사용자 문서의 카운터 필드로 뽑고 목록은 열 때만 읽는다(무료 한도 규율). ⓑ **`audit_logs` 재활용 유혹을 경계** — 감사 로그는 *"누가 무엇을 했나"*(행위자 축), 알림은 *"나에게 무엇이 일어났나"*(대상자 축)로 축이 다르고, 문구가 관리자 눈높이이며, 개인에게 열면 권한 문제가 생긴다 → 별도 컬렉션 권장. ⓒ **무엇을 알림으로 승격할지** — 전부 넣으면 소음이 되어 아무도 안 본다. 1차 후보는 "내 수업/일정이 바뀜 · 내 신청이 처리됨 · 내게 온 쪽지 · 나에 대한 행정 처리" 넷. ⓓ 상태 단일 원본 원칙(Phase 8 캘린더 격하와 같은 계열) — 알림은 원본을 **가리키는** 항목이지 원본의 사본이 아니다. ⓔ 보존 기한·파기(개인정보 인벤토리 등재 대상인지 판정).
  - **위치**: 쪽지·웹 푸시 인프라 위에 얹히는 층이고, Phase 8(업무 지시)의 수락·완료 통지도 자연히 여기 실린다 → **쪽지처럼 기반 인프라 성격**. 크기 중~대. 9c(11월 말 시한)와 자원 경쟁하므로 착수 시점은 사용자 결정.
  - **[2026-08-15 사용자 발상] 알림 센터를 "읽는 원장"에서 "수락하는 창구"로 확장 — 알림 항목에 [수락] 버튼**: 알림이 목록·배지에서 끝나지 않고 **항목 안에서 바로 수락할 수 있어야** 한다. 첫 용례 둘 — ① **양해 요청 수락**(아래 "양해 쪽지 전송" 항목의 구글 챗 텍스트 발송을 대체) ② **업무 배분 수락**(Phase 8 업무 지시). 사용자 표현: "알림 기능에 수락 버튼 같은 게 있어서 여러 가지를 수락하는 창구로."
    - **Claude 관찰(설계 시 이어받을 것)**: 이 발상은 새 기능이 아니라 **이미 예약돼 있던 [`consent_swap_opening_spec.md`](./docs/consent_swap_opening_spec.md) §13-3(플랫폼 내 동의 → 자동 신청)의 도입 경로 그 자체**다. §13-3의 전제가 "오픈 게이트 + **인앱 알림**"이었고, 양해 모델은 그때를 대비해 `SwapDraft.consentStatus`(NONE→REQUESTED→CONSENTED→DECLINED)를 이미 갖고 있다 — 수락 버튼이 생기면 이 필드가 실제로 전이하며 현행 "양해 받았음" **수동 체크(`consent.confirmed`)를 대체**한다. 즉 모델 변경 없이 UI·상태 전이만 붙이면 되는 자리.
    - **결정할 것**: ⓐ 수락의 법적·실무적 무게(수락 기록이 원장에 남는가 = 책임 보호 근거인가) ⓑ 거절·무응답 경로(만료·재요청) ⓒ 수락 대상 유형을 어디까지 여는가(양해·업무 외에 확산되면 소음) ⓓ 「시스템은 정보 전달까지만」 원칙과의 경계 — 수락 버튼은 당사자 본인의 의사표시이므로 원칙에 저촉되지 않으나, **시스템이 대신 조르는 독촉·리마인드는 금지**로 못박을 것.
  - **[2026-08-18 종결 ✅]** 스펙(결정 8건 사용자 전부 수용) → 서버 코어(Claude `63d94de`: 원장·카운터 무임승차·수락 창구·발생 3종 배선) → UI(Antigravity `c44e635`: 벨·목록·수락 버튼·양해 요청 버튼) → 실기기 혼란 피드백으로 입구 통일(§6-1, `8d67ed7`·`17ce9a0`: 푸시 스위치를 벨 패널로, 카드는 미설정자 배너로) → 실기기 확인 통과. 잔여는 위 상태 줄 2건(TTL 콘솔·프로필 승인 배선)으로 분리 등재.

- ⏸ **전체 부서(효명고) 신설** — 현 부서 배치 관행 수용하여 코드 변경 없이 종결
* **"효명고등학교" 전체 부서 — 부서 미소속 교직원의 소속처 (2026-08-13 사용자 발상 + 같은 자리에서 사용자 스스로 반례 관찰)**: 배경 = 전수 스캔 판단 항목 "부서 없는 실교직원은 쪽지 발송·수신·검색에서 빠진다"(현재 실피해 0). 발상 = 학교 전체를 하나의 큰 부서로 만들어, 개별 부서에 안 속한 사람은 "효명고" 부서 소속으로 트리 최상단(부서들과 같은 층위)에 열람되게. 단 사용자가 곧바로 실무 관찰을 덧붙임 — **쿨메신저도 경기도교육청 메신저도 남는 인원은 어떻게든 부서를 만들어 배치한다**(경기도교육청 메신저의 "휴직교사" 부서도 그렇게 임의 신설된 것으로 보임). Claude 의견 = **관찰 쪽이 정답**: 학교 현장의 기존 해법(부서를 만들어 넣는다)이 이미 있고, 이 시스템의 부서 목록도 자유 편집형이라 "교장"·"교감"·"행정실"·"휴직 교사"가 실제로 부서로 존재한다 — 즉 **코드 변경 0으로 해소되는 문제**. 가상 최상위 부서를 새로 구현하면 트리 층위·전체 발송 범위·자격 게이트 의미가 전부 복잡해지는 반면, 얻는 것은 관행이 이미 주는 것과 같다. → **"해당사항 없음(noDept)"은 도구·시험 계정 전용으로 유지하고, 부서 없는 실교직원이 생기면 부서를 만들어 배치한다**로 판단 항목 종결(2026-08-13 사용자 방향 수용).

- ✅ **쪽지 삭제·회수** — 회수 기능 서버·UI 완결, 삭제 고지 포함 스펙 확정 · docs/memo_spec.md
* **쪽지 삭제·회수 (2026-08-13 사용자 제기, 충돌 검토 완료 — 스펙 확정)**: 쿨메신저에 있던 두 기능. 삭제 = 내 화면에서만 감춤. 회수 = **이미 읽은 사람 것은 두고 안 읽은 사람 것만**. 사용자가 "수신확인 기능과 충돌할 수 있으니 절충하거나 버리자"고 제기 → 검토 결과 **둘 다 살린다**. 삭제의 유일한 충돌은 *안 읽고 삭제*(발신자 화면에 영원히 "안 읽음"으로 남아 거짓 정보가 된다) → **안 읽은 쪽지는 삭제 불가**로 해소. 회수는 `recipientEmails`에서 미열람자만 빼면 되고 `reads`를 안 건드려 이력이 보존되므로 **충돌 자체가 없다**. 단 푸시는 되돌릴 수 없어 "완전한 취소가 아님"을 화면에 알려야 하고, 회수 이력(`recalledAt`)을 남겨야 보낸 이력이 왜곡되지 않는다. **회수는 §11-1의 짝으로 서버·UI 전건 구현 완료 및 검증 통과 ✅ (2026-08-13)**. 삭제는 "지웠지만 원본은 보존 기간까지 남는다"를 고지 문안에 명시해야 한다. 상세 = [`docs/memo_spec.md`](./docs/memo_spec.md) §12.

- 🔜 **쪽지 서식·이미지·이모지·GIF** — 스펙 확정 ✅(2026-08-18), 구현은 STATUS 작업 큐 · docs/memo_richtext_spec.md
* **쪽지 서식(Rich Text)·클립보드 이미지·이모지·GIF 검색 (2026-08-13 사용자 발상 — 같은 날 사용자가 모바일 호환성 우려 동시 제기)**: "현재 메신저 서비스 대부분이 Rich Text를 지원하고, 클립보드 이미지 붙여넣기도 지원한다. 이모지는 물론이고. 여기에 Tenor 같은 곳에서 GIF를 검색해 가져올 수 있으면 더 좋겠다." → **내선번호(§11-6)와 같은 계열의 채택 논리** — 쿨메신저·카톡에 익숙한 사람에게 "글자만 되는 쪽지"는 퇴보로 느껴진다. 단 사용자가 곧바로 짚은 대로 **모바일 열람 호환성이 진짜 제약**이라 갈래를 나눠야 한다.
  - **⓪ 사용자 우려의 정확한 정체**: 문제는 "모바일에서 서식이 안 보인다"가 아니다(HTML은 폰에서도 보인다). 실제 셋 — ⓐ **작성**이 어렵다(`/m`은 애초에 열람 전용 설계, "관리 업무는 PC" 원칙) ⓑ **푸시 알림 본문**에 서식·이미지를 넣을 수 없어 미리보기가 태그 조각이나 빈 줄로 깨진다 ⓒ 좁은 폭에서 표·큰 이미지가 가로 스크롤을 만든다. → **해법 방향: 저장은 하나(정제된 HTML 또는 마크다운 서브셋), 표시는 경로별 다운그레이드.** 작성은 PC 전용, 모바일은 열람만, 푸시 본문은 **서식 제거한 평문 발췌**를 별도 필드로 스탬프. 이러면 우려 셋이 다 소멸하고 `/m` 원칙과도 충돌하지 않는다.
  - **① 이모지** — 가장 싸다. 저장 모델 변경 0(유니코드 문자일 뿐), 모바일 완전 호환. **단독 선착수 가능.**
  - **② 서식(굵게·목록·링크 등)** — 저장이 평문에서 구조로 바뀐다. **XSS 정제(sanitize)가 서버·저장 시점에 필수**(현재 `memos` 본문은 평문 전제). 허용 태그를 좁게 화이트리스트(굵게·기울임·밑줄·목록·링크·인용 정도), 표·임의 스타일은 처음엔 제외.
  - **③ 클립보드 이미지** — **여기서 무료 원칙이 걸린다.** Firestore 문서 1MB 제한 때문에 본문에 못 넣고 별도 저장소가 필요한데, Firebase Storage는 신규 프로젝트에서 유료 플랜을 요구할 수 있다 → **착수 전 실측 필수**. 0원 대안 = **학교 구글 드라이브 업로드 후 링크**(이미 Workspace 계약 안, 저장 비용 0, 권한도 학교 도메인 안에서 제어). 부수 의무 2건: ⓐ 365일 파기 크론이 **이미지도 함께** 지워야 한다(안 그러면 본문만 사라지고 첨부가 영구 잔존) ⓑ **개인정보 인벤토리 `memos` 행 확대** — 이미지에는 학생 명단·성적·생활지도 화면 캡처가 들어올 수 있어 텍스트보다 민감도가 높다. 고지 문안 갱신 동반.
  - **④ Tenor GIF 검색** — **신규 외부 서비스 연동이라 성격이 다르다.** 인벤토리의 "처리 위탁 현황" 표와 고지 문안 갱신 대상이고(검색어가 외부로 나간다), API 키 발급·무료 한도 확인이 필요하다. 게다가 *학교 공식 업무 메신저*에 GIF 검색이 맞는지는 기술이 아니라 조직 문화 판단 — **실무협의회 채택 논의에서 오히려 감점 요인이 될 수 있다**. ①②③과 묶지 말고 **가장 뒤로**, 채택 이후 별건으로.
  - **권장 순서**: ① 이모지 → ② 서식(정제 포함) → ③ 이미지(저장소 0원 경로 실측 후) → ④ GIF(보류). ②③ 착수 시 Claude가 저장 모델·정제 규칙·푸시 평문 발췌 스펙 먼저.
  - **[2026-08-18 ④ Tenor 판정 갱신]** "학교 공식 메신저에 GIF 검색은 채택 감점" 논거는 사용자 반증으로 철회 — 학교 도메인 구글 챗에 GIF 검색(Tenor)이 이미 내장·일상 사용 중임을 실기기 스크린샷으로 확인(같은 Workspace 전례라 문화 논거 무효, Tenor는 구글 소유라 위탁 상대도 기존 계약 생태계 안). 후순위 별건 지위는 유지(선행 = GIF 첨부). 상세 = memo_richtext_spec §1.
  - **[2026-08-18 사용자 발상 — 인라인 이미지(글자 사이에 끼는 이미지·GIF)]** 쿨메신저·경기도교육청 메신저처럼 이미지가 본문 박스 안에 줄글처럼 들어가야 하고, 특히 GIF는 글자 사이에 끼어야 의미가 있다는 지적(현행은 첨부가 본문 아래 별도 칸). Claude 판정 = 모바일 걸림돌 아님 + 설계는 "첨부를 가리키는 참조 토큰"(새 저장소·권한·파기 경로 0, 외부 URL 이미지는 보안상 불성립) — 설계 방향 확정 기록 = memo_richtext_spec §13, 착수는 v1 화면 완료 후 사용자 결정.
  - **[2026-08-18 v1.1 실기기 확인 통과 — 쪽지 서식 트랙 전면 종결 ✅]** 인라인 이미지·GIF 본문 재생·커서 삽입·"본문에 들어감" 표시 전부 정상(사용자). 제3자 차단도 실기기 확인 — 사용자가 연 것은 첨부 칸의 드라이브 원본 경로라 구글 권한 장치(액세스 요청 페이지)가 막았고, 본문 프록시 경로는 서버 403(셀프테스트 5케이스 기증명): **이중 방어 양쪽 다 실동작 확인**. 드라이브 "액세스 요청" 버튼은 hmnotice@(발신 전용, 사람이 안 봄) 수신이라 승인 경로 없음 — 소음일 뿐 구멍 아님.
  - **[2026-08-18 실기기 확인 통과 (사용자)]** 즉시 반영 서식 8종·이모지 판·번호 자동 잇기·평문 붙여넣기·GIF 첨부·옛 쪽지 평문 유지 전부 정상 — §12 순서 3 종결. 같은 자리에서 사용자 지시로 **v1.1 인라인 이미지 착수** → 코어(파서 토큰·직렬화기 IMG·strip·참조 수집)+바이트 프록시(GET /api/memo/attachment, 발신·수신자만·GIF 재생 경로)+상세 뷰 2곳 배선 당일 완결(스펙 §13 구현 노트), 작성기 배선은 Antigravity.
  - **[2026-08-18 사용자 확정 — 편집기는 즉시 반영(WYSIWYG)으로]** v1 토큰 삽입형 실기기 확인 중 "가능하면 안 갈 이유가 없다"로 전환 확정 → 스펙 §7 개정 + 직렬화기 코어(richtext_dom.ts, 리터럴 이스케이프 의무·중첩은 바깥만·붙여넣기 평문·셀프테스트 20케이스) 당일 완결, 화면 배선은 Antigravity. 저장·렌더·규칙 불변(입력창만 교체).
  - **[2026-08-18 스펙 확정 ✅]** [`docs/memo_richtext_spec.md`](./docs/memo_richtext_spec.md) — 핵심 결정: 저장 = 제한 마크다운 서브셋 `md1` + `contentFormat` 필드(부재=평문 — 옛 쪽지 재해석 금지로 하위호환), 렌더 = React 노드 파서(`dangerouslySetInnerHTML` 0 유지 — XSS를 정제가 아니라 구조로 차단), 푸시 = 제목만 실어 무변경(코드 실측). 이모지는 이미 성립(유니코드) — 남는 건 피커 편의뿐. GIF 2갈래 판정: 첨부 확장은 설계 확정(스펙 §9, 리사이즈 우회·바이트 서명), Tenor 검색은 보류 확정(재론 조건 = 기관 채택 이후). 위 ①~④ 권장 순서 중 ③은 완결, ①은 무단계화, 남은 실작업 = ② 서식 v1(코어 Claude→화면 Antigravity, 스펙 §12).

- ✅ **교직원 내선번호 추가** — teacher_profiles extension 필드 및 조직도/수신자 노출 완료 · docs/memo_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R04 (2026-08-18 이관)

- ✅ **쪽지 쓰기 개편 v1.1** — 기구현 실측 확인(MemoSection §11-1·§11-2 개편 2026-08-13 반영분, 2026-08-15 Antigravity 전수 확인·Claude 재검증) · docs/memo_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R05 (2026-08-18 이관)

- ✅ **플랫폼 쪽지 (사내 메신저)** — 1단계 MVP 구현 및 실기기 시연 통과 · docs/memo_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R06 (2026-08-18 이관)

- ✅ **중간점검 (midterm review)** — 5축 전부 종결(①②트리아지·보안 / ③운영 리스크 / ④UX 함정 / ⑤방향 역산) · docs/midterm_direction_2026-08-14.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R07 (2026-08-18 이관)
- ✅ **기간제 교사 수업 인수인계 프로세스** — 이관 마법사 전면 완결(코어·화면·양방향 실기기 왕복 검증) · docs/substitute_handover_spec.md
* **학기 중 휴직 → 기간제 교사 수업 인수인계 프로세스 (2026-08-11 사용자 발상, 미착수 메모)**: 학기 중 병가·휴직 등으로 교사가 빠지면 일정 기간 기간제 교사가 와서 **하던 수업을 그대로 가져가는** 실무 프로세스가 학교에 상존 — 이 흐름이 플랫폼에 있어야 함. 스펙 미작성, 착수 시점 미정. 건드리는 모듈(예상, 스펙 때 확정): ① 계정 생애주기 — 기간제 계정 생성·복직 시 처리(휴직 교사 계정은 정지? 유지? 실무 확인 필요) ② 시간표 — 담당 수업(교사×과목×학급)의 기간 한정 일괄 이관 + 결보강·주간 합성·NEIS 시수 귀속 ③ 조직도·프로필 — 담임/부서 승계 여부 ④ 클래스룸 교사 교체 ⑤ 생활지도 등 권한 승계 ⑥ 복직 시 역이관(기간 만료 자동?). 참고: 사용자 본인이 현재 휴직 중이라 실무 디테일의 1차 정보원 — 스펙 착수 시 인터뷰로 시작.
  - **[2026-08-18 스펙 확정 ✅]** 사용자 인터뷰 2라운드 + 컴시간 설명서 대조(전용 기능 없음 — 성명 등록부 이름 덮어쓰기가 실무)로 [`docs/substitute_handover_spec.md`](./docs/substitute_handover_spec.md) 확정. 핵심 = 「담당 일괄 이관 마법사」 하나로 수렴(걸치는 주는 주간 변경 자동 산출 + 다음 주부터 effectiveFrom 개정판 치환 — 날 단위 인수를 기존 장치 2개 재사용으로), 담임 승계 선택지(기간제/학년부장)·조직도 배치·클래스룸 공동교사 초대(종료 시 제거)·복직 역이관. 업무분장·계정 조치·나이스는 범위 밖(실무 그대로).
  - **[2026-08-18 아이디어 — 부재자 등록]** "이 교사는 X일부터 부재" 등록 시 매주 그 수업들이 자동 결강 후보로 오르는 기능(공백기 반복 보강 처리의 자동화). 인터뷰 판단 = 있으면 좋을 수도 있으나 현행은 일과계가 처리 — 실수요 신호 확인 후 재론(스펙 §9).
  - **[2026-08-18 전면 완결 ✅ — 스펙부터 실기기 왕복까지 당일]** 코어(handover.ts·/api/workspace/handover·셀프테스트 19케이스, 2e8fac2) → 화면(마법사 5단계, dd5d530) → 검수 보정 2건(복직 클래스룸 제거 대상 d72519c·운영 학기 출처 b461f4a) → 실기기 신고 2건 즉응(교사 검색 학생 노출 42477c2·역이관 문구 반전) → **실기기 왕복 검증 통과**: 지상인→이현준(개정판·담임 1-5 승계·클래스룸 초대 2건) 후 역이관으로 전 항목 원상 복귀 실측(걸치는 주 0건 = 알림 0 경로). 첫 실전 동반 검증은 STATUS 「날짜가 오면 확인」 존치.
- ✅ **로그인 화면 개인정보 안내 박스** — 사전 고지 요약 및 privacy 전문 링크 박스 구현 완료 · src/app/login/page.tsx
* **로그인 화면 개인정보 안내 박스 (✅ 구현 완료 2026-08-05)**: 로그인 버튼 아래에 처리 안내 요약 + `/privacy` 전문 링크 박스 표시 — 인증 전 사전 고지 보강. 단 "로그인 시 동의 간주" 문구는 넣지 않는다(이 시스템은 고지+명시적 확인 구조가 원칙 — 간주 방식은 증빙·재고지·비자발적 이용자 문제로 기각, project_notes 2026-08-05 판단 기록 참조).
- ✅ **앱 설치 안내 상설 메뉴** — 교사용 PWA 설치 3단계 안내 화면 배포 · docs/pwa_installation_guide.md
* **앱 설치 안내 상설 메뉴 (2026-08-05 사용자 확정, 구현 완료 ✅)**: 관리자 화면에 전 교사용 메뉴 "앱으로 설치하기"를 상설 — `docs/pwa_installation_guide.md`의 3단계(설치 → 컴퓨터 로그인 시 앱 실행 → 작업표시줄 고정)를 화면화 (`PWAInstallGuideTab.tsx`). 스크린샷 3장은 ✅ 2026-08-07 종결 (사용자 제작 iorad 튜토리얼 2754580 임베드로 갈음, 자리 표시 3박스 소멸). UI 문구에 PWA 등 개발 용어 금지.
- 💡 **아이폰 PWA 설치 안내 반영** — PWAInstallPrompt / PWAInstallGuideTab 문안 추가 안
* **아이폰 설치 안내를 화면 안내에 반영 (2026-08-11 Claude 제안, 미확정)**: 아이폰 전 경로(설치→로그인→알림)가 실기기 검증된 것을 계기로, 로그인 화면 `PWAInstallPrompt`와 관리자 "앱으로 설치하기" 메뉴(`PWAInstallGuideTab`)의 안내가 현재 일반 문구뿐이므로 **아이폰 단계별 안내를 추가**: Safari 필수(카톡 내장 브라우저 불가)→공유 ⬆️→홈 화면에 추가("웹 앱으로 열기" 체크 유지)→앱에서 로그인→알림 받기. 완성된 문안은 `docs/web_push_spec.md` §11-3 그대로 사용. UI 문구 개발 용어 금지 규칙 적용.
- 💡 **대시보드 미처리 건수 카드** — 단순 바로가기를 대기/미처리 건수 카드로 승격
* **대시보드 할 일 건수 카드 (2026-08-06 원칙 신설의 후속)**: 대시보드 카드 나열 원칙(project_notes [2026-08-06] 메뉴 재구성 핸드오버 ④)에 따라, 단순 바로가기 카드를 "대기·미처리 건수" 카드로 승격 — 프로필 승인 대기 N건, 미처리 생활지도 사안 N건, 수업교환 신청 대기 N건 등. 건수 표시가 붙기 전까지 기존 카드는 잠정 유지.
- ✅ **목표 지향 징검다리 체인 교체** — 역방향 탐색 및 direct_commit_batch 일괄 반영 구현 완료 · src/components/admin/timetable/DirectSubstituteTab.tsx
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R08 (2026-08-18 이관)
- ✅ **학사일정 기반 주차 자동 파생** — 학사일정 등록 시 주차별 운영 자동 파생 기능 배포 · docs/pre_opening_3features_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R09 (2026-08-18 이관)
- ✅ **동시수업 교체 불가 지정** — 후보 산출 제외 및 등록부 UI/배지 구현 · docs/pre_opening_3features_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R10 (2026-08-18 이관)
- ✅ **특별실 점유 제약 엔진 반영** — 공유 자원 장소 충돌 방지 후보 하드 제외 및 등록부 배포 · docs/pre_opening_3features_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R11 (2026-08-18 이관)
- ❓ **대시보드 최소 구성 (주간시간표 + 급식)** — 교사·학생 포털 메인 카드 세트 개학 준비
* **대시보드 최소화 — 교사·학생 메인 홈은 당장 "본인 주간시간표 + 급식 메뉴"만 (2026-08-07 사용자 지시)**: 개학 시점의 교사·학생 대시보드는 복잡한 카드 나열 대신 **본인 주간시간표와 오늘/이번 주 급식 메뉴 두 가지만** 보이게 단순화. 급식은 알리미 0원 경로 항목의 나이스 급식식단정보 API 재사용(학생·교사 공용). 기존 "대시보드 할 일 건수 카드" 아이디어는 폐기 아님 — "당장은" 최소 구성으로 가고 건수 카드는 추후 재검토. 학생 포털엔 StudentTimetableCard 기마운트라 급식 카드 추가가 핵심, 교사 대시보드는 구성 교체 필요.
- 💡 **효명고 알리미 (0원 경로)** — 학생 포털 내 나이스 급식 API + 웹 푸시 연동
* **효명고 알리미 (2026-08-06 사용자 제안 → 같은 날 네이티브 앱은 접음 — 무료 원칙 위배: Apple 연 $99, Google Play도 1회 $25)**: 대신 **기존 학생 포털(설치형 웹앱)에 같은 기능을 얹는 0원 경로**로 보존 — ① 오늘의 급식 카드(나이스 교육정보 개방 포털 급식식단정보 API — 공공 무료, 인증키만 발급) ② 자기 반 시간표 변경 시 **웹 푸시 알림**(안드로이드는 완전 지원, 아이폰도 홈 화면에 설치한 웹앱이면 iOS 16.4+부터 지원 — 스토어·연회비 전혀 불요). 학년·반 설정은 학생 계정 학번에서 자동 유도 가능해 최초 설정조차 생략 여지. 착수는 개학 안정화 후.
- ✅ **기초시간표 개정판 주차별 적용** — 이미 구현 실재 확인(2026-08-18 유령 정리): 개정 탭 effectiveFrom 입력(BaseRevisionTab)·주차별 합성(server loadBaseGridsByWeek가 effectiveFrom≤주 시작일 개정판 순서 적용)
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R12 (2026-08-18 이관)
- ✅ **Firestore users 문서 생애주기 정비** — 삭제 동반 정리·자가치유·유령 정리 완결 · 23516e0
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R13 (2026-08-18 이관)
- ❓ **교사 승인 안내 메일 알림** — 수퍼어드민 승인 시 이용 안내 메일 자동 발송
* **교사 승인 알림**: 수퍼어드민이 일반 교사를 승인(`role: "teacher"`, `isApproved: true`) 처리하면, 즉시 해당 교사에게 사용 가능 안내 메일을 발송.
- 💡 **학적 변동 타임라인 뷰** — 전입·진급·전출·졸업 학생 생애주기 시각화
* **학적 변동 타임라인**: 특정 학생 검색 시, 이 학생이 언제 전입했고, 몇 학년에 진급했으며, 언제 전출 또는 졸업했는지 전체 생애주기 이력을 타임라인 뷰로 시각화.
- ❓ **담임 전용 학생 비밀번호 초기화** — 담임 교사의 본인 반 학생 임시 비번 직접 초기화
* **교사 전용 비밀번호 초기화**: 담임 교사가 정보부에 연락할 필요 없이 본인 반 학생의 임시 비밀번호를 웹앱에서 즉시 초기화하여 부여.
- 💡 **크론 발송 모니터링 대시보드** — 메일/구글챗 발송 성공·실패 통계 차트
* **크론 발송 모니터링 대시보드**: 매일 밤 구동되는 크론 배치에서 메일 및 구글챗 발송에 성공/실패한 통계를 차트와 테이블로 관리자에게 제공.
- 💡 **학부모 연락처 데이터베이스** — 학부모 연락처 수집·보관 및 알림톡 연동 기반
* **학부모 연락처 데이터베이스**: 학부모 전화번호/메일을 별도로 관리하여 향후 카카오 알림톡 서비스 발송 대상자로 연동.
- 💡 **타 학교 도입용 범용 위자드 툴** — 1회성 초기 계정·OU·학생 세팅 웰컴 마법사
* **범용 위자드 툴**: 효명고 플랫폼 완성 후 타 학교가 최초 도입 시, 1·2·3학년 전체 OU 및 초기 학생 정보를 쉽게 세팅할 수 있는 단계별 웰컴 마법사 메뉴 제공.
- 💡 **타 학교 복제 구축 전 "학교 프로필" 단일화** — 효명고 고유값 설정 단일화 및 위자드 셋업 기반 분류
* **타 학교 복제 구축 전 "학교 프로필" 단일화 — 효명고 고유값 실측 분류 (2026-08-13 사용자 방향 확인 + Fable 전수 스캔 부산물)**: 확장 모델 확정(사용자 정정 반영) = **멀티테넌트 아님.** 우리 서버에 타교를 추가하는 것이 아니라, **다른 학교가 같은 구조를 자기 소유(자기 Firebase·Vercel·GWS)로 똑같이 구축하도록 돕는 것** — 즉 학교당 독립 배포이고, 일반화 대상은 GWS 관리(계정 생애주기·조직) 기능 위주, 시간표·쪽지·생활지도는 비대상. 이 전제에서 문제의 형태는 "다른 학교 데이터가 효명고로 새는 것"이 아니라 **"새 학교가 설치할 때 코드 곳곳에 박힌 효명고 고유값을 일일이 찾아 바꿔야 하는 것"**이다. 전수 분류 결과, 새 학교 설치 시 반드시 바꿔야 하는 값: ① 안내 메일·챗 원문("효명고등학교 드림"·문의처 `playviolin@` — lifecycle 라우트·TeacherLifecycle·GraduationTab·transfer-deadline) ② 발신 계정 `hmnotice@` 폴백(lifecycle·webpush) ③ 포털 URL `portal.hmh.or.kr` 폴백 ④ 보호 계정 목록(`blockedOu.ts`) ⑤ 교사 그룹 기본 목록(`teacherGroups.ts`) ⑥ **학생 판별 정규식 `5자리@hmh.or.kr`**(sync-user·admin.ts — 타교는 학번 체계가 다를 수 있어 가장 본질적) ⑦ `firestore.rules` `isSchool()` 도메인. **지금 리팩터하지 않는다** — 확장 착수 시 위의 "범용 위자드 툴"과 한 묶음으로, 흩어진 고유값을 **설정 한 곳(school profile: 도메인·학교명·담당자 연락처·발신 계정·포털 URL·보호 계정·학번 형식)**으로 모아 위자드가 채우게 하는 것이 정공법이고, 이 분류가 그때의 체크리스트다. 판단 목록의 "학번 정규식 3변종 11곳 단일화"는 이 관점에서 가치가 올라감(모듈 한 곳만 바꾸면 되는 구조 선행).
- ✅ **수업교환 사전 양해 워크플로** — phase9b §13 스펙 반영 및 양해 개방 연동 · docs/consent_swap_opening_spec.md
* **수업교환 사전 양해 워크플로 (3단계)**: 현장 관행("먼저 물어보고 오케이 받으면 신청")을 플랫폼에 담는다 — ① 양해 요청 이미지 클립보드 복사 + 교체안 임시저장(즉시 구현) ② 본인 구글 챗 DM 발송(준비 완료 상태로 보류) ③ 플랫폼 내 동의→자동 신청(오픈 게이트 후). 상세 스펙: phase9b_spec.md §13 (2026-08-04 사용자 제안).
  - **[2026-08-15 저녁] 쪽지 2단계(이미지 첨부) 스펙 작성 완료 — [`docs/memo_attachment_spec.md`](./docs/memo_attachment_spec.md)**: 사용자 확정 = **전부 Drive**(쪽지엔 참조만). 저장 소유 = hmnotice@ Drive 「플랫폼 첨부/쪽지/YYYY/MM」(폴더 체계화 아이디어 흡수), 권한 = 수신자 개별 reader(전 교직원 공지만 도메인 링크), staging 대조로 타인 파일 끼워넣기 차단, 파기 = 쪽지 크론이 Drive까지. **아래 「양해 쪽지 전송 버튼」 아이디어도 이 스펙 §5로 흡수** — §5c-9-3 개별 양해 카드에 [양해 쪽지 보내기] 버튼(카드별·일괄 금지). 구현 착수는 사용자 결정.
  - **[2026-08-15 사용자 발상] ②를 쪽지 인프라로 승격 — "양해 쪽지 전송" 버튼**: 쪽지 이미지 첨부(§2 쪽지 2단계 — 용량 문제로 Drive 저장 방식)가 구현되는 대로, 기존 양해 요청 이미지를 클립보드 복사 대신 **"양해 쪽지 전송" 클릭 한 번으로 상대 교사에게 쪽지(이미지 첨부)로 발송**하고, 동시에 **구글 챗으로는 텍스트만**("어느 수업을 어느 수업으로 변경 가능한가요?" 류) 전송. 발송 주체는 교체를 원하는 교사 본인(클릭 시점) — 시스템이 조율을 대행하는 게 아니라 본인 발신 경로를 한 번의 클릭으로 묶는 것이므로 「시스템은 정보 전달까지만」 원칙과 정합. 선행 = 쪽지 이미지 첨부 구현(A5 쪽지 v1.1 계열).
    - **[2026-08-15 사용자 지적] 묶음(동시수업) 이동 후보에는 양해 이미지 복사가 없다 — 의도적 제외가 아니라 미구현**: 공유 카드는 **상대 교사 1명 단위**로 묶여 있는데(그룹 키 = `counterpartEmail`, `ConsolidatedShareData.counterpartName`), 묶음 이동 후보는 상대가 **반마다 달라 `counterpartEmail`이 빈 문자열**(`mapSimulMoveCandidates` 규약 — chain·직권 커밋과 동일)이라 기존 그룹화에 태울 수 없다. 붙이려면 카드가 **반별 전개(steps)를 담는 새 모양**이어야 하고, 받는 사람도 여럿이라 "누구에게 보낼 카드인가"(당사자별 개별 카드 vs 전체 명단 1장)를 먼저 정해야 한다. 양해 이미지 자체는 없앨 기능이 아니라 **위 최종 형태(①)로 남는 기능**이므로, 알림 센터 도입 전까지는 묶음 이동만 이미지 없이 수동 양해로 남는다.
    - **[2026-08-15 사용자 수정] ②의 구글 챗 텍스트 → 알림 센터의 [수락] 버튼으로 대체**: 상대가 받는 쪽이 "챗 텍스트를 읽고 따로 답하는" 구조보다 **알림에 양해 요청이 뜨고 거기서 바로 수락**하는 편이 낫다는 판단. 상세·설계 논점은 위 「알림 센터」 항목의 2026-08-15 발상 서브불릿에 기록(§13-3 도입 경로와 동일 자리, `consentStatus` 전이로 수동 체크 대체). 따라서 이 항목의 최종 형태 = **① 쪽지로 양해 이미지 전송 + ② 알림 수락 버튼**, 구글 챗은 발송 수단에서 빠진다. 선행 = 쪽지 이미지 첨부 + 알림 센터 구현 둘 다.
    - **[2026-08-18 Claude 판정 — ①(양해 쪽지 전송 버튼) 폐지, ②는 구현 완료로 종결]**: 알림 양해 왕복(부탁 한 줄·[양해합니다]/[어렵습니다]·거절 사유·제출 효력, 교사 포털·직권 동등)이 2026-08-18 구현·종결되어 ①은 같은 행위의 **열등한 병행 채널**이 됨(consentStatus 전이·제출 효력 없음, 기관 채택 모델의 병행 채널 금지와 충돌) — 폐지. 근거 상세 = [`docs/memo_attachment_spec.md`](./docs/memo_attachment_spec.md) §5. "양해 이미지 만들기/카드 복사"(구두 관행 보조)는 존치, 이미지를 쪽지로 보내고 싶으면 쪽지 2단계의 일반 첨부 UI(§4)로 수동 가능.
    - **[2026-08-15 사용자 지적 3연발] 묶음 이동에서 「양해를 구할 시간과 재료」가 통째로 빈다 — 세 증상, 뿌리 하나**: 사용자 질문 *"묶음 수업 이동은 왜 담아두기가 없고 바로 배정이야? 그럼 대체 언제 양해를 구해?"* · *"양해 이미지 복사는 일부러 안 넣은거야?"* · *"미리보기 시간표가 비어있어"*. 실코드 대조 결과 **세 증상 모두 같은 원인** — 화면·문서·미리보기가 전부 **"상대 교사 1명·단건 교체"를 전제**하는데 묶음 이동은 **상대가 반마다 다르다**(`counterpartEmail = ""`, 실제 상대는 `coordination.simul.steps`가 원본 — `mapSimulMoveCandidates` 규약).
      - **증상 ①(담기 비대칭)**: 교사 화면에는 묶음 후보도 **담기가 된다**(`handleSelectCandidateAndSave`에 simul 가드 없음, 제출 시 `simul_move_create`로 분기 — 2026-08-15 A조치). **직권 탭에만 담기가 없다**(같은 날 B조치로 버튼 숨김) — 서버가 담기 일괄 경로(`direct_commit_batch`→`directCommit`→`createSwapRequest`)에서 묶음을 명시 거부하기 때문. 즉 일과계 직권에는 **양해를 구할 시간 창구가 구조적으로 없다.**
      - **증상 ②(담아도 안 보임)**: 담긴 묶음 초안이 **예상 시간표에 그려지지 않는다** — `buildVirtualChanges`가 `simul_move`를 조기 return(§5c-4, 반별 n건이라 단건 오버레이로 표현 불가). 더 나쁜 것은 **초안 경로에는 그 가드가 없다**: `loadMyVirtualOverlay`의 초안 루프가 전부 `type:"swap"`으로 밀어 넣어, 묶음 초안이 `counterpartEmail=""`인 **가짜 단건 swap**으로 새어 들어가 화면 변화는 0인데 "N건 반영" 건수만 부풀린다.
      - **증상 ③(상대 시간표 빈 표)**: `fetchPreviewForCandidate`가 `!cand.counterpartEmail`이면 즉시 return → 미리보기 셀이 전부 null인데 패널 제목은 **"🔍 상대 시간표 미리보기"** 그대로라, 사용자에게는 *"상대 시간표가 텅 비어 있다"*로 읽힌다(실제로는 상대를 특정하지 못한 것).
      - **해결의 열쇠는 이미 있다**: 초안 `candidate.coordination.simul.steps`가 **저장되고 있다**(`draft_save` 페이로드에 `coordination: sc.coordination` 포함). 즉 반별 전개를 다시 계산할 필요 없이 **오버레이·카드·미리보기를 steps 기반으로 그리면 세 증상이 함께 풀린다.** 착수 순서 제안 = ⓐ 담기 오버레이 steps 표현(②) → ⓑ 양해 문서·미리보기 모양 확정(③ + 위 「양해 이미지」 항목) → ⓒ 직권 담기 개방(①, ⓐ가 선행돼야 미리보기가 거짓말을 안 한다).
      - **✅ 사용자 결정 완료 (2026-08-15, 같은 날) → 스펙 §5c-9로 확정**: 「**바꾸는 사람은 전원을, 받는 사람은 자기 것만**」 — ⓐ 양해 문서(이미지) = **받는 사람 자기 것만**, 당사자 수만큼 개별 카드 ⓑ 미리보기 = **당사자 드롭다운**으로 영향받는 전원 중 골라 보기(사용자 발안 — 4명이 얽힌 교체를 판단하려면 전원을 봐야 한다) ⓒ 직권 담기 = **교사와 똑같이 개방**. 상세 설계·완료 판정은 `docs/consent_swap_opening_spec.md` §5c-9. *이 줄은 「결정 대기」였던 원문을 대체한다 — 대기 표기를 그대로 두면 유령이 된다(memory: 대기 표기 불신).*
    - **✅ [2026-08-15 사용자 지적 → 같은 날 해소, `9bb251e`] 묶음 교체가 한쪽 방향에서만 보였다** — 스펙 §5c-10으로 확정·구현. 역방향(내 일반 수업 → 묶음 자리)이 조율 필요 후보로 나오고, 신청은 canonical(그룹이 움직이는 방향)로 뒤집어 저장돼 승인·revert·중복 차단이 방향을 모른다. 검증 [11] 전건 통과 + 신고 자리(권성민 금1→화7) 실측 해소. 잔여 = 역방향 관점의 화면 문구(Antigravity, §5c-10-2). 아래는 해소 전 원문:: 실화면 대조로 확인. 김진만 선생님이 자기 묶음 수업(2-2반 일화 화7)을 클릭하면 금1(권성민 영Ⅱ)로 가는 묶음 이동 후보가 **뜬다**. 그런데 권성민 선생님이 자기 수업(2-2반 영Ⅱ 금1)을 클릭하면 화7이 **아예 후보로 안 뜬다** — `findSwapCandidates`(swap.ts:390)와 `findCrossSwapCandidates`(swap.ts:527)가 **목적지 셀에 묶음 수업이 있으면 하드 제외**(`if (l2.simul) continue`)하기 때문이다. **두 방향은 같은 연산이다**(같은 반들이 같은 두 교시를 주고받고, 양해 당사자도 4명으로 동일).
      - **사용자 판단(중요)**: *"교사들이 실제 수업교체를 할 때 더 자주 벌어질 일은 후자다. 내가 이 수업을 저기로 옮기고 싶어. 근데 수업이 묶여 있대. 근데 꼭 옮겨야 해. 그래서 여러 명의 양해를 구해서라도 옮기는 상황."* → 즉 지금 열려 있는 방향(묶음 담당 교사가 시작)은 **덜 흔한 쪽**이고, 정작 주된 수요인 **"내 일반 수업을 묶음 자리로"**가 막혀 있다.
      - **해결 스케치(엔진 신규 로직 0)**: 일반 소스 (d1,p1)에 대해 목적지 (d2,p2) 셀이 묶음 G를 담고 있으면, **방향을 뒤집어** `findSimulGroupMoveCandidates(G, source=(d2,p2))`를 호출하고 그 후보 목록에 (d1,p1)이 있는지만 본다 — 있으면 조율 필요 후보(kind simul)로 노출. 커밋도 기존 `simul_move`(source=묶음 슬롯, target=클릭한 슬롯) 그대로라 원장·되돌리기·알림 무변경.
      - **선행 판단 필요**: ① `resolveSimulMoveSource`의 소유 검증 확장 — 신청자가 그룹 담당이 아니라 **목적지에서 밀려나는 상대 교사**인 경우를 허용해야 한다(지금은 "본인의 수업만" 거부) ② 화면 문구가 뒤집힌 관점이어야 한다(*"이 자리에는 여러 반이 함께 듣는 수업이 있습니다. 옮기려면 N분의 양해가 필요합니다"*) ③ 하드 제외를 푸는 것이 아니라 **호출부에서 분기**해야 단건 교체·체인·보강의 방어가 유지된다(§5c-7-3과 같은 형태).
- ⏸ **공동교사 원클릭 탈퇴** — 비가역 정리 기능 보류 (필요성 재제기 시 스펙 작성)
* **공동교사 원클릭 탈퇴 (2026-07-26 사용자 건의, 보류 → 2026-08-05 감사에서 보관소 승격)**: 학기말 정리에서 비소유 클래스룸은 알림 제외로 처리했지만(구현 완료), 공동교사 상태 자체를 클릭 하나로 탈퇴하는 기능은 비가역 작업이라 보류됨. 필요성이 다시 제기되면 이중 확인 + 대상 제한을 갖춘 별도 스펙으로 설계 (archive/project_notes_2026-07.md의 [2026-07-26] 고아 폴더 세션 "보류: 건의 2번" 항목 참조).
- ✅ **PWA 설치형 웹앱 및 자발적 자동 실행** — 자발적 설치+iorad 튜토리얼 안내 배포 완결 · docs/pwa_installation_guide.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R14 (2026-08-18 이관)
- ✅ **모바일 전용 경로 /m 구축** — 동일 도메인 모바일 전용 경로 스펙 확정 및 구현 · docs/mobile_m_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R15 (2026-08-18 이관)
- ✅ **양해 기반 수업 교체 개방** — 서버·UI 전면 완결(Phase 1·2 + Phase 3′ 통 이동/조율 후보 렌더/신청 배선/당사자 선택/안내 문구) 및 실데이터 검증 통과 · docs/consent_swap_opening_spec.md
* **양해 기반 교체 개방 — 체육 실무 답변으로 설계 전제 전환 (2026-08-08 보류 질문 → 2026-08-10 체육교사 통화로 해소 → 同日 스펙 v1 확정 ✅ [`docs/consent_swap_opening_spec.md`](./docs/consent_swap_opening_spec.md), Phase 1 서버부 구현 완료 ✅ 2026-08-10 — 엔진 이원화(includeCoordination)+consent 검증·실측 590건 개방. **2026-08-15 실측 대조: Phase 1 UI·Phase 2 체인 개방(서버 `9d79ef0`+UI+검수)·§3-2 실기기 배치 전부 완결 — 로드맵 잔여 표기가 낡아 있었음(유령 2건). §4-1 공강 개방은 2026-08-11 철회로 종결. 실잔여 = Phase 3 통 이동 하나(상세 스펙 §5b 확정 ✅ 2026-08-15, **서버부 구현 완결 ✅ 同日** — ChangeType "move" 모델·합성기·NEIS 평탄화·웹푸시 확장 + `findSimulGroupMoveCandidates` 엔진 + `commitSimulGroupMove` 원자 커밋 + manage 액션 2종(`simul_move_candidates`·`simul_move_commit`) + revert requestId 묶음 확장, 실데이터 검증 `scripts/verify_simul_move_phase3.ts` 2사이클 전 항목 통과 — 잔여 = §5b-5 직권 UI(Antigravity) + Claude 표적 검수 §5b-6) + S4 체인 조율 허용(Phase 3 뒤 실수요 관찰 후 보류) — 8월 Fable-헤비 2대 축 중 이 갈래는 Phase 3 UI만 남음**): **실무 확인 결과** — ① 구장 수업 교체는 원칙 차단이나 **당사자 양해로 풀림**(체육교사가 "체육관 합반" 또는 "교실 수업" 선택해 동의하면 신청 진행) ② 동시수업 묶음을 **통으로 옮긴 실사례 존재** ③ 조율 주체는 일과계가 아니라 **교체 원하는 교사 본인**(일과계가 하면 책임 전가 구도 — 실무 관행). **설계 방향 4축**: ⓐ 후보 이원화 — 깨끗한 후보(현행) + **조율 필요 후보**(충돌 종류·양해 필요 당사자 명시, 신청 시 "양해 받음" 확인 단계 — **양해 기록이 책임 보호의 핵심**) ⓑ 공강 탐색·징검다리 체인을 일반 교사에 개방 — "탐색은 모두에게, 확정 권한은 역할대로"(교사=신청→승인, 일과계=직권 즉시) ⓒ 동시수업 묶음 그룹 통 이동 연산(모델 확장, 대형) ⓓ 사전 양해 워크플로(phase9b_spec §13)를 중심 축으로 승격 — 기존 §F 하드 제외·§A 차단은 "양해 게이트"로 완화. 사용자: "컴시간알리미 이 참에 이겨보자." 착수 시 Claude가 현행 후보 엔진·체인 구조 실측 후 스펙 작성.
  - **[2026-08-18 상태 정정 — 양해 기반 수업 교체 개방 전면 완결 ✅]** §5c-7 UI 재배선(TeacherPortalSection.tsx의 simul_move_create 신청 연동, DirectSubstituteTab.tsx의 getCoordinationParties 당사자 선택, CoordinationNoticeBlock.tsx의 묶음/장소 조율 안내 문구 분기)이 완료되어 양해 기반 교체 및 통 이동 전 과정이 구현 완료되었다.
- 💡 **징검다리 체인 탐색 실패 사유 표시** — 이동 불가 지배 사유(특별실/구장 충돌) 반환
* **징검다리 체인 탐색 실패 사유 표시 (2026-08-08 발상, 소형)**: 현재는 "N단계 안에서는 경로가 없습니다"만 출력 — 실패의 지배적 원인(예: 목적지 점유 수업이 특별실·구장 §F 하드 제외로 이동 불가 0건)을 서버가 알고 있으므로 한 줄 사유를 함께 반환·표시하면 일과계가 헛된 재시도(3단계 확장 등)를 안 하게 됨. 실증 사례: 1-9반 한국 월2→목2, 목2 체ⅠB(탁구장)가 전 후보 슬롯 구장 충돌로 이동 불가 → 어떤 깊이로도 원천 불가.
- ✅ **주간 목록 완전 자동화 및 노출 주수 설정** — 과거 주 제외 및 학사일정 마스터 동기화 배포 · eaa0b56
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R16 (2026-08-18 이관)
- ✅ **학사일정 개편 통합 및 나이스 마스터 자동 수집** — 통합 스펙 v2 확정 및 나이스 크론 수집기 가동 완결 · docs/calendar_events_taxonomy_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R17 (2026-08-18 이관)
- 💡 **교사별 개인 시간표 Calendar API 직접 동기화** — 지연 구독 ics 대신 Calendar API 직접 쓰기 B안
* **교사별 개인 시간표 캘린더 (2026-08-10 규모 판정 → 같은 날 사용자 결정: A안 폐기, 하려면 B안)**: ~~A안(개인 토큰 ics 구독)~~ **기각** — 구글 폴링 지연(반나절~하루) 때문에 당일·전일 교체가 늦게 반영되는데, 학사일정과 달리 **시간표는 하루 늦으면 "틀린 정보"가 되는 데이터**라 지연 구독은 없느니만 못함(사용자 판단). 착수한다면 **B안(Calendar API 직접 쓰기)만** — 교체 확정 시 각 교사 캘린더 이벤트 즉시 생성·수정·삭제. 필요 요소: DWD calendar 스코프(콘솔, 선배포 금지 원칙)·수업↔이벤트 매핑 동기화 엔진(중~대형, 며칠 단위)·**교시별 시각 = Workspace 환경 설정 §9 "일과표" 기구현**(OUConfiguration.tsx — 사용자가 선견지명으로 만들어 둠, B안 착수 시 바로 재료). 착수 시점 미정 — 실사용 수요 확인 후, 착수 시 Claude 스펙 먼저.
- ✅ **공동교육 학생 계정 포털 접속 차단** — 구현 완료 실측 확인(sync-user `isBlockedOuPath` 관문 + OUConfiguration 설정 UI, 2026-08-15 재검증) · docs/coop_account_block_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R18 (2026-08-18 이관)
- 💡 **HWP/HWPX 브라우저 미리보기 임베드 (rhwp)** — WASM 기반 클라이언트 단 HWP 뷰어
* **HWP 미리보기/편집 임베드**: 메신저·업무 기능에서 드라이브의 HWP/HWPX 파일을 다운로드 없이 브라우저에서 열람·편집. 자체 제작 아님 — 오픈소스 rhwp(MIT, Rust+WASM, `@rhwp/editor` npm 임베드) 활용. 전부 클라이언트 사이드 처리라 서버·저장소 비용 0, 문서 내용 외부 전송 없음. 순서는 뷰어 먼저 → 편집은 실제 학교 공문으로 완성도 검증 후. (2026-07-24 논의, 파일 처리 원칙과 연동 — project_notes 참조)
- ✅ **시간표 메뉴 IA 분리 (운영 vs 작성)** — 일과계 메뉴 분리 및 메타문구 일소 배포 · docs/timetable_ia_split_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R19 (2026-08-18 이관)
- ✅ **보호 계정 가드 (fb01·hmnotice·admin)** — workspace.ts 및 admin.ts 하드 차단 완결 · src/lib/google/workspace.ts
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R20 (2026-08-18 이관)
- ✅ **시간표 초안 위반 상세 조회 및 해결안 탐색기** — 접이식 패널 UI 및 fixFinder 엔진 배포 · docs/timetable_fix_assist_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R21 (2026-08-18 이관)
- ✅ **자리표시(창체·SLAT) 셀 이동 차단** — 가상 교사 연산 차단 및 개정 경로 가드 배포 · 93766f9
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R22 (2026-08-18 이관)
- ✅ **생활지도 종합 현황 부장 분석 뷰** — 학년×반 히트맵 및 단계 인원 드릴다운 배포 · docs/discipline_analytics_spec.md
  > 본문 → [archive/roadmap_2026-08.md](./archive/roadmap_2026-08.md) §R23 (2026-08-18 이관)

---

- 🚧 **시간표 화면 Firestore 읽기 다이어트 (부분 완료)** — ①서버 인메모리 캐시+③합성본 캐시 구현 완료(2026-08-16), 잔여 = ②직권 탭 이중 요청 통합 및 프로덕션 적중률 실측
* **시간표 읽기 다이어트 (2026-08-15 할당량 소진 사고 후 실측)**: 무료 한도(5만 읽기/일) 소진의 구조 확인. 실측치: 기초판 `classGrids` **30문서**·등록 주 8개·simul 21·venue 36. **후보 클릭 1회 = `computeCandidatesAllWeeks`가 8개 주 전부 합성** → 주당 기초판 30 + changes를 매번 재읽기 ≈ **클릭당 240+ 읽기**, 직권 탭은 `direct_candidates_all`+`direct_candidates` **2요청이라 그 2배**. 즉 하루 100~200클릭이면 무료 한도가 끝난다 — 8/8·8/15 소진 사고 둘 다 이 구조. **절감 지점 후보(효과 순)**: ① 기초판·주차 문서 서버 인메모리 캐시(학기 중 사실상 불변, `cacheVersion` 무효화 연동) — 클릭당 읽기를 수십으로 낮춤 ② 직권 탭 이중 요청 통합(후보+보강 한 응답) ③ 클릭 간 합성본 캐시(viewCache 계열). **지금은 급하지 않다** — Blaze 크레딧($300/90일) 기간이라 실사용 규모(교사 수십 명)로도 여유. **크레딧 만료(11월 중순) 전에 ①만이라도 넣는 것이 무료 원칙 복귀 조건.** 구현은 합성 일관성(무효화 시점)이 위험 지점이라 Claude 몫. **→ ①+③ 2026-08-16 구현 완료**(후보 경로 advisory 캐시 — 재료·등록부·컨텍스트를 버전 키로, 승인·커밋은 fresh 불변, `verify_read_diet.ts` 동등성·무효화 즉시성 통과). 잔여 = ②(직권 이중 요청 통합, Antigravity·경미)와 프로덕션 적중률 실측(x-tt-instance 방식 재활용 가능).

## 📌 3. 연동 서비스(교육과정 선택 시스템) 보안 감사 권장 사항

- 💡 **교육과정 선택 시스템 연동 보안 감사 권장사항** — HTTP 리퍼러 제한·Firestore rules 도메인 바인딩·이메일 API 인증·XSS 예방

> **과거 세션(`b2a82b0f-a881-444c-9c03-0113f51b2067`)에서 도출된 교육과정 선택 시스템(`course-selection-app`) 연동 시의 핵심 보안 조치 권장사항입니다.**

1. **Firebase API Key 및 도메인 바인딩**:
   * Firebase API Key 자체는 노출되어도 무방하나, Firebase Console에서 **HTTP 리퍼러 제한**을 추가하여 허용된 도메인(`*.vercel.app`, `localhost:3000`) 외의 비정상적인 Firestore 접근을 반드시 원천 차단할 것.
2. **Firestore Rules 보완**:
   * 데이터 노출 방지를 위해 `submissions` 및 `users` 컬렉션의 읽기/쓰기 조건에 `request.auth.token.email.matches('.*@hmh\\.or\\.kr')` 규칙을 적용하여 도메인 외의 탈취 방어선 마련할 것.
3. **이메일 API Endpoint 무단 사용 방지**:
   * `/api/email` 엔드포인트 호출 시 Firebase ID Token 검증 및 `Authorization: Bearer <Token>` 인증 헤더를 필수로 요구하고, 수신 주소도 반드시 학교 도메인으로 제한하여 외부 스팸 발송용 프록시로 악용되지 않도록 방어할 것.
4. **XSS(크로스 사이트 스크립팅) 예방**:
   * Q&A 게시판 및 이메일 내용 템플릿에 학생이 입력한 텍스트를 그대로 삽입할 때 `escapeHtml` 헬퍼 함수를 필수 연동할 것.

- ✅ **9c AI(LLM) 보조 결합 설계** — 결정론 솔버 우선 및 4역할 한정 스펙/셀프테스트 완결 · docs/phase9c_d_spec.md
* **9c에 AI(LLM) 보조 결합 — "탐색은 로직, 판단은 AI" (2026-08-11 사용자 발상 + Claude 경계 설계, 9c 스펙에 반영 예정)**: 발상 = 컴시간 로직의 100% 카피는 불가능하니 그 공백을 AI로 메꾸자. **Claude 판단: 방향 찬성하되 AI의 자리는 배치가 아니라 그 주변부** — 시간표의 정교함의 정체는 제약 충족 **탐색**이고 LLM은 하드 제약(교사 중복·시수·동시수업·특별실)을 신뢰성 있게 지키지 못함("거의 맞는" 시간표=0점). 배치·탐색은 결정론 솔버(9c 검사기+솔버 코어, 무료·로컬)가 담당하고, LLM은 ① 선호의 정식화(자연어 요구→제약·가중치 번역) ② 불능 진단(해 없음일 때 충돌 제약·완화 후보를 사람 말로) ③ 결과 설명(왜 이 배치인가) ④ 완성안 정성 비평 — 4역할로 한정. **철칙: AI 출력은 결정론 검사기를 통과해야만 시간표에 반영**(검사기가 100% 결정론이면 AI 오류가 시간표를 못 깨뜨림). **다중 계정 API 파밍(10키)은 기각** — Google 생성형 AI 약관의 할당량 우회 금지 위반이고, 적발 시 학교 워크스페이스 도메인 전체(Gmail·Classroom·DWD = 플랫폼 기반)가 어뷰징 리스크를 지는 비대칭 교환. 위 4역할이면 호출량이 작성 기간에만 집중되는 저빈도라 **정식 무료 1키로 충분**(무료 원칙 정합).
  - **[2026-08-16 사용자 발상] AI 한도 3단 레버 — lite 폴백 + 본인 키 지참(BYOK)**: 무료 한도(flash 계열 전부 20회/일)가 차면 ① 같은 키로 **flash-lite(500회/일) 자동 폴백**(약관 문제 없음, 품질 실측 선행) ② 그래도 막히면 **사용자가 자기 계정에서 발급한 키를 자기 세션에 입력**해 계속. BYOK 준수 조건 3(서버 저장 금지·본인 요청 한정·자동 로테이션 금지) — 이 선을 넘으면 기각된 "다중 계정 파밍"과 같아진다. 판정: 조건 준수 시 약관 위반 아님(각자가 자기 몫의 무료 한도를 쓰는 것). "새 키는 fb01@ 발급" 원칙의 의도된 예외(인프라 키가 아니라 개인 폴백). 첫 실수요 = 시수표 추출(E5)·11월 9c 리허설.
