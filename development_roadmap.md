# 🚀 효명고등학교 관리 시스템 — 복구된 개발 로드맵 및 아이디어 보관소

이 문서는 이전 대화 세션들(`2af17987-3499-43a7-854b-92319a40b0b2`, `f6bb0e2e-851a-4d9f-ac71-3a888b7f0015` 등)의 기록을 역추적하여 유실될 뻔한 개발 계획, 아키텍처 의사결정 맥락, 보안 지적 사항 및 브레인스토밍 아이디어들을 하나로 통합 정리한 공식 로드맵 파일입니다.

---

## 📌 1. 전체 개발 로드맵 (Phase 1 ~ Phase 8)

### Phase 1. 학적 관리 핵심 기능 (완료 ✅)
* **Google OAuth + RBAC**: 이메일 정규식(`/^\d{5}@hmh\.or\.kr$/`)을 이용한 학생(`student`), 미승인 교사(`teacher`), 수퍼어드민(`super_admin`) 자동 분류 및 라우팅 가드.
* **GWS 조직단위(OU) 및 그룹스(Groups) 관리**: 계층 트리 및 반별 메일링 그룹 생성/삭제 자동화.
* **감사 로그(Audit Log)**: 시스템 내 모든 계정 생성, 수정, 삭제 이력 Firestore 적재.

### Phase 2. 교직원 계정 생애주기 관리 (완료 ✅)
* **교사 전입**: 계정 일괄/단건 생성 시 **4대 교사 연동 그룹**(`ts@`, `classroom_teachers@`, `hmhteacher@`, `hmh_teachers@`) 가입 처리 및 Firebase Auth UID 청소.
* **교사 전출**: 4대 그룹 즉시 강제 탈퇴 처리 ➔ 메일/구글챗 안내 ➔ 교사가 직접 백업 데드라인 선택(최대 1년) ➔ 데드라인 경과 시 자동 일시정지 및 30일 후 자동 삭제(크론 배치).
* **명예퇴임**: 계정을 삭제하지 않고 **'OB 보존실' OU**로 이동 및 그룹만 탈퇴하여 이메일 영구 보존.

### Phase 3. 졸업 동의서 터치 서명 및 보관 강화 (완료 ✅)
* **터치 서명 패드**: 학생 포털 내 HTML5 Canvas 기반 친필 서명 구현.
* **영구 보존 컬렉션 (`graduation_consents`)**: 유저 계정이 삭제되더라도 행정 증빙을 위해 서명 데이터를 2~3년간 별도로 안전하게 보존.
* **인쇄 지원**: 감사 제출용 A4 인쇄 CSS 미디어 쿼리 최적화.

### Phase 3.5. UX 성능 개선 및 삭제 계정 복원 (완료 ✅) — 2026-07-16
* **클라이언트 메모리 캐시 (`clientCache.ts`)**: 브라우저 탭 단위의 인메모리 캐시(TTL 5분) 모듈을 구축하여 메뉴 전환 시 로딩 스피너 없이 즉시 렌더링.
* **로그인 시점 백그라운드 프리페치**: `AuthContext.tsx`에서 로그인 완료 직후 `users:all`, `groups:all`, `orgUnits` 데이터를 100ms 딜레이로 조용히 선행 캐싱.
* **Mutation 시 캐시 자동 무효화**: 사용자 생성·수정·삭제·일괄 처리 등 9개 데이터 변경 지점에서 `invalidateClientCache("users:")`가 자동으로 트리거되어 항상 최신 데이터 보장.
* **StudentRoster / GroupList 캐시 연동**: 학생 명렬표와 그룹 목록도 동일 캐시 패턴으로 적용하여 재진입 시 즉시 표시.
* **AutocompleteInput 로컬 검색 전환**: 전출 교사 검색 등 사용자 자동완성을 서버 API 디바운스 방식(300ms)에서 캐시 로컬 필터링(50ms)으로 전환하여 체감 반응속도 대폭 향상.
* **삭제된 계정 복원 기능**: 슈퍼어드민 전용 "삭제된 사용자 복원" 탭을 `UserList.tsx`에 추가. Google Workspace 보존 기간(20일) 내에 삭제된 계정 목록을 조회하고 원하는 OU를 지정하여 복원 처리 가능. `workspace.ts`에 `listDeletedUsers` / `restoreDeletedUser` SDK 헬퍼 추가, API 라우트에 `list_deleted` / `restore` 액션 추가, 감사 로그 자동 기록.
* **전출 교사 KST 시차 버그 수정**: Vercel 서버(UTC)와 한국 표준시(KST) 9시간 시차로 인해 자정 직후 일시정지가 안 되던 문제를 날짜 문자열(`YYYY-MM-DD`) 직접 비교 방식으로 완전 수정.
* **전출 교사 강제 마지노선 1년**: 교사가 기한 선택을 끝까지 안 하더라도 전출 등록일 기준 1년 뒤에 자동 일시정지되도록 크론 스케줄러에 방어 로직 추가.

### Phase 4. 구글 클래스룸 로스터 강제 배정 (완료 ✅) — 2026-07-16
* **배경**: 유료 기능인 `Class Tools`는 학생이 클래스룸에 단순 초대가 아니라 **로스터에 수락 상태로 등록**되어야만 정상 작동함. 기존에는 `Classright` 시트 부가기능으로 수퍼어드민이 수동 처리하던 영역을 웹 앱 내로 내재화.
* **동작 프로세스 및 구현 상세**:
  1. **최고 관리자 계정 사칭(Impersonation)**: 일반 교사는 Google API 규정상 학생을 클래스룸에 직접 추가할 권한이 없어 `403 Unauthorized` 에러가 발생함. 이를 우회하기 위해 학생 추가/제거 API 실행 시 백엔드에서 최고 관리자(`GOOGLE_WORKSPACE_ADMIN_EMAIL`) 권한으로 사칭하여 강제 배정을 즉시 수행하도록 설계.
  2. **동적 학급 스캔**: 학급 수(반 개수)를 하드코딩하지 않고, 클라이언트 캐시(`users:all`)를 통해 선택 학년의 조직단위(OU) 내 학생 목록을 조회함. 학생들의 학번 패턴(예: `10203` -> 2반)을 실시간으로 파싱하여 고유하게 존재하는 반들만 드롭다운에 동적 렌더링되도록 구현. 내년 학급 변동 시에도 완벽하게 유연 대응 가능.
  3. **초대 수락 과정 생략**: 학생들이 구글 클래스룸 초대 메일을 직접 열고 수락할 때까지 대기할 필요 없이, 즉시 소속 멤버 목록에 "가입됨" 상태로 강제 안착시킴.
  4. **단어 및 UI 개선**: 학생들을 대할 때 어색하고 불필요한 은유인 '바구니'라는 단어를 '배정 대기 명단' 등으로 전면 수정하고, 사이드바를 포함한 메인 명칭을 '클래스룸 학생 강제 배정'으로 격상.
  5. **예외 처리 한글화**: 존재하지 않는 계정(404)이나 이미 가입된 학생(409) 등 구글 API가 반환하는 원시 에러들을 한글 예외 처리하여 사용자(교사) 피드백 품질 개선.

### Phase 5. 크롬 관리 북마크 즉시 배정 및 실시간 히스토리 로그 (완료 ✅) — 2026-07-16
* **배경**: 교사들이 학생들의 크롬 브라우저 상단 북마크를 관리(추가/수정/삭제)하기 위해 최고 관리자에게 요청하거나 위험한 크롬 브라우저 정책 제어 권한을 포함한 관리자 계정으로 대리 가입되는 보안 위험을 근절.
* **구현 핵심**:
  1. **맞춤형 북마크 트리 편집기 (`BookmarkTreeEditor.tsx`)**: 학교 웹 플랫폼 안에서 폴더 생성, 북마크 노드 추가/삭제 및 Drag & Drop 편집이 가능한 간편 UI 제공. 기본 닫힘 폴더 구조, 드래그 중 자동 펼치기(650ms 호버), 모달 기반 추가/수정, 삭제 재확인 등 UX 세부 구현 완료.
  2. **조직단위(OU)별 배정**: 편집된 북마크 세트를 특정 OU에 즉시 일괄 적용. `allowedBookmarkOUs` 설정으로 관리자가 허용 OU 범위를 제어하며, 드롭다운은 경로 깊이 오름차순 정렬로 최상위 OU가 기본 선택됨.
  3. **최고 관리자 사칭 API 호출**: 백엔드에서 `GOOGLE_WORKSPACE_ADMIN_EMAIL` 권한으로 구글 Chrome Policy API (`chrome.users.ManagedBookmarksSetting`)를 호출하여 학생들 기기의 크롬 정책을 강제 업데이트. 로컬 DB 백업 모드(API 미인증 시 폴백) 지원.
  4. **북마크 변경 이력 로그 (스마트 diff 뷰)**: 누가 언제 어떤 북마크를 변경(추가/삭제/순서변경)했는지 구조적으로 비교하는 diff 엔진(`computeBookmarkDiff`) 구현. 전체 트리 덤프 대신 추가/삭제/이동 항목만 표시.
  5. **AuthContext 프리페치 연동**: OU 목록을 페이지 진입 시 별도 API 호출 없이 `useAuth().orgUnits`에서 즉시 사용하도록 최적화.

### Phase 5.5. 교직원 조직도 및 내 프로필 관리 (완료 ✅) — 2026-07-16

> **기록 누락분 소급 반영 (2026-07-22)**: 이 기능은 구현·커밋이 완료되었으나 로드맵에 기재되지 않아 뒤늦게 추가함. 관련 커밋 6건 — `3feb017`, `bc10ea2`, `8571127`, `3f69bbe`, `4ee3a9f`, `b6bc8f2`.

* **교사 자기 등록 및 관리자 승인 흐름**: 교사가 본인 소속·직위 정보를 직접 등록하고, 수퍼어드민이 `ProfileApprovals.tsx`에서 승인 처리하는 구조 구축.
* **부서 관리 및 부서별 부서장 지정**: 부서(Department) 관리 기능과 함께, 부서마다 개별적으로 부서장(`isDeptHead`)을 지정하고 표시하는 기능 구현.
* **소속 없음(noDept) 처리**: 부서에 속하지 않는 교직원을 위한 `noDept` 플래그 도입. 체크 시 관련 입력 섹션이 숨겨지고, 초기 선택 상태에서 버튼이 비활성화되던 버그 수정.
* **내 프로필 카드/모달 (`MyProfileCard.tsx`, `MyProfileModal.tsx`)**: 본인 정보 조회 및 수정 UI. 직위(positions) 반복 렌더링의 JSX 문법 오류 및 셀렉트 드롭다운 텍스트 색상 문제 수정.
* **학생 포털 정보 카드**: `UserData`에 `orgUnitPath` 필드를 추가하고, 학생 포털 사이드바에 본인 소속 정보 카드를 표시.
* **스케줄 설정 및 학급 수 캐시 연동**: 학급 수를 하드코딩 대신 클라이언트 캐시에서 산출하도록 전환. 사용하지 않는 `subjects` 필드와 직위 목록의 '계원' 항목 제거.

### Phase 6. 동적 폼 빌더 및 생활지도 기록 (미구현 📋)
* **마스터 폼 빌더**: 매년 달라지는 학생 상담 및 생활지도 양식을 코딩 없이 관리자가 필드(날짜, 텍스트, 체크박스 등)를 조합해 동적으로 설계.
* **학급별 배포**: 설계된 폼을 담임 교사에게 배정하여 학생별로 관찰 기록을 누적 입력 및 조회할 수 있는 환경 제공.

### Phase 7. 카카오톡 알림톡 연동 (미구현 💬)
* **카카오 비즈니스 API**: 학생/학부모에게 계정 삭제 D-Day 알림, 학교 공지 등을 카카오톡 채널로 자동 발송.
* **계정 매핑**: 학생 포털 로그인 시 카카오 OAuth를 1회 연동하여 구글 계정과 카카오 수신 정보 동적 매핑.

### Phase 8. 교사 업무 Task 관리 시스템 (미구현 📅)
* **협업 중심 업무 보드**: 업무 관리자가 담당 교사 지정, 기한, 리소스 링크를 포함하여 Task 발행.
* **구글 서비스 연동**: `Google Calendar API` 및 `Tasks API`와 실시간 동기화하여 교사들이 캘린더나 구글 Tasks 앱에서도 완료 체크가 가능하도록 연동.

### Phase 9. AI 기반 웹 시간표 및 교체수업 편성 시스템 (미구현 🤖)
* **제약 충족 알고리즘(CSP)**: 교사 가용 시간, 교실 제한, 과목 연속 수업 차단 규칙을 반영한 최적의 시간표 생성.
* **결강 자동 보강**: 결강 발생 시 당일 공강 교사 목록을 우선순위 규칙에 따라 최적 추천.
* **구글 캘린더 연동**: 확정된 시간표를 개인 캘린더에 자동 동기화.

### Phase 10. Teaching & Learning 라이센스 대여 자동화 (미구현 💡)
* **배경**: 학교당 라이센스 보유량(15개) 제한으로 인해 교사들이 필요할 때마다 정보부 관리자에게 수동 요청하여 할당받던 비효율을 해결.
* **구글 라이센스 API 연동**: Google License Manager API (`licensing.v1`)를 연동하여 `licenseAssignments`를 백엔드에서 자동 제어.
* **실시간 캘린더 예약**: 날짜별 가용 수량(15개 - 예약 확정 수)을 시각적 캘린더에 표시하여 교사가 원하는 기간(시작일~종료일)을 선택해 셀프 대여 신청.
* **크론 연동 스케줄링**: 매일 자정 크론이 돌 때, 시작일인 교사에게 자동으로 라이센스를 할당하고 만료일이 지난 교사에게서는 라이센스를 자동 회수 처리.

---

## 📌 2. 유실될 뻔한 기타 아이디어 메모

* **교사 승인 알림**: 수퍼어드민이 일반 교사를 승인(`role: "teacher"`, `isApproved: true`) 처리하면, 즉시 해당 교사에게 사용 가능 안내 메일을 발송.
* **학적 변동 타임라인**: 특정 학생 검색 시, 이 학생이 언제 전입했고, 몇 학년에 진급했으며, 언제 전출 또는 졸업했는지 전체 생애주기 이력을 타임라인 뷰로 시각화.
* **교사 전용 비밀번호 초기화**: 담임 교사가 정보부에 연락할 필요 없이 본인 반 학생의 임시 비밀번호를 웹앱에서 즉시 초기화하여 부여.
* **크론 발송 모니터링 대시보드**: 매일 밤 구동되는 크론 배치에서 메일 및 구글챗 발송에 성공/실패한 통계를 차트와 테이블로 관리자에게 제공.
* **학부모 연락처 데이터베이스**: 학부모 전화번호/메일을 별도로 관리하여 향후 카카오 알림톡 서비스 발송 대상자로 연동.
* **범용 위자드 툴**: 효명고 플랫폼 완성 후 타 학교가 최초 도입 시, 1·2·3학년 전체 OU 및 초기 학생 정보를 쉽게 세팅할 수 있는 단계별 웰컴 마법사 메뉴 제공.

---

## 📌 3. 연동 서비스(교육과정 선택 시스템) 보안 감사 권장 사항

> **과거 세션(`b2a82b0f-a881-444c-9c03-0113f51b2067`)에서 도출된 교육과정 선택 시스템(`course-selection-app`) 연동 시의 핵심 보안 조치 권장사항입니다.**

1. **Firebase API Key 및 도메인 바인딩**:
   * Firebase API Key 자체는 노출되어도 무방하나, Firebase Console에서 **HTTP 리퍼러 제한**을 추가하여 허용된 도메인(`*.vercel.app`, `localhost:3000`) 외의 비정상적인 Firestore 접근을 반드시 원천 차단할 것.
2. **Firestore Rules 보완**:
   * 데이터 노출 방지를 위해 `submissions` 및 `users` 컬렉션의 읽기/쓰기 조건에 `request.auth.token.email.matches('.*@hmh\\.or\\.kr')` 규칙을 적용하여 도메인 외의 탈취 방어선 마련할 것.
3. **이메일 API Endpoint 무단 사용 방지**:
   * `/api/email` 엔드포인트 호출 시 Firebase ID Token 검증 및 `Authorization: Bearer <Token>` 인증 헤더를 필수로 요구하고, 수신 주소도 반드시 학교 도메인으로 제한하여 외부 스팸 발송용 프록시로 악용되지 않도록 방어할 것.
4. **XSS(크로스 사이트 스크립팅) 예방**:
   * Q&A 게시판 및 이메일 내용 템플릿에 학생이 입력한 텍스트를 그대로 삽입할 때 `escapeHtml` 헬퍼 함수를 필수 연동할 것.
