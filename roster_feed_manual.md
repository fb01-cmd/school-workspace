# 명단 자동 공급 사용 설명서 (Phase 6a)

> 대상 독자: 외부 현황판·앱을 운영하는 관리자(비개발자 가능).
> 작성: 2026-07-26 Claude. 시스템이 바뀌면 이 문서도 갱신할 것.

---

## 1. 이게 뭐고, 왜 만들었나

학교에서 운영 중인 외부 도구들(지필평가·모의고사 현황판 시트, 교육과정 선택 웹앱 등)의 공통 골칫거리는 **학생 명단을 매번 손으로 최신화**하는 일이었습니다. 전입·전출·정지가 생길 때마다 시트마다 명단을 고쳐 붙여야 했죠.

이제 플랫폼이 구글 워크스페이스 디렉터리(명단의 단일 원본)에서 명단을 뽑아 **두 가지 통로**로 자동 공급합니다. 외부 도구는 명단을 "복사해 넣는" 게 아니라 "받아다 쓰는" 구조가 됩니다.

```
구글 워크스페이스 (명단의 원본)
        │
        ├── 통로 A: 명렬표 마스터 시트 (매일 자정 자동 갱신)
        │            → 시트로 된 현황판들이 IMPORTRANGE로 참조
        │
        └── 통로 B: 명단 API (실시간 조회)
                     → 웹앱·앱스스크립트가 API 키로 직접 조회
```

**어떤 통로를 쓰나?** 외부 도구가 **구글 시트**면 통로 A, **웹앱/프로그램**이면 통로 B입니다.

---

## 2. 통로 A — 명렬표 마스터 시트 (시트끼리 연결)

### 이미 돌아가고 있는 것

- 공유 드라이브의 마스터 스프레드시트("데이터베이스1")에 **1학년/2학년/3학년 탭**이 있고, 각 탭에 학번·반·번호·성명·이메일이 들어 있습니다.
- **매일 자정 크론이 자동으로 전체를 다시 씁니다.** 전출·정지된 학생은 다음날 자동으로 빠집니다. 손댈 일이 없습니다.
- 수동으로 즉시 갱신하려면: 관리자 페이지 → **Workspace 환경 설정 → 명단 API 키 관리 탭** → `🔄 지금 갱신`.

### 기존 현황판 시트를 연결하는 법

현황판 시트의 명단 영역을 마스터 시트 참조로 바꿉니다. 현황판 시트의 A1 등에:

```
=IMPORTRANGE("마스터시트URL", "1학년!A2:E400")
```

- 처음 한 번 "액세스 허용" 버튼을 눌러 연결을 승인해야 합니다.
- 이후 마스터 시트가 갱신되면 현황판도 자동으로 따라갑니다.
- 팁: 명단은 IMPORTRANGE로 받아오는 시트(예: "명단" 탭)에 두고, 성적·체크 입력은 다른 탭에서 VLOOKUP으로 학번을 참조하게 만들면 명단이 바뀌어도 입력 데이터가 어긋나지 않습니다.

---

## 3. 통로 B — 명단 API (웹앱·프로그램용)

### 3-1. API 키 발급 (수퍼어드민)

1. 관리자 페이지 → **Workspace 환경 설정 → 명단 API 키 관리** 탭
2. 용도 입력(예: "교육과정 선택 앱") → **발급**
3. **평문 키(`hmh_sk_...`)는 이 순간 딱 한 번만 보입니다.** 복사해서 해당 앱 관리자에게 안전하게 전달하세요. 다시 볼 수 없으며, 분실하면 폐기하고 새로 발급하면 됩니다.

> 🔑 **키 = 전교생 명단 열람 비밀번호**입니다. 카톡·메일 본문으로 보내지 말고, 시트 셀에 적어두지 마세요. 유출이 의심되면 즉시 이 화면에서 **폐기**하세요(폐기 즉시 무효).
> 앱(용도)마다 키를 따로 발급하세요 — 하나가 유출돼도 그것만 폐기하면 됩니다. 목록의 "마지막 사용"으로 실제로 쓰이는지 확인할 수 있습니다.

### 3-2. 호출 방법

```
GET https://school-workspace-eight.vercel.app/api/roster/feed
Authorization: Bearer hmh_sk_xxxxxxxx...
```

옵션(주소 뒤에 붙임):
| 파라미터 | 뜻 | 예 |
|---|---|---|
| `?grade=1` | 특정 학년만 | 1·2·3 |
| `?includeSuspended=true` | 정지 계정도 포함 (기본은 재학·활성만) | 결시 처리용 |
| `?format=csv` | JSON 대신 CSV로 (시트/엑셀 친화) | |

응답(JSON): 학생마다 `studentId`(학번 "10101"), `grade`, `classNum`, `number`, `name`, `email`, `suspended`. 학번 형식이 아닌 계정은 `unparsed` 목록으로 분리돼 옵니다.

### 3-3. 앱스스크립트에서 쓰는 예 (시트에 명단 채우기)

구글 시트 → 확장 프로그램 → Apps Script에 붙여넣고, 키만 바꿔서 실행:

```javascript
function 명단가져오기() {
  const KEY = "hmh_sk_여기에_발급받은_키";
  const res = UrlFetchApp.fetch(
    "https://school-workspace-eight.vercel.app/api/roster/feed?grade=1",
    { headers: { Authorization: "Bearer " + KEY } }
  );
  const students = JSON.parse(res.getContentText()).students;
  const rows = students.map(s => [s.studentId, s.classNum, s.number, s.name, s.email]);
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("명단");
  sheet.getRange(2, 1, rows.length, 5).setValues(rows);
}
```

(트리거를 "시간 기반 - 매일"로 걸면 이 시트도 자동 최신화됩니다. 다만 시트라면 통로 A가 더 간단합니다.)

### 3-4. 교육과정 선택 앱 연동

- 그 앱의 **서버 코드에서** 위 API를 호출해 학생 로그인 검증/명단 동기화에 쓰면, 지금의 "CSV 업로드" 단계가 사라집니다.
- 키는 그 앱의 서버 환경변수에 넣습니다 (앱 화면·클라이언트 코드에 넣지 말 것).
- 그 앱은 별도 Firebase 프로젝트로 계속 독립 운영합니다 — 이 연동은 명단 공급만 하는 것이고, 병합이 아닙니다.

---

## 4. 자주 묻는 질문

**Q. 명단이 바뀌면(전입·전출) 뭘 해줘야 하나요?**
아무것도. 플랫폼에서 계정 처리만 정상적으로 하면 마스터 시트는 다음날 자정, API는 즉시(다음 호출부터) 반영됩니다.

**Q. 키를 잃어버렸어요.**
찾아줄 방법이 없습니다(서버에도 원문이 저장돼 있지 않음 — 보안 설계). 폐기하고 새로 발급해서 앱에 다시 넣으면 끝입니다.

**Q. 이 API로 명단을 고칠 수도 있나요?**
아니요, 100% 읽기 전용입니다. 학적 변경은 반드시 플랫폼(계정 생애주기 메뉴)에서.

**Q. 학생 개인정보가 나가는 건데 괜찮은 건가요?**
내용은 학번·이름·학교이메일·학년반번호로 최소화돼 있고(전화번호·주소 등 없음), 키 인증 없이는 아무것도 응답하지 않습니다. 키 발급·폐기는 전부 감사 로그에 남습니다. 그래서 위 키 관리 수칙이 중요합니다.

---

## 5. 관리 요약 (수퍼어드민 체크리스트)

- 새 외부 도구 연결: 시트면 IMPORTRANGE(§2), 앱이면 키 발급(§3-1)
- 학기 초: 키 목록의 "마지막 사용" 확인 → 안 쓰는 키 폐기
- 담당자 교체·유출 의심: 해당 키 폐기 → 재발급
- 마스터 시트 위치/ID 변경: 명단 API 키 관리 탭에서 시트 ID 저장 후 "지금 갱신"
