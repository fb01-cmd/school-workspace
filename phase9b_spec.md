# Phase 9b 상세 스펙 — 결보강 워크플로 (컴시간 일과진행 대체)

> 2026-07-30 Claude 작성 (아키텍처·데이터 모델·엔진·워크플로 판단). 상위 문서: [`phase9_spec.md`](./phase9_spec.md).
> 입력: `일과진행설명서.pdf` 36p 정독 + `주간시간표설명서.pdf` 39p 관련부 추출 + `comcigan_analysis.md` + phase9a_spec §8(노출 원칙·교체 신청 UX 확정) + 9a-1 완주 실데이터(30학급/62교사, `2026-2` 학기 active) + **컴시간알리미 수업교환 실화면 튜토리얼**(iorad #2446857, 20스텝, 사용자 제공 — §1-1).

## 0. 전제 (기확정 사항 — 재론 금지)

| 항목 | 확정 내용 | 출처 |
|---|---|---|
| 승인 | **실무사(일과계) 단독 승인**, 교무부장은 알림만. 결재 단계 없음 | phase9a_spec §0-3 |
| 노출 | 9b 완성 전까지 일반 교사·학생에게 시간표 비노출. **9b-3 실사용 검증 후 오픈** | phase9a_spec §8-1 |
| 교사 진입점 | 내 수업 셀 클릭 → 옮겨갈 셀 선택 → 엔진이 후보 제시 | phase9a_spec §8-2 |
| 징검다리(체인) 교체 | 기본 접힘 + "통상 사용하지 않음" — **1차 구현 제외**, UI 자리만 | phase9a_spec §8-2 |
| NEIS | 자동 입력 없음. **입력용 수업교환 목록 제공까지만** | phase9_spec §0-4 |
| 데이터 구조 | 기초시간표(9a 확정 모델) + **주 단위 변경 오버레이**. 파생본 저장 금지, 합성 계산 | phase9_spec §1-③ |

## 1. 컴시간 일과진행 기능 → 9b 범위 매핑 (설명서 정독 결과)

| 컴시간 기능 (설명서 페이지) | 9b 대응 | 범위 |
|---|---|---|
| 날짜등록·휴업일(p.4~5)·요일별 시수(p.12) | §2 `timetable_weeks` + 주 등록 UI | **1차** |
| 일반보강 = 교환 가능 교시 자동 탐색(p.7~8) | §4 교체 후보 엔진(맞교환) | **1차 핵심** |
| 특별보강 배정(p.8, 22~23) | §4 특별보강 후보(공강 교사, 부담 공평 정렬) | **1차 핵심** |
| 수업교환 요청대장(p.17) — 알리미 신청→일과계 처리 | §5 신청→승인 워크플로 | **1차 핵심** |
| 변경 빨간색 표시·출처 툴팁(p.6, 8)·색상 설정(p.16) | §3 합성 응답의 `changed` 플래그 + UI 관례 계승 | **1차** |
| 나이스용 수업교환 목록(p.17, 32) | §8 기간 지정 조회·엑셀 | **1차** |
| 시수누계·수업시수 열람(p.13~15) | §8 시수 집계(계산, 저장 안 함) | **1차** |
| 수업없는 교사열람(p.14) | 9a `free` action 기존 구현 + 주간 합성 반영 | **1차(확장)** |
| 특별보강 일괄배정 — 수학여행·야영(p.10) | §8 일과계 일괄 특별보강 도구 | **1차(후순위)** |
| 저장 시 알림 팝업·담임 알림(p.12~13) | §5 승인 시 구글 챗 알림으로 대체 | **1차(축소)** |
| 작업기록·시점 복원(p.16) | 불변 변경 로그(§2)가 대체 — 역방향 변경으로 복원 | **1차(구조로 해결)** |
| 연쇄이동 미리보기·이동방법 4종(p.6~7, 18~22) | 일과계 직권 편집 도구 | 2차 (9c 수동 조정 UX와 통합) |
| 교시간/요일간 수업교환·동일교시 학급교환·합반(p.9) | 학년 단위 대량 이동 | 2차 |
| 정기고사 수업처리(p.9~10)·온라인시간표 일괄배정(p.10) | 시험 주간 모드 | 2차 |
| 알리미 미공개(엠바고, p.12~13) | 주 등록 시점으로 갈음 | 제외 |
| 서버관리실·전출 교사 삭제(p.31~32)·PC 이사(p.31) | 플랫폼이 이미 자동 (GWS 연동·전출 자동화) | 해당 없음 |

### 1-1. [2026-07-30 추가] 알리미 수업교환 실화면 확인 (iorad 튜토리얼 #2446857)

사용자가 제공한 교내 배포용 튜토리얼(20스텝, 실화면 캡처)에서 확인한 **알리미의 실제 교사 흐름**: 수업교환 요청대장 → 요청하기 → 내 시간표에서 변경할 수업 클릭 → 공강 칸에 **커서를 올려가며** 교환안 확인("이리저리 커서를 옮겨가며 적절한 교사를 찾아야") → 맞교환이면 [연쇄 이동하여라] → 이렇게 수업교환 목록을 쌓은 뒤 → **교환사유 드롭다운(출장 등) 선택** → [일과계에게 수업교환 요청하기] → 일과계 처리 시 서버 반영·전 교사 공개.

**스펙 검증 결과** — 본 스펙의 결정 3개가 학교 현행 지도와 일치함을 실물로 확인:
1. **사유 필수 드롭다운**(§12-4): 알리미에도 교환사유 드롭다운이 있고 튜토리얼이 선택을 지시 — 교사들에게 이미 익숙한 절차.
2. **다인 체인 비추천**(§0): 튜토리얼 자체가 "4인 교환… 너무 많은 교환이 필요한 경우는 추천하지 않습니다"라고 지도 — 1차 체인 제외는 현행 관행과 일치.
3. **동시수업 하드 제외**(§4-3): 튜토리얼이 "선택과목 수업이 통째로 이동… 타임형 시간표가 아래에 뜨면 **절대 교환하시면 안됩니다**"라고 경고 — 알리미는 교사의 주의에 맡기지만 우리는 엔진이 원천 차단(후보 미제시). 명백한 개선점.

**개선점**: 알리미는 교사가 공강 칸마다 커서를 옮겨가며 교환안을 하나씩 확인해야 하지만, 우리는 셀 선택 즉시 전 슬롯 후보를 일괄 계산해 초록 하이라이트 + 감점순 정렬로 제시(§4-4) — 탐색 부담 제거.

**용어 확정**: 사용자 노출 명칭은 교사들이 이미 쓰는 **"수업교환 신청"**으로 통일한다 ("교체 신청" 아님). 요청대장·수업교환 목록 등 컴시간 용어를 그대로 계승해 이행 비용을 낮춘다. (내부 타입·컬렉션명은 swap_requests 유지)

## 2. 데이터 모델 (Firestore, admin SDK 전용 — 9a 모델에 추가)

```
timetable_settings/{domain}                     ← 9a 문서에 필드 2개 추가
  ├ lunchAfterPeriod: number = 4                ← "N교시 후 점심" (감점 '점심' 판정 기준)
  └ observerEmails: string[] = []               ← 열람 전용 참관자 (교무부장 등) — 요청대장·변경
                                                   내역을 접속해서 볼 수만 있음. DM 발송 없음 (§5)

timetable_weeks/{domain}/weeks/{weekId}         ← weekId = 월요일 날짜 "2026-09-07"
  ├ termId: "2026-2"
  ├ startDate: "2026-09-07"
  ├ days: [{ day: 1~5, date: "2026-09-07",
  │          holiday: boolean,                  ← 휴업일(공휴일·재량휴업)
  │          periodsByGrade?: {1:n,2:n,3:n} }]  ← 요일별 시수 축소(단축수업). 없으면 설정값
  ├ note?: string
  └ createdBy, createdAt

timetable_changes/{domain}/changes/{changeId}   ← 불변 로그. 수정·삭제 금지
  ├ termId, weekId
  ├ type: "swap" | "substitute" | "revert"
  ├ requestId?: string                          ← 신청 경유 시. 일과계 직권이면 없음
  ├ swap?: {                                    ← 맞교환: 같은 학급 내 두 슬롯 교환
  │    grade, classNum,
  │    a: { day, period, subjectName, teacherEmail, teacherName },
  │    b: { day, period, subjectName, teacherEmail, teacherName } }
  ├ substitute?: {                              ← 특별보강: 슬롯의 교사만 대체
  │    grade, classNum, day, period, subjectName,
  │    absentTeacherEmail, absentTeacherName,
  │    subTeacherEmail, subTeacherName, subSubjectName? }
  ├ revertOf?: changeId                         ← 취소는 역방향 기록 (원본 불변)
  └ appliedBy, appliedAt

swap_requests/{domain}/requests/{requestId}
  ├ termId, weekId, type: "swap" | "substitute"
  ├ requesterEmail, requesterName
  ├ source: { grade, classNum, day, period, subjectName }   ← 신청자 본인 수업 (서버 검증)
  ├ candidate: {                                 ← 엔진이 제시한 후보의 스냅샷
  │    targetDay, targetPeriod,                  ← swap: 옮겨갈 슬롯
  │    counterpartEmail?, counterpartName?,      ← swap 상대 / substitute 보강 교사
  │    counterpartSubjectName?, score, penalties: string[] }
  ├ reason: { type: "출장" | "연수" | "병가" | "공가" | "학교행사" | "기타",
  │           note?: string }                    ← 사유 필수, 드롭다운 선택 (2026-07-30 사용자 확정.
  │                                                "기타"만 자유 텍스트 병기. 항목은 실무사와 최종 확정)
  ├ status: "PENDING" | "APPROVED" | "REJECTED" | "CANCELED"
  ├ decidedBy?, decidedAt?, decisionNote?        ← 반려 사유 필수
  ├ appliedChangeIds?: string[]                  ← 승인 시 생성된 change 참조
  └ createdAt
```

- **주간 실제 시간표 = 기초(classGrids) + 그 주 changes를 `appliedAt` 순서로 적용한 합성.** 어디에도 합성본을 저장하지 않는다 (생활지도 "저장하지 않고 계산" 원칙).
- changes가 불변 로그이므로 컴시간의 "작업기록·시점 복원"이 별도 기능 없이 성립: 취소 = `revert` 항목 추가. NEIS 목록·감사 추적의 원천도 이 로그 하나다.
- weekId·요일·교시의 유효성은 서버가 week 문서·설정으로 검증 (휴업일·축소 시수 슬롯에는 어떤 변경도 불가).

## 3. 주간 합성 규칙 (`src/lib/timetable/weekly.ts` — 순수 함수, Claude 구현)

1. 입력: `classGrids[]`(기초) + `week` 문서 + 그 주 `changes[]`(appliedAt 오름차순).
2. 휴업일(`holiday: true`)의 요일 셀 전부 제거, `periodsByGrade` 초과 교시 제거.
3. changes 순차 적용:
   - `swap`: 학급 그리드에서 a·b 슬롯의 lesson을 서로 이동. 적용 셀에 `changed: { type, origin: {day, period}, changeId }` 마킹.
   - `substitute`: 해당 슬롯 lesson의 teachers를 보강 교사로 교체(+`subSubjectName` 있으면 과목 병기), `changed` 마킹.
   - `revert`: `revertOf` 대상 change의 역연산. 마킹 제거.
4. 적용 불능(대상 슬롯 상태가 change와 불일치 — 이중 revert 등) 발견 시 **해당 change만 건너뛰고 `integrityWarnings[]`에 수집**해 일과계 화면에 노출. 절대 throw로 전체 조회를 죽이지 않는다.
5. 교사 뷰·공강 뷰는 합성 결과에 기존 `synthesizeTeacherTimetable`·`synthesizeFreeTeachers`를 그대로 적용 (9a 유틸 재사용).
6. `view` 라우트 확장: `weekId?` 파라미터 추가. 지정 시 합성본+`changed` 플래그 반환, 미지정 시 현재 주(등록돼 있으면) 폴백 → 기초. 학생 반 강제 덮어쓰기 등 기존 가드 불변.

## 4. 교체 후보 탐색 엔진 (`src/lib/timetable/swap.ts` — 순수 함수, Claude 구현)

**모든 계산은 주간 합성본 기준** (기초가 아니라 — 이미 승인된 변경 위에서 후보를 찾아야 이중 배정이 원천 차단된다).

### 4-1. 맞교환 (swap) — 컴시간 "일반보강" 대응

신청자 A의 수업 셀 s = (d₁,p₁, 학급 C, 과목 X)에 대해:

1. 후보 목표 슬롯 t = (d₂,p₂) 전수 탐색 — 조건 ①: A가 t에 공강, ② t가 휴업일·축소 교시 아님.
2. 학급 C의 t 셀에 있는 lesson의 교사 B에 대해 — 조건 ③: B가 s에 공강.
3. ①~③ 충족 시 후보 성립: A의 X와 B의 수업을 맞바꾼다. **학급 C 입장에서는 과목 순서만 바뀌므로 수업 결손이 없다** — 이것이 맞교환의 본질이자 컴시간 방식.

### 4-2. 특별보강 (substitute) — 교환 없이 대신 수업

- s 슬롯에 공강인 교사 전원이 후보. 정렬: **① 특별보강 누계 오름차순** (changes에서 subTeacherEmail 집계 — 보강 부담 공평화, 컴시간 계승) → ② 동일 과목 담당자 우선 표시 필터(토글).
- 가상 교사(SLAT·창체 — 이메일 없는 lesson)는 후보에서 제외.

### 4-3. 제외·감점 규칙

**후보 제외(하드)** — 컴시간도 중대 속성(연속수업·특별실·이동금지·일괄배정) 셀은 이동 실행을 비활성화한다(주간시간표설명서 p.27). 같은 원칙:
- 동시수업 셀(lessons 2개 이상) 또는 복수교사 lesson(teachers 2명 이상)이 s나 t에 걸리면 제외 — 1차에서는 단일 교사·단일 수업만 교체 대상. UI에 "동시/복수교사 수업은 일과계에 직접 요청" 안내.
- **특별실 충돌**: 옮기는 lesson에 `room`이 있고, 목표 슬롯에서 그 room을 타 학급이 이미 사용 중이면 제외 (합성본 전 학급 room 대조).
- 가상 교사(SLAT·창체) lesson이 걸린 셀 제외.

**감점(소프트 — 후보는 살리되 점수순 정렬, 사유 표기)**: 컴시간 배정 품질 조건의 정의(주간시간표설명서 p.27 '연쇄 이동시 문제점') 계승.
- 학급 C가 같은 요일에 같은 과목 2회가 됨 (중복 — 최대 감점)
- 교사(A 또는 B)가 같은 요일에 3과목 이상 수업하게 됨 ('학년' 조건)
- 교사의 요일별 시수 쏠림 — 이동 후 특정 요일이 과다/과소 ('최적' 조건)
- 교사에게 연속 3교시 이상이 새로 생김 ('연속3')
- 점심 직전·직후 연속 수업이 새로 생김 ('점심' — 점심 위치는 일과시간 설정 기준)
- 오전·오후 배정 균형이 깨짐 ('오후')

점수 = 감점 합 (0이 최선). 응답에 `penalties: string[]`로 사유를 사람이 읽는 문장으로 담아 교사·일과계가 판단 근거를 본다. 후보 목록을 [원본 슬롯 | 대상 슬롯 | 상대 | 문제점] 표로 제시하는 방식은 컴시간 '이동가능 교시' 창(주간시간표설명서 p.29)의 계승.

### 4-3b. 교차 주(cross-week) 맞교환 — [2026-08-04 신설, 파일럿 요구]

> 근거: 실무 관행상 교체는 같은 주 안에서만 일어나지 않는다 — 이번 주 출장 수업을 다음 주(이후 주) 수업과 맞바꾸는 진도 조절이 일반적. 1차 범위에 포함한다.

- **모델**: 교차 주 교환 1건 = `timetable_changes` 문서 **2개(각 주에 1개)**, 공통 `exchangeId`로 연결. 각 문서는 자기 주의 셀 1개 치환만 기술: `crossSwap: { exchangeId, otherWeekId, grade, classNum, day, period, out: 슬롯수업, in: 슬롯수업 }`. 주간 합성 로더(loadWeekChanges)가 weekId 등호 조회를 유지할 수 있는 구조가 목적. 적용 시 셀 현재 수업이 `out`과 일치할 때만 치환, 불일치는 integrityWarning.
- **revert**: `exchangeId` 단위로만 취소 — 두 문서에 동시 역기록(한쪽만 취소 금지, 단일 트랜잭션).
- **신청**: `swap_requests.targetWeekId?` 추가(없으면 기존 같은-주 교환). 후보 스냅샷에 targetWeekId 포함.
- **엔진**: (sourceWeek, targetWeek) 두 합성본 입력 — 같은 학급의 targetWeek 수업 중, ① 상대가 sourceWeek의 source 슬롯 시간에 공강 ② 내가 targetWeek의 그 슬롯 시간에 공강 ③ 하드 제외(가상·블록·동시·복수·특별실)는 각 주 기준으로 각각 적용. 감점은 두 주 각각 계산해 합산.
- **승인 트랜잭션**: 두 주의 changes를 모두 재읽고 양방향 재검증 → 두 문서 원자 커밋(동일 컬렉션이므로 단일 Firestore 트랜잭션으로 가능).
- **운영 전제**: 대상 주가 **등록돼 있어야** 교환 가능 — 일과계가 주를 2~3주치 미리 등록하는 운영을 권장(주 등록이 곧 교환 가능 범위).
- **파급 정합**: 시수 집계·NEIS 목록은 문서가 주별로 분리돼 있어 기존 로직과 자동 정합(NEIS "변경전 교시"는 otherWeekId의 날짜로 표기).
- **UI**: 신청 패널에 "교체할 주" 선택(기본 = 조회 중인 주). 주 변경 시 후보 재조회. 변화 요약 문장에 주 표기 필수("내 음악(1-3): 8/13(목2) ➖ → 8/17(월3) ➕").

### 4-4. UX 흐름 (phase9a_spec §8-2 확정안의 구체화)

1. 교사가 내 주간시간표에서 **내 수업 셀 클릭** → 서버가 그 셀 기준 전 슬롯 후보를 일괄 계산해 반환.
2. UI는 **교체 가능한 내 공강 셀을 초록 배지로 하이라이트** (컴시간 초록 관례 계승). 셀 클릭 시 그 슬롯의 상대·감점 상세 표시.
3. ~~"교환 없이 보강만 요청" 토글 → 특별보강 후보 목록(4-2 정렬)으로 전환.~~ **[2026-08-04 개정, 파일럿 확정]** 특별보강은 교사 신청 대상에서 제외 — 결강은 일과계가 직권 배정(§6 direct_*)으로 처리한다(컴시간 관행 유지). 교사 화면에는 보강 UI를 두지 않고, requests 라우트도 type=substitute 생성을 거부한다. 맞교환 후보 0건일 때 "일과계 문의(특별보강)" 안내만 노출.
4. 후보 카드에서 [신청] → 사유 입력(선택) → 제출. **후보는 엔진 제시분만 선택 가능, 임의 셀·임의 교사 입력 불가** (AGENTS.md §4 선택 강제와 동일 정신).
5. 징검다리 체인: 접힌 영역 + "통상 사용하지 않습니다" 문구만. 클릭 시 "일과계에 직접 문의" 안내 (1차 미구현).

## 5. 신청→승인 워크플로

### 상태 기계

```
PENDING ── 일과계 승인 ──> APPROVED (changes 생성, 되돌리기는 revert로만)
   ├───── 일과계 반려(사유 필수) ──> REJECTED
   └───── 신청자 취소 ──> CANCELED        ← PENDING에서만 가능
```

### 서버 검증 (AGENTS.md §5 이중 방어 — 클라이언트 신뢰 금지)

- **신청 생성 시**: ① source가 신청자 본인의 수업인지 합성본에서 확인 ② candidate를 서버가 재계산해 실존 확인 (엔진 결과에 없는 후보면 400) ③ 같은 source 셀에 PENDING 신청이 이미 있으면 중복 차단.
- **승인 시 (Firestore 트랜잭션)**: 그 주 changes를 다시 읽고 **후보를 재검증** — 다른 승인으로 상황이 바뀌었으면 승인 실패 + "재신청 필요" 사유 반환. 통과 시 change 생성 + request 상태 전이를 원자적으로 커밋. (승인 간 경합이 이 시스템의 유일한 동시성 위험 지점이므로 여기만 트랜잭션)
- 승인·반려·revert 권한: `timetable_settings.managerEmails` + super_admin만 (9a `manage` 라우트 판정 재사용).

### 알림 (AGENTS.md 발신자 규칙 — `hmnotice@hmh.or.kr`)

| 시점 | 수신자 | 내용 |
|---|---|---|
| 신청 생성 | 일과계(managerEmails) | 새 교체 신청 도착 (신청자·슬롯 요약) |
| 승인 | 신청자 + 상대 교사(swap 상대/보강 교사) | 확정 내용 (변경 전→후 슬롯) |
| 반려 | 신청자 | 반려 사유 |
| 승인 취소(revert) | 원 승인 알림 수신자 전원 | 취소 안내 |

- 상대 교사의 **사전 동의 단계는 두지 않는다** — 컴시간과 동일하게 오프라인 협의 관례를 유지하고 플랫폼은 확정 알림만. (2026-07-30 사용자 확정. 2차 후보: 상대 수락 단계 옵션)
- **교무부장은 DM 수신자가 아니라 열람 전용 참관자** (2026-07-30 사용자 확정): `observerEmails`(§2)에 등록된 계정은 일과계 화면 중 요청대장·변경 내역 탭을 **읽기 전용**으로 볼 수 있다 — 접속해서 확인하는 방식. 승인·반려 버튼은 렌더하지 않고 서버 판정도 거부. 직책 자동 탐색 같은 마법 금지, 명시 등록.

## 6. API 설계

### `POST /api/timetable/requests` (신규 — 교사 본인용)

| action | 기능 |
|---|---|
| `candidates` | source 셀 지정 → 맞교환 후보 일괄 + 특별보강 후보 (4-1~4-3) |
| `create` | 신청 생성 (§5 검증) |
| `my_list` | 내 신청 목록 (상태·결정 사유 포함) |
| `cancel` | 내 PENDING 신청 취소 |

### `POST /api/timetable/manage` 확장 (기존 라우트 — 일과계·super_admin 판정 재사용)

| action | 기능 |
|---|---|
| `week_register` / `week_update` | 주 등록·휴업일·요일별 시수 (§2 weeks) |
| `request_list` | 요청대장 (주·상태 필터) |
| `approve` / `reject` | §5 트랜잭션 승인 / 사유 반려 |
| `revert_change` | 승인 취소 — 역방향 change 기록 |
| `direct_substitute` | 일과계 직권 특별보강 (신청 없이) — 수학여행 일괄은 이 action의 배열 입력 |
| `neis_list` | 기간 지정 수업교환 목록 (§8) |
| `hour_totals` | 시수 집계 (§8) |

### `POST /api/timetable/view` 확장

- `weekId?` 파라미터 (§3-6). 응답 셀에 `changed` 플래그. 기존 action·권한 가드 불변.

## 7. 화면 (IA — 임의 배치 금지)

- **교사 (9b 오픈 게이트 후 노출)**: 사이드바 "내 시간표" 메뉴 신설 → ① 내 주간시간표 (변경 셀 빨간 배경 + 텍스트 마커 병행(색약 대비), 마우스오버 시 "화3 ← 월2에서 이동" 출처 툴팁 — 컴시간 관례 계승) + 셀 클릭 교체 신청 플로우(§4-4) ② 내 신청 내역 ③ 다른 교사·학급 시간표 조회 (9a 탭 재사용, 주간 합성본으로 전환).
- **일과계 ("시간표 관리 (일과계)" 메뉴에 탭 추가)**: ① 주 운영 (주 등록·휴업일·단축시수) ② 요청대장 (PENDING 상단 고정, 승인/반려, `integrityWarnings` 배너) ③ 직권 배정 (특별보강·일괄) ④ NEIS 목록·시수 집계 (조회+엑셀).
- **학생 포털**: `StudentTimetableCard` 재마운트 (주간 합성본, 변경 셀 강조) — **오픈 게이트 통과 후**.
- **셀 표기 원칙 [2026-08-04 사용자 확정]**: 시청자가 누구냐에 따라 메인 정보가 다르다.
  - **교사 대상 그리드 전부**(내 주간시간표·다른 교사/학급 시간표 조회·상대 미리보기 미니 그리드): **학급("1-4")을 메인으로 표기**, 과목명은 마우스오버 툴팁(`title`)로만. 교사에게 자기 과목명 도배는 정보량 0 — 어느 반에 들어가는지가 핵심.
  - **학생 대상 화면**(`StudentTimetableCard`, 오픈 게이트 후 구현 시): 반대로 **과목명을 메인으로** — 학생에게 학급 표기는 무의미하고 무슨 과목인지가 핵심. 교사명은 보조 표기.
- **오픈 게이트**: 9b-3까지 배포 + 일과계가 실제 1주 이상 운영(실신청 승인 포함) + 사용자 승인 → 교사 메뉴·학생 카드 노출 플래그를 켠다. 그 전까지 신규 화면 전부 일과계 전용.

## 8. 운영 도구 (9b-4)

- **NEIS 입력용 수업교환 목록**: 기간(시작~종료) 지정 → changes를 평탄화해 컴시간 양식 계승: `변경있는 교시(일자·교시) | 교사 | 과목 | 변경전 교시 | 비고(특별보강 대체교사)`. 특별보강만 필터. 화면 표 + xlsx 다운로드 (SheetJS 기존 의존성 재사용). 실무사의 현행 "승인 즉시 NEIS 수기 입력" 흐름을 그대로 지원하는 것이 목적 — 자동 입력 아님.
- **시수 집계**: 교사별·과목별·학급별 — 학기 시작~지정일까지 등록된 주의 합성본에서 실시수 카운트 (휴업일·변경 반영). 특별보강 누계 별도 표기 (엔진 정렬 §4-2와 동일 집계 함수 공유). 저장하지 않고 계산. **고교학점제 이수시수 판정과 직결되므로 실제 운영된 수업만 집계한다 — 가공 시수 입력 경로를 만들지 않는다** (§12-3 결정).
- **결재용 일람표**: 요청대장 화면의 인쇄 뷰 (기간 필터 + 브라우저 인쇄). 전용 양식 개발은 실무사 요구 확인 후 (1차는 표 그대로).

## 9. 보안·권한 요약

- 전 판정 서버 사이드 (admin SDK). 교사는 자기 신청 CRUD + 열람만, 학생은 열람 전용(반 강제) — 9a 가드 불변.
- 신청·승인·직권 배정 전부 **후보 재계산 검증** (§5) — 검색→식별자 사고(gotest 유령 레코드)의 재발 방지 구조를 처음부터 내장.
- changes 불변 + `appliedBy` 기록 = 감사 로그. 별도 audit 컬렉션 불요.
- 이메일 입력이 존재하는 유일한 지점은 `notifyEmails` 설정(§5) — AGENTS.md §4 선택 강제 + §5 형식·실존 검증 적용.

## 10. 명시적 범위 제외 (1차)

징검다리 체인 교체(§4-4-5), 연쇄이동 직권 편집기(2차 — 9c 수동 조정 UX와 통합), 교시간/요일간·학급 단위 대량 교환, 정기고사 모드, 알리미식 엠바고, 상대 교사 사전 동의 단계, 담임 대상 학급 변경 알림, NEIS 자동 입력, 학부모 열람.

## 11. 구현 순서·분업 (phase9_spec §5 분업표의 구체화)

| 순서 | 작업 | 담당 | 비고 |
|---|---|---|---|
| 1 | §2 types 확장 + weeks CRUD(`week_register/update`) + §3 합성 함수 `weekly.ts` + view `weekId` 확장 | **Claude 직접** | 판정·무결성 핵심부 |
| 2 | §4 엔진 `swap.ts` + §6 `requests` 라우트 + §5 승인 트랜잭션(manage 확장) | **Claude 직접** | 〃 |
| 3 | 일과계 화면: 주 운영·요청대장·직권 배정 탭 | Antigravity | 1·2 배포 후 |
| 4 | 교사 화면: 내 시간표 + 신청 플로우 + 내역 | Antigravity | 오픈 게이트 전까지 일과계만 렌더 |
| 5 | §8 NEIS 목록·시수 집계 + 학생 카드 재마운트 | Antigravity | |
| 6 | 실사용 리허설: 실무사가 실제 1주 운영(주 등록→신청→승인→NEIS 목록 출력) | 사용자+Antigravity | 통과 시 오픈 게이트 |

- DoD: 단계마다 `npx tsc --noEmit` + `npm run build` + 핸드오버 (AGENTS.md 규칙 ①·④). 엔진·합성 함수는 실데이터(`2026-2` 학기 30학급) 기반 재현 스크립트 실측 필수.
- Claude 표적 리뷰 지점: 합성 무결성(§3-4), 승인 트랜잭션 경합(§5), 학생·교사 권한 가드 회귀.

## 12. 사용자 확인 (2026-07-30 답변 — 3번만 미결)

1. ✅ **상대 교사 사전 동의 없음** 확정 (§5 반영).
2. ✅ **교무부장 = 열람 전용 참관자**(`observerEmails`) 확정 — DM 없이 접속해서 확인 (§2·§5 반영).
3. ✅ **정기고사 주 = 요일별 시수 축소로 운영** 확정 (2026-07-30). 사용자 맥락: 과거에는 시험일에 실수업 없이 6교시 한 것으로 잡는 관행도 있었으나, **고교학점제에서 과목별 시수가 이수 기준 판정과 직결되므로 가공 시간표는 불가** — 실제 운영 시수만 기록해야 한다. 따라서 §8 시수 집계는 합성본 기준 실시수만 세는 현 설계를 유지하고, 시험일은 주 등록 때 교시 수를 실제만큼 축소한다. (시험 시간표·감독 배치 표시는 범위 외 유지)
4. ✅ **사유 필수 + 드롭다운** 확정 (§2 반영 — 항목 목록은 실무사와 최종 확정).
